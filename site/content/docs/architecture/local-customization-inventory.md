---
title: "本地二开能力清单"
weight: 90
description: "记录当前稳定分支相对作者基线已生效的本地能力，并作为后续上游升级逐项迁移的核对基线。"
---

## 2026-09-23 Push 暂存区格式校验补修（工作树待提交）

重新运行后索引锁问题已解除，实际停止在 git diff --cached --check，退出码 2。完整暂存区确认三个新增文件末尾空行：scripts/doubao-browser-inspect.mjs、scripts/verify-image-preview.mjs、site/content/docs/architecture/manual-media-selection.md。前次 git diff --check 只覆盖未暂存的已跟踪差异，没有覆盖当时未跟踪的新文件，验证范围不足；不能将其表述为全部待提交文件检查通过。

本批仅将这三个文件末尾规范为单换行并更新其暂存内容，保留其他全部暂存改动；未关闭格式检查，不修改全局换行设置，不提交或推送。LF will be replaced by CRLF 为提示，不是本次失败原因。最终必须以完整 git diff --cached --check 及 git diff --check 均通过作为格式验收。LC-001/014，全部原计划保持，无运行服务部署或业务改动。


## 2026-09-23 Push 索引残留锁与生成文件格式修复（工作树待提交）

用户双击 Push-Jellyfish.cmd 时 git add 因 .git/index.lock 已存在退出 128。现场确认该锁为 2026-09-10 15:03:04 UTC 的 0 字节残留，检查时无 Git 进程，已移至 backups/push-lock-20260923/index.lock.stale 保留。未删除索引、reset、暂存、commit 或 push；索引摘要在暂存预演前后保持一致。

Jellyfish.ps1 在预检查及实际 git add 前检查 git rev-parse --git-path index.lock（兼容 Git 工作树路径），存在即提前显示中文诊断与恢复说明；不按锁龄自动删除锁、不终止 Git。未来出现此提示，应先等待其他操作结束，只有核实为无人持有的残留锁才备份移走。14 项模拟脚本测试通过，含原有锁、确认前出现的新锁，保持不写入、不自动删锁；补齐原测试遗漏的本机助手空操作夹具。

同时发现 OpenAPI 生成器会新增 EOF 空行，导致后续 git diff --cached --check 失败；新增 front/scripts/normalize-generated-eof.mjs，openapi:gen 自动执行，只处理 generated TypeScript 末尾单换行，不修改契约内容。重新生成与 tsc 验证通过；首轮遇 Windows 临时文件访问错误，顺序重跑成功。最终真实 Push -Preview、git add --dry-run -A 及 git diff --check 验证记录在 local-reports/push-lock-20260923；预演不表示远程认证/联网推送成功。

LC-001/009/014；本批不涉及业务 API 变化、数据库或运行服务部署，其他未关闭计划保持。用户可关闭旧失败窗口后重新运行 Push-Jellyfish.cmd，核对范围并按原流程确认。


## 2026-09-23 图片与视频手工素材采用（工作树待提交批次）

发布完成（2026-09-23）：仅更新 API/front，正式 7788 页面及真实新接口只读验收通过，9 个业务入口、0 页面异常、0 真实写入；没有替换用户现有图片/视频。新 API 镜像 sha256:5a11c9e34f248c5575587aa70a953bcb0cd3b07f54f77703dc22676737b263f7，front sha256:3c3faab5b7e29bf0defc7c6dcf5586afb5f5f9128d51aca25da69f748a8929e1，资源 index-CneNH27v.js；OpenAPI 与线上完全一致。启动标签 latest/web-flow-20260923 已同步，rollback-manual-media-20260923 保留；worker/beat 与数据库未改。本批仍未提交。


覆盖资产管理演员/场景/道具/服装卡片、五类资产（含项目角色）各角度详情、项目工作台五类资产卡片，以及分镜工作室首帧/关键帧/尾帧和当前视频。新增“修改图片 / 修改视频”：从文件管理选择或上传素材→预览→确认采用；上传本身不修改业务对象，取消后文件仍可在文件管理复用。资产卡片快捷修改当前展示的正面/封面槽位，项目卡片修改正面图，其他角度从详情修改；原图片/视频文件保留，不发起模型任务。

共用 ManualMediaButton 与现有 MediaFilePicker/FilePreviewModal、generated 文件客户端；新增 media-assets/manual-selection 的 GET 快照与 POST 手工采用接口，业务放 services/studio/manual_media.py。服务核对七类目标归属、图片/视频类型，原子比较并递增槽位/视频版本；空槽位在明确采用时创建，不能仅打开窗口就写入。手工采用使用 API/网页发布器相同版本字段，旧生成不能覆盖用户新选择；冲突必须重新核对。镜头信息确认状态、时长、提示词不因采用而更改；镜头/项目角色/共享资产项目引用同步，原文件不删除，不伪造生成候选记录。既有“参考素材”关联用途保持独立。

验证：48 项后端回归通过（9 项新手工采用、39 项既有网页任务/交接），OpenAPI/generated 已同步，tsc 与生产构建通过（保留原大包提示）。7 类目标隔离浏览器验证覆盖文件选择、上传预览、显式采用、409 冲突、对象切换和窄屏；实际路由覆盖资产列表/详情、项目工作台五类资产和工作室帧图/视频，测试项目没有道具/服装关联时只在浏览器响应内补关系，不写入生产。无真实生成/收费、无生产素材替换；生产正式读取验收和部署状态见 local-reports/manual-media-20260923/runtime.json。

映射 LC-005/006/007（复用/预览/用户入口）、LC-009/012（契约、目标与版本保护）、LC-014/015（验证、文档与成片素材）；PL-004 本次入口阶段完成，原架构收尾与其他全部未关闭计划保持，不关闭 PL-020 网页生成真实验收。源码备份 backups/manual-media-20260923；无数据库结构/迁移、无 Git 提交或清理。


## 2026-09-23 同批追加：账号状态提示精简

## 2026-09-23 同批追加：账号状态统一

生成弹窗、账号下拉与网页平台账号页共用 webAccountStatus，同一后端 readiness 显示相同的“可用 / 待登录 / 待验证 / 忙碌，可排队 / 不可用”；登录、验证与不可用处理提示一致。账号页不再使用旧 session_state 长描述，不再宣称历史型号缓存是分配门槛。执行器连接及模型适配信息与账号状态分开显示；自动分配汇总最佳可受理账号，受限邻居不影响整池。全局模型或本机助手阻塞仍由提交检查单独解释。

类型检查、生产构建及两个真实组件的只读浏览器验证通过：可用、待登录状态跨页面一致，登录等待仍可提交，不可用提供管理入口，无业务写入、无新生成。验证夹具初次导入方式错误已修正；不计为业务验收。前端发布记录见 local-reports/web-account-readiness-20260923/runtime.json；compact-ui.json 为页面证据。LC-006/010/011/015（交互）、LC-001/014（验证部署），PL-020 仍未关闭，工作树待提交；其他未关闭计划不变。


按用户要求，生成弹窗不再逐账号展示执行器离线、型号尚未核实等重复技术明细。可用仅显示“账号可用，可提交任务”；需要登录/验证或排队时保留一句自动继续说明；不可用时显示首要原因及重新检查/管理账号等必要入口。账号下拉标签精简为可用、待登录、待验证、忙碌可排队或不可用。后台整池分配与唯一任务绑定规则不变。

tsc/生产构建通过，可用/等待登录/不可用三种界面状态测试通过，验证按钮可用性、不可用操作入口和零业务写入。前端已发布，证据 local-reports/web-account-readiness-20260923/compact-ui.json；该目录 runtime.json 为最终镜像记录。LC-006/010/011/015（交互）、LC-001/014（验证部署），PL-020 保留未关闭，工作树待提交。


## 2026-09-23 账号可用性修复已部署

已登录账号不再因未探测型号或执行器关闭而不能接单；未登录/验证码可先受理，原账号完成人工操作后自动继续。忙碌账号支持排队。自动分配优先已登录空闲账号，单账号异常不阻断整池；仅未领取的自动任务可调整失效预留，已执行任务/指定账号不换号。刚关闭的执行器不再因新鲜退出心跳等待 90 秒才唤醒。实际型号在执行时核实，已适配边界、停用、目录排除与实际权益限制保持独立说明。

41 项后端、5 项执行器回归及 OpenAPI/tsc/构建通过，真实后端的三豆包账号和 Seedance 2.0 Fast 弹窗只读验证通过，0 新生成。详见可靠性计划同日批次；LC-001/006/010/011/014/015，PL-020 未关闭，工作树待提交。


## 2026-09-23 参考附件消息组修复已生效

豆包可能把参考图与提示词拆成多条用户消息。执行器现在按独占会话中的完整消息组绑定原任务，核对冻结附件数量，保留文本/附件各自 ID，图片与视频均从组后采集。无附件、单附件、多附件及前后/合并布局已有离线覆盖；未知空节点或额外输入不猜测归属。实际单参考图“铅笔”任务已恢复原结果并正确回填第 7 槽位，账号释放，未重新生成。文件校验及真实业务页通过。详见可靠性修复计划同日批次；其他平台与新增官网多附件/长视频仍待验收，PL-020 未关闭，LC-011/012/014/015，工作树待提交。


## 2026-09-23 用户确认：所有网页模型沿用统一生成要求

用户再次明确：流程畅通、响应快、尽量全自动；登录/人机校验完成后自动续跑；结果尽量 100% 正确归档并回填，多个结果全部保留且在原业务入口选取；覆盖同模型、同模型同账号并发提交、跨账号及多模型并发，不能串单。若可取得/处理得到无水印文件，优先回填无水印版本，保留原件与派生来源关系，不虚报能力。完整长期约束已固化在根目录 AGENTS.md“网页图片/视频生成统一要求”。

状态口径：豆包图片/视频通道已有实现，可作为参考；新生成长视频、实机验证码及并发组合仍有待验收项，同账号当前串行排队。其他模型尚未完成实现验证，登录或平台骨架不代表生成已支持。后续逐模型按相同标准实施与验收，不直接复制豆包专属页面假设；无水印能力尚未核验，100% 回填是目标而非既成保证。

本次仅固化用户要求与验收边界，未改代码、API、部署或业务数据，未新增生成。关联 LC-001/006/010/011/012/014/015，PL-020 保持未关闭，其他计划不变；工作树待提交。


## 2026-09-23 性能优化第一批已部署

宿主 1 秒、执行器空闲 750ms 接单轮询，心跳合并，参考图最多三路并行并保序校验。原件按任务/消息/来源逐个持久化检查点，重试只复用 SHA256 校验通过的文件；源变化不复用，视频缺来源保守重下。4MiB 分块下载与文件流 multipart 上传减少整文件搬运。前台活跃任务 2 秒观察，候选监听完成事件立即刷新；仍等整批原件完成后服务端归档。回执记录准备/提交/收集/归档累计耗时，未完全分离官网生成等待。

48 项相关离线用例、tsc/构建、原业务真实页面只读核验通过，0 新生成。前端与空闲宿主已更新；服务端推送、逐件归档、后端流式传输及真实新生成验收仍在 PL-020，其他未关闭计划保留。详见可靠性计划同日批次；LC-001/006/010/011/012/014/015，工作树待提交。本节优先于下方历史实现。



# 本地二开能力清单

## 2026-09-23 同日追加：候选选择回到原业务页面（已部署）

用户指出任务详情链路过深。本追加已完成并部署：资产卡片/资产编辑页、工作室对应关键帧与视频区域直接显示“选择生成结果（N）”，就地弹窗按生成批次预览、下载、采用，无需跳转任务中心/网页任务详情。自动网页任务受理后留在原业务；人工交接仍进入交接单。结果按原对象/槽位查询，刷新或返回可恢复；新结果提示不抢焦点。

复用 WebResultCandidates，后端 handoff/jobs 增加 target_type/entity_id/slot_id 服务端筛选，摘要不返回提示词；generated client 已同步。主动换选使用 expected_version + expected_current_file_id 双重 CAS；允许用户在保留全部候选的前提下换选旧任务结果，迟到请求不得覆盖新选择；自动回填的冻结版本边界不变。未提供新字段的旧采用客户端仍按旧原版本规则处理。

验证：56 项相关后端再次通过；隔离 React 验证就地采用、当前文件/版本 CAS、切换对象和 540px 弹窗边界；tsc/生产构建通过。真实道具编辑页已验证“选择生成结果（10）”、批次选择与图片加载，URL 不变，页面零异常、零写请求。证据 local-reports/web-flow-20260923/inline-ui.json、inline-candidates.png；最终 runtime-inline.json 核对 349 源文件与生产 schema 一致。当前 API 镜像 c4354bd03c1c9f3c19cb4623644b0e1a4cbebf75f9bde5881ab0c252c0ddb1da，前端 df0d3f2f6774d646ed61f596333b7ffd88ae839173b7885e9e29b74ee4f345b8；latest 已同步，第一批修复镜像另留 rollback-inline-20260923 标签。

本追加映射 LC-006/010/011/012/015（用户入口、结果采用）、LC-001/014（契约/测试/部署/文档），PL-020 继续。工作树待提交；0 新官网生成，不改变其他未关闭计划。以下第一阶段镜像与 UI 操作位置仅作为同日历史记录，以上文为准。

## 2026-09-23 网页生成续批（PL-020，工作树待提交）

本节优先于下方历史状态。2026-09-20 改动由用户通过其他智能体完成；当前 E:\JellyfishNew，local/stable-codex-0718，HEAD 700333cb17e926caa73f68070cec4803c02a4d78。开始时已有 569 项工作树变更，保留原有改动，禁止整体回退。

- 持久化 dispatch_account_id，跨轮询仅唤醒任务预留账号，优先在线账号；指定账号不换号。同账号串行、跨账号并行。复用已核实型号缓存，取消每次任务前后重复探测图/视频入口，实际提交仍核对型号。
- 新任务使用独占空会话，绑定本地任务 ID、账号、官网会话 ID、用户消息 ID 与结果消息 ID。v2 不用提示词替代缺失 ID，冲突时保留原证据。旧回执兼容恢复，不能伪造为 v2；ID 缺失或人工干预时不保证全自动来源证明。
- 图片可等待文字确认后的延迟回复；图/视频可一次归档 2—16 个原文件。全部关联原任务，完成后释放自身账号租约；多个候选不擅自以第一张覆盖业务槽位，用户在网页生成详情选择采用，仍校验原目标、原文件与版本。重复归档幂等，局部修图的原件不提供候选采用按钮。
- 登录/验证码使用 awaiting_user，人工完成后自动继续原任务；长生成 WAITING_RESULT 不消耗异常重试额度。明确暂停、归属冲突和权限变化仍需处理。取消由循环检查，不等待官网生成完毕；不自动重发受理未知生成。
- 新接口 POST /api/v1/studio/web-generation/tasks/{task_id}/results 仅执行器可调用；OpenAPI/generated 已同步。56 项网页后端、44 项主执行器/隔离 Edge 回归通过；补充多视频 ID 与官方 blob 下载验证通过。tsc/生产构建通过，原大 bundle 提示保留。0 新官网生成、0 付费重试。
- API/前端已定向部署，后端仅 5 文件相对上线前 API 变化，无 schema 变化、不执行迁移。备份 backups/web-flow-20260923：SQL、上线前 API 源码、原任务回执；旧镜像标签 rollback-web-flow-20260923。候选镜像 jellyfish-backend:web-flow-20260923 / jellyfish-front:web-flow-20260923，部署 overlay .local/web-flow-20260923/compose.yml。默认 latest 标签也已同步本批镜像；恢复旧版需显式使用回滚标签。
- 真实旧结果恢复成功：828acf68098b5825ab4337039c7c0d43 = 4 张，e17740dd67dd54c895c00b10af54c245 = 3 张，b543dfec057b55fdaa2e08fb57ddb17c = 3 张，共 10 张。用户确认三任务都是官网多图已生成未回填；通过同一任务重启前诊断中的唯一用户消息 ID 补齐旧锚点、原窗口实际核验、官方下载、原文件归档完成。任务均 succeeded/completed，账号释放；候选已归档但未自动采用到槽位。未新生成或取消旧任务。
- PL-020 未关闭：新图片/长视频、多账号同型号及多模型并发、验证码实机协作仍待新增生成验收。六秒 DOM 稳定观察不等于官网最终产物总数契约，极迟新增产物仍需核对。真实收费验证需本轮单独授权，旧结果恢复不能替代新生成验收。
- 历史部署差异保留：worker/beat 仍历史镜像，backend-migrate 上次失败于 MySQL 连接；API/前端沿用原端口配置，不宣称仅 localhost。未改 RustFS/业务卷或清理备份。9/20 自动拉起条目误用 PL-021，本续批归 PL-020，原 PL-021/022 运维语义及其他未关闭计划保留。

本批最终核验：349 个后端源码文件与运行容器一致，生产 OpenAPI 与 generated 源 schema 一致；API 镜像 020a8f302a31e844c1a8dc8da11c9a863386cdd364057f01ad3a4097d06aeab8，前端 ef13336480f2dbe2d6776b835ee940bc1af96f18c4eae66805a2829c5615f2e5。三个账号均 ready 且 active_task_id=null。三个真实业务详情展示 4/3/3 张候选，10 个文件下载 SHA256 全部与原执行器回执一致，页面零异常、零写请求；验收时第三任务已有 1 个候选被用户采用，未改动该选择。证据 local-reports/web-flow-20260923/{runtime.json,recovered-results.json,git-diff-name-status.txt}。默认 jellyfish-backend:latest / jellyfish-front:latest 已同步新镜像，旧镜像回滚标签保留。已审查 git diff --name-status（256 个历史及当前已跟踪变更），未提交或回退其他智能体改动。

语义映射：账号/交互/恢复 LC-006/010/011/015；身份/多产物/CAS LC-011/012/015；契约/验证/部署/记忆 LC-001/014。代码、离线测试、部署、旧结果恢复、新生成验收分开记录。


## 当前交付：2026-09-17 四批可靠性修复已部署

四批工程实施已完成并部署，原十步逐项映射见 site/content/docs/plans/web-generation-reliability-repair.md。后端全量795项、Node/隔离Edge34项、Windows模拟与真实启动、OpenAPI/generated、tsc/生产构建通过。正式资产场景/分镜首帧目标与版本、视频三种布局、账号登录启动、暂停恢复取消与小屏回归通过，写请求全部拦截，0新增官网生成。

北京时间18:57：API/worker/beat/migrate同镜像26435c4fc9a615908e3a576bda60e5d6216ae191ce8814d389ea603e530b8c28，前端b6d5ad7da0fd84ada2127c9f9b0d7edd419a996020c31278406d8a75e4e9d6e7；migrate退出0，DB仍b4d6f8a0c016，worker ping pong，助手在线。349后端源文件与运行容器一致，正式schema与front/openapi.json一致；隔离迁移55表内容不变。备份 backups/web-deployment-20260917-182154，回滚标签 rollback-reliability-20260917，未删除任何旧备份/业务卷。

原任务9d2428f0ab475c52bab41fe26ef3f5c1仍为受理未知，已迁移为持久暂停，账号与回执保留。可从网页生成任务详情“恢复原任务”或“取消并释放账号”；不重发、不宣称已回填。新版本真实图片/视频产物、多参考官网语义顺序和其他平台仍按PL-020待验收。PL-021/022运维修复完成；PL-016/017原业务含义及其他未关闭计划保留。未提交Git，仍为工作树待提交批次。

本批语义映射：账号/入口/恢复LC-006/010/011/015，媒体与回执LC-011/012/015，守护/镜像/迁移/测试/文档LC-001/014。已审查git diff --name-status和未跟踪清单，并以本批前源码SHA隔离原有二开。验证详情及剩余边界见四批计划；全树历史generated EOF空行与原大bundle提示保留。

## 本轮阶段证据（以下为部署前记录）

第二批更新：持久暂停与恢复版本CAS、原结果人工接手、取消后迟到报告保护、自动拉起退避、独立Windows守护已实现。49项后端、33项Node/隔离Edge、Windows模拟入口及真实React恢复/取消页面通过；OpenAPI/generated与tsc通过。暂停仍保留原账号，可在网页生成任务详情恢复原任务或显式取消；只有已绑定回执且暂停的任务可人工上传原件。关闭页面不取消。第三批部署处理中，候选代码不等于生产已更新。当前Redis鉴权只读探测正常，旧Celery仍停止；积压2条官方资料同步、2条已取消outbox，无待执行收费任务。

用户授权连续实施四批；LC-001/006/010/011/012/014/015，PL-020延伸。第一批代码及离线验收完成：账号池任一合格账号可用、完整候选扫描、助手离线提示；图/视频共享消息匹配、视频容器去重、严格型号/画幅/时长确认。原件下载不点击任意高亮按钮，多候选不默认首张；规格不符保留历史，时长容差min(0.5秒,3%)、画幅相对误差3%。后端45项、Node/隔离Edge32项通过，0真实生成。源码快照 backups/web-reliability-20260917-175335（1471文件及SHA清单）。第二批恢复/启动闭环进行中，部署未开始，不能将候选当作当前运行服务。完整十步及四批映射见 site/content/docs/plans/web-generation-reliability-repair.md。

原PL-016仍为多参考创作、PL-017仍为非阻塞任务；此前重复编号的Celery和迁移问题分别改用PL-021/PL-022，原未关闭计划不丢弃。历史“最新状态”按原日期解释，原“采集暂停即释放账号”表述不成立：暂停保留占用，显式取消才释放。

## 2026-09-08 新电脑部署与数据迁移说明（LC-001 / LC-014）

工作树待提交批次。新增 docs/Jellyfish-新电脑部署与数据迁移.md，主手册、Windows说明与升级 SOP 已设置入口。区分空库和旧数据恢复；覆盖旧机冻结/备份/校验、未提交二开、WSL/Docker与数据盘、新机配置、先恢复后迁移、延迟启动worker/beat、文件验收及双机/回滚/清理边界。迁移先恢复同版本，不顺带更新上游；日常脚本只适用于已创建容器。主手册补充 run --rm 后一次性容器不存在属正常。

本批仅文档，依据当前 Compose、Dockerfile、迁移入口及 Docker 官方安装/WSL/Compose说明核对；检查代码块 PowerShell 语法和文件链接，不代表新机部署/备份恢复演练通过。未安装软件、未迁移/重启、未推送或调用模型；既有 PL 待办不变。换电脑后需更新本记忆实际路径/版本/数据快照。

## 2026-09-08 启停说明统一（LC-001 / LC-014）

工作树待提交批次。直接改正中文手册第3节：日常启动/停止统一调用根目录 CMD 的共享 PowerShell 实现，不再推荐 compose up -d 作为 start 的等价替代；停止实际旧命令是 compose stop，不是 down。新增重启、加载 .env、代码部署、上游升级的边界表，修正“启动必须加载安全覆盖”的泛化说法，并为第9节全栈升级示例补充备份、隔离验证、迁移与数据容器重建范围的前置条件。

当前事实：日常脚本沿用已有容器环境与镜像，不加载修改后的 .env；运行状态查询加载 Compose 不等于配置生效。配置修改需要审查后重建受影响服务，数据库/存储凭据需协调变更。WINDOWS-SCRIPTS.md 同步；本批只改文档，不改脚本行为、不启停/重建/迁移/推送。验证为原章节与脚本对照、旧歧义检索及差异检查；既有12项模拟测试是前批证据，本批未重跑业务测试。所有 PL 待办保持。

## 2026-09-08 Windows 脚本复查修正（LC-001 / LC-014）

工作树待提交批次：共享脚本新增 API/页面 HTTP 就绪检查和最终容器复核，避免 running 即报成功；停止/提交前输入 YES 确认，自动化需显式 -Yes，Preview 始终只读；add 后再次检查实际索引敏感路径。三份根目录 CMD 入口路径与退出码处理正确，保留不变。详细用法见根目录 WINDOWS-SCRIPTS.md。

12 项隔离模拟测试、三项本机只读预览、UTF-8 BOM 检查通过。未实际启停、暂存、提交、推送、迁移或调用模型。停止仍需人工确认任务已结束；推送包含全部未忽略改动，文件名防护不能代替源码内容密钥审查。当前 personal origin 与固定分支通过本地预览，未验证远程登录权限。所有业务 PL 待办保持不变。

## 2026-09-08 Windows 运维入口（LC-001 / LC-014）

根目录三份 Start/Stop/Push-Jellyfish.cmd 复用 scripts/windows/Jellyfish.ps1，支持只读 Preview。日常启停仅操作七个现有常驻容器；顺序、健康等待、项目目录验证及失败中止已实现，不执行迁移/seed/构建/删卷。RustFS 的旧目录标签通过当前业务卷与后端共享网络核对兼容，不重建数据服务。

Push 固定个人仓库和稳定分支，提交全部未忽略差异；检查冲突及常见敏感路径，普通推送、不自动合并或强推。文件名检查不等同完整密钥扫描，测试与语义台账仍由维护者完成。三项只读预览与七项模拟测试通过，未实际启停或推送。操作见根目录 WINDOWS-SCRIPTS.md。

## 2026-09-08 跨目录与新会话项目记忆（LC-014）

根目录 [PROJECT_MEMORY.md](../../../../PROJECT_MEMORY.md) 已作为上下文交接入口，AGENTS.md 要求新任务先读并在每批二开后更新受影响内容。它集中记录核心目的、架构/状态边界、证据分层、当前快照、未关闭计划及升级规则，详细能力仍以主台账为准。

入口及引用采用仓库相对路径，随候选代码智能迁移；换目录必须核实运行服务是否切换。此机制是文档与协作约定，不是跨对话自动记忆或后台同步服务。本批只更新文档，无代码、数据库或部署变化；既有真实调用及浏览器验收待办未关闭。

## 2026-09-08 总览筛选下拉菜单被遮挡（LC-011 / PL-014，已修复并部署）

工作树待提交批次。用户反馈总览四个筛选框无选项；核对API仍返回67条型号/11个场景，组件选项存在。根因：本地Ant Design 5.10.0的Select默认zIndexPopup为1050，低于总览Drawer的1200；菜单挂到body后被抽屉遮挡，并非模型目录丢失。

- 修复：ModelSelectionGuide局部ConfigProvider统一设置Select菜单层级为GUIDE_LAYER+50（1250），覆盖四个筛选框及Pagination每页条数菜单。保留body挂载，避免抽屉滚动容器裁剪；不改全局主题，不影响其他弹窗。
- 测试：前端16项通过、tsc与生产构建通过（仅既有大bundle警告）。新增四类选项非空、筛选结果、局部菜单层级高于抽屉、页脚分页继承主题的组件契约测试。测试转译target修正为与生产一致的ES2020，避免ES5下Map迭代造成测试假空列表。
- 部署：15:34（北京时间）仅构建并替换front；线上index-DrsraUNG.js及首页HTTP200，镜像446bda5c3b69。backend/worker/beat未重启，数据库、配置、默认模型及生成任务未修改，无收费调用、无迁移。旧前端镜像保留为jellyfish-front:rollback-overview-dropdown-20260908；未删除备份。
- 实机边界：本轮浏览器工具两次启动退出，未取得实际点击/截图证据；不将组件测试、静态资源检查写成浏览器验收。下一步用户Ctrl+F5后检查四个菜单、选择后筛选、每页条数及关闭重开。原PL-014未完成项保留。
- 迁移定位：front/src/pages/aiStudio/models/ModelSelectionGuide.tsx；front/tests/modelOverviewLifecycle.test.cjs。未来上游升级需保留或兼容该局部弹层规则，不靠增加模型数据解决遮挡。

## 2026-09-08 场景选型改为完整模型总览（LC-011 / PL-014，已部署）

工作树待提交批次：本轮在 E:\JellyfishNew 调整模型选型。用户追加“部署重启”授权后，2026-09-08 15:17（北京时间）构建并替换backend/celery-worker/celery-beat/front，本节总览已部署至7788。未修改生成执行链、数据库、凭据或默认模型；未运行迁移/seed，未调用收费模型。

部署验收：前后端health/首页200；model-overview国产范围62条（已保存7、未配置55）、全部厂商67条（已保存7、未配置60），均有11个场景；线上OpenAPI与index-aOZLRtSh.js均已更新并可读取。Celery ping为pong（1节点）。数据库revision仍为a7b9c1d3e508，任务计数仍为成功29/失败14/取消4；MySQL/Redis/RustFS容器ID及启动时间未变。

回滚保障：四个旧应用镜像保留为jellyfish-<服务>:rollback-overview-20260908-1515（服务为backend、celery-worker、celery-beat、front），既有备份未删除。新镜像前缀：backend a36155fa4588、worker 64a7cf54b7af、beat c5fbcdde9919、front f3161e072fd7。无需数据库回滚；如需回退只切应用镜像，不清空或还原业务卷。

- 新增只读 GET /api/v1/llm/model-overview，将离线精确型号目录与全部已保存模型、供应商合并；不依赖模型列表的分页/搜索，无模型配置也显示目录。默认国产，空数据库返回62个国产“型号＋类型”条目，可切全部厂商。
- 以供应商协议＋精确型号＋类别为唯一行，同型号多个账户分别展示、编辑；同名图片/视频不合并。分别呈现系统接入、配置状态、场景、使用限制、账户条件和官方文档。未配置、已保存待完善/核验、已配置待实测不混用；没有当前配置的真实验收证据时不冒充验证通过。
- 已保存目录外型号保留并标记待核验，不能借供应商默认媒体能力获得认证；自助兼容文本保留“协议适用方向”，不认证精确型号。无离线精确目录的厂商明确提示覆盖边界，不抓取凭据去自动发现型号。
- 拆出 builtin_provider_catalog 与原静态目录发现复用，保留阿里实时混合、火山404目录回退、DeepSeek实时目录路径；补齐已映射万相2.7图片、千问3.8文本与DeepSeek V4文本目录。MiniMax人物参考、即梦图片公网导出等PL-014待办仍未关闭。
- 前端由场景单项切换改为多列总览；支持场景、生成类型、供应商、配置状态、型号/账户搜索；已保存优先，分页固定Drawer页脚（8/16/32），切筛选回首页，关窗取消请求并丢弃旧响应。读取失败可重试，不持续转圈。
- 未配置型号：有供应商时预填型号/类型、多个账户时由用户选择；无供应商时引导到供应商页。已保存型号按ID实时读取编辑，不依赖当前分页或搜索结果。上述操作只打开表单，不自动保存、购买或改变默认路由。
- 迁移定位：backend/app/core/contracts/model_recommendations.py、core/integrations/model_catalog.py、services/llm/{model_overview,scenario_recommendations}.py、api/v1/routes/llm.py；front/src/pages/aiStudio/models/{ModelSelectionGuide,modelOverviewFilters,ModelsTab,ModelManagement}；OpenAPI及generated客户端；test_model_overview.py、modelOverviewFilters.test.cjs、modelOverviewLifecycle.test.cjs。
- 验收分层：代码与离线测试已执行，部署健康/API/静态资源检查已通过。此前浏览器工具两次因本机sandbox deny-read ACL错误退出，仍未取得浏览器截图或实机交互验收证据；不能用组件模拟或HTTP检查代替。未做收费调用，未删除任何备份。

编码阶段自检：全量后端590项通过；前端15项通过（含筛选、多个账户预填、关闭重开旧请求、失败重试与页脚分页契约）；OpenAPI/客户端重新生成，tsc及生产构建通过，仅既有大bundle警告。补修多个匹配账户选择时型号预填被清空的问题。18765隔离schema服务已关闭；15:17部署已完成，浏览器实机与真实生成验收仍待完成。

目录来源补核（2026-09-08）：[DeepSeek当前API型号](https://api-docs.deepseek.com/)；[百炼文本模型](https://help.aliyun.com/zh/model-studio/text-generation-model/)；[万相2.7图像API](https://help.aliyun.com/zh/model-studio/wan-image-generation-and-editing-api-reference)。这些是协议/目录来源，不代表账户余额、免费额度或已完成所有高级功能。

## 2026-09-08 国产供应商扩展（PL-014，已部署，真实生成待验收）

本批修改位于 E:\JellyfishNew，工作树待提交。用户追加授权“现在部署重启”后，2026-09-08 14:17（北京时间）已重新构建并替换 backend、celery-worker、celery-beat、front；仅应用服务重启，未运行迁移/seed、未修改模型配置、未购买或调用收费模型、未外发素材、未删除备份。

部署验收：前端7788、后端两处health均HTTP 200；场景建议接口返回11项、供应商支持接口返回16项；线上OpenAPI包含model-scenarios及requires_last_frame；前端资产index-DOMcQMbP.js可读取；Celery ping返回pong（1节点）。MySQL/Redis/RustFS启动时间未变；数据库revision仍为a7b9c1d3e508，任务计数仍为成功29/失败14/取消4，无进行中任务。镜像前缀：backend d2293f4d7b08、worker 513c282921dd、beat 3ccd121c95bc、front b6042c0ddb2d。

四个旧应用镜像保留为 jellyfish-<服务>:rollback-pl014-20260908，未清理业务卷或备份。部署健康验收不等于真实浏览器交互、账户额度或收费生成验收；以下PL-014未完成项仍保留。

- P0：新增 GET /api/v1/llm/model-scenarios（生成客户端调用）；场景选型从全部已保存模型计算，不受列表搜索/分页影响，区分禁用、缺少凭据、错误端点、能力不匹配和未知型号。页面默认国产，展示候选理由及官方文档，不冒充质量排行榜/免费额度核验，不修改默认模型。
- P1：MiniMax 文本复用原链；image-01/image-01-live 标准档文生图；Hailuo-2.3/Fast/02 视频提交→轮询→文件查询；HTTP TTS speech-2.8/2.6/02/01 的 hd/turbo 返回 hex MP3，接入镜头配音/音轨保存链。必须显式选择账户对应端点，配音配置 audio_endpoint 与 voice。
- P2：智谱 GLM 文本接入通用聊天/剧本链；GLM-Image/CogView 原生文生图；CogVideoX-3 文生/首帧/首尾双帧异步视频，5/10秒。明确不把图片类别当作支持参考图。
- P3：混元采用 2026-09 官方 TokenHub 中国站协议（不自动迁移旧账户），HY 文本、hy-image-v3（最多3图参考）、hy-video-v1.5（5秒720p文生/首帧）；即梦独立 Visual AK/SK 签名，t2i_v40_jimeng 标准2K单图、jimeng_i2v_first_tail_v30 的720p首尾双帧5/10秒。
- 共用原生成任务、受控文件解析、图片/视频结果归档和业务发布；新增 requires_last_frame 从能力→API→工作室/实验室校验。新厂商不支持的素材、数量、时长、档位及最终提示词预算在提交前拦截；网络/结果错误不自动重复提交。不对既有厂商强套新门禁。
- 凭据：扩展执行期 ProviderConfig.api_secret（repr 隐藏 AK/SK），仅通过当前凭据引用读取；不写任务快照。现有 Provider 已有 api_secret 列，因此本批无新增数据库迁移。Pillow 新增到 pyproject/uv.lock，用于参考图片格式、尺寸、比例检查。
- 官方核查：新增国产文档域名白名单（保持无凭据、限制大小、不跟随重定向）；配置弹框增加开通/购买前置说明；未实现“自动读网页后自动修改接口代码”，旧 PL-007 待办仍保留。
- 最终自检：后端全量571项通过（新增45项国产适配定向用例），前端刷新/帧图回归7项通过；OpenAPI/客户端重新生成，tsc及生产构建通过（保留既有大bundle提示）。补测未知旧厂商型号不得借默认能力获得推荐。关闭了仅用于导出schema的18765临时服务，未停止生产服务。Mock不代表真实账户权限、出片质量、实际费用或浏览器验收。

### PL-014 未完成项及下一步（升级迁移必须携带）

1. MiniMax 人物参考协议已有适配代码，但业务尚无显式人物语义确认入口；当前拒绝通用素材，场景推荐不声明可用。补充入口和证据后再开放，不能把场景/服装图当人物脸。
2. 即梦图片4.0官网要求公网 image_urls；当前本地 RustFS 文件不能直接被厂商访问。当前仅文生图，公网签名导出需单独安全方案/授权；不开放匿名桶或偷偷第三方上传。视频双帧支持本地 Base64，不依赖公网导出。
3. 高分辨率扩展、MiniMax S2V 主体参考、音色克隆、其他语言/情绪精细矩阵、火山独立 TTS、音乐音效仍待后续接入；不要标成全部音频能力。
4. 远程取消/退款不作保证；本地取消停止等待，已提交远端可能继续计费。远程任务 ID 尚未实现提交即持久化和进程崩溃后接续轮询；不得宣传自动恢复或自动重提。
5. 用户实际账户的小额授权验收、真实浏览器交互、跨型号结构化剧本质量、产物质量和片段音轨对齐待验证。API 额度统一未核实；未承诺附件中的免费额度仍有效。
6. 剪映仅作为后期交接工具，不注册模型；PixVerse 区域/账户归类未定，本批不纳入国产推荐。已有百炼、方舟、可灵、Vidu 不重复建供应商。
7. 上游升级按既定流程智能对比并迁移本批文件、生成契约、测试和文档；不得整目录覆盖、漏迁未完成项或未经验证删除旧备份。

主要代码：backend/app/core/integrations/{minimax_images,minimax_video,minimax_speech,domestic_media,jimeng_media}.py；core/contracts/{provider,model_recommendations}.py；services/llm/{scenario_recommendations,provider_bootstrap,provider_resolver,integration_audit,documentation}.py；services/generation/{domestic_preflight,prompt_budget,gate}.py；core/tasks 的注册/分派；shot_tts、image_task_runner、generated_video 的执行凭据；front/src/pages/aiStudio/models/{ModelSelectionGuide,DomesticProviderSetup,ProvidersTab}.tsx；ChapterStudio、VideoExperimentMode；测试 test_domestic_provider_media.py、test_provider_execution_matrix.py。对应 LC-010/LC-011/LC-012/LC-014，计划 PL-014，不覆盖 PL-007/PL-013 历史与待办。


## 2026-09-08 部署确认（PL-013，当前运行版本）

已获用户授权，约 11:51（北京时间）从 E:\JellyfishNew 构建并替换 backend、celery-worker、celery-beat、front；部署后健康及资源核验通过。本节部署状态优先于下方历史“候选未部署”记录。工作树待提交，本次未变更业务代码。

- 使用同一 jellyfish Compose 项目、.env 和 secure-local 覆盖，执行 up -d --no-deps --no-build --timeout 60，仅替换应用四服务。未运行数据库迁移/初始化，未重启 MySQL、Redis、RustFS，未删除业务卷或既有备份；迁移版本仍为 a7b9c1d3e508。切换前没有 pending/running/streaming 生成任务。
- 后端 /health、/api/v1/health、前端首页及新 JS 均 HTTP 200；前端 index-C0l3Rg-U.js 包含编辑接口。线上 OpenAPI 已出现 video-edit-preflight、quality-review、video-edits、video-edits/{task_id}/adopt；Celery Worker ping 返回 pong，定时任务正常投递执行。
- 运行镜像摘要（sha256 前缀）：backend f847190a2b15；worker 0eb771c74f0a；beat a8306190fe27；front 28dbf18299c3。四个原镜像均保留 rollback-pl013-20260908 标签；不自动清理。
- 验证边界：这是部署/健康验收，不是付费端到端验收。未调用收费模型，未修改模型配置；真实浏览器交互、真实编辑输出/音轨对齐仍待验收，PL-013 不整体关闭。此前 526 项后端、7 项前端及隔离音轨验证保持为代码自检证据。

## PL-013 双供应商编辑与智能预检（2026-09-08 最新候选，未部署）

最新实现覆盖下方历史“尚未接通”的描述：已有 fal.ai/Kling O3 Pro 与 Runway Aleph2 独立视频编辑执行链，包含输入规格预检、源片与参考图、费用确认、任务幂等、历史、新旧比较和 CAS 人工采用。原视频不删除、不自动改时间线；Runway 保留音轨使用本地后处理，先归档付费原始结果。

首/关键/尾帧与视频提示词预览接入通用质量规则、来源指纹和可选 AI 预检。文本复用已实现的供应商协议；图片须明确选择，当前仅核验的百炼 Qwen 型号启用。来源覆盖项目、章节、镜头、邻镜头、关联资产/演员以及存在导入关联时的源文件哈希；未知语义不硬判断。模型预算逐精确型号核验，未知不伪造限制。

新增 GenerationSubmitRequest 可空字段、编辑输入契约与四个业务 API；生成客户端已更新。任务与来源使用既有 JSON/关联表，无新增迁移。编辑型号不能设为普通视频默认值。迁移必须一起核对 provider 注册→模型 revision→gate→worker→Artifact/TaskLink→历史/采用→UI，不能只合并供应商选项。

后端526项、前端既有状态/图库7项、类型与构建通过。隔离 Docker 合成素材音轨命令实测通过，脚本为 backend/scripts/verify_video_edit_audio.py；真实供应商输出仍需验收。浏览器运行工具失败，收费端到端和生产部署尚未验收；不能称生产已可用。Luma、百炼编辑、VACE、裁剪拼接及其余供应商视觉/更多预算仍列条件任务，详见 `../plans/video-quality-and-editing.md` 最新矩阵。二开改动与未完成计划后续仍须同步完整台账和使用手册。

## PL-013 本地来源冻结续批（2026-09-08，候选未部署）

统一门禁新增 quality_sources：提交时读取镜头所属章节和已关联资产，冻结实体 ID、字段、文本哈希及最终提示词逐字包含状态。章节只记哈希/摘录区间，匹配失败为未知而非语义冲突。视频 pack 改为按已关联 ID 读取资产当前描述，明确清空也生效。内部快照兼容旧任务；43 项定向回归通过，未部署、无迁移。预览一致性及语义审核仍未完成，来源链不宣称完整。

## PL-013 进度复核与执行追溯候选代码（2026-09-08）

GenerationEntityGate 已在供应商提示词编排后生成 quality_trace，现有任务 payload 保存最终提示词 SHA256、实际保留的规则及明确的 final_execution_prompt 证据范围。旧任务可缺省，不声称完整章节/资产证据或视觉检查；未改外部 API 或数据库 schema。22 项定向测试通过，未部署。P0 仍部分完成，P1/P2/P3 未完成；fal.ai/Runway 未接通。完整进度与剩余验收以 plans/video-quality-and-editing.md 顶部矩阵为准。

## 候选代码边界：PL-013 基础质量编排（2026-09-08）

工作树新增共享 generation_quality 契约与质量编译器，实际用于帧/视频提示词；预览提供有来源的规则报告，明确尚未视觉验证。修复局部连续性标记屏蔽其他指导、道具描述仅停留在上下文等问题。27 项后端回归与前端类型/构建通过。此批未部署，当前运行服务未改变；智能检查与视频编辑执行能力仍不具备，详见 PL-013。

## 分镜帧图库兼容补充（2026-09-07）

18:49 前端已部署，HTTP 200，资源 index-C7aWE9Y5.js；仅前端更新，后端任务和现有数据保持不变。

LC-003/LC-012/LC-015：图库同时查询 shot_frame_slot 与旧 shot_frame_image，按 file_id 去重，并从当前 ShotFrameImage 槽位读取已采用图，不依赖任务历史存在。完成生成/手动采用后刷新父层帧列表；帧任务在当前面板每 10 秒跟踪到终态，切换镜头后隔离迟到响应。数据库内现有帧归属未修改，不能跨镜头混用。4 项图库加载器与 3 项共享轮询测试、类型检查通过，部署状态见主台账最新验收。

## 视频输入约束补充（2026-09-07）

18:38 已部署至 backend/worker/beat/front；前端/API HTTP 200、Worker pong、容器修复代码确认加载，前端资源 index-Cg-bgXMb.js。本批没有数据库迁移或模型配置变更。

LC-011/LC-012/LC-015 的当前代码按默认视频型号检查参考模式：I2V 首帧要求不能被纯文本准备度绕过；单镜头保留显式帧 ID 和顺序，批量按每个镜头实际首帧提交，不把资产推荐误当首帧。火山 Plan 模型拒绝保留错误码与请求 ID，不做端点/型号/计费回退。46 项离线回归通过；不宣称火山套餐权限或真实视频已验收。部署结果以主台账本批验收记录为准。

当前正式状态（2026-09-07 18:15）：自助供应商与新协议批次已部署，生产 revision a7b9c1d3e508，backend/worker/beat/front 全部切换。486 项完整后端测试、前端类型/生产构建通过，7 个现有模型免费核查通过；2 个供应商、7 个模型、23 个文件未丢失。十家供应商与自定义兼容文本选项已返回，具体模态以注册矩阵为准。备份保存于项目 backups/provider-adapter-a7b9-20260907；不复制或替换线上业务数据库。无付费模型验证。下面保留的候选/未部署描述是阶段历史，最新状态以本段与主台账为准。剩余能力继续见 PL-007/PL-010。

候选验证结果：停电后新增供应商续批 67 项定向测试通过（含迁移回归），前端类型/构建通过；全量回归尚未完成。本批仍未部署，不改变当前线上六家版本。精确变更、未完成项与恢复启动记录见主台账 2026-09-07 自助供应商续批。

停电恢复核对：原有长期容器已恢复，MySQL/Redis healthy，前端/API HTTP 200；生产迁移版本仍 f6a8b2c4d507。未运行初始化/seed，未切换候选代码。自助供应商批次需 a7b9c1d3e508，不能把这次恢复启动当成新版本部署。

候选工作树补充（2026-09-07，LC-009/LC-010/LC-011/LC-012/LC-014，尚未部署）：供应商名称与 adapter_key 分离；兼容文本供应商可自助保存。Google/Claude/MiniMax 文本及 BFL 图片执行器已有代码，文本 Worker 保留 revision 协议，AgentBase 对无工具协议只做单次文本结构化提取。BFL 图片返回统一产物契约。新增可空字段迁移 a7b9c1d3e508 待人工确认执行。当前生产仍是下述六家版本；不得把候选代码描述为线上已生效。未完成模态和智能文档治理继续见 PL-007/PL-010 与主台账本批记录。

官方文档读取/场景选型新批已部署：backend/celery-worker/front 已切换，113 项回归、前端类型与生产构建通过，HTTP 200 和新页面 bundle 已确认。无数据库迁移或付费模型调用，未做浏览器交互验收。

2026-09-07 新批（LC-011/LC-012）：DeepSeek 文本接入与协议声明启动检查；百炼专属 enable_thinking 不再自动发给其他文本供应商。核查页新增官方正文读取（纯文本、时间/哈希、安全限制、无模型调用），模型页和添加/编辑弹窗新增场景选型说明。46 项定向测试与公开文档本机读取验证通过。正文尚未保存为长期证据，AI 语义核对/自助扩展与另外四家接入仍属 PL-007/PL-010。

模型接入首批部署验收：backend/celery-worker/front 已切换，7 个已配置模型的只读核查实机通过，HTTP 200，80 项定向测试、前端类型/生产构建通过。官方文档来源由 ProviderSpec 注册元数据提供；自动语义核对和自助扩展供应商仍未实现。

2026-09-07（LC-011/LC-012）：模型管理更多菜单新增免费只读接入核查，包含当前配置、官方文档入口、四模态业务检查清单与套餐风险；不自动抓官网、不宣称具体模型已核验、不改变模型可用状态。新增 API/generated client，无迁移。LC-004 增加拒答/截断分流；LC-008/LC-012 增加媒体 HTTP 安全诊断及 TTS 家族/指令约束。78 项离线定向测试、前端类型检查通过。完整智能核对仍在 PL-007/PL-010。

能力边界补充：当前深度分析尚未实现按供应商/具体模型自动选择原生结构化输出；现有兼容仅覆盖消息外壳及安全本地校验。五家供应商官方文档审计与缺口见 `../plans/model-aware-script-analysis.md`，归 PL-010/PL-007，不能把未读到的文档或未测试模型标记为已接通。

部署状态更新：下述 2026-09-07 深度分析与资产槽位/音频校验修复已部署到 backend/front/celery-worker；此前暂缓状态解除。Worker 源码哈希已比对，33 项定向测试通过（含三种模型响应形式），无真实付费验收。

2026-09-07 深度分析调用修正（LC-004，工作树待提交）：专用 Agent 把完整输出 Schema 与语义规则传给模型，仅执行一次显式调用，不再结构失败后静默重跑；明确标量包装安全转换并保留证据与保守来源，缺失身份/归属继续拒绝，错误不输出剧本文本。空候选及证据校验后全空失败。未实现的响应留存、重解析和阶段进度列入 PL-010，不作为当前能力。

2026-09-07 资产图片目标修复代码（尚未切换服务）：五类图片表自增 ID 并非全局唯一，Gate、版本读取、Publisher 均按槽位 ID + 父资产 ID 约束。音频选择校验扩展名和大小，MP4 不作为音频资产接收；原始视频文件仍可在文件库使用，不自动删掉失败创建留下的文件。对应 LC-006/LC-008/LC-012；因剧本分析仍在运行，本批仅构建完毕，部署待任务结束，详见主台账。视频 Plan 接口 404 仍归 PL-007 待核对配置。

2026-09-07 刷新补齐（LC-002/LC-003/LC-010，工作树待提交）：资产卡片和详情共用已提交任务终态跟踪，10 秒串行查询，不再 60 秒后放弃；成功刷新图片槽位和卡片封面，不重置基础资料表单。模型导入提交事务后响应，前端合入新记录并切换所保存的分类、清空搜索/复位分页。回归包括前端跟踪器 3 项、LLM API/事务响应顺序 12 项及 typecheck；没有新增业务数据迁移或付费调用。

本文只记录 `local/stable-codex-0718` 当前真实生效的二开能力。详细文件、提交和迁移边界见仓库根目录的 `docs/Jellyfish-二次开发变更清单.md`。每次二开代码、数据库迁移、配置或用户流程发生变化，必须在同一提交中更新两份文档；上游升级时，不以“提交能否直接 cherry-pick”为判断标准，而以业务结果、数据契约和验收条件为迁移基线。

最后核对本地提交：`fa6a75d`。核对新版本时还应执行 `git log`、`git diff` 和测试，不能只依赖这个静态提交号。

## 当前能力

| 领域 | 已生效的二开能力 | 上游升级时必须保住的结果 |
| --- | --- | --- |
| 中文化与部署 | 中文界面和配置使用手册；`E:\JellyfishNew` 部署；本机端口约束；MySQL、Redis、RustFS、FFmpeg 与安全 Compose 覆盖 | 不泄露 `.env` 和密钥；不把服务意外暴露到公网；不丢 E 盘部署约束 |
| 数据刷新与列表 | 项目、章节、资产、提示词、模型等变更后刷新；非系统数据可修改/删除；章节及任务分页和可视高度修复 | 新增、修改、删除后无需整页重进；系统内置数据保护规则不退化 |
| 任务中心 | 异步任务持久查询；默认未结束；范围与行状态统一语义；终态和类型筛选；失败原因按需查看；低频轮询；固定筛选/分页区；重复收起展开恢复；分镜任务成功前校验实际可落库结果 | 完成任务可查询；已取消不误显“取消中”；失败可定位；加载态可退出；禁止“任务成功但分镜为 0”的假成功；任务中心保持通用轻量 |
| 剧本导入 | TXT、Markdown、PDF 文字层、DOCX 的后端语义解析；解析器 1.1 将标题时间范围独立结构化，并把不同格式统一为保留“主题/画面/镜头/时长/配音/字幕/画面提示词”标签的业务正文；未知内容无损保留；章节级解析质量提示和格式规范帮助；人工保存后才进入分页草稿/历史列表、继续编辑、未提交草稿删除和已导入记录只读追溯；用户明确同意后的证据化 AI 分析；分析取消/失败与任务终态对账；资产匹配/人工决策；章节、资产、镜头、对白和声音计划事务提交；视频模型能力规划 | 上传解析只形成当前窗口的临时工作记录，关闭未保存预览会清理该记录且保留原文件；原始文件和来源块可追溯；分析取消后恢复可操作且历史卡死状态可自愈；删除草稿不删原始文件，已导入历史不允许修改/删除；不能只保留字段值而丢失语义标签；推荐格式不能成为固定模板门禁；外发前显示并冻结供应商/模型；候选默认不落库；项目级和作者提示只作软参考；导入不自动触发图片/视频付费生成 |
| 文件与素材库 | 图片、视频、音频、文本和文档统一上传；未关联文件进入全局文件管理；搜索、分类、分页、预览、下载、删除保护 | “仅上传”仍可下次选择；图片可完整/放大预览；视频可网页播放；被引用文件不可误删 |
| 资产素材关系 | 演员、角色、场景、道具、服装可选关联素材；主参考、排序、用途、说明、启停和解除关联 | 关联始终可选；未关联时文字流程正常；解除关联不删除底层文件 |
| 资产安全删除 | 删除前由服务端列出具体项目、章节、镜头、角色、对白、音频及参考文件影响；确认后在同一事务中先显式解除关联、再删除资产 | 影响查询失败时禁止删除；任一步失败整体回滚；底层上传文件继续保留 |
| 生成素材引用 | 资产图片和分镜视频按模型能力智能采用显式/自动参考；文档正文作为软上下文；不支持的媒体不强发 | 用户显式选择优先；素材只作柔性参考；任务快照保存稳定 `file_id`，不保存密钥或大段 base64 |
| 配音音效 | 配音、旁白、背景音乐、环境声、音效、转场音素材库；镜头音轨；剧本导入声音计划；阿里百炼对白转配音；可选 FFmpeg 后期合成 | 声音建议与真实音频文件分离；AI 配音必须显式确认；临时供应商 URL 立即归档；音频失败不抹掉已成功视频 |
| 模型供应商 | 阿里百炼、火山引擎、Vidu、OpenAI、可灵等模型目录、能力约束和文本/图片/视频适配；独立 audio 类别当前仅声明已打通阿里百炼；四类默认模型和真实连接测试 | 管理页展示能力必须与执行适配器一致；供应商专有参数不得串用；付费生成需显式触发 |
| 成片交付 | 真实镜头草案与服务器剪辑工程；裁剪排序、原声与多音轨混合、字幕校时；固定版本导出与人物人工复核；MP4/SRT预览下载 | 不用 Mock 片段冒充业务时间线；默认不静默跳过缺片；导出不调用模型且不破坏源镜头 |
| 统一生成链路 | 基于作者 generation contracts、submission、outbox、runtime、publisher 和 artifact 架构提交任务；取消和失败状态可恢复 | 不恢复作者废弃旧链路；任务创建失败必须可见；数据库时间默认值和 Beat/Worker 分派正常 |
| 提示词编排 | 10 个生产系统模板；资产/场景图片和镜头视频真实渲染；连续性方法；Wan/Vidu 最小供应商规范适配 | 模板不是孤立配置；预览、冻结快照和实际执行提示词一致；自由实验室提示词不被暗改 |
| 图片实验室结果 | 图片 Worker 支持 experiment_session，归档文件、任务关联及会话结果一起提交；无效目标先于模型调用拒绝 | 实验室无资产槽位 CAS；成功预览使用归档 file_id；对象归档后数据库回滚的历史记录需凭确切证据恢复，禁止自动重调模型 |
| 数据迁移与种子 | Alembic 迁移；旧库安全 baseline/reconciliation；系统模板幂等种子；正式库已到 `d5f1a7c9e306`，模型 revision 回填和默认语音模型槽位生效；MySQL `TEXT` 默认值兼容 | 不覆盖作者 revision；多迁移头用 merge revision；不覆盖用户自定义模板和业务数据 |
| 文档与验证 | 中文配置手册、架构事实和计划文档同步；后端测试、前端类型检查/构建及运行烟测 | 代码、迁移、OpenAPI、generated client 和文档同批更新、同批验证 |

## 二开变更的同步规则

任何新增或调整二开能力都必须同时完成：

1. 更新本清单中对应领域、行为边界和验收条件。
2. 当前实现细节写入 `site/content/docs/architecture/`；尚未落地的事项写入 `site/content/docs/plans/`。
3. 用户操作、部署或配置发生变化时，同步更新 `docs/Jellyfish-中文版配置使用手册.md`。
4. API 或类型改变时，以后端 OpenAPI 为真值重新生成前端客户端，不手工拼接 generated 文件。
5. 数据结构改变时新增 Alembic revision，并记录升级、验证与回退边界。
6. 提交前记录对应测试；未完成文档同步的二开修改不视为完成。

## 上游升级的使用方式

升级前把本文每一行复制到“迁移决策台账”，逐项标记：

- `采用上游`：上游已经以相同或更好的方式完整满足结果，本地旧实现不再迁移。
- `保留本地`：上游没有等价能力，迁移本地实现并适配新架构。
- `兼容合并`：两边各有优势，保留上游结构并补齐本地缺口。
- `重新实现`：本地旧代码不适合新架构，只迁移业务意图、数据和测试。
- `淘汰`：本地代码已被上游替代、属于临时补丁或会恢复废弃链路。

台账必须给出上游证据、本地证据、最终决定、涉及文件/迁移/API、测试结果和文档更新，不能只写“已合并”。完整流程见 [上游更新与二开智能迁移 SOP](/docs/guide/upstream-upgrade-sop/)。


## 2026-09-08 提交脚本分页停顿修复（LC-001 / LC-014）

工作树待提交批次。用户双击 Push-Jellyfish.cmd 后遇到 Git 输出分页停在冒号提示；共享 Invoke-Native 对 Git 统一增加 --no-pager，不更改全局分页或换行符配置。LF/CRLF warning 本身不是提交失败。已经打开的旧窗口可按 Q 退出分页，确认范围后按原提示输入 YES；之后启动脚本直接输出清单。保留现有确认、敏感路径复查及健康检查。12 项隔离模拟回归通过；本批未执行实际提交、推送或服务操作，未关闭任何 PL。


2026-09-08 提交收尾：清理35个文件末尾多余空行以通过 Git 暂存差异检查，业务契约不变；前端类型与相关 Python 语法检查通过。本轮保存累计二开，既有部署/真实验收边界及全部未关闭 PL 不变，无服务重启或数据迁移。提交 SHA 见主台账后续追溯记录。


## 2026-09-08 文字编辑视频素材可视选择（LC-006 / LC-015，PL-013）

工作树待提交批次。原视频/参考图选择由纯文件名按钮改为共享 MediaFilePicker 卡片：图片缩略图可放大，视频可本地播放并通过现有 FilePreviewModal 打开兼容预览，预览与明确选择按钮分离。显示标题、不同的原文件名、已有尺寸/时长/大小和文件 ID，已选择素材标记；保留搜索与分页（每页8项），增加加载、空态、失败重试，并在关闭/搜索/翻页时取消旧请求、隔离迟到响应。关闭主编辑窗口同时关闭选择器。缺失元数据不伪造，不自动选择或触发编辑生成。

位置：front/src/pages/aiStudio/files/MediaFilePicker.tsx、chapter/components/VideoEditPanel.tsx；复用 FilePreviewModal、generated StudioFilesService 和现有受控文件 URL。列表视频只请求原文件 metadata，原编码无法播放时提示使用兼容预览，不自动给整页视频转码。无后端/API/schema变化，未重新生成 OpenAPI，无付费请求或数据迁移。本批仅前端代码，生产页面尚未重新部署；浏览器工具启动退出，未取得实际播放/点击验收证据。PL-013 的真实视频编辑与音画验收等待办继续保留。

验证续记：最终 pnpm exec tsc --noEmit、pnpm exec vite build 通过（仅既有大 bundle 提示），git diff --check 通过。浏览器工具启动失败，未做实机播放/点击验收；本批未提交或部署。


## 2026-09-08 19:11 素材可视选择前端部署（LC-006 / LC-015，PL-013）

用户授权部署后，仅构建并替换 front；本条覆盖上一代码批“待部署”状态。Docker 内 tsc 与 vite build 通过（仅既有大 bundle 提示）。首页与新静态资源 /assets/index-BQanViR2.js 均 HTTP 200，资源包含选择此视频/图片、放大播放和失败重试文案；后端 health HTTP 200。镜像 sha256:e26491dd25630538573ca160dccee2fa10578e67301527f11b496d2f6c38c002，前端启动时间 2026-09-08 19:11:29（北京时间）。切换后首次请求发生短暂连接中断，有限重试后正常。

backend/worker/beat/MySQL/Redis/RustFS 容器ID保持不变，未执行迁移、seed、模型调用或凭据配置变更。旧前端镜像保留为 jellyfish-front:rollback-media-picker-20260908-190958；原备份、数据卷和历史初始化容器均未删除。Compose 提示的历史孤立初始化容器不在本次清理范围。

用户 Ctrl+F5 后打开“文字编辑视频 → 从素材库选择原视频/参考图”，可查看卡片、放大/播放并明确选择。部署健康与资源检查已通过；浏览器工具启动失败，实际点击/播放与 PL-013 真实付费编辑验收仍待完成，不因前端部署而关闭。代码及部署文档为工作树待提交批次，本轮未自动提交或推送。


## 2026-09-08 全局文件管理入口与关联删除保护（LC-005 / LC-006 / LC-012 / LC-014）

工作树待提交批次。核对原 /files 文件管理页面已存在，但 MainLayout 菜单漏入口；新增左侧“文件管理”，保留资产详情“从文件管理选择”的关联快捷入口。页面查询数据库全局文件（含未关联上传和生成文件），支持类型/名称筛选、分页、预览、下载、名称标签编辑；增加文件计数和查看关联。点击删除先只读盘点，有关联/查询失败时禁用确认；无关联时人工明确永久删除，后端再次检查，显示具体拒绝原因。

新增 GET /api/v1/studio/files/{file_id}/delete-impact 和 FileDeleteImpactRead/FileReferenceGroup；api仅组织请求响应，service统一盘点。动态覆盖全部注册 files.id 外键（包含CASCADE/SET NULL）、时间线 source_id、业务JSON历史中的精确文件ID及本系统预览/下载URL、相同storage_key的其他文件记录。关联展示安全的名称/ID/用途，不返回任务正文或配置；每组最多20条明细，计数不截断。FileUsage、任务及导入历史也属于使用，不自动解绑或清除审计历史。JSON候选查询兼容MySQL/SQLite，当前采用候选筛选再精确匹配，历史量大时需按实际性能评估索引化。

实际删除先锁文件、重查引用并flush数据库约束，再删对象；原对象存储失败返回502并由请求事务回滚，停止旧的“存储失败也删记录”行为。兼容预览是可再生缓存，清理失败不撤销源文件删除。MySQL与对象存储不是分布式事务，提交/网络故障仍需备份恢复；不得据此宣称跨存储绝对原子性。没有强制删除被引用文件的入口。未新增数据库迁移。

验证：43项后端文件/媒体/存储定向回归通过（含预览后新增引用、共享存储、真实计数、失败回滚）；pnpm run openapi:update已通过一次性隔离schema服务执行，客户端重新生成；前端tsc及生产构建通过，仅既有大bundle提示。同步规范化生成文件末尾空行，无手工契约修改。本轮用户明确授权修改完成后直接部署重启受影响服务；部署证据后续登记。既有素材可视选择批次保留，不关闭未完成PL，也不进行付费生成或真实业务文件删除测试。


## 2026-09-08 19:32 文件管理部署验收（LC-005 / LC-006 / LC-012 / LC-014）

按本轮授权已构建并替换 backend/front，前后端健康及 /files 页面HTTP200，新资源 index-Ct62KRL7.js 包含全局文件管理、查看关联及删除前检查。使用正式MySQL对全部28个文件调用只读delete-impact：28个请求均成功，21个有引用受保护、7个当前未发现引用；部署前后文件总数均28，无真实删除请求。

新镜像：backend sha256:fced191cd92aec556b070b39b15710273a46b9f593d958f3ad11fcea7e046feb；front sha256:aaef3c8c36661b3d862817481549c13d105f9c112a4d034080b0eb038c1991d8。回滚保留 jellyfish-backend:rollback-file-management-20260908-193010 与 jellyfish-front:rollback-file-management-20260908-193010。Worker/Beat/MySQL/Redis/RustFS容器ID和启动时间未变，不重启无关服务。无迁移、seed、凭据调整、付费调用、备份清理或历史容器清理。

43项后端回归、前端类型和本地/镜像生产构建通过；OpenAPI及generated客户端已同步，一次性schema服务已退出。浏览器工具此前启动失败，未把HTTP/静态资源核验写成实机点击验收。用户Ctrl+F5后从左侧文件管理进入；仍保留原资产素材选择快捷入口。工作树待提交，未自动推送；此前素材可视选择改动已包含在本次前端构建中，原PL待办不关闭。


## 2026-09-09 文件管理固定分页布局（LC-005 / LC-014）

工作树待提交批次：FileManager 复用 app-workspace/app-fill-card 高度约束，顶部提示与筛选、底部分页保持在页面可视区域，中间文件列表独立滚动。分页移出列表 Card，加载中及空列表也保留底栏，窄屏分页允许换行。未改变接口、关联保护和文件操作语义；无需重新生成 OpenAPI。既有未关闭 PL 保持不变。部署及验证结果见本批续记。

验证与部署续记：pnpm exec tsc --noEmit、Docker 内 tsc + vite build、git diff --check 通过（构建仅既有大包提示）。2026-09-09 09:45:06（北京时间）仅替换 front，镜像 sha256:88fce31afc952ab30f046b9d132f54e057b81fa4be42e5b99571cec827e8eeca；/files 与 /assets/index-DbPGKaaq.js HTTP 200，新资源包含文件列表分页标记。回滚镜像 jellyfish-front:rollback-file-pagination-20260909-094202 保留。其他服务容器ID未变。浏览器工具此前无法启动，尚未取得实际视口截图验收。用户 Ctrl+F5 刷新后，可直接使用页面底栏翻页、修改每页数量，仅中间卡片列表滚动。文档与改动仍为工作树待提交批次，无自动提交或推送。

## 2026-09-09 生成规格与调用追溯（PL-015）

工作树增加按 first/key/last 保存参考选择、统一当前模型规格解析及默认值、费用依据冻结、HTTP 调用审计和官方文档同步。任务中心保留通用列表并提供型号筛选、独立详情和脱敏导出。官网来源去重、失败保留有效版本、兼容规则回退固定；业务页面不维护特定型号分辨率表。详见 [当前实现与精确自动化边界](./generation-governance.md)。

关联 LC-003/007/010/011/012/013/014/015；本批迁移 b8c0d2e4f609。代码、离线测试、部署、收费验收分别记录在主台账最新条目；PL-015 未整体关闭，既有其他计划保留。

### PL-015 部署证据更新（2026-09-09 11:37）

上述功能已部署至应用四服务，数据库 b8c0d2e4f609；前端 index-DBpuzRdC.js。后端全套611项及最终9项定向回归、前端类型/构建、隔离 MySQL 迁移、线上健康与 Worker ping 通过。运行数据文件31/镜头详情3/任务51保持。官网实际完成22源检查，2条价格/1条首帧兼容规则生效；不可读来源及额外型号适配保持待核对。完整镜像/备份/回滚和未完成项见主台账同时间条目，不将离线/健康验收作为收费产物或浏览器交互验收。

### 2026-09-09 六项复盘边界更正

统一规格复核窗口已部署，但工作室旧侧栏仍有固定2K/3K和页面初始standard覆盖保存默认值的问题；Seedance1.5Pro额外规格未核实。价格规则计算不等于每次实时官网询价。同步是文档变化监测加有限白名单规则，不是全部型号的请求/响应自动适配；调用审计非JSON/安全版本请求头与任务搜索跨范围交互仍有缺口。详见PL-015六项复盘，当前未修复这些业务问题；既有部署事实保留。

## 2026-09-09 四项优先补齐与模型编辑联动（PL-015，工作树待提交批次）

本节更新此前六项复盘的当前差距，保留其历史证据。LC-003/007：模型编辑与新增统一读取供应商目录，供应商/类别变更清空旧名称；过期响应取消，编辑支持当前类别候选和自定义名称，JSON参数要求对象。LC-010/011/013：移除工作室固定2K/3K与初始standard，模型默认值由统一复核消费；批量缓存区分规格集合/比例/版本。Wan按总像素预算提供1K/2K/4K，参考模式最高2K，9张参考/4张输出；Seedream5.0Lite提供2K/3K/4K并限制输入输出总数15；Seedance1.5Pro提供480p/720p/1080p及原生音频开关（未配置默认false），最终HTTP传参并校验。既有模型名和套餐地址不自动改写。

LC-012：调用请求增加audit_format=2的安全headers/body信封、表单文本、上传文件摘要、嵌套任务ID，屏蔽鉴权回显；任务搜索在本地范围也生效。LC-007/014：官网动态Delta正文只读提取，Seedance/Seedream/Wan协议规则扩展，界面区分协议兼容与结构待适配。收费端点报价未知时，另标官方北京按量原价参考，不冒充套餐扣费。没有新增数据库迁移，仍为b8c0d2e4f609。

验证：全套后端616项通过，最终定向回归另记；OpenAPI通过隔离schema服务重新生成；前端类型和生产构建通过。隔离Edge浏览器表单联动回归通过，GET以外请求拦截，真实模型配置未保存。可复用脚本scripts/verify-model-edit.mjs使用外置Playwright与Edge，设置PLAYWRIGHT_MODULE绝对模块路径后运行；不把模型编辑验收扩展成逐帧/全任务诊断/真实生成验收。未付费调用、提交或推送。

PL-015仍保留：套餐/地区完整计费、未核验供应商规则、任意鉴权/路径/响应结构的破坏性变动适配、真实付费产物与逐帧保存完整验收。其他全部未关闭PL不变。部署快照在后续条目登记；回滚镜像已保留jellyfish-{backend,celery-worker,celery-beat,front}:rollback-priority-20260909。

## 2026-09-09 生成治理补齐摘要（LC-007/010/011/012/013/014）

批量关键帧/视频先准备及审阅，再确认整批提交；支持未知项计数、人民币/AFP分开汇总。模型默认PATCH原子保存规格和原生音频。Wan按型号字段块解析模式/档位/像素边界/数量，候选字段变化经过10组本地规格矩阵再应用。Seedance1.5支持官方在线与AFP参考，Seedream支持99AFP/张套餐参考；Ali实际Credits换算未知。计费页变化未验证时暂停静态数字估算。隔离浏览器验证逐帧交互、任务搜索/重试/导出、默认和整批取消；SQLite新会话验证持久化。此为离线和模拟验收，任意API重构、账户实际账单与真实付费产物仍属PL-015边界。部署事实见主台账最新快照。

部署续记：2026-09-09 15:23已完成本批部署（前端15:15:55），健康200/worker pong；全量619项及最终14项定向、类型/构建、隔离浏览器回归通过。另修复仅价格来源恢复自动应用被契约校验误拦截的边界。刷新页面Ctrl+F5可使用；完整镜像、25源同步和未验收边界见主台账15:23快照。数据库版本与业务数据不变，无付费/提交/推送。


## 2026-09-09 生成参数与费用前置展示（LC-007/010/011/013/014，PL-015）

工作树待提交批次。修正此前仅在最终提交复核弹窗显示参数/费用、生成面板不可直接发现的问题。新增共享GenerationOptionsPanel：工作室“关键帧与参考图”“视频生成”、实验室图片/视频输入区、资产图片生成提示词弹窗直接显示当前默认或选定型号、可选分辨率、支持时的音频开关、人民币/AFP费用参考。字段随型号/比例/参考数量刷新，费用随数量/秒数/音频选项变化；失败可刷新，切回窗口可核对最新默认模型。未知实际套餐扣费仍显示未知，按量/AFP参考另列。

页面选择与最终复核通过模型revision绑定传递，模型版本变化不沿用旧选择，最后仍重新核对合法规格；这不修改模型全局默认，主动保存默认仍在最终复核窗口。工作室公共图片面板按关键帧参考数量预览，首/尾帧使用不同参考数量时由最终复核按实际输入更新合法档位；不代表三帧输入完全一致。删除隐藏的模型A/B、ControlNet、固定时长示例参数。

使用位置：展开工作室右侧“属性/分镜生成面板”，切换“关键帧与参考图”或“视频生成”，在“图片参数与费用/视频参数与费用”区域选择。实验室在输入区查看；资产图片在生成提示词窗口查看。金额未知时仍可看下方有依据的原价/AFP参考，不代表免费。

前端tsc通过；实际工作室页面连接正式只读API的浏览器回归通过，动态读取现场默认Seedream/视频型号，可切换分辨率，所有写请求受拦截（本次0写尝试）。隔离fixture另验证面板720p选择进入最终复核结果，原逐帧/任务/默认/整批回归通过。仅前端变更，无API/schema修改，不需重新生成OpenAPI或跑后端测试；历史619+14项属于上批证据。已审查本批语义差异，保留既有未提交改动及全部未关闭PL；生产部署结果后续登记，无付费生成或正式配置试写。


部署续记（2026-09-09 16:31）：参数前置展示已部署，仅重建front，启动16:31:22北京时间；镜像sha256:4091c0e2216b4204200da4be3a759ac65e5391b4ca674a3743b2ecf22459a47d，资源index-DibNv7Oi.js。保留rollback-visible-parameters-20260909；backend/worker/beat及数据服务保持原运行版本，无迁移。Docker内tsc/vite build通过；正式7788工作室浏览器再次验证图片/视频参数区域可见、动态读取实际默认型号、可切换分辨率、费用参考显示，0写请求。资源及后端健康200，开发5174已停止。无付费或提交推送。验证脚本scripts/verify-visible-generation-options.mjs使用PLAYWRIGHT_MODULE、JELLYFISH_TEST_URL与JELLYFISH_STUDIO_PATH环境变量，不把固定业务ID写入脚本。PL-015原有未完成范围保留。

## 2026-09-09 视频编辑当前边界核实

编辑入口目前只接通 fal Kling O3 Pro edit 与 Runway aleph2。生产配置只有 Seedance1.5Pro、HappyHorse1.1I2V，因此无可选编辑型号。编辑专属分辨率参数、有效时长费用估算及参考图排序尚未实现，不能从普通生成面板的完成状态推断编辑入口已覆盖。11个视频供应商的官方核查与后续补齐见 [计划](../plans/video-edit-official-api-audit.md)；文档核查不代表新增接口已接通。

## 2026-09-09 准备页与编辑协议当前实现

LC-002/006/007/009/015：准备页候选显示匹配资产实时照片；明确匹配未关联语义、人工选择已有或本页新建，没有候选同样可新建。新建后关联镜头并刷新，无需重新提取。候选ID和项目边界验证，名称不同按实体ID聚合。LC-001/014：nginx对资产入口（实际路径 `/assets` 与 `/assets/`）单独返回SPA，防目录重定向丢失端口。

LC-010/011/012/015：原生编辑协议、共享型号目录、动态参数、免费本地检查及估价、最终确认和版本冻结、参考图排序已落代码。URL型编辑仅签名已选对象，需明确公网HTTPS桶配置，不改ACL和套餐。官方来源核验、离线通过、实际付费分开记录；部署见项目记忆最新快照。完整边界见generation-governance与video-edit-official-api-audit，PL-013/015未整体关闭。

现场核对补充：截图候选“清晨小区林荫道”与已有“小区林荫道”存在前缀差异，名称检测增加双向包含建议、精确优先；只展示匹配照片，不自动关联。完全不同名称仍需用户选择，不能根据照片猜测角色身份。

部署续记：2026-09-09 19:14已联合部署四应用服务；630项后端全量、最终3项定向、类型/镜像构建及隔离浏览器通过。正式新建深链接、截图妈妈/场景图片、本页新建入口通过只读浏览器验证。数据库不变、无付费；精确镜像/回滚和未验收边界见PROJECT_MEMORY同时间快照。

## 2026-09-10 Docker 回滚镜像与缓存整理（LC-001 / LC-014）

按用户本轮清理授权核对全部容器引用和备份用途，移除26个已被替代且无容器引用的历史回滚镜像。保留当前应用镜像，以及rollback-preparation-edit-20260909（最近回滚）和rollback-generation-governance-20260909（迁移边界）两套四服务镜像。此记录覆盖历史章节中的旧镜像“保留”状态；旧部署记录是历史证据，不能再直接使用其中已删除的标签回滚。精确删除清单在backups/cleanup-20260910.json。

清理可再生闲置构建缓存；没有删除任何已有容器或业务数据卷，没有启动/重启业务服务。原有12个容器维持停止状态；仅删除无引用前端依赖缓存卷，五个其余数据卷保留。源码工作树未提交，迁移SQL/原始代码备份及官方证据合计约8.9MiB，继续保留；测试MySQL数据库副本按既有确认边界保留，不能用volume prune清掉。所有PL原有状态不变。

后续批次应检查新增临时备份和回滚镜像，优先保留当前、最近可用回滚及仍需保留的迁移边界；验证结束后清理已被替代的缓存与冗余镜像，不为每次构建永久增加回滚副本。数据库/对象备份、代码目录和测试数据库的破坏性清理仍按AGENTS.md精确清单与确认规则执行。此为批次收尾要求，不代表已安装定时清理任务。

Docker内部回收空间与Windows虚拟磁盘文件缩小分别统计；本轮未停Docker引擎、未压缩或删除VHDX。最终空间统计见下方续记。

最终清理结果：构建缓存分两轮回收148.2MB+879.3MB（合计约1.03GB）；26个旧回滚镜像已删除。另核查jellyfish_front_node_modules零容器引用、只读检查含.pnpm/react/typescript后删除此可再生依赖缓存卷，回收218.8MB。缓存合计约1.25GB；没有删除数据库/素材卷，测试MySQL卷227.7MB继续保留。Docker报告剩余构建缓存为共享/不可回收引用，RECLAIMABLE=0B；不为追求缓存显示为零删除正常镜像。VHDX逻辑文件仍约10.79GiB，未进行离线压缩，不能将Docker内部回收量称为Windows立即增加的可用空间。


## 2026-09-10 分镜生成面板布局与全局省略提示（LC-002 / LC-006 / LC-014）

工作树待提交批次，基线700333cb17e926caa73f68070cec4803c02a4d78。ChapterStudio右侧取消占宽的左侧标签，六个入口顶部两行展示；内容独立滚动，标题和导航保留可见。诊断卡片使用可收缩列，长候选名自然换行，关联状态不再竖排。准备/生成职责保持。

应用根组件接入OverflowTextHints，通过事件委托为实际发生CSS单行省略或多行截断的文本补充浏览器原生title；覆盖页面与弹窗中的异步内容。保留已有title/说明，不读取输入框、隐藏字段或原始凭据；移开删除临时属性。全局检索truncate/line-clamp/ellipsis及手动字符串截断；模型参数摘要已有完整Tooltip，保留。新页面优先保留完整DOM文本再用CSS截断；主动裁短的字符串必须显式提供完整提示。

验证：pnpm --dir front exec tsc --noEmit、Docker生产构建通过（既有大bundle提示）；scripts/verify-overflow-layout.mjs在开发及正式7788通过单行/多行/动态文本/已有提示/短文本/密码排除、320px和420px面板、顶部固定和横向溢出检查。verify-visible-generation-options.mjs正式图片/视频参数、费用与档位切换通过。正式仅GET及只读existence-check，其他写请求拦截；参数脚本拦截1次页面自动保存尝试，实际写入0，无付费生成。

2026-09-10 09:38:53（北京）仅front最终重建重启；镜像sha256:c23389d468da034360a57a45d2b5036df33e9b1981b2fdb1d899e6e316f0d0b5，资源index-BkLF7oNH.js / index-BAmZUFKQ.css。后端health200，数据服务不动，无API/迁移变更，不需要重生OpenAPI。既有两套回滚标签保持，未额外永久增加回滚集。所有未关闭PL状态不变；未提交/推送。已审查git diff --name-status，本批仅上述前端、验证脚本与文档；之前工作树的其他改动保留。


## 2026-09-10 中文 Docker 镜像清单（LC-001 / LC-014）

工作树待提交批次。新增根目录“查看Docker镜像清单.cmd”，调用scripts/windows/Show-DockerImages.ps1，只读查询当前Docker context的镜像及全部容器引用，生成local-reports/docker-images.html并由默认浏览器打开。模板scripts/windows/docker-images.template.html提供用途说明、引用状态、保留原因、大小、搜索与筛选；同一不可变镜像ID合并多个标签。引用区分运行中、非运行容器及无引用，无法对应当前镜像的容器单独提示；未知用途及未知回滚标签明确待核对，不自动判定可删除。

每次双击重新采集覆盖同一快照；浏览器刷新不采集Docker。页面标明时间/context；查询失败不覆盖旧清单，报错退出。仅提取镜像元信息、容器名/状态/镜像ID，不采集环境变量和凭据；JSON嵌入转义，渲染使用textContent。local-reports加入Git忽略，避免提交本机清单。无新增服务、端口、Docker socket挂载、业务API和部署重启，无删除或付费调用。

实际采集及Edge浏览器搜索、筛选、重置、空状态和ID去重校验通过。现有未提交变更及所有未关闭PL保留；本批不改变生产部署版本。入口说明已同步中文手册与WINDOWS-SCRIPTS.md。


## 2026-09-10 Docker 镜像清单集成 Web 与 Windows 编码修复（LC-001 / LC-014）

工作树待提交批次。新增 Jellyfish“系统设置”右上角“Docker 镜像清单”，路由/settings/docker-images，提供用途、容器引用、保留原因、搜索、筛选、分页与刷新。Windows采集器额外原子发布local-reports/docker-inventory.json，安全本机Compose仅给front只读挂载报告目录，nginx只开放/local-inventory/docker-images.json精确文件，其他同前缀路径404。未挂载Docker socket，未开放Docker控制接口，后端API不变，无OpenAPI重生。

本批默认继续手动采集：网页刷新只读取最近快照，显示采集时间/context及读取失败；已向用户询问自动/手动选择，未收到答复，不安装定时任务。该状态覆盖“入口仅在Windows文件夹”的旧说明，独立HTML/CMD仍保留。之后选择自动更新时再实施及核验，不把它写成已完成。

复现Windows PowerShell 5.1 / CP936解码Docker UTF-8导致JSON解析失败，设置Console.OutputEncoding及OutputEncoding=UTF-8，并将container ls改为只读ID，不解析包含截断命令的整行JSON。新增JSON原子替换使用NullString.Value，修复PS5.1把null备份路径转换为空字符串的问题；CMD失败保留非零退出码。实际CMD完整入口及重复运行通过，不仅-NoOpen模式。

验证：前端tsc、Docker生产构建、正式浏览器scripts/verify-docker-inventory.mjs通过设置入口、搜索、引用过滤、刷新失败/恢复、其他报告文件404。11:23仅重建front，镜像sha256:e4c7952250f20dd634e27f7ddd897c6ba7e3d438aad49325435b2895f5187c3c，资源index-jkv1VCn5.js；正式快照HTTP200，重新采集包含新容器引用。无数据库/素材修改、无付费调用、无提交推送；所有未关闭PL保持。原先本地页面验证不等于Windows双击所有编码路径已覆盖，本次补充真实失败路径验证。


## 2026-09-10 镜头情绪标签与视频缺失项提示（LC-006 / LC-010 / LC-011 / LC-014）

工作树待提交批次。确认原“愤怒、反转、紧张、温馨、压抑”只是ChapterStudio前端固定展示，无点击/自定义逻辑，也不是模型提取或当前已选数据。改为按当前ShotDetail.mood_tags显示、多选/删除/清空/输入回车自定义；建议词不默认选中，反转不再作为情绪建议。独立即时PATCH保存、去空白与去重、失败恢复原选择；保存结果只合并同镜头mood_tags，不覆盖其他本地字段，旧防抖不重复提交标签。

生成链之前未完整消费mood_tags；现在已保存情绪加入首/关键/尾帧frame_specific_guidance及视频pack.atmosphere。视频渲染补充遗漏的情绪行，兼容未消费氛围变量的自定义模板并避免重复追加；清空不再自动追加。不会改写用户已保存的提示词文本，也不会追溯删除用户手写或历史提示词里的情绪描述；已提交任务保持其原快照。无API/DTO/数据库结构修改，不需OpenAPI重生。

视频准备度将未通过条件置顶为中文提示卡，列出后端实际原因；时长/提取/参考帧提供跳转，模型/供应商引导模型管理，任务运行中引导任务中心。参考模式和帧名称转换中文；通过项折叠，未知原因明确提示需重新检查，不再只显示“待补齐”或英文key。保持信息确认状态、运行状态、视频准备度三者分离。

验证：28项后端定向测试通过，隔离SQLite验证保存情绪进入图/视频上下文、清空移除、自定义模板执行文本及不重复追加；前端tsc与镜像构建通过。scripts/verify-shot-mood-readiness.mjs覆盖选择、自定义、删除、清空、重开回显、失败恢复、具体缺失项与跳转；5次模拟标签写入，正式写入0，无付费调用。部署前worker active/reserved均为空；部署快照另记，全部未关闭PL保留，未提交/推送。


部署续记（2026-09-10 16:25）：backend/worker/beat于16:23:14–15重建，front最终16:25:40重建。backend sha256:a872f399d71fd4e120cdd243c08b35cfa1f747e6e048677bdff6fd1ab6cce668；worker sha256:603453f264ae3a5a8529d1d0ef6c34259738ede1bb775b5ea87627dbfccc77d4；beat sha256:94bd2421ea023f0d45682d79ee7ab0438793abbbbc2318ec7c5a7a0b4b762cc9；front sha256:380bd2a0fa4a68838338a692d208fa2d16d0f9227266dd850515432f7a96f72b，资源index-C-pjJfoe.js。正式7788浏览器情绪/缺失项回归通过，5次模拟写入、正式0写；health200，镜像清单手动重新采集，开发5174停止。无迁移、MySQL/Redis/RustFS不重建，无付费；既有回滚标签保持，不为本次构建额外增加永久回滚集。28项定向测试及tsc/镜像构建/diff检查通过，不冒充全量测试或真实付费产物验收。工作树待提交，各PL原有边界保留。


## 2026-09-10 帧图基础提示词输入与版本标识修复（LC-006 / LC-010 / LC-011 / LC-014）

工作树待提交批次。根因：400ms自动渲染期间禁用基础输入框，每次请求hydrate清空预览并回写基础文本；慢请求没有草稿版本校验，可能覆盖较新编辑。改为700ms停顿同步、中文composition期间等待；后台渲染不禁用基础输入，不hydrate重建预览或自动覆盖用户参考顺序，保留原输入空白。渲染回调依赖稳定方法，useGenerationDraft在基础/上下文/重置/换草稿时递增版本，旧结果/错误返回null，不覆盖较新草稿。生成提交仍需当前有效派生结果。

多版本核查：历史帧图关联保留多张，ShotFrameImage.file_id仅保存该帧当前生效图片；“使用”PATCH该槽位并刷新，视频按reference_mode读取生效槽位，不枚举历史关联。发布器以version_id CAS尝试更新当前图片，冲突不覆盖；已提交任务冻结原快照。图片生成参考列表与历史结果列表分开，可显式选择参考，不因历史数量自动全部加入。UI增加缩略图使用中/历史版本标识、当前版本优先、入口改为“选择使用版本”，弹窗解释每种帧只用一张。

浏览器scripts/verify-frame-prompt-edit.mjs针对首/关键/尾帧分别模拟慢旧请求/快新请求，验证不中断输入、最新结果、输入空白、中文组词等待；9次模拟渲染，0付费/业务写入。没有执行真实多版本生成或切换正式图片，版本语义依据前后端实现核对；新增UI不改变发布/选择协议。前端tsc和镜像构建通过，正式部署续记另记。无API/数据库/后端改动，不需OpenAPI重生；全部未关闭PL保持，未提交推送。


部署续记（2026-09-10 16:40:26）：仅front重建，镜像sha256:9634ec5dfe8fc337f6c1dda4c98b88ec4912464b4e052fbdc28d0e71188053f8，资源index-CwNP64H1.js。正式7788三帧输入回归通过（9次模拟渲染，0付费/写入）；tsc、镜像构建、diff检查通过。后端及数据服务不重启，无迁移，开发5174已停止。未执行真实生成或替换用户生效帧；旧回滚集保留，无额外永久回滚标签。本批仍工作树待提交，各PL待办不变。


## 2026-09-10 视频参考选择与准备度模式同步修复（LC-006 / LC-011 / LC-014）

工作树待提交批次。截图下拉已选首帧但准备度仍显示纯文本：原检查读取videoPromptDraft.context.referenceMode，仅打开视频提示词预览时才更新；下拉修改refImageType不触发检查。现在准备度直接消费可见refImageType（清空=text_only），切换触发GET重新检查；模式或镜头与旧响应不一致时先显示加载，既有取消标记拦截慢旧响应。视频预览草稿及实际提交仍按其原构建/校验链，未改变厂商能力规则或将不具备条件的镜头强制标记ready。

验证：tsc及Docker生产构建通过；正式浏览器scripts/verify-video-reference-readiness.mjs用模拟只读响应验证选择首帧请求first、迟到text_only响应不覆盖、清空请求text_only，0写入/付费。模拟first通过不代表截图镜头真实其他条件全部通过。仅front于2026-09-10 16:47:16重建，镜像sha256:90584dfbac5bd455ff3b46deeb2a940d751a204190037c73376f251100b0749e，资源index-CHqPEwFK.js。后端/数据服务未重启，无API/迁移/OpenAPI变更；diff检查通过，历史工作树保留，未提交推送，全部未关闭PL保持。

## 2026-09-10 全局生成图片细节预览（LC-006 / LC-009 / LC-010 / LC-014）

工作树待提交批次。新增共享PreviewImage，覆盖分镜当前/历史帧图、资产与项目封面、素材库、文件管理、编辑/质检参考图、实验室生成结果与历史参考。保持缩略图布局，点击和Enter/空格打开Ant Image缩放/旋转/拖动预览；Escape优先关闭图片层，关闭恢复焦点。文件预览端点只在放大时转换为原文件下载端点；帧图显式使用fileId原图。已有Ant Image/PreviewGroup保留，取消资产视角图禁止预览设置。

图片事件与外层素材选择、版本使用隔离；实验室参考删除改为角落按钮，资料库点名称选择，资产全部视角保留独立入口。视频封面沿用视频播放，Logo不预览。没有更改生效帧、参考排序、模型参数、API或数据库，不需OpenAPI重生。新图片展示优先复用PreviewImage或Ant Image，不能新增仅展示生成结果的裸img。

浏览器scripts/verify-image-preview.mjs隔离夹具验证放大、旋转、关闭、键盘、选择计数隔离与嵌套弹窗；无业务写入或付费调用。类型检查与最终生产构建/部署结果见续记。已审查git diff --name-status，本批映射上述LC；先前工作树和全部未关闭PL保持，不提交推送。

部署续记（2026-09-10 17:17:36）：仅front重建重启，镜像sha256:56a65188e11e866ac483989e179471930f33fe0b7433af5320287bcd6b4a359f，资源index--rTDKecd.js。正式7788资产页点击图片、放大、旋转、关闭浏览器验证通过；隔离夹具额外验证键盘/嵌套弹窗/选择隔离。前端tsc、最终Docker生产构建及diff检查通过，后端health200；后端与数据服务未重启，无付费或业务写入，无迁移。原有回滚集保留，无新增永久回滚集。工作树待提交，全部未关闭PL不变。

## 2026-09-10 预检复用、智能调整与视频版本关联（LC-006 / LC-010 / LC-011 / LC-014；PL-013 / PL-015）

工作树待提交批次。QualityReviewPanel免费分页读取当前镜头历史预检及优化方案，首/关键/尾帧与视频用途分开；旧任务解析已冻结提示词并标为用途未标注，不改旧数据。刷新/进入/改稿均不调用模型。用户主动重新预检可另起一次调用；智能调整关联同镜头成功旧报告，结合当前提示词、来源和补充约束，单独明确费用。已有方案查看/应用不调用模型，无自动收费补问/重试或换模型。

新增contracts/quality_review及service工作流：冻结任务输入、来源报告与具体视频上下文；优化返回完整提示词、修改说明、未解决事项。保留供应商原文，离线兼容JSON/围栏/说明前后缀/常见包装/中英明确字段/分节Markdown；歧义或未知格式保留原文并提示，不将整段审片报告当提示词。仅凭发型衣着猜性别/精确年龄不可当事实；保留已确认身份、剧情、道具和图号，换图重绘及不支持参数留待核对，不能声称文字修复了像素。

应用先免费本地渲染，保存持久标记成功后更新视频草稿；失败还原原派生稿。记录应用前后、时间、源预检、引用图序和视频参数，区分方案生成/曾应用/当前内容不同。重新打开仅当默认原稿及完整生成上下文吻合时恢复已应用稿；其他版本仅展示历史供参考，不自动覆盖。业务修改内容留在生成面板，任务中心职责不扩展。

视频任务请求冻结quality_review_task_id及quality_revision_task_id；后端在提交前校验同镜头、成功报告、优化来源、已应用状态、实际执行文本、首尾关键帧及顺序/模式、视频模型revision、比例、分辨率、时长、原生音频及本地来源指纹。预检文本模型与目标视频模型名称分别保存，不能只按shot_id或最近记录冒充同版预检。历史旧报告可作为新调整的线索，不代表当前图文已重新审查；AI建议/已优化都不是已通过质量保证。

无数据库结构迁移，沿用任务payload/result与强关联表；API/OpenAPI/generated client已同步。新增后端与浏览器离线回归覆盖历史复用、跨镜头拒绝、格式归一化、旧稿应用保护、标记持久、生成模型/文本变更拒绝绑定。637项后端全量通过（最后展示字段补充后定向回归另记）；浏览器模拟预检/优化/应用通过，无真实业务写入或收费。真实厂商优化质量/图文判断仍需用户显式调用验收，PL-013/015及所有其他未关闭计划保持，不宣称任意厂商格式或语义绝对正确。部署续记另记，未提交推送。

部署续记（2026-09-10 18:43:46–50）：backend/worker/beat/front已重建重启。backend sha256:2833db7d75b15a17eb176ff3eb7fccc192d1c6ec5413e825d54843803deba053；worker sha256:c8732dd7ce26035ec3bb2f421a03a5e0046c4b73d4103ad96642b238c16c7867；beat sha256:cd76107854d8bae6fdeaf7257991e256efc62ff33c74005319d9e59b1325d501；front sha256:0b09cc358650af820e15753f32eb9b5cdebeb5c9d909b8039b49db9c299e8c32，资源index-CmAnwGNw.js。切换前active/reserved为空，切换后health200/worker pong；MySQL/Redis/RustFS未重建，无迁移。

正式历史API读取样例镜头已有3条，刷新后仍3条。scripts/verify-quality-review-live.mjs正式页面验证旧报告显示与免费刷新；所有非GET请求均拦截，video/render只返回本地fixture，1次模拟渲染，0正式写入/模型任务/付费。最初允许本地渲染的测试被自动审批以可能收费风险拒绝，改为全POST隔离后通过。637项后端全量通过，最后字段/来源校验补充后10项及6项定向通过；前端tsc、最终Docker构建、隔离工作流浏览器、git diff --check通过（既有大bundle提示）。

应用阶段新增来源指纹校验；保存失败还原旧派生稿；任务输入冻结具体预检/优化ID供追溯。代码与文档仍工作树待提交，所有未关闭PL保持；没有真实付费验收，也没有改变默认模型/账号/媒体。原有两套回滚标签保留，无新增永久回滚集。开发服务收尾停止，业务服务保持运行。

## 2026-09-10 视频提示词上下文溢出修复（LC-006 / LC-014）

工作树待提交批次。只读核对截图镜头：长“妈妈蹲下扶鞋准备穿鞋……”确为ShotDetail.action_beats第二条，当前共4条，不是本次AI预检新增内容。后端优先保存节拍，否则由镜头描述/剧本摘录/对白整理；trigger/peak/aftermath由本地关键词与位置规则推断，页面增加来源说明。此批不改节拍、阶段判定或生成内容。

视频预览弹窗限制视口高度，正文独立滚动、标题与底部按钮固定；节拍从不换行Tag改为完整换行条目，+N改为明确展开其余条目。无API/后端/数据库修改，不需OpenAPI重生；部署与浏览器验证续记另记。其余工作树及全部未关闭PL保持，未提交推送/付费。

部署续记（2026-09-10 19:01:45）：仅front重建重启，镜像sha256:dfdbc400f6a12e28abeb43194d9c6233284f7e96129835f0e5b4aa31c43ef3d6，资源index-CckLzUY0.js / index-ChJUYhUM.css。tsc、生产构建及diff检查通过。正式页面用全POST隔离的长文本fixture验证1440×790、900×640下节拍换行、无横向溢出、底部按钮可见，同时验证历史预检读取；1次本地模拟渲染，无真实写入/付费。后端与数据服务未重启，未修改原镜头动作节拍；工作树待提交，各PL保持。

## 2026-09-10 预检优化交互复盘与时区修复（LC-006 / LC-014）

工作树待提交批次。复盘前版缺口：操作按钮位于长报告之后、前后文本分散展示无直观对比、顶部标记跟随选中记录未明确当前视频状态；这不能算交互需求完整满足。本批预检面板默认展开，操作区前置，明确手动触发“旧报告→一次模型优化→完整稿→免费应用→直接生成”，不要求再次预检。已有方案可免费跳转，支持返回来源报告；无自动调用或付费重试。

新增优化前/优化后并排文本，小屏上下排列；已应用方案展示实际保存的应用稿。结合latest_applied与当前提示词/生成上下文区分当前已应用、历史应用但版本不同、方案生成未应用、已有报告未优化。保持原先后端严格版本关联与费用确认，不修改数据库或API。

8小时差根因：历史created_at无时区UTC被new Date当成本地时间，任务中心用Unix时间戳转本地。新增共用studioTime格式化，缺时区按UTC解释，有Z/offset保留，统一浏览器本地显示；记录明确创建时间，任务中心开始时间仍可能因排队相差。未改原始时间数据。

浏览器隔离测试覆盖入口、前后稿值、旧稿应用禁止、标记持久、读取不调用模型，并验证无时区UTC/Z/+08:00三个等价时间在Asia/Shanghai一致为17:00。本批只改前端及文档，无真实付费；后端637项是前批证据，不充当本批新增测试。部署/构建续记另记，所有PL保持，未提交推送。

## 2026-09-10 预检历史仅展示有效成功结果（LC-006 / LC-011 / LC-014）

工作树待提交批次。历史查询在后端分页前限定 succeeded 且 result.text 非空白，失败、取消、执行中及空结果不进入可选历史，total 与分页同步过滤；失败排查继续使用任务中心。新发起任务独立显示执行进度和本次失败原因，执行中禁用历史切换，不将任务ID当成历史选项。已有成功报告和优化方案仍可免费读取、应用，不触发重新预检。

这是历史查询行为补充，不改变DTO或数据库结构；已运行 openapi:update 同步接口。SQLite内存定向测试6项通过，覆盖失败/取消/执行中/空结果排除；前端tsc及模拟工作流浏览器通过，无真实模型调用或业务写入。与前批优化入口、前后对比、应用标记及时区修复一并交付，部署续记见下文。全部未关闭PL保持，未提交推送。

部署续记（2026-09-10 19:26:50–51）：backend/front已重建重启，包含本批有效历史筛选及前批优化交互/时区修复。backend sha256:51f5f9531364d8c1e8ff5b111a5cff66bcf66e4c77323aad3217fd60bf9754e9；front sha256:a15de0fd15279f6b20eac020042bc438cef9058f5b0f9e1e0a11dbaebef0050b，资源index-DtXZXqc7.js / index-CX5ActlL.css。正式health200；样例镜头历史由3条变为2条有效成功记录，返回失败或空内容数量为0，原任务未删除。

前端tsc、OpenAPI同步、生产构建及6项后端定向测试通过。浏览器模拟工作流覆盖前后对比、应用标记和时区；正式页面只读验收覆盖历史复用及1440×790/900×640长文本布局，所有非GET拦截，1次本地模拟渲染、0正式业务写入/付费调用。保留既有大bundle提示。worker/beat及MySQL/Redis/RustFS未重建，无数据库迁移。已审查本批git diff --name-status并映射LC-006/011/014，git diff --check通过；工作树待提交，全部未关闭PL不变，无真实付费质量验收。

## 2026-09-10 视频生成确认工作台与场景核对（LC-006 / LC-011 / LC-014）

工作树待提交批次。以剧情和生成质量为主线：先显示本镜头目标、关联场景/角色/道具与实际帧图，明确首/尾帧用途并可放大，提供修正镜头和更换帧图入口；不把文字优化当成已修复参考图。桌面1180px弹窗，提示词与发送预览、可选AI预检优化、免费来源规则三页签保留状态；宽屏双栏、窄屏上下排列，固定页脚。

编辑稿与本次完整发送稿分开，显式免费更新并显示末尾补充；旧预览或视频模型/模式/图片/时长/比例/分辨率/音频上下文变化后禁止生成和新预检/优化/应用，历史仍可免费查看。预检改为使用已同步的完整发送稿；应用后返回主预览，前后对比与持久版本标记保留。镜头来源冲突需修正来源，不提供虚假的机器优先级或质量保证。

视频渲染新增可选model_revision_id，复用提交端现有厂商prompt profile与相同帧槽位；单镜头/参考编号规范提前进入预览，避免预检后隐藏追加。旧调用可省略，不扩展模型能力或新增付费调用。API/OpenAPI/generated client同步。费用确认对本入口锁定已预览模型版本与规格，配置失效拒绝静默回默认；通用其他入口保持可选规格。界面同步展示厂商型号、实际参数和各计费来源，不把未知价格当免费。

单页提交同步锁防重复点击，发送前取消与请求已发出但未取得编号分别提示。没有自动收费重试；跨刷新/网络不确定结果的普通视频请求端到端幂等尚需专项完善，不能宣称完全消除重复扣费。已生成视频原有选择/播放链保持，不改模型画质参数或自动替换历史产物。真实成片质量、任意图文冲突识别与PL-013/015真实付费验收仍未完成，其他未关闭PL保持。

13项后端定向测试通过，覆盖厂商规则预览幂等、参考槽位、渲染与路由；前端类型检查、隔离工作流/正式读取加写请求拦截测试覆盖预览同步、编辑保留、禁止旧稿提交、费用页规格锁定、三个尺寸页脚及历史复用。所有生成/渲染测试写请求仅本地模拟，无正式业务写入/收费；部署续记另记，未提交推送。

部署续记（2026-09-10 20:01:58–59）：backend/front重建重启，backend sha256:e2d4b120ea072aa0681a90d2eff4385482545e85e44abf6dafb80f72a7d6beae；front sha256:10f70a69cbdb48597928be11050eb40cad927a595501b6c7d4e6ef5037cbacde，资源index-DcgBjvke.js / index-BcA2KqVJ.css。health200，正式OpenAPI含预览model_revision_id。worker/beat及数据服务保持，无迁移。

13项后端定向、最终tsc/生产构建通过；正式页面scripts/verify-video-send-preview.mjs验证镜头场景目标、剧情展开、编辑/发送稿、免费更新、禁止旧稿生成、费用确认规格锁定及取消、页签保留草稿、1440/900/540宽布局；2次模拟渲染，0正式写入/付费。原历史及长文本脚本验证另1次模拟渲染，预检完整流程隔离夹具也通过。已审查本批name-status并映射LC-006/011/014；diff检查通过，保留原有大bundle提示与旧工作树，未提交推送。全部未关闭PL及真实成片/供应商付费质量验收边界保持。


## 2026-09-10 镜头主体多图与成片采用

LC-006/010/011/014/015，工作树待提交批次。当前工作室支持独立选择本次视频模型，参考模式跟随能力；HappyHorse 1.1 R2V、Vidu Q2/Q1/2.0 主体图片路径已接通用途/顺序/预览/预检/实际任务。帧约束与主体分开，未配置模型不自动切换。总图数和型号时长检查、Vidu resolution 投影已补齐。历史视频区显示实际成片引用，明确采用验证同镜头成功产物及并发状态；保留原 CAS 自动采用语义。

浏览器未提交参考草稿仅本机恢复，真正任务上下文持久保存。完整当前规则见 [视频参考与采用](short-drama-video-references.md)，阶段验收/未完成项见 [PL-016](../plans/short-drama-reference-workflow.md)。真实质量、专业多轨剪辑和未核验协议未宣称完成。


## 2026-09-10 多轨工程与一致性复核（工作树待提交）

LC-006/014/015：新增独立project_edits及修订保护、固定媒体、裁剪排序、配音/音乐/音效混音、字幕校时、人物参考与人工状态、精确回跳分镜、工程快照导出和文件引用保护。当前实现见 [多轨架构](project-multitrack-editing.md)，部署见PROJECT_MEMORY最新记录。650后端、前端构建、隔离MySQL/FFmpeg及浏览器模拟验证通过，真实成片质量尚待人工验收。PL-011增强阶段与PL-016多角度参考待办保持，三视图付费生成未执行。


## 2026-09-10 四项剪辑与人物增强（工作树待提交批次）

映射 LC-006/014/015，推进 PL-011 与 PL-016。代码与离线验证已完成，新部署结果另记；全部其他未关闭 PL 保留。

- 角色/演员编辑页复用 FRONT、LEFT、BACK 槽位，建立缺少槽位不调用模型。已有所有角度刷新可见；明确选择并确认主形象后，其他角度只携带该张参考图，每次仍走模型参数/费用确认。原图、生成历史和各角度采用逻辑保留；不把三视图默认全部送给视频模型。
- 剪辑片段出场支持交叉叠化、溶解、向左擦除、向右滑动、淡入黑场；本地 FFmpeg xfade/acrossfade 实际编码。重叠扣除成片时长；最后片段和过长重叠拒绝保存；音轨/字幕采用绝对时间，越界需主动校时。混合直接切镜的时基/帧率衔接已经真实验证。
- 真实 Web Audio 波形支持拖动移动、两侧裁剪、方向键微调、撤销和服务器保存恢复；波形失败回退数字校时。下载限制30MB、解码波形支持30分钟以内，波形内存缓存20项，不显示随机假波形。
- 剪辑工作台新增独立大窗口 AI 视觉检查：先对已保存工程本地抽取当前片段起止、前片段结束画面，再补人物基准，最多4图。预览准确图号和原片秒数；明确确认外发及费用后，调用已配置且现有视觉白名单核验的模型。独立 editor_visual scope 不混入视频生成预检历史；任务记录冻结工程修订、源片、抽帧映射、人物基准、模型修订与用户确认值。只显示有正文的成功历史，UTC时间明确转换到浏览器时区，查看不收费。工程或基准变化后旧报告标过期、旧证据拒绝再次提交。保留任意格式原文，不自动判定质量通过或修改片段。

验证：后端全量652项；后续定向回归、前端tsc/build、无网络真实FFmpeg五种转场/混合切镜/时长/采样抽帧/混音/软字幕，以及浏览器模拟三视图主图锁定、真实PCM波形移动/裁剪/保存恢复、视觉费用确认/成功报告复用和1440/900/540布局。无真实模型调用、无业务工程保存、无新数据库迁移。新增API已运行pnpm run openapi:update并同步generated client。

真实供应商三视图身份质量与视觉报告准确性尚未付费验收；不承诺全片逐帧、声音审查、转场合成画面审查或身份绝对一致。复杂任意特效、叠画、多机位、视频主体组内多角度编辑、跨浏览器参考草稿仍为未关闭阶段。证据文件作为项目引用保留，不自动删除历史证据或备份。


## 2026-09-11 四项增强部署收尾

本批代码、离线验证、正式部署阶段完成，仍为工作树待提交。2026-09-10 23:58:03–06（北京时间）已替换 backend/worker/beat/front；后端健康8000和Web7788均200，worker pong，线上视觉模型目录实际返回已配置qwen3.8-max。正式7788三视图参考锁定、真实PCM波形拖动/裁剪/保存刷新、视觉显式费用确认/成功历史免费复用与1440/900/540布局通过模拟写请求回归，未调用真实模型。

镜像：backend sha256:cfcedbf612f47c87e973eb1b798aee3a90268326d53428c539c1956dad3b1c94；worker sha256:11cd5ae2fdaba7de7e524fb9f80d98b2ea1166afa59d8c9307fc67570d0218c5；beat sha256:5521d39b37e411681cda42435d2879f3ba15e20f0fab2dfd4f9d326ae34a7e17；front sha256:e27dd1464028622875e602fb605c963f4df9eb9d83e325b9ec6bba7b7191b5e2。正式资源index-CDX6XJ1Q.js / index-DWjtRY1g.css。

重启前active/reserved为空。正式数据只读核对projects=2、shots=3、files=50、project_edits=0，数据库仍c9d1e3f5a701，无新增迁移。未改默认模型/端点，未删除备份/旧镜像/业务卷，未提交推送，原孤立init容器保留。git diff --name-status与新增文件已核对，映射LC-006/014/015和PL-011/016；主台账、架构摘要、架构、计划、手册与本记忆同步。前端现有大bundle提示保留。

剩余：真实付费三视图身份质量、视觉报告准确性、实际短剧成片人工验收；任意特效/叠画/多机位、组内多角度引用和跨浏览器参考草稿仍未关闭。本批没有把离线验证等同于真实模型验收。


## 2026-09-11 角色演员关联入口统一（工作树待提交批次）

LC-006/015：项目新建角色取消演员必选校验，明确提交actor_id=null，支持先建立剧情角色后选演员。角色编辑页新增独立“角色演员关联（可选）”区域，可补选、更换、解除；候选取项目演员列表并保留当前历史关联，保存仅PATCH actor_id，不覆盖角色基础信息或既有图片/视频。更换/解除已有演员需确认影响，提示重新核对主形象。未关联角色卡片显示明确文案。

复用既有允许null的后端契约，未修改API/数据库，无需重新生成OpenAPI。前端tsc通过；浏览器模拟验证未选演员创建、关联、刷新恢复、确认解除及仅提交actor_id通过，0真实业务写入/模型费用。部署与最终构建另记。所有未关闭PL保留，演员形象到既有角色图片/成片的自动传播未实现，也不自动发起收费重生成。


2026-09-11 17:56 选角入口部署收尾：前端tsc及生产构建通过，正式7788浏览器模拟未选演员创建、后补关联、刷新恢复、确认解除、三视图原流程均通过；无真实业务写入/付费。17:56:27北京时间前端容器已替换，镜像sha256:bb456b1984ca470d7dd41d3ed60f2b7e92e3a41fe7bf387887afc99062dfa079，资源index-B81T8kXM.js。后端健康200；本批仅部署前端，没有数据库迁移或后端重启。主台账/摘要/手册/架构/计划/记忆同步，已核对本批diff及新增RoleActorPanel与浏览器验证脚本，LC-006/015；全部原PL待办保持，工作树待提交。临时Vite5174按核实进程停止。没有提交推送或清理旧备份。


## 2026-09-11 三视图业务贯通（工作树待提交批次）

LC-006/010/011/014/015，PL-011/016：新增镜头人物角度目录，限定本镜头已关联角色及其演员的有效图片槽位，明确角色定妆/演员基准和角度，不自动全选。分镜首帧、关键帧、尾帧编辑共用目录，独立勾选、排序并沿用每帧参考保存。视频主体模式可将同角色多角度放入同一命名组，支持组内/组间排序、补图、移除，预览展示所有实际发送图片。准备度按图片总数计数；组数、每组和总图数独立按当前模型限制，不静默丢图。

剪辑视觉检查可显式选择本镜头角色/演员基准，选择起始画面/起止画面/邻镜连续性采样以分配最多4张输入；基准按选择顺序展示并冻结。显式空列表不自动推荐图片；旧客户端省略字段保留原主图兼容行为。材料缓存纳入采样方式、基准顺序、人物角度目录及文件版本；变化需重新准备，付费提交再校验指纹。历史查看仍免费，检查仍需主动外发与费用确认；不代表全片逐帧或人物质量通过。

OpenAPI及generated client已同步，新增GET /api/v1/studio/timeline/shots/{shot_id}/character-views，视觉准备DTO新增baseline_file_ids和sample_mode；无数据库结构迁移。代码/离线验证与部署、真实收费验收分开。已通过653项后端全量、后续11项定向（含新增Vidu 3/7图片边界）、前端tsc/build；浏览器模拟验证逐帧角度保存、命名主体分组及最终请求、视觉基准顺序与历史免费读取。最终部署状态见本批续记。

PL-016本轮补齐逐帧多角度、组内多图和视觉基准选择的代码阶段；跨浏览器未提交草稿、完整形象版本管理、其他未核验型号、真实收费出片质量仍未完成。PL-011保留真实成片验收与任意特效等原待办，不关闭其他PL。没有自动切换默认模型/套餐/端点，没有真实模型调用、Git提交推送或历史备份清理。


### 2026-09-11 18:35 三视图贯通部署收尾

已于北京时间18:35:47–49替换backend/worker/beat/front。8000健康、7788页面200，worker pong；线上OpenAPI包含character-views，真实镜头54e86904-9363-4317-b876-1000e0db2070只读返回2个角色组、4张角色/演员槽位图。正式7788多角度分组/改名/排序/全部图片预览与最终请求一致、视觉基准选择顺序及成功历史免费复用、剪辑保存/导出回归通过，全部业务写请求均模拟。逐帧角度选择/排序/清空/独立保存已在开发组件夹具验证。未执行真实付费生成或检查。

镜像：backend sha256:540d94b58611e9efc7df8d84d8906891b073d09a33a31cb8e91f9ae2e57a961d；worker sha256:e846d677f3153c9beb601f1d62a0eec0544544cb0b423b40376449b836ee3ea0；beat sha256:216ee5cf468fae3b21a264c088cda9fe2088c1be48f8aa8a148e3d0e0da536ec；front sha256:5aec3fb15f021e90aa4d3e247f572a618b6cfdad170baab1cf799a31b53cfadc。前端index-umuRSKGo.js / index-DWjtRY1g.css。最新最终前端改动经tsc和Docker生产构建通过，保留既有bundle大小提示。

重启前active/reserved为空。没有迁移数据库/重建存储容器，没有修改默认模型或调用收费接口；原初始化孤立容器和备份保留。本批已审查git diff --name-status及新增文件，映射LC-006/010/011/014/015，PL-011/016仅关闭本轮三处贯通代码/离线/部署阶段，真实质量验收及其他待办继续保留。仍为工作树待提交批次。


## 2026-09-11 分镜生成默认模型与跨标签同步修复（工作树待提交批次）

LC-006/010/011/014/015：本次视频模型回填当前默认配置，保留显式手选，清空恢复默认。项目设置现有入口指向系统设置，Project没有单独默认视频模型字段；未新增或修改实际配置。未配置默认时明确显示需选择，不把空框误称为已回填。

分镜详情改为按镜头字段队列串行保存，页面立即显示新值；迟到响应只确认本次字段，不整份覆盖其他标签页编辑。保存失败保留草稿并阻止生成，可显式重试；切换镜头后旧请求仍绑定旧镜头。标题/剧本备注排队并区分用户编辑与服务器刷新，避免相互覆盖和把刷新误作编辑。

视频准备度等待保存完成后重新检查，绑定保存确认版本及当前详情/参考变化，取消旧检查结果；主体空数组稳定引用避免无意义重复请求。返回视频tab、打开生成预览、进入费用确认及正式提交前先完成保存并读取最新镜头详情/已采用帧/镜头基础信息/项目比例/默认模型。读取途中又有编辑则停止，防止旧读取覆盖新保存。预览绑定源内容和参数；费用确认期间发生变化拒绝提交并要求重看预览，不自动重试收费调用。

GenerationOptionsPanel刷新同一模型修订且选项仍有效时保留用户分辨率/音频选择；模型或修订变化重新按合法默认初始化。选择、准备度、费用和提交均使用本次有效model_id；已有模型契约及后端来源校验保持。

验证：前端tsc通过；浏览器实际保存队列模拟慢回包/连续字段修改/不同镜头隔离/失败保留与重试通过；整页模拟覆盖默认回填/手选及清空、时长16→5秒保存后解除旧提示、比例防抖保存、维护标题/备注保存、刷新不重置720p、费用确认中外部修改拦截、最终请求使用最新时长比例及失败阻止生成。原主体多图预览/最终请求及生成治理回归通过，全部写请求本地模拟，0真实业务写入/模型费用。无API或数据库变更，无需OpenAPI重生成或后端部署。最终构建及前端部署另记。

本批修复不关闭其他PL；PL-004/011/013/016等既有未完成阶段保留，真实收费生成质量验收仍未进行。已按本批文件映射审查，不把既有大量工作树变更归为本次新实现；未提交推送或清理旧备份。


### 2026-09-11 19:06 默认模型与参数同步部署收尾

前端已于北京时间19:06:49替换启动，镜像sha256:2e602ad949c69ae48dcdc8719c122236236e8e156b114a98a2a876d5ccfed89c，正式资源index-NzN_6LZy.js / index-DWjtRY1g.css。生产构建（含tsc）通过，既有大bundle提示保留。8000健康及7788首页200；backend仍18:35镜像sha256:540d94b58611e9efc7df8d84d8906891b073d09a33a31cb8e91f9ae2e57a961d，worker/beat/数据库/对象存储未重启或迁移。

正式7788执行verify-studio-settings-sync.mjs通过：默认回填、手选/清空、慢保存后准备度、比例/维护文本保存、分辨率刷新保持、费用确认期间外部参数变更拒绝提交、最终新参数载荷及失败阻止/重试。全部写请求为本地夹具，0真实业务写入/费用。另以真实GET与所有写请求拦截只读打开正式页面，确认默认模型实际回填为“火山引擎 · doubao-seedance-1.5-pro”。保存队列和既有主体多图/生成治理测试结果见本批正文。

本批新useShotDetailSync.ts、两项新浏览器测试及既有ChapterStudio/GenerationOptionsPanel修改已审查，LC-006/010/011/014/015映射与架构/手册/计划同步；diff检查通过。所有原未关闭PL及真实付费/成片质量验收边界保持，工作树仍待提交。临时Vite5174按已核实进程停止；未清理历史备份、镜像或业务数据。

## 2026-09-11 19:21 百炼图片低分辨率任务修复（工作树待提交批次）

LC-010/011/012/014：任务 aa9d96dc5a5c4c648679932537424382 失败于 Worker 构造 ImageGenerationInput，冻结档位 preview 被旧 Literal[standard,high] 拒绝；审计 calls=[]，尚未发出供应商请求。共享图片输入契约补齐已接入规格中的 preview/ultra，继续由当前精确模型规格决定合法档位与尺寸，不改变用户模型、套餐端点、提示词或参考图。资产槽位、分镜关键帧、实验室共享执行链均受益。

2026-09-11核对百炼官方 https://help.aliyun.com/zh/model-studio/wan-image-generation-and-editing-api-reference ：wan2.7-image-pro支持1K/2K，带图最高2K；本次1024x1024转换为1024*1024符合尺寸约束。preview是系统内部档位，不是发送给供应商的参数。没有切换Token Plan或付费重试；实际产图与质量仍待用户主动生成验收。

验证：21项定向测试、后台全量661项测试通过；新增真实Worker快照投影→MockTransport适配器回归覆盖三类目标、有序参考图、精确尺寸和响应解析，原投影测试覆盖全部四档及空值。已运行pnpm run openapi:update，生成接口来自本地后端schema，前端tsc通过；diff检查通过。没有数据库迁移或前端运行逻辑变化，前端沿用19:06镜像。

backend/worker/beat已于北京时间19:21:26部署启动，分别为sha256:fe6f3f3c2dd20593d3bbd26f1aa1ee67d18ece1e0cebbef37fe975b31a9ff652、sha256:7daca7fb3d714eabd53714d3b4451cf12a5c03dcecf8b831741648c8e9f2b7a8、sha256:fad3d0f689a08ccfdd21b729c404ff9d1f831e9aadbc12c10df95b9483dbbe17。重启前active/reserved为空；部署后健康接口/首页200、worker pong，容器内preview契约验证通过。保留原失败记录，不自动重试、不修改业务数据、不清理历史镜像备份、不提交推送。所有未关闭PL阶段继续保留。

## 2026-09-11 手动分镜详情初始化修复（工作树待提交批次）

LC-006/011/014/015：手动POST /studio/shots原来只创建Shot，编辑准备聚合和工作室读取ShotDetail均404。现同一请求事务创建Shot与ShotDetail，基础默认与自动分镜一致（中景、平视、固定、4秒）；动作/情绪/提示词为空，保持pending，不伪造提取或生成就绪。调用方无需再单独POST详情。GET仍只读，不靠浏览器访问自动写库。

旧数据通过backend/scripts/repair_missing_shot_details.py补齐，默认只读列ID，--apply才提交；父记录加锁、复查缺失，已有详情不覆盖，可重复运行。正式库检查仅19dad913-ced4-4cd8-bbd4-de1d5bc0a606缺失，已补齐这一条，标题、剧本、已有素材及其他镜头不变；无schema迁移。脚本显式关闭数据库连接池，避免一次性进程退出时event loop closed。

验证：36项定向及全量663项后台测试通过，覆盖手动创建→详情读取/编辑→准备聚合、旧数据补齐/幂等/回滚/保留现有内容；OpenAPI update与前端tsc通过。正式7788只读浏览器回归scripts/verify-manual-shot-pages.mjs通过，编辑页不跳回列表、工作室精确定位该镜头并切换生成参数/视频生成，无pageerror或HTTP错误，所有写请求拦截（2次），无收费调用。截图保存local-reports/manual-shot-edit.png与manual-shot-studio.png。未修改前端页面逻辑。

最终部署快照另记；未关闭PL保持，不代表真实生成质量验收完成。本批只映射以上服务、测试、修复脚本、浏览器验证与文档，保留既有大量工作树二开，未提交推送、清理镜像或备份。

### 2026-09-11 19:40 手动分镜修复部署验收

backend/worker/beat最终于北京时间19:40:56启动，镜像依次为sha256:9a6da3b1a579d9143ea0e11f61d7c48757678586879446b30f94c4e037f2b2db、sha256:d27dcf471ff32d7066e8ff1f734211e141189fb03f5a6dd210360a02e18cfd91、sha256:abed7cb94a7f25f2901b59be9e75bcfec42a7d8322a2ab74f7dcd32d1b3889c5。重启前active/reserved为空；部署后健康200、worker pong，旧详情只读检查缺失ID为空且连接池正常退出。前端保持19:06版本，数据库仅补1条缺失详情，无Alembic迁移。新建事务/已有内容保护测试及正式浏览器读验证见上节。代码与新增维护/验证脚本、手册、台账、架构及项目记忆同步，差异检查通过；未触发模型费用或提交推送，所有未关闭计划保留。


## 2026-09-11 单帧质量辅助与初始提示词（工作树待提交批次）

LC-006/010/011/014/015，推进PL-016。首帧/关键帧/尾帧弹窗首次没有保存文本时，免费读取本镜头剧本摘录、画面描述、镜头设置、项目风格和已关联资产，按对应动作阶段整理可编辑草稿；不调用模型，不自动保存或覆盖已有文本。提供“保存本帧提示词”；按规则渲染使用当前编辑稿。资料不足明确提示补充，不把整章和邻镜动作搬入本帧。

单帧主流程为参考/规格→提示词→生成→采用；免费基础检查与默认折叠的可选AI辅助分开。可主动进行一次review_and_revise调用，返回完整优化稿、修改说明、未解决项；保留原文与严格解析，不额外付费重试。已有成功报告可显式复用。应用保存版本标记，最终预览和生成使用同一完整优化文本；支持恢复原稿，参数/来源/参考变化使旧标记失效。保存的基础草稿与优化后的最终稿分开。

历史严格按镜头、video/first/key/last用途、生成前后阶段隔离；生成后再按具体产物file_id隔离。历史无用途记录进入legacy，仅显式引用，不当成本帧已检查。图片历史提供“检查此图片（可选）”，明确目标图与最多3张自选角色角度基准，送检最多4张且目标图不能取消；后端验证图片属于同镜头同帧。只展示成功且有正文的记录。文本模型能力明确显示，未接入看图的模型不能携图提交；生成前不声称已检查产物像素，生成后应用文本只影响下一次生成。

API新增免费帧草稿、免费基础检查、检查模型目录和恢复标记，扩充检查动作/context及历史筛选；已运行pnpm run openapi:update，前端统一generated client。无数据库schema迁移，业务细节留在生成弹窗，任务中心仍为通用状态面板。真实厂商输出格式和视觉判断准确性尚未付费验收；未知输出保留原文且不生成可应用假稿。PL-016原有跨浏览器未保存草稿、完整形象版本及真实出片等待办保持，其他未关闭PL不变。

验证：前阶段全量668项后端通过；新增初始草稿后16项定向通过，最终回归/部署见后续快照。前端tsc通过；浏览器模拟验证历史隔离、一次联合调用、对比/应用/恢复、刷新恢复、具体图片/视觉能力约束、空稿填充/保存和最终请求精确使用优化稿。所有业务写请求均模拟，0真实模型调用。未提交推送或清理历史备份、镜像和业务卷。


单帧批次最终离线复盘：669项后端全量通过；前端tsc及生产构建通过。浏览器新增空稿免费填充、手动修改/显式保存、已有稿不触发整理及真实组件最终生成载荷验证通过；原视频历史/优化流程回归通过。修复切换检查模型后费用确认的延迟重置，并使已撤销记录不再显示为已应用。部署快照另记。


正式环境补充核验：实际三种帧初稿及最终渲染通过只读浏览器验证，0业务写入/模型调用；免费基础检查真实返回ready。第二章旧2条报告只在legacy范围返回，video/first/key/last均0，避免冒充本帧预检。生产发现并修正检查模型目录的状态枚举及中文历史供应商名称识别，改为沿用执行层不可变修订和注册表，active/testing可选、disabled排除；新增11项定向回归通过（包含此目录与初稿）。原669项全量、前端构建及正式模拟提交证据保持。最终镜像另记。


### 2026-09-11 21:12 单帧改造最终部署与自检

五项改造及追加的初始提示词填充已完成代码、离线/模拟验证和正式部署。backend/worker/beat于北京时间21:12:14–15、front于21:12:18启动；worker pong，8000健康与7788页面正常。正式三帧只读浏览器验证初稿非空、最终渲染、免费检查通过；1700/900/540宽度验证底部生成/取消始终可见，内容在弹窗内滚动。正式页面模拟联合检查→对比→应用→费用确认→生成载荷通过，空稿填充/手动保存通过；0真实业务写入、0收费调用。检查模型目录实际返回qwen3.8-max支持看图，doubao-seed-2.0-lite仅文字（当前接入边界）。

镜像：backend sha256:8e5251b0e3eb37fc28a1b94f35a71255df21f37954cf6faaea4e8e806f3c6952；worker sha256:0bdd954e5595ea8e6205afd7d404860d1c8d7085ad06fc3522ed8263a8119d95；beat sha256:137ba95e8b37be37afbb8a662a6f4fab06f1d641343ca0fa6f39e4321c2d9972；front sha256:3abefb160712625fffa64fb36896f1fd93d53464983bca29e3e51755415a83d4。资源index-BC83Y1aB.js / index-DWjtRY1g.css。

验证为669项全量后端、目录修正后的11项定向、最终生产构建含tsc、真实只读与模拟写浏览器回归；不称为真实厂商质量验收。没有schema迁移或修改默认模型，原初始化容器/历史备份/业务卷保留。临时Vite5174、HTTP8011已按进程核对停止，临时OpenAPI文件已删除。git diff --name-status与新增脚本/组件已审查，LC-006/010/011/014/015及PL-016映射、手册、摘要和记忆同步，工作树待提交。所有其他未关闭计划及真实付费质量验收保持。


## 2026-09-12 模型任务非阻塞交互（工作树待提交批次）

映射 LC-006（逐帧/章节交互）、LC-011（通用任务与实验室）、LC-014（规范与部署）、LC-015（生成与质量追溯），新计划 PL-017 来源 `site/content/docs/plans/nonblocking-model-tasks.md`。代码和离线验证完成，部署快照另记；全部既有未关闭 PL 保持，不以本批替代。

- 帧图、资产图片受理后关闭生成弹窗，后台继续；旧帧完成不关闭后开的窗口。章节分析/精简/优化可关闭；模型文本形成待采用候选，不自动覆盖编辑稿，采用后仍需主动保存。
- 剧本导入分析可离开，任务及已产生结果不随关闭丢弃；仅保存批次恢复ID，不把未保存编辑冒充已保存草稿。任务中心可回跳原批次；章节处理回编辑区、帧图回原镜头。
- 文本实验室改走已有 `/labs/text/sessions/{session_id}/tasks` Outbox 异步入口；不再依赖页面SSE持续连接，完成文本保存在原会话。当前入口改为完成后显示完整结果，不再逐字流式展示。三种实验室均可切换会话，当前会话的依赖操作局部限制。
- 任务运行时覆盖全部分页、跟踪已受理ID和漏过最近时间窗口的任务；断网保留状态、不自动重提。取消是显式独立动作。移除常驻运行通知，短暂完成提醒不抢焦点；任务中心保留轻量状态与业务回跳。
- AI检查历史新增 `active_tasks`，按镜头/用途/前后阶段/产物隔离，只有同输入上下文才恢复为当前运行状态；成功正文仍单独分页，失败不参与复用。剪辑视觉检查按工程/片段/版本恢复任务指针；图片/视频实验室及视频编辑展示统一后台运行提示。
- 根目录 AGENTS.md 已写入“模型任务非阻塞交互”7条长期规范。架构说明见 `site/content/docs/architecture/nonblocking-model-tasks.md`。

验证：后端全量671项通过，OpenAPI generated已同步；前端tsc及构建通过，通知修订后最终构建另核实。真实组件浏览器模拟通过文本跨会话并行/刷新/断网保留/完成提醒/结果隔离、章节关闭后结果恢复与明确采用、帧图受理后关闭及旧任务不关闭新窗口、AI检查运行恢复/跨帧隔离。测试请求拦截，0收费模型调用。服务端并发仍受worker资源和供应商限额约束；不承诺无限并发、跨浏览器恢复未保存草稿或浏览器关闭时操作系统通知。


### 2026-09-12 08:50 非阻塞任务交互部署验收（LC-006/011/014/015，PL-017）

本轮方案已完成代码、离线/模拟验证与正式部署。backend/worker/beat北京时间08:50:07启动，front 08:50:11启动；8000健康、7788页面、worker pong正常。正式API返回active_tasks独立字段。正式页面慢帧模拟验证：受理关闭、继续打开其他帧、旧任务完成不关闭新窗口、0取消请求；三帧只读草稿/渲染与页面回归通过，0业务写入/0收费调用。

最终镜像：backend sha256:63b24a764df0ef568e690f0e5796c0c7d1c4d859485426dec337084e2e73bc17；worker sha256:6be4bdb48d3734f594d44a21ec0ae7b5cad3d9fc4480e29e7226f5e061640b23；beat sha256:55015dc0454a71b72838bcdf3c9228113c6f9a9712ac2f13e721a978928d52f2；front sha256:c4e4bb738d1fbf9da1a77dd73c5854b4e35c1923004a2cc5f8707931cb77fd3f。生产资源index-C7TSGhqz.js / index-DVSn2yrJ.css。

验证：671项后端全量通过；最终前端tsc与Docker生产构建通过。真实组件模拟3项后台任务覆盖跨会话并行、刷新恢复、断网保留、完成提醒、章节关闭后结果恢复及明确采用；质量辅助覆盖运行恢复/跨帧隔离；正式帧图模拟和只读浏览器通过。git diff --name-status、相关新增文件和diff --check已审查，主台账/摘要/手册/架构/计划/AGENTS/记忆同步。既有大bundle提示保留。

没有schema迁移、收费模型调用、默认模型修改、提交推送或删除业务卷/历史备份；初始化孤立容器保留。PL-017关闭本轮实施/部署阶段，真实供应商延迟与并发限额尚未付费验收；所有其他未关闭PL保留。工作树待提交，Git分支local/stable-codex-0718，HEAD仍700333cb17e926caa73f68070cec4803c02a4d78，不代表工作树全部修改。

## 2026-09-12 即梦服务版本与场景分流（工作树待提交批次）

LC-010/011/012/014，推进PL-014。模型管理新增/编辑即梦时选择服务版本，目录为“即梦AI-图片生成3.0”“即梦AI-图片生成4.0”“即梦AI-视频生成3.0”；不再推荐固定SP或双帧req_key作为通用型号。类别/供应商切换继续清空旧选择并拒绝迟到目录回填。版本必须明确选择，目录不代表账户已开通。

图片3.0无参考→jimeng_t2i_v30；1张本地JPEG/PNG参考→jimeng_i2i_v30（4.7MB内），多图拒绝，不静默升级4.0。文生标准/高清尺寸与参考标准档由同一矩阵生成；3.0参考接口输出宽高由厂商调整，不承诺精确等于请求尺寸。图片4.0普通服务→jimeng_t2i_v40，当前保持2K单图文生；原SP配置t2i_v40_jimeng保留原语义，不混用。3.0文生关闭供应商默认扩写，保留用户确认的完整提示词。

视频3.0按纯文字/首帧/首尾帧与720P/1080P选择六个官方req_key；时长5/10秒→frames121/241。纯文字发送aspect_ratio，带图发送按序Base64，检查比例/尺寸；仅尾帧、关键帧组、主体组及未知参数拒绝。提交和轮询复用同一req_key，沿用AK/SK签名及CVSync2AsyncSubmitTask/GetResult、Version2022-08-31；不重复提交。旧固定视频配置保留原模式及分辨率，需用户编辑改选服务版本才开启动态分流。

官网已用浏览器正文核验：2533614为产品动态入口；图片3.0文生1616429、图生1747301；4.0普通1817045、SP1863351；视频720P目录1792710、1080P目录1792711及其六项接口正文。图片与视频使用相同Visual异步提交地址，Body参数按接口区分。原台账中“即梦全部仅SP文生/双帧”的边界由本节更新。

验证：680项后端全量通过（新增9项分流/请求Mock）；OpenAPI已同步，前端类型初检通过，最终构建/浏览器/部署结果另记。本批无schema迁移，不修改账户凭据、默认模型或已保存服务版本，0收费调用；所有其他未关闭PL保留。

未完成边界：专用运镜template_id/camera_strength的生成页面控件及贯通尚未实现；3.1/4.6/视频Pro、4.0公网参考/4K和真实收费质量验收继续PL-014，不将这些模式冒称已支持。

### 2026-09-12 11:38 即梦版本目录部署验收

backend/worker/beat于北京时间11:38:13–14、front于11:38:17部署重启。API8000健康200、Web7788为200、worker pong；正式浏览器verify-jimeng-catalog.mjs验证图片/视频版本目录、清除旧类别选择及0写请求通过，原verify-model-edit.mjs验证其他供应商编辑联动通过。最终前端tsc和生产构建通过，资源index-CQcSk86f.js / index-DVSn2yrJ.css；后端680项全量通过。未做真实收费生成/账户额度验收。

镜像：backend sha256:78bc98f0373c6e23cca4e26ba5b5b69c0d8a700b5a0d2f003704fb7e408bfe5c；worker sha256:c9cacbd62fb65fe92409ccb31141d97395edabfe174707d718445a02aaedf88a；beat sha256:da2d1bdf9571e48b416f96779a4ed81da09742f33404fb658ed668a9a57ba8dd；front sha256:b63c9ed748124bbea854d7048381efd5758b8a1d09f969d67ca8d0e60524b316。

本批文件映射：LC-010=模型目录/ModelsTab/选型与审计/provider_bootstrap；LC-011/012=jimeng_media、图片/视频capabilities、domestic_preflight、specifications、prompt_budget及GenerationOptionsPanel；LC-014=官方核验、测试脚本与台账/记忆/手册/计划。git diff --name-status与空白检查已审查，既有工作树变更保留原归属。无schema迁移、凭据/默认模型/已有配置写入、付费重试、提交推送或历史镜像/卷清理。PL-014仅完成本次服务目录和基础场景分流的实现/离线/部署阶段，专用运镜及其他未接入模式继续待办，其他未关闭PL保持。


## 2026-09-12 官方来源与型号匹配复核（工作树待提交批次）

LC-010/011/012/014；PL-007/014继续跟踪。动态枚举16个注册供应商，并只读核实生产7个型号（阿里4、火山3；即梦账户未保存型号）。阿里Token Plan与火山/api/plan/v3配置保持原计费渠道，不自动转标准API。

本批修复：官方来源按具体型号/类别/计费渠道绑定，厂商总入口不再冒充每个型号的接口依据；即梦语言查询参数可读取，3.0/4.0与旧固定req_key分别绑定对应文档。MiniMax中文文档迁移到platform.minimax.cn，API账户地址不随文档地址改动。Vidu Q2/Pro/Turbo、Q1 Classic、Q3 Pro按官方模式矩阵分别约束，禁止错误帧/主体模式。BFL精确型号检查、Kontext参考图场景推荐与尺寸16倍数校验补齐；MiniMax未实现分辨率/音频参数明确拒绝，不静默丢弃。

同步规则绑定来源URL与正文SHA，读取失败/正文变化后未解析出的规则不再沿用；显式回退固定版本单独标记。价格仅接受已登记精确型号、北京区域、人民币单位和唯一完整价格行；不推断其他币种、不把标准原价当套餐实扣。场景选型区分本地接入与官方型号证据；接口与价格同步页显示来源覆盖、尚未检查/读取失败/变化待核验、当前生效规则与北京时间。

首轮公开文档只读审计55地址，42可直接读取。腾讯/可灵等动态正文及部分国际站无法被后台读取的来源保持待核对；浏览器/搜索可读不等于后台定时同步成功。注册覆盖不等于全部型号官方字段或真实生成验收。审计脚本scripts/audit-official-model-sources.py仅访问公开文档，原始快照在.local；新增来源后按动态范围继续检查。无收费调用、凭据/默认模型变更或schema迁移。完整测试及部署状态另记；既有未关闭PL不取消，复杂API变动仍需适配代码与验证。


### 2026-09-12 12:46 来源治理部署验收

工作树待提交。689项后端全量通过；1条既有SQLite测试线程关闭警告，未导致失败。前端tsc、Vite构建及Docker构建通过（保留已有大bundle提示）；OpenAPI同步完成。git diff --name-status / diff --check已审查，相关新增文件归属同批LC-010/011/012/014；既有工作树批次保留。

正式backend/worker/beat于北京时间12:46:31–32、front于12:46:34重启；API健康200，正式Web只读验收脚本verify-model-source-governance.mjs通过：7项已配置模型、来源覆盖、价格目标隔离、0写请求和0页面异常。运行报告当前73个型号/类别目标，17个绑定型号来源、56个仅协议/总览来源，60个去重文档地址；这不是73个型号完整官方字段验收。已排队一次免费文档检查，完成状态以接口与价格同步页刷新为准。

实际镜像：backend sha256:566c412a46cb0ff79b409db9daf9dfbe7d289f40974fd04555c375e861a787e6；worker sha256:a712eafec0f505a3b6a5de19d791f35c51744ef1fdb52742289ee84d6ba6e522；beat sha256:1a4a2dfefb5c824d5acfdd4c783a14b6a0d8fe1da688a2df91c2d5b8f14a47f0；front sha256:18c3bbafb8b91ff28844d04c0be5918e7a30ca7b19499637dbe3a67abf059fb3。前端资源index-Bz2MRhGd.js / index-DVSn2yrJ.css。

无schema迁移、收费调用、凭据/默认模型修改、Git提交推送、历史镜像或业务卷清理。PL-007/014中的动态文档读取、精确型号深度核验及真实账户质量/计费验收继续；不能把本次来源治理交付等同全部厂家协议自动适配完成。


12:49最终同步验收：本轮生产免费文档任务已完成，60个来源中49个正文可读、4个访问失败、7个动态正文不可读。HappyHorse与Wan2.7的精确型号/北京/人民币价格规则已重新解析并绑定当前SHA；当前套餐端点不因此改为标准计费。正式即梦目录回归亦通过（图片/视频版本联动、旧选择清空、0业务写入）。后台读取失败及仅协议来源保持待核验，原始证据由系统同步记录保留。


## 2026-09-12 生成可靠性与质量闭环（PL-018，本批代码与离线验证）

工作树待提交批次，关联 LC-006/010/011/012/014/015；没有提交或推送。已完成前端类型/生产构建，后端整套699项通过，新增表迁移在隔离SQLite中升级/回退通过；部署状态见后续正式验收记录，未发起付费模型调用。

- LC-011/012：普通图片/视频任务新增`generation_recovery`私有回执。提交前持久化、受理响应在对象存储保留，恢复重放原响应并继续原查询；无回执的历史提交、结果不确定或输入变化时禁止重提。租约保护重复投递；每60秒检查有可靠回执的中断running任务，failed任务需在业务调用详情点击恢复。
- LC-010/011：厂商结果先保存再归档；归档对象及FileItem身份稳定，数据库回滚后复用已存字节。成功后清理本任务临时响应副本。厂商链接在首次下载前已失效、厂商任务已失效或对象存储不可用时，不能凭空恢复，不自动收费重试；清理异常保留待查日志，不删除历史业务文件。
- LC-010/014/015：场景选型说明展开行按业务模式展示输入和本地参数约束，区分型号来源绑定、仅协议来源和缺少来源；来源绑定不是全参数核验或账户验收。原视频编辑列表对HappyHorse1.1I2V、Seedance1.5Pro显示具体不适用原因及官方依据，长说明换行；其他未接通型号不推断整个厂商不支持。
- LC-006/010/014：本帧参考增加免费本地方案建议，按角色角度优先角色定妆、缺少时明确演员基准；场景/服装/道具只建议已关联资产，显式替换后仍可选择排序和保存。缺角度不借其他角度，不承诺自动识别镜头最佳参考或零穿帮。
- LC-010/014：图片实验室提供框选修正，当前精确范围为即梦图片3.0家族/固定图生图3.0及FLUX Kontext Pro，一张原图、一张新图；带框原图+文字送入已有编辑接口，再本地合成选区，保留原文件与前后对比。这是框选指导加本地合成，不是原生掩码API，不承诺低价；其他型号拒绝而非静默换型。当前入口在实验室，未把资产/关键帧全部入口改成原生掩码编辑。
- 历史记录（2026-09-12已按用户要求移除业务）：LC-010/012/014/015新增`account_quota_records`，模型管理“额度与预算”登记人工来源、核验/到期时间、单位、余额与同单位预算。绑定账户及端点变化检测，超过24h标记需复核；未知非零、非免费。仅人工快照与预算提示，不接入实时账户余额，不自动扣减/强制预算封顶。
- 迁移`d0e2f4a6b802`，父版本`c9d1e3f5a701`，仅新增两张表。API变更已按当前后端运行`pnpm run openapi:update`同步generated client。

PL-018六项基础实现已落地；真实模型质量/费用验收、原生掩码扩展、账户官方余额接口仍需后续证据和专门验收，不能标记已验收。其余未关闭PL保持不变。


### 2026-09-12 14:38 PL-018 正式部署与验收

本批基础实现已部署：后端/worker/beat于14:22重启，前端弹窗高度修正于14:36部署。MySQL已从`c9d1e3f5a701`升级至`d0e2f4a6b802`，两张新增表只读确认存在。切换前active/reserved均为空；worker ping通过；模型治理原页面只读验收通过。

- 后端镜像：`sha256:466e32956afa6649273f3d8d0fb4effa38423f53d8c50cf39826b4880dd7ce20`。
- Worker：`sha256:cbc09816f1b9d9809fca5267006303cc7c6940658ca9c0f788b6904bb9467e0f`；Beat：`sha256:97d2f55e79a1d55dd17fcd0ba81e29a5e3fca28a1c82cf75f302a747fe636fa9`。
- 最终前端：`sha256:2ae633eaf6b9596139811e44d1d16dfa6cba7f1909bf57e029afc84a0b38ccda`；资源`index-DqehSLAL.js`、`index-DVSn2yrJ.css`。
- 备份：`E:\JellyfishNew\backups\generation-reliability-20260912-141725\database.sql`，2,975,240字节，SHA256 `5430be7640a52edab4d4d82f30993c623121b3a4b2995ed721ffaa13154088ed`；未清理旧备份/业务卷。
- 验证：整套后端699项通过，随后新增归档回滚/过期URL测试通过（恢复测试8项通过）；前端tsc与Docker生产构建通过，git diff --check通过。只读浏览器验证两个精确编辑排除原因与官方链接、模式契约、人工额度、配置页面、保存按钮可视区域，无API写入及页面脚本错误。
- 浏览器脚本：`scripts/verify-generation-reliability.mjs`、`scripts/verify-model-source-governance.mjs`。原生掩码扩展、实时账户余额和真实模型效果/费用未冒称已验收。本次没有发起付费模型任务，没有更改供应商凭据/默认模型，没有Git提交/推送。

仍为工作树待提交批次；PL-018本批基础交付完成，扩展和真实验收继续保留，不关闭其他PL。


## 2026-09-12 恢复与重复提交保护修复（工作树待提交）

关联 LC-010/011/012/014、PL-018。用户复盘后明确本轮优先修复现有缺陷，不继续扩展额度或框选功能。保留其他未关闭计划。

- 普通图片/视频的续查资格必须具有适配器明确确认的任务回执及已持久化响应；HTTP 2xx 本身不代表业务受理。公共图片/视频任务基类、即梦、百炼异步图片、BFL、MiniMax内部轮询已接确认钩子。原结果缓存仍允许仅补归档；旧回执缺少确认标记时不冒充已核验，不自动重提。
- 恢复收尾异常记录日志并等待租约过期，不再向执行器抛出并覆盖已经提交的成功状态。扫描中断任务不再由前100个活跃任务遮挡后续任务。
- 图片/视频实验室在同一事务中锁定会话，比较完整请求（含模型、提示词、按序参考和参数）；相同请求处于pending/running时返回409，不创建新消息、任务或Outbox。不同输入可并行，终态后可主动再生成。范围是同一会话，不宣称跨会话的全局计费去重。
- 图片实验室新增即时提交锁，拦截同一事件周期双击并展示服务端重复提示。重复投递同一任务仍由恢复租约保护。

无schema、凭据、默认模型修改；未发起付费调用。接口结构保持，已运行OpenAPI更新。隔离数据库/HTTP测试不等于真实供应商跨重启验收；数据库不可用、未保存回执、链接失效等仍不能保证自动恢复。最终回归与部署结果另记。


## 2026-09-12 附件网站/型号/API专项核对（工作树待提交）

LC-010/011/012/014，PL-007/014继续。用户要求回到附件与项目模型匹配核对，不以额度登记代替。详细矩阵见`site/content/docs/architecture/official-model-identity-audit.md`，列出附件、官方证据、本地实现、修正和待核验项。

动态枚举16个供应商，确认7个已配置型号；重点核验附件重合的7组国产厂商。MiniMax H3不等于Hailuo02；即梦Visual req_key不等于方舟Seedream名称；TokenHub hy-image-v3/hy-video-v1.5、智谱API枚举及阿里Wan2.7/HappyHorse有对应官方依据。可灵HTTP/浏览器仅有标题，仍待核验，不改名猜协议。

必要修复：ViduQ3Pro时长下限3→1秒，Q3/Q3Turbo补全请求7张参考总量及三档分辨率；腾讯/智谱/Hailuo精确型号文档身份补齐，Vidu增加中国站来源，目录说明即梦与H3的协议区别。当前配置Seedance1.5Pro在Agent Plan官方正文标注“即将下线”，未见明确日期；选型说明增加有日期的提示，不改默认型号/渠道。完整回归及部署结果另记。API结构无变化，无迁移、付费调用或账户配置写入，其他未关闭PL保留。


## 2026-09-12 移除人工额度与预算业务（工作树待提交）

用户明确人工登记没有实际作用，要求移除。LC-010/012/014/015、PL-018第6项取消；其他计划与生成业务保留。

移除模型表格/编辑弹窗入口、AccountQuotaButton、后端GET/PUT account-quota接口、人工登记DTO/service/ORM及专属测试；OpenAPI/generated client同步移除。模型操作列恢复紧凑宽度。更新只读浏览器验收为确认入口/接口均不存在。

官方价格同步、生成规格费用估算、历史任务费用快照、恢复与防重复提交均独立保留，不把删除人工登记扩展成取消费用估算。已执行的d0e2f4a6b802迁移保持原文；历史account_quota_records表不再被应用读写，此批无数据库删除/迁移。未修改凭据或默认模型，无付费调用。完整测试与部署结果另记。

### 2026-09-12 16:14 人工额度业务移除验收与部署

工作树待提交批次；对应 LC-010/012/014/015，PL-018 第6项按用户要求取消。全量后端706项通过（删除1项已取消业务测试），前端 tsc、生产构建、OpenAPI更新及差异空白检查通过。正式浏览器只读验证模型列表与编辑弹窗均无“额度与预算”，OpenAPI无 account-quota 路径，场景选型/模式约束/编辑适用性信息正常，无写请求和页面异常。Web/API health均200，worker ping成功。

16:12 已重建 backend/worker/beat/front；前端资源 index-DhxFPgqw.js。镜像：backend `sha256:b08fff96e6637e6985e472c2ad7bb74747caec033c1d5ebebca33bb11773e5a8`；worker `sha256:d8fe137d89ea4787fd746d0677d76d3960de0fb9c091aa7ed1be03ca0cc57648`；beat `sha256:989ff6c48f2e2bd4cabefe4bffc3eab6d8008a344beab6ff46788d913e034d55`；front `sha256:7b4c5a57bb6fb6facaf4b1e2f24a33cd6bc92cc2feaea1c8af695a1edfeacb89`。

费用估算、官方价格同步、恢复与防重复提交保留。本批无付费模型调用、无数据库迁移或数据删除，历史迁移/旧额度表仅保留兼容，不再被应用读写；其他未关闭计划保持。

## 2026-09-12 项目全局种子实际作用说明（工作树待提交）

LC-014/015：核对ProjectLobby、Project schema/ORM、全部seed引用和生成输入构建，项目seed仅保存/展示，没有继承到图片/视频请求。新建页面Math.floor(Math.random()*99999)产生0～99998的伪随机整数，可重复；API未提供seed时默认为0。供应商生成输入seed与项目字段独立，不能据其存在声称已贯通。

新增/编辑项目增加共用常显说明，项目详情悬停显示完整说明，明确“暂未用于生成”。修正原“固定种子可确保整部短剧视觉调性一致”和手册误导。修改项目seed不改变已有素材、运行任务或后续生成参数；保留原保存行为，不擅自启用跨厂商参数。无API/schema/模型调用变更；其他未关闭PL保持。类型检查与部署验收另记。

2026-09-12 16:25 验收续记：前端tsc、生产构建、git diff --check通过；16:22:41北京时间仅部署front，镜像sha256:3d90dd2b9cc9b236a6c2c26e84023cb64bdb79b609ee187cc2a81b7aeaef802d，资源index-9IDKpA6f.js。正式浏览器确认新增项目常显说明与默认整数范围，0写请求/0页面异常。首轮角色选择器包含图标名称导致超时，改用精确可见文字后通过，非页面故障。后端、生成参数、数据库与运行任务未修改，无付费调用。LC-014/015工作树待提交，所有未关闭PL保留。

## 2026-09-12 十种项目/资产风格扩展（工作树待提交）

LC-006/010/011/014/015：评估与当前实现见site/content/docs/plans/project-style-expansion.md。保留现实/动漫两类和旧七种风格，按用户最终要求新增真人玄幻修真/仙侠/穿越，动漫穿越/古装/武侠/玄幻修真/仙侠/国风神话/末世科幻。共用枚举覆盖项目、角色与资产契约；前端统一后备目录并解释视觉与题材区别。现有剧本brief、资产模板变量、帧初稿和视频prompt pack已传递风格，不修改厂商参数或旧素材。数据库String(32)无需迁移；未付费调用。OpenAPI、回归和部署结果续记；所有未关闭PL保留，未宣称新风格质量验收或自定义模板必然消费风格。

### 2026-09-12 用户要求先确认风格联动方案

用户最新要求：先输出扩展类型与视觉/视频风格联动关系，确认后再实施。当前十种新增风格仅为本地候选改动，尚未部署，不得作为已生效能力；暂停后续实现与部署，既有生产仍为原七种风格。前轮八种候选测试714通过不代表最终十种已验收；最终候选接口同步/测试已启动，但用户确认是继续推进的前提。后续必须以用户确认方案为准调整，不因已有本地代码推定授权。关联LC-006/010/011/014/015为候选待确认，其他未关闭PL不变。


## 2026-09-12 创作设定全链路已部署（PL-019）

当前实现见[创作设定、继承与生成上下文](./creative-direction)。四维分类、字段继承/来源、全用途提示词、预检版本绑定、实验室独立/关联项目、旧数据有据补值和删除引用保护已部署。对应LC-002/003/004/005/006/007/009/012/013/014/015；模型能力和计费渠道未更换。后端722项、类型/生产构建、只读页面和全模拟慢任务验证通过。29对象补值，48原业务表摘要不变，数据库e1f3a5b7c903。真实付费视觉质量未验收，PL-019该后续项与其他未关闭计划保留。详细运行镜像和备份见项目记忆/主台账，工作树待提交。

## 2026-09-12 第六项入口复盘纠正（覆盖上轮“全部完成”口径）

用户要求核对原方案“六、还要覆盖容易遗漏的入口”，已恢复原会话原文并核对当前部署代码。PL-019不是只剩付费视觉质量验收：同一角色多造型版本、模板管理的项目上下文预览、跨镜头完整设定审查仍未完成。上轮将这些标为完成不准确。

1. 剧本异步导入/分析/提取、手动对象继承、三帧统一渲染与重生成、普通视频/视频编辑、项目关联或独立实验室已有接入。
2. 演员/角色/三视图接入，但未实现“同一角色的服装/造型版本”。原方案明确不能靠新建另一个同名角色替代；上轮独立角色建议偏离要求，撤回此替代方案。
3. 生成端最终预览与历史版本校验已接入；模板管理仍是模板内容/变量及静态预览文案，没有绑定项目/章节/分镜设定的专门试渲染入口。
4. 成片视觉检查已有入口、抽样图、人物角度基准、当前片段设定、成功历史与指纹检查。缺口：只冻结当前片段的有效设定，未完整冻结上一采样镜头及相关角色/服装/场景的独立设定；送模metadata只含当前clip与当前角色，缺少逐采样镜头世界/时代/剧情变化依据。前端只显示图片与工程版本，不展示逐镜头有效设定/变化依据，也未监听设定保存后主动失效。
5. 隔离复现：修改上一镜头时代/题材后当前报告fingerprint不变(false)，修改当前镜头设定后fingerprint变化(true)。证据.local/creative-sixth-entry-audit.json；0模型调用、0生产写入。当前抽查源片起止/上一片段结尾，不检查导出成片转场字幕合成效果，也不是全片逐帧扫描。
6. 另发现未被当前调用者引用的同步辅助函数generate_division_result使用未定义run_args；主异步任务路径正常，但此遗留函数若被调用会报错，需修复或在核对调用方后清理，不能以722项通过掩盖缺失覆盖。

本次为代码与隔离审计、文档状态纠正；未修改业务代码、未重启、未付费、未修改生产库。PL-019上述内容恢复待办；其他未关闭PL不变。补齐验收需新增：上一镜头/角色服装设定变化导致证据失效；送模每张样图有正确镜头及设定映射；穿越/回忆的显式变化依据；页面展示实际送检设定；同角色造型版本；模板管理上下文预览。


## 2026-09-12 第六项缺口补齐（工作树待提交，LC-006/010/011/012/013/014/015，PL-019）

同角色新增不可变造型版本及逐镜头选用关系：角色页保存名称、造型描述、服装事实、创作设定和按角度图片；克隆另存新版，不复制另一个同名角色。镜头人物参考目录选用版本，再逐帧勾选角度；切换版本移除该角色旧勾选并使提示词/检查证据指纹变化。原角色身份、演员关系、历史图片与视频不改写。造型图先经角色原图片区生成/上传后选入保存，保存造型本身不调用模型，不承诺自动换装或跨造型质量必然一致。

模板管理新增“绑定业务对象试渲染（免费）”：按名称查询项目/章节/分镜/资产，使用实际模板引擎、当前设定、模板默认变量及显式试填值，显示模板版本、缺项和来源。此为试稿，不替代生成端含本次参考图、参数与执行约束的最终预览，不自动保存到业务提示词。

剪辑视觉检查逐一冻结当前和上一片段的有效设定、角色造型、独立资产设定及真实剧本摘录。每张视频样图记录clip_id/shot_id；送模采用相同快照，页面可展开实际材料。上一镜头设定变化会使旧材料失效，重新打开/设定保存后只刷新状态，不自动付费复检。穿越/回忆标签不自动证明时代或造型变化合理，要求具体剧情依据，未知标待核对。历史缺少逐镜头设定的材料明确提示不完整。

当前仍为源片起止/前片结尾的抽样审查，不是导出成片逐帧扫描，不检查转场/字幕合成像素或声音。此局限页面常显；真实模型视觉质量与实际计费仍未验收。其他未关闭PL原样保留。

同步辅助generate_division_result修复未定义run_args，显式传creative_context。文件删除动态JSON引用检查覆盖造型参考图；新增中文用途。API已运行pnpm run openapi:update；前端tsc通过；完整后端727项通过，含角色隔离/并发选择/图片保护、前镜头失效、真实送模映射、模板变量和同步辅助回归。新增迁移f2a4b6c8d014父e1f3a5b7c903，仅两张新表，隔离升降级保留原表；正式迁移已成功。前后端Docker生产构建通过，最终正式运行与浏览器验收结果续记。


### 2026-09-12 21:43 第六项补齐正式验收

已完成代码、OpenAPI、部署和只读页面验收；工作树待提交。完整后端727项通过；最后造型来源/引用校验补强14项专项通过；前端最终tsc与生产构建通过，git diff --name-status及diff --check已复查。正式角色造型入口、模板上下文试渲染及原创作设定页面验收通过，0业务写入、0模型调用、0页面脚本错误。首轮模板脚本未选中模板导致超时，按真实“先选模板”流程复验通过。切换造型批量移除旧角度一次更新，镜头切换后迟到响应不改新对象。

MySQL已迁移f2a4b6c8d014；48张原业务表行数和内容SHA256不变，只新增character_appearances、shot_character_appearances。备份E:\JellyfishNew\backups\creative-completion-20260912-212635\database.sql，3,141,770字节，SHA256 167ffe27caa0d43408497120cee8720e8aa1252754bccc3b2b1927c98ca5c789；同目录保存前后完整性摘要。未清理旧备份/业务卷、未更改供应商凭据或默认模型、未提交推送。

backend/worker/beat北京时间21:39:33启动，front最后于21:43:02启动；API/Web健康200，worker ping成功。运行镜像：
- /jellyfish-backend-1：sha256:b0dbc1ee13ac892691d1db78248d930b6cf48a8605ae28ed6abe1805f5424f80
- /jellyfish-celery-worker-1：sha256:4dac1daa30d0f8661b9443e30b00ad7ce5d596cfcc03e607fb1584af47eb6a2e
- /jellyfish-celery-beat-1：sha256:f2ca21ed2807ae3ceed293f59df79534e8b41d062e70b8a5b22d2934a05eaca3
- /jellyfish-front-1：sha256:46e4063ea80d9818e64b284967b88a375d6f6ba2ddfdcefda7ce5f0e695711b0

PL-019原第六项缺口已补齐，正式功能验收完成；真实付费视觉质量仍未验收。成片审查是源片抽样对照，不是导出合成/音轨/全片逐帧扫描。其他未关闭PL保持，不把本轮补齐等同全部历史计划完成。新增模型/迁移/接口/选择器归LC-006/012/014/015，模板上下文与编排归LC-013，跨镜头质量证据归LC-010/011/012；文档与部署记录归LC-014。


## 2026-09-12 创作表单简化与混合题材约束（工作树待提交）

LC-013/014/015、PL-019交付后用户反馈修正。新增共用表单把表现形式+画风合成“画面表现”，与故事主题材两列常显；辅助题材/标签及世界/美术以可选折叠内分页呈现。演员和资产弹窗正文限制65vh，宽度适配屏幕，保存区保持可见。编辑抽屉保留逐字段继承/恢复能力，改为紧凑网格与分组。

新增项目主动提交创作设定必须具备画面表现和主题材；其他可继承对象按有效值校验，自动剧本导入/迁移不被新表单规则误拦截。主题材不能同时为辅助题材，多选不允许重复/空白，辅助最多4项，标签最多10项、每项40字。空白文本归null，长文本沿用200/3000限制。字段有用途、示例与未知可留空说明。

用户强调混合题材矛盾风险：有辅助题材必须有主题材，并填写混合题材说明（复用world_rules，避免新增重复字段）；新建/编辑保存均校验有效继承值。主辅重复或缺少融合依据的旧配置仍可读取，历史记录不改写，但新预览/生成会要求修正，不能绕过客户端直接收费生成。都市修真、穿越等合理混合不按名称一刀切禁止。填写说明只是提供依据，不代表系统能证明自由文本无矛盾；显式AI预检仍核对时代、科技和超自然规则，不能宣称免费语义检查已覆盖所有冲突。

实际业务用途：辅助题材/叙事标签以及时代、地域、世界规则、美术约束进入统一direction_prompt，经资产/帧/视频/剧本用途编译与预检/优化/剪辑检查快照使用。新增主辅优先级指令强调按世界规则融合、不得自行拼接背景。字段是提示词上下文和审查依据，不是供应商强制参数；设定参考文件只记录依据，实际外发图片仍由生成页面选择。保存不重绘旧媒体、不自动付费预检。新tests/test_creative_form_validation.py覆盖重复空白、必填/旧调用兼容、继承后冲突、多用途字段消费、混合说明与旧配置生成门禁。

OpenAPI已重新同步，前端tsc通过，后端732项全量通过；先前校验误拦截内部剧本导入的问题已修正。正式部署/只读页面结果续记。无数据库迁移、付费模型调用或供应商配置变动，其他未关闭PL保持。


### 2026-09-12 22:42 创作表单与题材约束正式验收

本批已部署重启。732项后端全量、前端tsc及生产镜像构建、OpenAPI同步和diff --check通过。正式浏览器验证两项基础选择、主辅排重、混合说明必填提示/与世界规则联动、时代示例、原设定来源与免费预览；1366×768下演员弹窗展开世界美术后创建按钮仍在视口内。0业务写入/0模型调用/0页面脚本异常。首轮启动时目录尚未就绪、随后脚本虚拟列表定位重复分别修正时机/选择器后复验通过，未把初次失败冒称成功。

Web/API均200，worker pong；启动时间北京时间backend/worker/beat22:42:10，front22:42:14。无schema迁移、旧业务数据改写、默认模型/凭据变更、付费调用或Git提交推送。LC-013/014/015工作树待提交，其他未关闭PL和真实质量验收保留。运行镜像：
- /jellyfish-backend-1：sha256:d5f70341db1c0a857445ea1c510eae9c0ff8862f9cde3379ac046673c1c945a8
- /jellyfish-celery-worker-1：sha256:f24f9d2d8618172ff7420ffb739f0a678d36f4e57518adf1947a06c863ecb763
- /jellyfish-celery-beat-1：sha256:fe0a0333943877c13d164dc8df2f37080d1e214f5e08a35dfdc16c8d3014ad56
- /jellyfish-front-1：sha256:823235052199fb07302fffd605e995ac24e6670a0e88147b5bb950c51cd9b8c6


## 2026-09-12 已保存有效设定显示修正（LC-014/015，工作树待提交）

用户确认时代/地域/世界规则/美术约束是填写后主动删除；正式只读核实项目版本5中这些值为null，辅助题材和叙事标签为[]，没有保存丢失证据。按用户最新要求，已保存有效设定表过滤null、空白文本和空列表，只显示非空有效值；不展示“已清空”或空列表空白行。保留有效字段来源，区分本层与继承；标题注明本对象版本，来源列显示来源对象版本，两者无需相同。无有效项时仅显示暂无有效设定。

只改前端显示，不改保存/清空/继承语义、生成上下文、API或数据库。无需OpenAPI变更；前端类型与构建/正式验收续记。此前拟定的“无（已留空）”展示按用户要求撤回，未作为最终交付。


2026-09-12 23:07 显示修正已仅部署front。前端tsc、Docker生产构建、diff --check通过；正式只读浏览器逐字段对比接口有效值，空文本/空列表隐藏且非空字段无遗漏，0写入/0页面错误。运行镜像 sha256:1e99d736ccdd0c7f3a36043f8dd41954325f8ded1fbea7cb50b5dcbc9bebe9cf，启动UTC 2026-09-12T15:07:05.345688717Z。后端、数据库、生成及清空继承语义未改动；LC-014/015工作树待提交。

## 2026-09-13 网页生成候选边界

PL-020已开始，生产现有API生成不变。候选网页任务/账号锁/回填核心及离线测试存在，尚未部署或提供正式业务入口。独立豆包图片真实验证遇人机验证后无对应受理回执，未完成闭环；视频不开放。详见plans/doubao-web-generation.md，不将候选代码列为已生效能力。

### 2026-09-13 用户允许一次追加图片验证后的进展（未部署）

用户明确允许一次追加的相同楼房参考图测试，已取得豆包正式会话与生成消息，网页显示 Seedream 4.5；参考图和提示词生成一张建筑插画成功。独立 Edge 重启后按固定会话/消息恢复成功，使用网页原图下载按钮取得 PNG 2048×2048、1464525 字节，SHA256 为 013f34d2608368e9a8c05485ca9da15ef170b2c196cbfd844afcba93f9540216。聊天缩略图实际仅384×384，已区分缩略图与原图，不能把元素标注尺寸当作下载尺寸。首轮未知提交仍保留，未伪造成功回执；追加尝试与首轮分开记录。

原图采集增加页面异步加载等待及唯一图片/下载控件核对；后端会话与消息仅接受实际格式的数字标识，拒绝临时会话、无效ID及带其他参数地址。3项后端、5项Node离线回归通过（pytest缓存目录权限警告不影响测试结果）。这些验证不是多账号并发或正式业务验收。

本批归LC-001/010/011/012/014，工作树待提交。真实图片网页生成与原图下载已验证；Jellyfish正式账号配对、领取执行、业务入口及回填端到端仍未完成，未部署、未迁移、未回填任何现有分镜；视频仍待图片业务闭环后进行。图片带AI生成标识，公开发布授权及水印处理仍待核对；其他未关闭PL不变。
### 2026-09-13 02:01 网页账号池与图片候选入口已部署，真实验收按用户安排转下午

PL-020 / LC-001/006/009/010/011/012/014/015 工作树待提交。新增“系统设置→网页平台账号”维护、启停/配对状态/实际型号/执行器在线状态；生成请求默认自动账号，按平台和实际模型能力筛选、90秒在线窗口、空闲账号最久未使用优先。分派后assigned_account_id固定；每账号至多一个任务，忙碌/离线/未登录/受限不参与，不换号重发未知提交。领取前本地保存凭据，领取HTTP回包丢失仍恢复原任务；冻结参考下载与sha校验、任务原图回填CAS保留。

首/尾/关键帧最终提示词弹窗新增独立“网页生成”，默认自动账号。新增启动网页生成执行器.cmd、scripts/windows/Start-WebGeneration.ps1 和 browser-runner/worker.mjs；账号资料和令牌位于忽略的local-browser，亦从Docker构建上下文排除。当前已登记豆包本机创作账号并复用用户已登录的独立Edge；不读取日常浏览器密码或复制手机号。

验证：736项全量后端通过，后续6项网页专项及5项Node通过、前端tsc/生产构建通过。独立临时MySQL容器中实际Alembic全量迁移、账号表升降级、双账号并发和领取恢复通过。直接ORM建全库曾因既有MySQL约束差异失败，已改为真实Alembic链路验证，不据SQLite冒称MySQL通过。自动审批拒绝提取现有MySQL root凭据的原验证方法；未执行该方法，改用独立无业务挂载测试容器通过。

正式业务账号只读备份：backups/web-generation-20260913-015944/database.sql，SHA256 EBCAEFB0F433142E38DDC375E40DDA58FAD5D5E789A0064CEAAD5B45E6210E40。active任务为空后迁移a3c5e7f9b015，仅新增网页账号表；backend/front已切换，worker/beat保持。HTTP health与账号接口正常。

真实链路仍未完成：新增独立场景“网页通道验收·楼房参考图”（54e02bef-17a2-43ab-ab0f-3940050bdc87）、图片槽5、任务f1e7bc8db7b158efade6c46e2209c200；参考来自本地合成楼房。任务停在prepared，尚未提交平台。联调发现网页会保留未发送草稿/附件，重复prepare可能累计参考图，必须在提交前清理且精确核对；型号模式切换也需与此一并修复。保留回执不重建任务，已暂停真实执行并按用户最新安排下午人工配合。首轮网页单图生成/2048原图下载曾成功，不代表本次Jellyfish端到端通过；视频未打通。

当前Windows执行器已停止，无自动平台提交。后续自动推进文档、代码和离线测试；下午再实测登录/验证码/图像视频。需补：草稿与参考清理、人工核对/恢复入口、取消锁协调、资产生成入口复用、视频DTO/执行/业务回填、其他平台官方流程与适配、正式页面验收。去水印最后核对官方导出权益与授权，保留AI来源标识边界。所有其他PL保持。

### 2026-09-13 网页通道后续离线补齐（候选，尚未再次部署）

PL-020 / LC-001/006/009/010/011/012/014/015，工作树待提交。本批在02:01已部署版本之后继续补齐：资产图片生成弹窗复用网页入口；七平台账号登记和官方核验状态，非豆包执行器拒绝启动；补视频请求契约、参考角色数量校验、图片/视频分队列领取、真实MP4校验和原镜头版本保护，视频提交仍关闭。视频时长/画幅/分辨率不符时归档为skipped，不采用，不改变shot.status。已确认远端结果的重复绑定校验覆盖恢复阶段；实际参考文件哈希漂移拒绝发送。草稿附件逐一清除并校验数量/顺序/上传完成，禁止清除历史会话。已提交后恢复不再依赖重新下载参考；后端阶段回报丢失保留已核实会话。当前回执文件原子更新，取消/409时停止执行保留回执。

新增官方核对表：site/content/docs/reference/web-platform-integration-audit.md。网页产品与API供应商分别核实；即梦官网协议存在自动化工具限制，本阶段转人工交接/已有官方API路径设计，不能自动改收费通道。可灵文档读取受限，万相/元宝/清言动态页面不足以确认当前生成流程，海螺地区站不能混用；未知参数不硬编码为已支持。豆包协议本次展示2026-09-17生效版本，核验日期早于生效日，保留版本边界。水印最后核对官方导出权益，不承诺自动去除AI标识。

验证：OpenAPI已重新生成；前端tsc和执行器语法检查通过；12项网页后端专项、6项回执测试、3项无网络真实Edge DOM测试通过；独立无网络Docker容器实际生成1秒测试MP4并使用ffprobe解析320×180成功（仅本地合成，无模型调用）。测试发现规格不符不能使用conflicted，已修为现有skipped规则，专项复验通过。完整后端复验结果另记。MySQL临时验证容器已核实无业务挂载/宿主端口、仅tmpfs数据后按精确名称清理；未删除业务卷或备份。

仍待完成：Jellyfish图片回填真实端到端、豆包视频页面真实型号/参数及浏览器提交下载适配、未知提交人工核对入口与取消锁协调、其他平台逐项适配验收。并未因新增视频DTO就完成网页视频生成。用户下午配合真实验收，当前测试任务f1e7bc8db7b158efade6c46e2209c200保持prepared/needs_user，执行器停止，不新增平台提交。既有02:01部署保持；本次候选未部署。其他未关闭PL全部保留。

本批最终离线复验：744项后端测试全部通过（89.43秒）；前端tsc通过；Node回执6项、隔离Edge页面3项通过。临时OpenAPI服务已停止，临时MySQL验证容器/网络已清理；真实执行器仍停止。未进行本次候选部署或新增平台生成。


### 2026-09-13 多平台交接与官方导出代码批次（未部署）

用户明确要求其他平台与水印先实现代码，真实场景测试统一下午。本批归PL-020、LC-001/006/009/010/011/012/014/015，工作树待提交；全部其他PL保留。原先先图片验收再扩展的代码推进顺序按本次授权调整，真实验收与自动启用门槛不放宽。

新增web_handoff服务及contracts/API/OpenAPI：七平台人工交接、LRU预留账号、冻结输入与有序参考、回填指纹/实际模型/官网回执校验、提交未知保留账号、明确未提交取消释放。手工交接不被自动执行器领取；同条结果不复用，引用下载核验原快照。资产/三帧图片和视频确认入口增加交接，网页业务工作台恢复原任务，任务中心保持轻量。视频只允许明确纯文本/首帧/首尾帧，不压平主体参考组；无动态官网规格保证。

水印不只是登记：新增同一官网结果的官方无水印原文件导入、原件保留、SHA/尺寸/时长校验、幂等和显式CAS采用。相同带水印原件字节和缩略尺寸拒绝；新导出默认历史，采用时检查原目标仍选原文件及版本。人工来源标为user_attested，不冒称自动水印检测或发布授权；不裁剪/涂抹/剥离来源、购买或重生成。海螺国际站官方会员无水印规则有资料依据，不映射国内账户权益。

新增“打开网页平台账号.cmd”和PowerShell实现，按账号独立Edge目录打开固定官网，只供人工操作；代码未实际打开任何新账号。入口：系统设置→网页生成交接与导出（/web-generation），或网页平台账号的原任务链接；原任务页查看导出版本并选择采用。架构与手册详见architecture/web-platform-handoff.md、guide/web-platform-handoff.md。

验证：755项全量后端通过；最后水印原件/缩略图保护补强后11项专项通过。前端tsc/生产构建通过（保留既有大包提示）；隔离真实Edge运行前端代码，所有API与外站请求拦截，验证刷新不提交、multipart原件/导出/采用、1280/540宽布局，无页面异常、无真实写入或平台调用。PowerShell语法检查通过，非法账号参数负向检查另记。无数据库迁移、本批生产部署或付费调用。

边界：其他平台本批是可用的人工交接降级代码，不是七平台自动生成全部完成。豆包图片完整真实业务回填、豆包视频DOM适配及各平台自动登录/型号/参数/下载核验、自动执行器未知提交与取消锁完整交互仍待下午/后续逐项实现验收。自动化目标未取消；当前原豆包测试任务仍暂停，不新增相同测试。不宣称水印视觉检测或所有平台免费无水印可用。

最终收尾：最后调用身份字段补齐后23项网页专项通过；最终tsc与9项Node/离线Edge回归通过。非法账号路径在Windows PowerShell中按预期拒绝，脚本未打开官网。临时5187前端及8011 OpenAPI服务已停止；既有5174端口非本批启动，未终止。运行服务保持，本批0平台生成/0付费/0生产迁移，候选代码尚未部署。


## 2026-09-13 统一生成通道与网页型号选择（候选，未部署）

用户要求账号类型覆盖平台、所有图片/视频业务保留 API 并增加平台方式，以及网页选择具体模型。本批仍为工作树待提交；LC-001/006/009/010/011/012/014/015，PL-020 保持未关闭，所有其他计划不变。

- 账号平台来自后端七平台目录，前端不再重复硬编码。网页模型目录独立于 API：模型名来自本机执行器观察、官方网页作品示例和历史成功回执；未知可输入官网名称并进行人工交接。官网示例不证明当前账号有使用权限，不把 req_key 或 API 型号混入网页目录。
- 统一 API 调用 / 平台网页选择，默认保留 API；网页再选平台、实际型号、自动分配/指定账号、人工/已验证执行器方式。切平台/模型清除不兼容选择。视频网页准备不依赖 API 模型；网页材料控件不沿用 API 规格限制。自动浏览器图片仍限已观察豆包能力；没有自动启用其他平台。
- 接入资产列表快速生成、资产详情/演员/角色/三视图、首/关键/尾帧、分镜视频主入口与确认、批量帧/视频、Ctrl+Enter、图片/视频实验室、文字编辑视频。实验室任务原子追加原会话消息；视频编辑冻结原片及保留要求，完成另存，显式采用检查原镜头版本。主体参考按名称保留图片/视频/音频组及全局顺序，逐份冻结并展示下载。官网是否支持所选组和规格仍需核对。
- 局部图片修正保留选区，人工交接返回整图后复用已有局部合成，只改变选区；平台原件与合成结果均归档。比例不符拒绝合成；合成结果不允许直接由官网无水印整图替换，避免破坏局部保留语义。
- 批量交接单事务接收、逐任务独立请求号和目标；空闲账号最久未用优先，忙时排队，成功/明确未提交取消后才分配下一项，重新启用账号也触发待办分配。不释放受理不确定的账号，不切换 API 或自动重发。
- 新增平台模型查询、批量交接契约及实验室/编辑目标，OpenAPI 已重生成。无新增数据库迁移、真实平台调用、付费、生产部署；02:01部署及原豆包 prepared 任务保持。

阶段验证：前一完整回归 758 项通过；新增编辑、主体多媒体与局部合成后 29 项网页专项通过。通道控件隔离 Edge 测试、原件/导出/采用测试通过；最终类型/构建和全量复验结果另记。

真实账号模型选择、网页参数、图片/视频真实回填与各平台自动化验收统一下午/后续进行；人工交接代码不等于七平台自动生成全部完成。当前动态模型来源能力未知时明确待核对，不臆造已验证能力。


最终验收（2026-09-13，本批候选）：761项全量后端测试通过（98.12秒），最后网页专项29项通过；OpenAPI已同步；前端tsc及生产构建通过（既有大包提示）。隔离Edge通道选择测试通过平台/型号清空、固定目标及无隐式API提交；交接测试通过刷新不提交、原件/官方导出/采用及1280/540布局。所有HTTP业务与外站均被离线fixture拦截，0真实平台生成、0付费、0生产写入/迁移/部署。git diff --name-status清单已审查并映射本节LC/PL，工作树未提交；本轮额外关闭自建5187与8011测试服务，既有5174和正式服务不动。原豆包prepared任务保持，下午仍需真实验收，不能将候选代码等同七平台全自动完成。

## 2026-09-14 网页实测恢复与图片闭环（进行中，PL-020）

LC-001/006/010/011/012/014/015，工作树待提交。用户现场确认专用Edge已登录。七个原常驻容器从停止恢复，未迁移数据库或清理业务卷。修复豆包附件预览保持blob地址时HTTPS-only校验超时；准备/采集失败暂停等待核对，不再循环上传/刷新。10项Node回执/离线DOM测试通过，包含blob预览与原任务防重。

原任务f1e7bc8db7b158efade6c46e2209c200仅提交一次，豆包Seedream 4.5，官网会话38441498861456898、结果消息55431669099140866。下载曾因全屏图片层阻挡暂停，重开只续收同一结果，未再次生成。原图2048×2048、2034108字节，SHA256 3dfa57249c572c5017a62698fd9991b262c605a5aa1ee9cf359f443afeff1f78。文件b696f44c-52f3-4e35-8743-2b00a60177cc回填场景54e02bef-17a2-43ab-ab0f-3940050bdc87图片槽5；GET和正式页面实际图片加载通过。此项真实图片业务闭环已验收，不代表视频/多平台自动化完成。

上次统一网页通道候选backend/front已构建（含前端tsc）并替换；worker/beat及数据服务未重建。无schema迁移。部署前尝试按原容器镜像摘要创建本批回滚标签失败（本地镜像不可用），没有成功创建该回滚副本，不能照抄为已保留；既有历史标签/备份未删除。

用户追加授权Seedance 2.0 Mini与Seedance 2.0 Fast分别一次4秒视频，用同一楼房图/提示词、1:1；不升级会员、不切API。实际官网可见Mini/Fast、2.0与2.5（后两者提示升级）；时长为4–15秒滑块（默认10秒），另有多个比例。视频实验室独立会话37d51fd9-0e81-4e1c-bcbe-0b173b50cc27；Mini任务fef8a76295f850a8970cb822d07946b0先执行，Fast任务e7d8589f1d745aa8b465305de3282036排队，同账号不并发占用。当前视频走已部署的受监督交接任务，专用浏览器逐步实测，不将其冒称通用无人值守视频执行器完成；结果续记。所有其他未关闭PL保留。


## 2026-09-14 豆包双视频真实回填与 Edge 下载恢复验收

PL-020 保持未关闭；LC-001/006/010/011/012/014/015，工作树待提交。本轮完成的是豆包图片和两种视频型号的受监督真实闭环，不等于通用视频无人值守或七平台自动化全部验收。

- Mini：任务 `fef8a76295f850a8970cb822d07946b0`，官网会话 `38441549385184514`、结果 `55430007152144386`；文件 `85d8f6ed-3989-41e7-96a7-d6e691494792`，720×720、4064ms、227890字节，SHA256 `c604d1efb535b94a700a8f28a60928a920a297f3283308646618a4df40eebaf9`。
- Fast：任务 `e7d8589f1d745aa8b465305de3282036`，官网会话 `38441603005621250`、结果 `55436615793111554`；文件 `abaae5b3-fe5c-4d92-b778-b417366ada20`，720×720、4064ms、184969字节，SHA256 `21edbba5a48a240c37ea6c37faaf39d093fdf620196fdbc41a1cf7db84b2f4e8`。
- 两项使用同一原图、提示词、1:1和4秒；各一次生成，下载重试不再次生成。均 succeeded/completed/published，spec_issues 为空。原视频保留可见AI水印，未做无水印导出或购买升级；网站声明使用每日免费额度，未查询官方账单扣量。
- 同账号先Mini后Fast，未知提交期间不释放；Mini回填成功后分派Fast，两项成功后 active_task_id=null。两项追加到独立实验室会话 `37d51fd9-0e81-4e1c-bcbe-0b173b50cc27`，正式页面两条视频均可见；交接来源为 `user_attested` 受监督核对，不宣称平台签名证明。
- Mini初次超时源于官网将提示词加前后缀/空格，且把参考、文字、参数确认和最终视频分成多条消息。旧私有测试脚本精确匹配图片消息失败，并在finally关闭窗口。通过原历史会话恢复，没有重新生成；后续监督脚本先保存会话地址及确认意图，结果消息独立绑定。尚需将视频状态识别及恢复正式接入通用执行器后，再验收无人值守能力。
- Fast下载阶段本机 Edge `152.0.4191.66` 多次产生Crashpad记录，与Playwright上游问题42506的复用资料目录下载崩溃表现相符，根因未做原生堆栈证明。新增 `official-download.mjs`：仅在明确点击官网原文件下载时短暂捕获该操作产生的同源Blob下载，避开原生下载/saveAs/path；保留字节与水印、校验尺寸/类型边界、原子写入，finally恢复临时钩子。不同来源/多文件歧义停止，不能重新生成。图片执行器已使用此实现；视频本轮监督采集也使用它。
- 启动保留 Chromium 沙箱，取消默认 no-sandbox；专用窗口关闭旧崩溃恢复气泡，业务按持久回执恢复。历史profile.exit_type仍可能留Crashed，不能把隐藏旧提示说成修复Edge自身全部崩溃。实际新窗口UI Automation检查未发现“还原页面/Edge意外关闭/no-sandbox”提示，主进程无no-sandbox参数。
- 原图重新采集SHA与首次完全一致；离线同一Edge资料目录连续两次原始Blob保存和非法来源拒绝通过。两视频真实媒体解析与业务回填通过，实验室正式页面通过；通用自动视频、其他六平台真实生成、多账号并发压力、官方无水印与公开发布权益仍待后续验收。
- backend最终镜像 `sha256:4221488f509f546e36f28e2ed62aed394ee0332120acab16e6df43ef3407f9a0`，UTC启动 `2026-09-14T08:56:21.063981696Z`；front沿用本轮 `sha256:85b085a8b0010ec3c1f5805639ee45ba09ad0a1f09d6c933a071eb559de66155`。健康检查200；未迁移schema，未删除备份/业务卷，回滚标签创建失败的前述边界保留。
- git diff --name-status 与未跟踪文件均复盘，浏览器采集/启动修正归LC-001/011/012，目录说明归LC-010，测试归LC-014，台账/手册归LC-015。已有generated模型文件EOF空行告警未在本轮擅自批量改写。所有其他未关闭PL保留。

最终收尾：11项Node/隔离Edge回归全部通过（含同资料目录两次原文件保存、未知来源拒绝、原任务防重）；后端目录说明语法、执行器语法与本批文档diff检查通过。最终服务健康200，正式实验室两视频可见。专用只读复核窗口可由用户关闭；未启动自动生成循环。


## 2026-09-14 网页账号由页面自动打开（PL-020，工作树待提交）

用户要求系统自动传递本机账号编号，不再复制编号或运行单账号脚本。LC-001/006/010/011/012/014/015：新增本机助手、固定账号操作接口、账号页按钮与自动登录打开流程；保留此前图片/双视频实测和其他未关闭PL边界。

新增账号保存成功后自动请求“打开官网登录”；已有账号列表可点击“打开官网登录”或“启动图片执行器”。备注可改，编号/资料目录/既有任务不变。登录后先关闭登录窗口再切换执行器，忙碌账号不能另开登录；同账号同操作不重复启动。页面显示助手在线与窗口排队/打开/关闭/失败，打开窗口不冒称已核实官网身份。

Windows助手 `desktop-host.mjs` 使用已有执行器凭据主动轮询本机后端，没有新增浏览器访问Windows的HTTP端口、任意命令执行或前端凭据。服务端只允许login/runner固定操作；runner仅已启用豆包账号。每个命令绑定固定账号，浏览器启动脚本和官网地址为本机白名单；账号资料留在local-browser，不读取日常浏览器Cookie。助手启动子进程前持久记录意图，已领取命令重送只续报状态，不重复spawn。助手异常重启时未知窗口保守提示核对，不抢占原资料或取消生成。

沿用GenerationTask隐藏记录保存网页桌面控制命令和主机心跳，executor_type=web_desktop，不进入Celery、模型队列或任务中心。请求号幂等、账号行锁、主机身份固定；60秒未领取点击过期，30秒无心跳提示离线，GET刷新无启动副作用。正式数据库autoflush=False下先flush回报再查询待领取命令，防止opened被旧数据库状态重新覆盖为starting；已补对应回归。无数据库结构迁移。

新增 `Start-WebDesktop.ps1`，Jellyfish一键启动服务自动启动助手；本机部署登记当前Windows用户的“Jellyfish 网页本机助手.lnk”登录启动项。助手后台运行，只有明确页面打开命令才显示官网窗口；不自动发起图片/视频生成。目录迁移需在新目录重新执行该脚本的RegisterLogon更新启动项。其他单账号CMD保留维护兼容，日常操作不再需要它们。

API新增desktop/status、accounts/{id}/desktop、desktop/poll；已运行pnpm run openapi:update，前端使用generated client。14项网页/桌面后端专项通过；前端tsc、生产构建通过；隔离Edge页面验证新增自动传编号、改名不启动、刷新不启动、助手离线禁用通过。真实页面打开已有改名账号已到达专用Edge，状态回报修正后正式复验另记。没有新增图片/视频生成调用，旧任务/文件/账号身份不变。工作树未提交，其他PL不关闭。


最终部署与验收（2026-09-14）：backend `sha256:0984ea349a9ff70b567f1c98cfc660ed159cb0b3464d0b3259fb871360e71afd`，UTC启动2026-09-14T10:02:51.065759009Z；front `sha256:f95da47a24ebd9ba4706e6febf60cedd32f1a39364f366d7ab1fb5205fedbc21`，UTC启动2026-09-14T09:49:21.679388909Z。无schema迁移；本机助手在线，已登记当前用户登录自启，未实际重启Windows验收。

正式页面按固定账号编号而非备注定位，已打开用户改名后的“熊先生”原豆包资料目录；第一次命令117822683c5a57c3b695d66aaf42f9cd正常关闭，修正autoflush状态后再次由页面点击打开成功，状态opened，浏览器页面无JS错误。没有新增测试账号，也没有提交图片/视频生成。图片执行器按钮使用既有worker固定路径，未为了启动测试额外执行模型任务。14项后端专项、OpenAPI生成、tsc/构建及隔离页面验证通过；本批diff和未跟踪文件已映射上述LC/PL。代码未提交，真实官网用户ID识别与通用视频自动执行仍保留待办。


## 2026-09-14 网页型号维护与执行可用性（工作树待提交）

LC-001/006/010/011/012/014/015，PL-020继续；其他全部未关闭计划保留。网页型号目录按平台和image/video独立维护，支持新增、改名、停用、默认值、限制说明及排除账号；API模型/凭据/默认值不变。新表web_model_catalogs以revision进行CAS更新，迁移b4d6f8a0c016父a3c5e7f9b015。代码/离线验证与最终部署证据分开记录。

目录合并初始官网证据、执行器图片型号与成功回执；维护策略优先，旧名称停用记录避免被历史成功重新启用。名称不等于适配能力，只有已验证豆包Seedream 4.5单张普通图片开放自动执行；视频、其他平台、批量、局部修图继续人工交接。目录新增不扩大适配器白名单，不自动升级会员/切收费模型。免费权益未能可靠读取时始终未知，没有恢复人工额度预算登记。

生成弹窗自动读取本平台本用途默认型号，用户手选后刷新不覆盖；每3秒串行读取状态，迟到响应隔离。常显缺少条件（用途/平台/型号未适配、执行器离线、登录验证、账号限制/占用），可刷新并打开账号或型号管理。只有同一最新上下文检查通过才开放自动选项；失败或离线立即禁用。弹窗按视口限制正文高度，固定标题与操作区。

新任务后端复核停用和账号排除，冻结目录修订与选定型号快照；已受理请求幂等重取先于账号/目录的新策略，不重新提交。排队待分配任务按当前策略筛选；已经绑定的运行任务保留账号、型号、输入、目标与回填关系，不因目录修改重发或串用。网页模型接口和execution-status契约已同步OpenAPI/generated client。

验证：首次完整后端765项通过；随后补原任务在停用账号/目录后幂等恢复的专项，最终数量与部署另记。隔离迁移升降级保留原表通过；真实React离线浏览器通过改名/刷新恢复、媒体目录隔离、默认型号、在线状态刷新、草稿保持、小屏页脚，0平台请求。前端tsc和生产构建通过，最后布局微调复验另记。本批未调用收费模型、未修改真实型号目录或账号策略、未提交推送。通用自动视频/其他平台/真实网站权益探测仍属PL-020后续，不冒称已完成。


### 2026-09-14 网页型号维护最终部署验收

本批已部署，仍工作树待提交。完整后端765项通过；增加原请求在账号/目录停用后的幂等恢复后34项专项通过，最后逐账号原因修正3项专项通过。OpenAPI/generated、前端tsc及Docker生产构建通过；隔离React改名保存/刷新恢复、媒体隔离、默认、动态在线状态、草稿保留与小屏页脚通过。正式7788网页模型图片/视频目录及账号入口只读浏览器通过，0写请求/0外部平台调用/0页面异常。初次正式验收工具未放行实际localhost:8000 API，修正本机GET/OPTIONS白名单后通过；不是生产目录故障。

正式迁移a3c5e7f9b015→b4d6f8a0c016成功，只新增web_model_catalogs。未写入真实目录默认值或账号排除策略，保留当前账号与API配置。部署前SQL备份：`E:\JellyfishNew\backups\web-model-catalog-20260914-190558\database.sql`，3228408字节，SHA256 `f0a66309050a937b4e58751582520e213ecb2f4cca6b8139cae401260cd2e395`；未删除既有备份或业务卷。

- /jellyfish-backend-1：`sha256:e2fe9fc60906281a0e4cfd3636198f5a7e4e6fb782405a9146279b514294ae34`，UTC启动 2026-09-14T11:19:30.476027136Z。
- /jellyfish-front-1：`sha256:f820ab779401196d51adc0aa8260abbca4c2924c529012e9a839b2e31aeb3a48`，UTC启动 2026-09-14T11:15:38.783849331Z。

实际可用状态按账号明确返回：原豆包账号执行器离线，另一账号待登录/型号未核实，第三账号待人机验证且原任务占用。原任务56eec9d8708f5ed7812091911d3d6ebb仍running/needs_user；本轮未关闭官网登录/执行器窗口、未取消/重发该任务，亦未启动新的生成。Windows执行器与worker/beat/数据服务保持；只替换backend/front。

交付入口：系统设置→网页平台模型，或网页平台账号中的同名链接；生成弹窗常显不可用原因、刷新状态、账号管理与型号维护。PL-020仅完成本批可维护目录和执行交互，通用自动视频、其他平台自动化与可靠官网权益探测等原待办保持；不以目录维护冒称动态生成适配全部完成。git diff --name-status及未跟踪文件已复盘，映射LC-001/006/010/011/012/014/015；其他未关闭PL保留，未提交推送。


### 2026-09-14 网页多账号与自动执行恢复

豆包官网登录和图片执行器合为同一独立账号窗口；登录后自动核验、回报心跳并执行已受理任务。生成弹窗优先自动执行，条件不足明确原因，人工交接由用户主动降级选择。离线旧登录记录不作实时登录判断；在线窗口不可重复打开。视频和其他平台的自动化边界不变。

官网已识别功能介绍弹窗自动处理；验证码/授权仍由用户完成。验证码期间暂停提交等待计时，完成后接续原请求；原会话+用户消息checkpoint用于恢复。下载或后端短暂故障只重取原结果/回填原文件，不重发。没有正式回执时禁止把local_地址或其他任务最新图片当结果。取消请求不显示成可继续生成。

本轮三次真实单图发送：指定主账号成功、自动分配跳过待验证账号并成功；第二账号验证后官网仍显示发送错误、仅有本地会话，原任务4ef6cccc8a3e5939bd37f826f5a56753保持待核对且不重发。两项成功均独立实验室回填，原业务未覆盖。第二账号完整闭环、通用视频与其他平台自动生成仍待验收（PL-020）。详见二开主台账本批和PROJECT_MEMORY最新记录。


## 2026-09-14 网页视频自动执行与即梦登录状态（工作树待提交）

LC-001/006/010/011/012/014/015，PL-020继续，其他未关闭计划保持。图片与视频统一默认本机自动执行，人工交接只能显式选择；修复视频选择browser仍调用handoff接口及批次强制manual。新增自动批次接口，共享账号队列允许image/video/any，旧图片执行器不能领取视频。同一账号仍只保留一个原任务，未知提交不换号重发；任务目标、版本、型号、参考顺序及结果回执保持绑定。

豆包自动视频适配Seedance 2.0 Mini/Fast，当前支持纯文本或单张首帧；首尾帧、多图/主体组、原视频编辑及指定分辨率未通过当前网页控件适配，提前给出原因，不丢弃参数或压平语义。实际型号、画幅、时长在官网控件核对；时长滑块内部值是档位编号，必须读取显示秒数，不能当作秒数。准备阶段可重试控件水合，不能重发生成。

视频保存原用户消息，最终视频可能在另一条回复；只允许本任务的一次普通生成确认。确认前核对模型/画幅/时长并持久化意图，未知确认不二次发送；安全/授权/验证码仍由用户处理。唯一原结果下载沿用同源官方Blob，哈希/媒体规格/原目标版本校验后回填；离开页面不取消任务。

账号接口分开展示supported_models图片和supported_video_models视频，存储仍沿用原账号能力列表，无schema迁移。即梦专用窗口新增只读登录观察和心跳：官网用户头像及创作入口确认后标记signed_in，验证码优先；不会把浏览器同步登录当官网登录，也不把已登录冒称自动生成已适配。其他平台自动生成仍待独立实现/验收。

验证：36项后端专项通过，22项Node回执/DOM/原件下载通过，追加时长档位回归另记；OpenAPI已重新生成，tsc通过；真实React视频默认browser、最新时长可用性检查、POST/videos通过，图片/目录旧交互回归通过。真实官网只核验准备控件，Mini/Fast均完成单张首帧上传、1:1、4秒及确切型号选择，未发送生成。原Mini/Fast再次下载SHA与既有文件完全一致，未发生原生下载崩溃。

本批没有新生成图片/视频，不消耗新增生成权益；不能将旧视频下载复核称为新自动视频端到端付费验收。李女士原任务4ef6cccc8a3e5939bd37f826f5a56753保持不确定/原账号现场，未重发。新自动视频完整生成验收、即梦及其他平台自动生成、真实账号权益探测继续PL-020。部署与最终镜像证据另记。


最终部署验收：backend镜像`sha256:2ed4a86a9113e87d27185f30d86573ba99656dc605b40706f0b1b49528e0a946`，UTC启动2026-09-14T13:12:41.65833675Z；front镜像`sha256:1fc96c8c09b3574981862cb2f0e16dd1ea8b07d01a19c2ad9b0dc9678ac259c2`，UTC启动13:12:43.172797075Z。健康200，数据库仍b4d6f8a0c016，无schema迁移，无备份/业务卷清理，未提交推送。

最终36项后端专项（包括any领取图片/视频、旧图片worker隔离、重复回填）、23项Node/隔离DOM/原件保存测试通过；最后即梦隐藏头像优先级单项再通过。OpenAPI、tsc、Docker生产构建、真实React图片与视频交互通过。正式7788只读浏览器核对：豆包已列Seedream 4.5及Seedance 2.0 Mini/Fast，图片/视频目录隔离；即梦2026-09-14T13:23:07Z回报signed_in/online，页面展示官网已登录且自动生成待适配；零写请求、零外部平台生成请求、零页面异常。

真实核验修正了两个细节：视频时长控件内部0..11是档位并非秒数，现从页面文字读取；即梦响应式页面保留隐藏头像，现读取可见实例，不伪造登录状态。空闲主豆包和即梦独立窗口已加载新版脚本；原待核对/取消任务窗口不重发。保留登录资料，正常重开加载，不删除profile或使用原生强制终止。git diff --name-status及未跟踪清单已审阅，映射上述LC/PL；其他批次共享工作树改动保留。


## 2026-09-14 豆包自动视频现场验收、即梦接入开始（工作树待提交）

用户本轮明确要求现场测试豆包视频，完成后开始其他平台。LC-001/006/010/011/012/014/015、PL-020继续。

独立实验室14909469-497b-48f4-a36b-14a4304bb801：首测任务4ebf601d06435fd6853cc0e3dc2c82b0，用户明确回复手动点击“开始生成”。官网将提示词加前缀且在数字两侧插空格，旧精确匹配没保存anchor。保留原会话38441705883958274，只读核对唯一原消息55469832915978242，补回执后继续原任务；结果55461931799613954，文件d9ad5193-1e00-4aab-a886-edff5cac66d9，221176字节，SHA b5f4a59aef81ab6284822c4e2d910fffef913cbde48caea66e9f1dd7c46154fd。此项是人工协助恢复，不算全自动验收。

修复：正式数字会话出现时立即持久化URL；匹配时仅规范化空白，原业务提示词不改写；仍要求当前会话唯一用户消息，歧义不猜。视频可从已保存URL补查唯一消息，再恢复，绝不再次提交。23项回执/图片/视频DOM专项通过（含数字空格、URL-only checkpoint与确认防重）。

用户再次明确授权仅追加1条Mini 4秒、不升级、失败不重发：任务f6d1c87992ca5aa299d10a78eb8b4de1。执行器自动保存会话38441706101135362和用户消息55470259572208898，2026-09-14T13:49:40.474Z保存confirmation_intent后自动发送确认；最终结果55470321220993026，文件76c38794-d642-4607-8b6d-2ab66e6e4f8c，276031字节，SHA 85090d6f0a6696d129651738f8a21800a0f36de317ca40f7aea8d04542067f2e。完整自动任务succeeded/completed/published，spec_issues=[]；无人工代点或原会话接管操作。两条均同一楼房首帧、Mini、4秒、1:1，各一次提交，无失败重发/换号/套餐升级，实际扣量未查官方账单。

正式实验室页面两条video可见、0页面异常；只读验证器阻止2次页面初始化POST，未实际发送业务写入，不能描述成0请求尝试。Windows主豆包worker已加载修复；没有新的后端API/schema变化，现有Docker镜像保留。

即梦正在接入：官网首页文生图链接实际指向/ai-tool/image/generate?need_login=true，登录后重定向/ai-tool/generate/?type=image&need_login=true；与Agent偏好界面独立。原账号当前选中图片3.0，推荐卡片图片5.0 Lite不代表当前所选型号。已核对原生模型菜单、实际禁用项/VIP标注和图片分辨率；输出数量仍待核验。接下来实现原生输入准备、唯一任务绑定和官方下载；尚未提交任何即梦生成，不冒称已自动接入完成。


## 2026-09-14 22:21 收尾部署重启与暂停实测（工作树待提交）

用户要求今天停止推进、部署重启前面修改。豆包Mini新增自动4秒任务f6d1c87992ca5aa299d10a78eb8b4de1已成功，原会话/唯一消息/确认意图/原文件/业务回填证据见上一节；第一条4ebf601d06435fd6853cc0e3dc2c82b0含用户手动确认，不计全自动。两结果在独立实验室14909469-497b-48f4-a36b-14a4304bb801，不覆盖现有分镜。

当前Windows主豆包执行器已加载URL提前持久化、空白规范化唯一匹配和URL-only原任务恢复修复，并已用第二条真实任务验证。24项正式回执/图片/视频/原件保存回归通过。即梦候选jimeng-controls.mjs只包含菜单观察、确切型号选择和文本草稿，未接入正式调度/提交；4项离线测试最终通过。曾发现hasText读取textContent丢失可见换行导致匹配失败，改为innerText标题唯一匹配；尚未再次真实核验该修复，不等于即梦完整闭环完成。

纠正上一节“单张数量已核对”：尾部数字1尚未证明是输出数量，也可能是积分显示，不能据此承诺单张模式。用户虽授权1张即梦测试，随后要求停止；实际0次即梦生成提交，保留为下次待办，不擅自延后收费执行。待完成：原生数量/参考上传/参数、唯一任务与结果身份、官方下载和回填，然后真实验收。其他平台与第二豆包账号原不确定任务均继续PL-020，所有其他未关闭PL保留。

已重新构建backend/front（业务层缓存一致），显式restart两服务成功；运行镜像仍backend sha256:2ed4a86a9113e87d27185f30d86573ba99656dc605b40706f0b1b49528e0a946，UTC启动2026-09-14T14:21:20.997332729Z；front sha256:1fc96c8c09b3574981862cb2f0e16dd1ea8b07d01a19c2ad9b0dc9678ac259c2，UTC启动2026-09-14T14:21:20.154295834Z。健康及7788首页200。无API/schema变化，无需再次生成OpenAPI；沿用此前36项后端、tsc及生产构建验收，本轮不冒称重跑后端全量。Windows主执行器保持已验证版本，即梦只读登录观察恢复；待核对任务不重发、不取消、不强关原窗口。没有删除备份或业务卷，未提交/推送代码。

git diff --name-status及未跟踪文件已审阅：正式执行器修复归LC-001/011/012，已有交互/型号目录归LC-006/010，回归归LC-014，文档交接归LC-015；即梦尚未开放项留PL-020。共享工作树其他既有修改保留。

最终重启后只读复验：网页模型目录/图片视频隔离/账号入口通过，0写入尝试、0外部平台调用；实验室两条视频仍可见、0页面异常，该页面1次初始化POST被验证器阻止，未发送业务写入。即梦观察器恢复signed_in，未提交生成。文档diff检查通过。


## 2026-09-15 资产网页图片恢复、取消释放与空白窗口修复（工作树待提交）

LC-001/006/011/012/014/015；PL-020继续，其他未关闭计划不变。用户真实资产任务33c40ae0d6fe5638b39a1ef41f8bd633已在官网生成，但本地只有URL checkpoint。现场核对：官网提示词增加“生成图片：”前缀，将·显示为・、:显示为：；旧精确匹配未识别用户消息，图片reconcile又要求已有user_message_id且未规范化，导致循环等待。此次修复以NFKC、已观察到的中点映射和空白规范化匹配原会话唯一用户消息，补持久checkpoint，正文内容/参考顺序/原业务目标不改写，不按相似度猜选。

取消缺陷：通用取消只设置cancel_requested，Windows任务没有Celery确认；旧check_token拒绝恢复，执行器hold永久等待，active_task_id一直保留。新增web_generation.cancel_browser_task/settle_cancelled，由通用取消API按执行器分派；本地任务原子进入cancelled、保存结束时间、清除租约，仅CAS释放属于该任务的账号。官网已受理生成不承诺撤回；迟到stage/result仍拒绝，不回填新任务。凭据校验后的resume可返回取消终态，worker在held/登录阻断前检查终态，保留原回执/原文件，仅清除current指针，再核验官网恢复可调度。取消后用户可主动以新请求再次生成；旧任务不自动重发。

about:blank原因：有本地current时跳过discover导航，恢复又被取消拒绝或hold拦住，空白初始页一直保留。现在启动先导航已验证原会话或豆包首页，导航初始化后才回报窗口opened；取消任务也完成终态恢复，不永久停空白页。已通过系统账号页真实点击两个“启动自动执行器”和一个“打开官网登录”，由现有本机助手启动三个新执行器，而非私有脚本替代用户入口。

正式部署backend镜像sha256:e78596782740d8c12f1700230da2e87d9ba6532aca0faf5b05518bc07aeeb05c，UTC启动2026-09-15T02:21:44.282863287Z（北京时间10:21:44）；前端镜像不变。本轮无schema迁移。OpenAPI重新生成，首次文件锁失败、一次恰逢重启连接失败后在服务健康时重试成功；tsc通过。40项后端网页专项通过，再补服装/分镜槽位正常回填和版本冲突矩阵后20项web_generation专项通过；15项图片/回执Node回归通过。没有新增官网生成或收费重试。

通过正式取消API完成三个已由用户请求取消的任务：33c40ae0d6fe5638b39a1ef41f8bd633、4ef6cccc8a3e5939bd37f826f5a56753、56eec9d8708f5ed7812091911d3d6ebb。最终任务中心共用status API三项均cancelled，三个豆包账号均ready、online、active_task_id=null；正式账号页面三行空闲/已登录/在线通过，0页面异常。原等待任务窗口正常关闭加载修复，未强杀日常浏览器、删除资料或换账号。

服装原件已通过修复后的正式image adapter核对并下载：原会话38441706878807810，用户消息55474391773069058，结果消息55473875297159938；文件local-browser/accounts/486263ef32a74bbd87b57954c59986fd/33c40ae0d6fe5638b39a1ef41f8bd633-recovered-original.png，2667798字节，SHA256 01d6e924891e46a0078280f11d6491d8cc20b6546260a3f58bc15278dd91ca16。因用户已取消，仅保留原件，不复活任务、不覆盖资产、不新增付费生成。另两项取消任务未声称已找回图片。

验收边界：真实验收覆盖用户原资产提示词/原结果定位与下载、系统账号按钮、取消终态和释放；新资产生成到回填全链路本轮未重新收费调用，不把离线回填矩阵当作新增实测。即梦及其他平台原待办继续，不在本轮扩展。工作树未提交/推送，未清理备份或业务卷。


## 2026-09-15 视频预检、优化稿恢复与失败重试（工作树待提交）

LC-006/011/012/014/015；PL-013/015继续，其他未关闭计划不变。用户真实失败记录 fe72c1ada9ea4b120efda8a4ed0d742e05099d1ae94989b75498c8c97cd5eb0e、08578ae65c6c150a2da342ef8118adcaa9ed0fc370116cd258dbaa8857c2e9c5 仅保存 Connection error；同 qwen3.8-max、同参考图另有成功预检，不能判定该型号不支持带图。历史底层原因无法反推，本批补充失败阶段、异常类型链和实际图片数量，连接失败不自动收费重试、不换端点/模型。未进行新增收费预检或生成，连接稳定性真实复测仍待确认，不能宣称历史网络根因已修复。

已应用优化 bca7abde44f89f85022bef971f6ac07dda9578e0be0c2d1902b232fec3389757 的用户手工原稿与默认重建稿不同；旧界面以二者相等作为恢复条件，导致已应用稿丢失。改为历史记录返回只读来源指纹：同来源、同生成参数恢复精确保存发送稿，不二次编译已应用稿；应用后同步编辑区/发送预览/标记。已应用视频方案可免费微调后再次应用，服务端以当前应用稿为CAS基准防旧页面覆盖；图片帧原契约保持。

现场发现保存优化后镜头duration确已变化，不能冒称旧预检适用于新时长。来源、参考图或型号参数改变时仍载入用户保存稿，标记需要重新核对，点击免费更新发送预览后才可生成；历史报告不绑定新视频，前端来源指纹和后端任务谱系双重校验。无需跨标签复制，不强制收费重新预检。关闭/切换过程中迟到历史不写入另一个镜头。

另修正智能调整失败后无retry_request_id导致再次点击仍命中失败任务：显式确认的新调用生成新请求编号，受理响应丢失保留同一提交意图编号；同步点击锁避免连点，刷新/切页不自动收费调用。终态历史仍只列成功结果。

验证：36项后端预检运行、工作流、视频渲染专项通过，追加应用CAS/来源指纹后12项工作流通过；OpenAPI已重新生成，前端tsc通过。真实React测试覆盖历史免费读取、应用/旧稿拒绝；真实工作室使用实际历史、拦截POST和5秒视图夹具，验证已保存稿在参数变化下保留、免费更新后发送文本一致、切标签及重开保留，0页面错误、0真实写入/模型调用（1次初始化写入被拦截）。真实当前15秒超当前vidu型号上限，不为测试修改生产参数。部署与最终补充验证见下文。


### 本批最终部署与验证（2026-09-15 11:30，北京时间）

正式运行backend sha256:8638eff2e07de05a9d123cc7442ee8873298e0482b9fa94a38a6a448e802da58；worker sha256:6416b39aa446529219a58ebada4ad33c7d1836bb775c758dd3681659847f629a；beat sha256:53d36fe3dcf943a37e3471f6155113f8ea83757c46b630dbd05631161e7dca6e（UTC03:00:53启动）。front sha256:6b6cd09e06ed0115f3204ce629e0ba6196088c50d236c1085241d8146594cb47，UTC03:30:18启动。API健康及7788首页200，OpenAPI新source_fingerprint字段已生成；无schema迁移、没有新收费调用、没有清理备份/卷。

最终tsc、Docker内tsc/vite生产构建通过，保留原大bundle提示。正式7788实际工作室回归通过：真实历史+5秒参数视图夹具与本地渲染响应，全部POST拦截；来源变化保留优化稿/先更新再生成、三处文本同步、切标签/重开、来源参数相符时直接恢复已应用标记均通过，4次模拟免费渲染、1次初始化写入被拦截、0页面异常。真实React优化组件验证免费微调再次应用、受理编号、旧页面保护与历史恢复通过；早期断言未等待React更新和夹具null/false音频差异已修正，最终通过，不是业务请求重试。

读取真实历史并核对本地免费render时曾被自动审批误判为可能收费而拒绝；读取路由与纯本地模板实现证明无模型调用/任务创建/写入后获准完成验证。该验证仅用于来源指纹对照，未执行真实预检/生成。

git diff --name-status与未跟踪清单已审阅：ChapterStudio、QualityReviewPanel、VideoPromptWorkbench归LC-006/015；质量契约、工作流/错误诊断、生成关联归LC-011/012/015；OpenAPI、回归与文档归LC-014/015。工作树待提交，已有其他共享修改保留；PL-013/015图文连接稳定性/真实质量验收、PL-020各平台自动化及其他未关闭计划继续。


## 2026-09-15 视频入口确认流程核查（工作树待提交）

用户反馈点击生成视频未显示确认页、直接调用API。用户明确强调确认页面未出现，不能将反馈直接当作误认网络请求。当前后端最近记录只有两个 /video/render 本地预览请求，无新增视频任务；86400秒任务列表中最近视频仍为旧任务。已读代码单镜头入口只调用openVideoPromptPreview，实际任务POST仍位于提示词预览与reviewGenerationRequest费用确认之后；尚未复现跳过确认的触发条件，不冒称根因已修复。

LC-006/014/015：将第一步改为“预览生成内容（免费）”，预览底部改为“下一步：确认生成费用”，保留费用窗“按此规格生成”；加载区及预览顶部明确当前未调用视频模型。真实工作室拦截式回归补至最终确认：打开/恢复/更新/切标签/进入费用窗均0任务提交，最终确认才出现1次任务POST（拦截，未转发），请求提示词等于所见优化稿。用户最近另一镜头fbe67337-1c26-41ba-87c3-fb69598684a3另测push/overlay两种布局，不创建真实任务。所有测试仅本地mock写请求及只读GET，生产时长未修改，无付费调用。

用户具体入口问题仍待确认；PL-013/015及其他未关闭计划保持。此次只调整交互表达并增加确认链路测试，不声称修复一个尚未复现的自动提交问题。部署证据随后追加。


确认入口批次部署：2026-09-15 12:10，仅front替换为sha256:89ae32c09dd8a773c3eb9922b9ba656cbdc77783bf08b4e6db2422495ab74a02，backend/worker/beat不变。tsc及Docker生产构建通过，正式7788两个布局确认页均出现、0生成任务提交、0页面异常（初始化POST各1次被拦截）。完整优化稿与最终费用确认拦截验证见脚本结果。无API/schema改动，无需重跑OpenAPI；未提交/推送，未调用收费模型或删除数据。

已审查本批git diff --name-status与未跟踪脚本：ChapterStudio交互LC-006/015，三个更新脚本及新增verify-video-entry-confirmation.mjs归LC-014，主台账/摘要/手册/记忆同步LC-014/015。用户明确说确认页未出现，本批未复现，具体单镜头API/网页/批量入口确认仍待用户回复；不能写成跳过确认缺陷已修复。


## 2026-09-15 恢复统一生成视频入口（工作树待提交）

用户截图明确要求分镜生成面板保持单一“生成视频”，进入生成视频页面后再选择API/平台网页。本批覆盖上一12:10入口文案方案：外部移除通道切换与“预览生成内容（免费）”，统一按钮只打开生成视频弹窗；通道选择保留在弹窗底部，提示词、参考图、历史优化与最终费用/网页账号确认保留。

同时移除旧外部网页直达prepareWebVideo路径，避免绕开统一确认页面。参考选择拆为不依赖通道的预览收集与API提交校验：进入页面不再被API型号时长/比例/是否配置挡住；API提交仍先核对能力和参考模式再进入费用确认，网页按平台能力校验。当前15秒超API型号上限也可进入统一页面选择网页，不改写时长、不擅自切换模型。

LC-006/014/015；无API/schema变更，无付费调用。tsc通过；完整预检保存稿及最终费用确认拦截回归通过，0真实任务提交；push/overlay两种布局验证入口无通道选择、弹窗有通道选择、15秒视图可进入网页模型账号窗口。没有API配置的场景及最终部署证据见下文。其他既有未关闭PL不变，本批未宣称历史Connection error或上一未复现收费跳过问题已解决。


统一入口正式部署（2026-09-15 13:18）：front镜像sha256:f79d1597be975c96eb9525a7ae5e1f82faca3dfa460a728020021b279c2a4196，前端重建重启；后端与worker/beat不变，无schema/API变化。tsc及Docker生产构建通过。正式7788验收push/overlay/no-api-model三场景：外部仅生成视频、内部切换API/网页、15秒条件仍能打开网页模型账号窗口，均0任务提交、0页面异常、各1次初始化POST拦截。完整优化稿/最终费用确认拦截测试继续保证先预览再确认，不真实收费。

本批git diff --name-status与相关未跟踪验证脚本已审阅，映射LC-006/014/015；工作树未提交，未清理备份/数据。上一12:10入口文案方案已按用户本轮截图要求替代，不再要求用户选择外部通道。早先Connection error真实稳定性与其他未关闭PL继续。


## 2026-09-15 视频API按钮禁用原因与就地恢复（工作树待提交）

用户截图中API通道已选中，实际置灰为“下一步：确认生成费用”，原因是恢复旧优化稿后来源/参数有变化、尚未更新发送预览。LC-006/014/015：底部固定区域直接显示禁用原因，按钮悬停同文提示；具备更新条件时提供“现在更新预览（免费）”，切回提示词页并本地更新，不自动提交生成。

禁用条件与可见说明统一使用videoApiDisabledReason，分别说明内容加载、镜头同步/保存失败、模型配置未就绪、提示词空、正在更新、更新失败或预览过期。保持统一外部生成视频入口和内页通道选择，不放开过期稿提交，也不强制再次收费预检。

tsc通过。真实React工作室回归：底部明确过期原因、就地更新解除限制、优化稿/发送稿一致、最终费用确认才尝试POST（全拦截）；push/overlay/无API配置回归通过，无API配置也在按钮旁给出原因并可切换网页。0真实生成/收费、0页面异常，无API/schema变化；部署证据随后补充。其余未关闭PL保持。


禁用原因批次部署（2026-09-15 13:36）：front sha256:df234834801b226f002db4276c96465330e8d6987e763ad99ca1344815567ff3已替换重启，backend/worker/beat保持。tsc、Docker生产构建通过，首页200。正式7788底部禁用说明/免费更新解除限制、优化稿一致及最终确认拦截回归通过；push/overlay/no-api-model专项同样检查原因提示和网页仍可访问，0页面异常、0真实生成，最终确认的1次任务POST仅测试拦截。无需OpenAPI/schema同步。git diff --name-status与相关未跟踪脚本已审阅并归LC-006/014/015，台账/摘要/手册/记忆已同步；工作树未提交，无备份/卷清理，其他未关闭PL保持。

正式入口回归首次收尾遇到测试浏览器关闭时route.fetch仍在途，非页面业务异常；补充unrouteAll等待/清理后重跑push、overlay、no-api-model全部通过，0页面异常。生产代码无需再次构建。


## 2026-09-16 网页生成图片/视频两卡点修复（LC-001/011/012，PL-020，工作树待提交）

用户反馈豆包网页生成两类间歇卡点（完整流程有时能通、有时不行）：①图片生成参考图已上传但提示词未进入浏览器文本框，流程卡住；②视频生成已生成视频文件但未回传系统对应流程，流程卡住。本轮只修改既有执行器适配器，不新增文件/插件、不动框架/后端/前端，执行器为 Windows 本机脚本且从 Docker 构建上下文排除，未部署（用户环境无法由我重启执行器）。

LC-001/011/012（网页生成执行器与供应商执行适配）：
- 图片提示词未提交（scripts/browser-runner/doubao-adapter.mjs）：原 prepare/submit 用笼统 getByRole('textbox').fill 且填充后无校验，附件上传后页面多文本框或重渲染导致提示词静默丢失/填错框。新增 composerBox（多文本框时优先选已含本提示词或带图片编辑占位符的框）与 fillPrompt（填充后读回 inputValue 归一化校验、失败重试3次、仍失败才报错，杜绝空内容提交）；prepare/submit 改调用二者。
- 视频未回传（scripts/browser-runner/doubao-video.mjs）：原自动确认要求官网确认文案精确包含 requested_model+aspect_ratio+"Ns"，措辞/格式差异即抛错阻断后续等待；结果检测只认 [class*="block-video"]，卡片 class 变化则永久检测不到。放宽自动确认：仅对授权/会员/购买/支付/安全确认类提示或型号明显不符抛错，画幅/时长回显差异改为软通过；结果检测增加 video 标签兜底（[class*="block-video"], video）。手动确认（按钮点击或输入开始生成视频）本不触发"其他输入"守卫，配合兜底后已生成视频可被正常采集回填。videoThread 的"其他输入"守卫保持原精确语义（仅拦真正不同的用户输入、防串用），未破坏既有安全回归。

git diff --name-status 语义审查：正式执行器修复归 LC-001/011/012，项目记忆与台账同步归 LC-014；共享工作树其他既有修改保留。

验证：托管 Node 22 跑隔离 Edge 离线测试，doubao-video.test.mjs 9/9 通过、doubao-adapter.test.mjs 与 receipts.test.mjs 通过（exit 0）；两文件 node --check 语法通过。真实浏览器端到端（用户已登录独立 Edge + 豆包账号）仍待用户实测；执行器下次启动即使用本批工作树代码，无需 Docker 重建。工作树未提交/推送，未清理备份或业务卷。

本批补充前端一致性（LC-006/010）：账号下拉追加“（执行器离线）”标注，账号页离线 Alert 给出双击 Start-Jellyfish.cmd 或运行 Start-WebDesktop.ps1 的自助恢复指引，页面每 3 秒自动刷新。本机助手自愈（LC-001/014）：Start-WebDesktop.ps1 新增 -KeepAlive 守护（每30秒校验、僵死自动重启）与 -RegisterLogon 登录自启（带 -KeepAlive）；原“进程存在即跳过”改为心跳新鲜度校验（进程在但 /desktop/status online=false 时强杀重启，新进程留40秒首次心跳宽限、后端不可达不误杀）；Jellyfish.ps1 一键启动改调 -KeepAlive。两 .ps1 语法校验通过、前端 tsc 通过；真实自愈与 Windows 自启待实机验收。工作树未提交。

网页生成鲁棒性修复（R1–R5，已完成）：doubao-images.mjs 多图采集改为取首张（count===0 才抛错，其余留会话）；doubao-adapter.mjs clearDraft 显式新建对话避免旧会话占死；doubao-video.mjs 视频自动确认放宽（模型名 + 画幅或时长其一，时长兼容 10s/10秒）；web_media.py 视频规格容差放宽（时长 0.5s→1.0s、画幅 3%→5%）；worker.mjs 新增 collectFailures，采集连续失败 10 次后转 needs_user+held 释放账号。四个 .mjs node --check、web_media.py py_compile 均通过；真机端到端待验收。

网页生成鲁棒性 R1–R5 实机回归（PL-020，工作树待提交）：R1 实测豆包图片编辑器为 contenteditable，Playwright inputValue() 对其抛错致填充后再读永远为空、假准备失败；R5 的 collectFailures 声明遗漏致 catch 内 ReferenceError 逃逸至 finally 关闭浏览器并置 offline——即用户所见“关闭+不可用”。已修：composerText 替代 inputValue + insertText 兜底、补齐 collectFailures 声明、diagnostic 最先写入并加字段、state 不再钉死 offline；desktop-host 改为按账号落盘 worker 日志。新增 contenteditable 隔离回归用例通过。李女士账号 task 94a72d52 仅卡 preparing 未提交，重 fork 即自动 resume，无需重登。

## 2026-09-16 部署回归修复：CRLF、执行器令牌与一键启动脚本（LC-014/016，PL-020，工作树待提交）

让 R1–R5 生效时踩到两个部署回归并补一键启动入口；均不改业务代码、未删备份，git 提交由用户手动。

LC-016（前端 CRLF 构建回归）：`10-generate-env-js.sh` 仓库源为 LF，Write 在 Windows 改写成 CRLF 打进镜像 → 前端容器 `env: can't execute 'sh'` Exited(127)；Read 不可靠，须进镜像 `cat -A` 看 `^M`。Python 二进制模式转 LF 重建镜像，`cat -A` 复核无 `^M`，git status 干净（仅恢复仓库原 LF，无净改动）。`docker compose up -d --no-deps front` 绕过 backend-migrate 漂移 b4d6f8a0c016；front Up，:7788 200。

LC-016（后端执行器令牌回归）：`web_desktop.online` 由本机助手成功 `poll` 决定，`/desktop/poll` 与 `/accounts/{id}/desktop` 均依赖 `runner_auth` 要求 env `WEB_GENERATION_RUNNER_TOKEN`；该令牌原只在运行态容器、repo 的 compose/.env 从未定义，重建后端时丢失 → 执行器永久离线、fork 提交不了。compose backend environment 增加该令牌、`.env`（gitignore）写入与 `runner-config.json` 同值，`docker compose up -d --no-deps backend` 仅重建后端注入；`/desktop/poll` 由 503→409（鉴权通过）。令牌已固化，后续重启不再丢失。（注：`.env` 与 `runner-config.json` 均 gitignore 不进 git，令牌值仅存本地；上游升级 / 换机重建须重新写入 `.env`，否则回归离线 + fork 503，须纳入迁移"本地安全覆盖"映射清单。）

LC-014（新增一键启动脚本）：根目录新增 `启动网页本机助手.cmd`，复用既有 .cmd 写法，调用 `Start-WebDesktop.ps1 -RegisterLogon`，双击即拉起本机助手并注册开机自启，免敲长命令；与 `启动网页生成执行器.cmd`（单账号手动）、`打开网页平台账号.cmd`（仅开官网登录页）职责不同。用户实机运行后 `/desktop/status` 转 online，重 fork 豆包账号使 R1–R5 生效；真实验收待豆包实机。

预存在隐患（非本次，不影响主链路）：celery-worker 因 CELERY_BROKER_URL 带密码而 redis 无密码 Exited(1)；backend-migrate 因 alembic 漂移 b4d6f8a0c016 失败。

## 2026-09-16 预存在隐患正式登记为待完成计划（PL-016 / PL-017，工作树待提交）

按用户要求把上节两条"预存在隐患"正式登记进 §3 PL 台账（已加 PL-016、PL-017 两行）。PL-016：celery-worker AUTH 失配（CELERY_BROKER_URL 带密码而 redis 无密码），待统一 redis 鉴权后重建验证；网页生成主链路走本地执行器不依赖 celery，暂不影响。PL-017：backend-migrate Alembic 漂移 revision `b4d6f8a0c016`，可经 `up -d --no-deps <svc>` 绕过，待修复漂移后再迁移；禁止以"迁移失败但服务能跑"掩盖未迁移状态，上游升级须先处理。

## 2026-09-17 豆包 ProseMirror 改版适配、本机助手启动链修复与待完成事项登记（LC-001/006/014/016，PL-020，工作树待提交）

- 豆包改版适配（LC-001/011/012，已 E2E 验证）：豆包官网 2026-09-17 凌晨改版，composer 由 `<textarea role="textbox">` 换成 Tiptap ProseMirror `div[contenteditable]`（占位符在 `data-placeholder`，无 role），`getByRole('textbox')` 匹配 0 个，prepare 全链失效。doubao-adapter.mjs 新增 `firstComposer(page)`（`.ProseMirror` 优先、旧 textbox 兜底），clearDraft/openImageEntry/composerBox 换用；`fillPrompt` 因 ProseMirror 忽略程序化 `fill()`，attempt>=1 改 click+Ctrl+A+`keyboard.insertText`；doubao-video.mjs 视频确认入口同步。探针三连（prepare-probe*.mjs，验证后已删除）+ 真跑业务 E2E：worker 恢复任务 a1f818a4 →提交→生成→下载原图 2.6MB→哈希校验→回填，后端 succeeded/completed、published=true；diagnostic 零新失败。
- 本机助手启动链修复（LC-001/014，实机验收通过）：①`启动网页本机助手.cmd` 重写为纯 ASCII + CRLF——原 UTF-8 中文 + `chcp 65001` 中途切码页 + 非 CRLF 行尾致 cmd 批处理解析错位、powershell 行从未执行（用户多次双击无痕失败根因；教训：.cmd 启动器永远纯 ASCII + CRLF、批处理内禁 chcp）；②Start-WebDesktop.ps1 加启动日志（`logs/desktop-host-launch.log`）、node 解析回退、3 秒秒退校验，ps1 补 UTF-8 BOM；③desktop-host.mjs 加固：host 级同步日志 `logs/desktop-host.log`（console 镜像 + uncaughtException/unhandledRejection/exit 全捕获）、per-account 流 error 兜底与幂等 endLog（修掉 error+exit 同触二次 `log.end()` 崩溃点）。
- 部署修复（LC-016）：compose backend 加 `UV_NO_SYNC: "1"`（入口 `uv run` 不再运行时联网同步依赖，重建容器不依赖外网）；前端 WebAccounts.tsx 强刷闪现"本机助手离线"修复（初始 state 即渲染离线横幅所致，新增 `statusLoaded` 首请求返回前不渲染），tsc+build 通过、front 镜像重建（install 层缓存）+ `up -d --no-deps front` 生效。
- 教训（再次）：单服务改动执行 `up -d` 未带 `--no-deps`，连带重建全栈并触发 PL-017 迁移失败致短暂停机；已逐服务恢复。**单服务操作必须 `up -d --no-deps <svc>`。**

待完成事项（本轮登记，标记待完成）：① PL-017 新增实测与修复路径——backend 镜像旧于 DB schema（DB 已应用 b4d6f8a0c016 而镜像缺该文件），可在发布窗口 `docker compose build backend` 解除 migrate 门禁失败，此前不带 `--no-deps` 的 `up -d` 仍会卡死全栈；② PL-016 维持待修复（celery Exited(1)，无主链路影响）；③ PL-020 新增两项待验收——带参考图任务实测（本轮 E2E 为无参考图，上传链路未经改版后真实验证）、三个豆包账号卡住任务恢复确认；④ git 提交由用户手动，`scripts/browser-runner/` 未纳入跟踪需决策。

## 2026-09-17（二）参考图上传校验适配：预览图移出卡片、卡片乱序（LC-001/011/012，PL-020，工作树待提交）

用户实测带参考图任务：附件进浏览器但提示词未填。诊断＝prepare 附件上传校验 60s waitForFunction 超时（fillPrompt 未获执行）；探针4 用真实参考文件抓到改版后 DOM：卡片数与 aria-label 文件名正确，但真实预览图（img[alt=image]、blob: URL）已移出卡片元素（卡片内仅剩 54px svg 占位图标），旧 `card.querySelector('img[alt=image]')` 永远 null；且卡片顺序不再等于上传顺序。修复：doubao-adapter.mjs 校验抽为导出 `waitAttachmentsUploaded`，集合匹配（卡片数=文件数、aria-label 集合覆盖全部文件名、每个文件名经 closest('[aria-label]') 找到已解码 https:/blob: 预览），新旧 DOM 兼容，文件对应关系仍逐一核实、错漏依旧失败闭锁。验证：探针5 生产函数 4 图 1.5s 通过；探针6 完整 prepare 链（上传→校验→fillPrompt）提示词 424ms 落入并读回确认，clearDraft 清理，未提交。task cfd123f2 卡 needs_user（从未提交），账号页重发"启动自动执行器"即 resume 不重复生成。验证探针（prepare-probe4/5/6.mjs）已删除。

## 2026-09-17（三）提交回执绑定适配：用户气泡按富文本渲染，逐字回显假设失效（LC-001/011/012，PL-020，工作树待提交）

用户实测：参考图修复后 cfd123f2 走完 上传→提示词→生成（10:58 已出图），但任务仍"进行中"未回填。诊断＝stage submission_unknown、报错"提交结果不确定"，worker 每 15s 无限重试 reconcile。探针7/8（真实会话只读）定位根因：豆包把用户消息气泡按富文本/Markdown 渲染——提示词含 `##` 标题时语法字符被剥离、气泡开头多出「生成图片：」标签，旧 matchingPromptIds 要求气泡 innerText 逐字包含完整原始提示词，在缺口处永远断开 → submit 60s 无唯一用户消息回执（只有 conversation_url 无 user_message_id）→ reconcile 同败死循环。偏差按提示词写法选择性暴露：不含 Markdown 字符的短提示词归一化前后等价（此前一直正常），含 `#*` 等符号的长提示词必卡；官网无网页端渲染规则文档。修复（绑定正确性优先、绝不按时间/到达顺序）：①归一化升级为渲染不敏感内容指纹（NFKC+全角点+去空白+去除 `#*`~>|` 后比对）；②submit 双因子绑定——Enter 前快照旧用户消息仅作排除，新增用户消息中内容指纹唯一匹配才绑定，0 匹配 >5s 或 ≥2 匹配一律 fail-closed（张冠李戴/串用），空附件节点被指纹天然过滤；并行由每账号独立窗口隔离，窗口内由内容唯一性保障；③reconcile 兜底自动继承新指纹。验证：adapter 13/13（新增指纹绑定/重复拒绝/无关消息拒绝/Markdown 恢复匹配 4 项）、receipts 7/7；探针9 真实会话只读调用生产 reconcile 成功绑定 cfd123f2 user_message_id 55725704118715906 → 助手回复 55728754473896962（带生成图节点），账号页重发"启动自动执行器"即 resume 下载回填、不重复生成。验证探针（prepare-probe7/8/9.mjs）已删除。

## 2026-09-17（四）执行器生命周期改造 A–E：任务驱动自动拉起、可用性单一事实源、空闲收尾+代码指纹（PL-020④，工作树待提交）

方案 A–E 经用户确认实施（硬约束：公共方法不写死豆包，新平台接入零改动）。A＝web_desktop.auto_launch 挂入 poll：pending 任务且候选执行器心跳超 90s、或执行中执行器消失（active_task_id+心跳过期），自动补发 runner 命令（每轮≤2 窗口、每账号 90s 冷却、uuid5 时间桶幂等、活跃命令去重）——登录过且未过期的账号无需再手动点"启动自动执行器"，浏览器关闭卡"进行中"的任务自动续跑。B＝web_accounts.readiness(row,model) 单一事实源（code/reason/action/blocking），/accounts 增 readiness、/execution-status 增 notices，语义修正：执行器停止且登录态上次核验正常＝runner_offline 非阻塞（提交排队+A 自动拉起；模型未核实仍阻塞防排队后无人认领），needs_login/verification/limited/busy/停用仍阻塞；前端账号页"可用性"列、生成页下拉与提交按钮旁原因全部渲染 readiness，删除两处前端自拼文案。C＝worker 启动指纹（browser-runner 全部 .mjs 除 probe/test 的 sha256），领取新任务前与磁盘比对，不一致且空闲即干净退出由 A 换新代码拉起（改代码无需人工重启，执行中绝不换）；无任务且 ready 空闲超 10 分钟自动退出释放窗口（登录态保留于 Edge profile）。注册表化＝AUTO_PLATFORMS 由 AUTOMATIC_MODELS 派生，readiness/execution_status/手动 launch/auto_launch 统一使用，删除分散 doubao 硬编码。部署：backend 5 文件热修复+单独重启（镜像重建受 pypi 阻塞=PL-017，旧文件备份 .hotfix-backup 可回滚）；front 镜像重建单独 up --no-deps。验证：pytest 44/44（新增 test_auto_launch_follows_pending_and_stuck_tasks；test_web_models 更新为新语义+补登录异常仍阻塞断言）、node adapter 13/13+receipts 7/7、front 构建通过；线上 /accounts readiness、execution-status notices、desktop-host/worker 自动重连均核验。旧 worker 已停，下个任务自动以新代码拉起。待实测 PL-020⑤：登录账号提交任务→窗口自动打开→回填，全程零手动；git 提交用户手动。

## 2026-09-17（五）修复 C 改造缺陷：worker 指纹/收尾辅助定义缺失致启动即崩溃

实测发现 worker.mjs 第 118/101 行引用 codeFingerprint/startFingerprint/IDLE_EXIT_MS/lastActive 但四个定义全部未落盘（（四）C 项实施不完整），worker 打开 Edge 进入领取循环即 ReferenceError 崩溃退出，auto_launch 每 ~110s 重拉形成闪退循环（15:36-15:39 三次）。验证手段不匹配是漏网根因：--check 只查语法、adapter/receipts 测试不 import worker.mjs。修复＝启动区补齐四个定义（codeFingerprint 对 browser-runner 全部 .mjs 除 prepare-*/​*.test.* 求内容 sha256，读失败双侧返回 null 禁用比对）；防回归规则＝新增运行期代码路径必须有"运行到"的实测，不得仅凭 --check＋不相关测试完成自验。任务 46bd8ede 保持排队，修复后 auto_launch 自动拉起续跑。


## 2026-09-20 网页生成人工协同增强：A+B+C+D（LC-001/006/011/012/014/015，PL-020，工作树待提交）

用户反馈标准套餐账号被频繁误判为"需付费/中断"，并要求"全自动 + 必须人工时（人机验证/付费确认/手动点开始生成）不中断、人工配合完能继续"。本轮在 R1–R5 安全阀之上做最小改动以同时满足两类期望，所有改动均经实测代码路径或全量 import 冒烟。

- **A 纯展示型卡片分类**：doubao-session.mjs 新增 `affordanceUI(page)`。识别五类纯展示卡片（升级 Pro / 解锁更高画质 / 今日免费次数已用完 / 生成内容存在风险，请确认 / 生成内容真实性自负责任）。判定 = 节点无 `role=dialog/alertdialog`、祖先链无 `position:fixed + 遮罩/overlay/mask/backdrop`。返回 `'affordance'` 给 worker 走 dismiss 分支，不进入 `needs_verification`。
- **C 主动关闭升级/风险横幅**：doubao-session.mjs 新增 `dismissAffordance(page)`，按候选列表依次尝试关闭按钮（× / 稍后再说 / 知道了 / 我已知晓），点不到就放过。`dismissAnnouncements` 自动调用它。inline 文案"安全确认"不再被 `sessionBlocker` 当 captcha：仅在祖先链有 modal backdrop 或 `role=dialog/alertdialog` 时才计 `needs_verification`，inline 文本放过。
- **B 收窄确认正则**：video.mjs:76–87 `/授权|会员|购买|支付|安全确认/.test(confirm.text)` 由"裸文本正则 fail-closed"改为"节点在带 backdrop 或 dialog 角色时才抛错"。豆包回复气泡偶发的 inline "安全确认"提示不再误中断；纯 inline 升级横幅已由 A dismiss。`confirmNode.evaluate(walk up to find role=dialog/fixed+zIndex>=100+backdrop)`。
- **D `awaiting_user` 状态**：
  - 后端 `WebRunnerUpdate.stage` Literal 仅扩 `'awaiting_user'` 一项，未改其它字段；OpenAPI enum 自动同步。
  - worker catch 块新增 `classifyHumanHandOff(error.message)`：命中"官网需要单独授权或权益确认 / 请先处理官网登录或验证 / 人机验证"三类则走 `awaiting_user` 分支——`held=true + paused=true + state='ready' + 8s 后轮询`，**不释放账号、不进持久的 needs_user held**；原 hold / retry_prepare / collectExhausted 路径不变。
  - 前端 WebGenerationWorkbench：`stageNames` 新增 `'awaiting_user': '等待您完成人机交互'`；workbench 顶部 Alert 在 `bundle.stage==='awaiting_user'` 时升级为 warning 级，显示"原账号已保留，您在原账号窗口完成后，请回到这里点恢复原任务继续"。
  - 用户在原账号窗口完成（点掉付费卡/过 captcha/手动点开始生成）后，回到 workbench 点现有"恢复原任务"按钮 → 走 `request_recovery` → `runner_paused=false + recovery_epoch+=1` → worker `releaseTerminal()` 检测到 paused=false 自动 reconcile 继续采集，**全程不重发、不另起账号、不切任务**。

`awaiting_user` 与 `needs_user held` 并存：前者保留账号等用户恢复，后者允许用户取消并释放账号或继续重试。原有 R5 采集重试上限、原 hold / retry_prepare / collectExhausted 路径零回归。

- **回归**：scripts/browser-runner doubao-adapter 新增 2 个 offline 用例（inline 安全确认非 verification + 真 modal 安全确认仍 verification）；doubao-adapter 15/15、doubao-video 10/10、worker-runtime 2/2、doubao-messages 2/2、receipts 7/7、official-download 1/1、jimeng-controls 4/4、pytest test_web_handoff+test_web_reliability 23/23 全过；pydantic schema 校验 `awaiting_user` 已加入 enum；前端无 node_modules，未运行 tsc 直接做了 brace 平衡与字符串断言。
- **范围与边界**：本轮只动 doubao-session.mjs / doubao-video.mjs / worker.mjs / web_generation WebRunnerUpdate Literal 一行 / WebGenerationWorkbench.tsx 文案与样式；不动 WebTaskRead schema、不动 web_handoff 端点、不动 ready 状态机、不动账号池绑定策略、不动回执/官方导出/采用流程。**git 提交由用户手动**。
- **未关闭事项**：PL-016/017、原任务 7a7842b110c1532592aa7fd3afaa9b56（仍在 e2604097 busy 占位）的 A/B 路径用户选择、PL-020⑤ 全程回填实测。
