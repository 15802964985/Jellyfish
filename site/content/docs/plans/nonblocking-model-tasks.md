---
title: "模型任务非阻塞交互改造"
description: "提交后可离开、恢复状态、明确采用结果与全局任务提醒。"
---

# 模型任务非阻塞交互改造

PL-017；2026-09-12工作树待提交批次，LC-006/011/014/015；本计划与既有未关闭计划并行，不替代它们。

1. 排查：帧图轮询占用弹窗、章节文本处理禁止关闭、导入分析关闭被拒、质量检查运行状态恢复缺失。文本实验室SSE依赖页面连接，已改用现有异步Outbox；图片/视频实验室等已有异步入口，补统一提示与恢复。运行通知常驻遮挡关闭按钮，已移除。
2. 统一：仅提交时防重复，受理后后台运行；关闭不取消；需要结果才能执行的操作局部限制，不锁页面导航和其他对象。
3. 任务中心跨页面跟踪、完成提醒、刷新恢复已知任务；短暂网络错误保留状态，不误报完成/消失、不付费重试。
4. 逐用途恢复运行状态；文本处理结果作为待采用候选，不覆盖后续人工编辑；导入暂存批次只保存恢复ID，不自动保存草稿。
5. 验证：慢任务→关闭/换页面→第二个任务→刷新→完成恢复/回跳；不同帧/对象隔离、失败与取消、重复提交限制。所有模型调用模拟，不发起收费验收。

状态：五项代码/离线阶段已完成，671项后端与浏览器慢任务验证通过；已于2026-09-12 08:50部署重启并完成正式只读与模拟验收，精确快照见PROJECT_MEMORY。真实供应商延迟/并发限额验收仍未进行，保留此边界；不关闭其他计划。
