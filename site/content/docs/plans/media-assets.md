# 富媒体资产后续计划

当前已完成图片、视频、音频和文档上传，资产附件、配音音效库、镜头音轨及非阻塞 FFmpeg 合成的主链路。

后续增强项：

1. 使用 `ffprobe` 在上传后自动回填音视频时长、宽高和编码信息。
2. 已完成：TXT/Markdown/PDF/DOCX 受限正文解析，并作为启用附件的软生成上下文；后续可增加 OCR 和生成前可编辑摘要。
3. 已完成首批：按供应商和具体模型声明参考图数量、视频参考能力和参考音频模式；后续随供应商 API 版本持续补充。
4. 已完成：全局文件重新选择关联、图片完整预览、音视频 Range 和非兼容视频预览转码；后续可增加缩略图后台预生成。
5. 增加镜头级波形时间线、拖拽定位和独立轨道静音控制。
6. 增加项目级素材权限与多用户隔离；在完成前继续保持仅本机监听。
