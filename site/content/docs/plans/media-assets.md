# 富媒体资产后续计划

当前已完成图片、视频、音频和文档上传，资产附件、配音音效库、镜头音轨及非阻塞 FFmpeg 合成的主链路。

后续增强项：

1. 使用 `ffprobe` 在上传后自动回填音视频时长、宽高和编码信息。
2. 对 TXT/Markdown 提取正文，对 PDF/DOCX 增加受限文本解析，并在生成前提供可编辑摘要。
3. 按不同供应商和具体模型声明最大参考图数量、视频参考能力和支持的音频模式。
4. 增加镜头级波形时间线、拖拽定位和独立轨道静音控制。
5. 增加项目级素材权限与多用户隔离；在完成前继续保持仅本机监听。
