# Jellyfish 中文版配置使用手册

## 2026-09-23 Push 暂存区格式校验补修（工作树待提交）

重新运行后索引锁问题已解除，实际停止在 git diff --cached --check，退出码 2。完整暂存区确认三个新增文件末尾空行：scripts/doubao-browser-inspect.mjs、scripts/verify-image-preview.mjs、site/content/docs/architecture/manual-media-selection.md。前次 git diff --check 只覆盖未暂存的已跟踪差异，没有覆盖当时未跟踪的新文件，验证范围不足；不能将其表述为全部待提交文件检查通过。

本批仅将这三个文件末尾规范为单换行并更新其暂存内容，保留其他全部暂存改动；未关闭格式检查，不修改全局换行设置，不提交或推送。LF will be replaced by CRLF 为提示，不是本次失败原因。最终必须以完整 git diff --cached --check 及 git diff --check 均通过作为格式验收。LC-001/014，全部原计划保持，无运行服务部署或业务改动。


## 2026-09-23 Push 索引残留锁与生成文件格式修复（工作树待提交）

用户双击 Push-Jellyfish.cmd 时 git add 因 .git/index.lock 已存在退出 128。现场确认该锁为 2026-09-10 15:03:04 UTC 的 0 字节残留，检查时无 Git 进程，已移至 backups/push-lock-20260923/index.lock.stale 保留。未删除索引、reset、暂存、commit 或 push；索引摘要在暂存预演前后保持一致。

Jellyfish.ps1 在预检查及实际 git add 前检查 git rev-parse --git-path index.lock（兼容 Git 工作树路径），存在即提前显示中文诊断与恢复说明；不按锁龄自动删除锁、不终止 Git。未来出现此提示，应先等待其他操作结束，只有核实为无人持有的残留锁才备份移走。14 项模拟脚本测试通过，含原有锁、确认前出现的新锁，保持不写入、不自动删锁；补齐原测试遗漏的本机助手空操作夹具。

同时发现 OpenAPI 生成器会新增 EOF 空行，导致后续 git diff --cached --check 失败；新增 front/scripts/normalize-generated-eof.mjs，openapi:gen 自动执行，只处理 generated TypeScript 末尾单换行，不修改契约内容。重新生成与 tsc 验证通过；首轮遇 Windows 临时文件访问错误，顺序重跑成功。最终真实 Push -Preview、git add --dry-run -A 及 git diff --check 验证记录在 local-reports/push-lock-20260923；预演不表示远程认证/联网推送成功。

LC-001/009/014；本批不涉及业务 API 变化、数据库或运行服务部署，其他未关闭计划保持。用户可关闭旧失败窗口后重新运行 Push-Jellyfish.cmd，核对范围并按原流程确认。


## 2026-09-23 图片与视频手工素材采用（工作树待提交批次）

发布完成（2026-09-23）：仅更新 API/front，正式 7788 页面及真实新接口只读验收通过，9 个业务入口、0 页面异常、0 真实写入；没有替换用户现有图片/视频。新 API 镜像 sha256:5a11c9e34f248c5575587aa70a953bcb0cd3b07f54f77703dc22676737b263f7，front sha256:3c3faab5b7e29bf0defc7c6dcf5586afb5f5f9128d51aca25da69f748a8929e1，资源 index-CneNH27v.js；OpenAPI 与线上完全一致。启动标签 latest/web-flow-20260923 已同步，rollback-manual-media-20260923 保留；worker/beat 与数据库未改。本批仍未提交。


覆盖资产管理演员/场景/道具/服装卡片、五类资产（含项目角色）各角度详情、项目工作台五类资产卡片，以及分镜工作室首帧/关键帧/尾帧和当前视频。新增“修改图片 / 修改视频”：从文件管理选择或上传素材→预览→确认采用；上传本身不修改业务对象，取消后文件仍可在文件管理复用。资产卡片快捷修改当前展示的正面/封面槽位，项目卡片修改正面图，其他角度从详情修改；原图片/视频文件保留，不发起模型任务。

共用 ManualMediaButton 与现有 MediaFilePicker/FilePreviewModal、generated 文件客户端；新增 media-assets/manual-selection 的 GET 快照与 POST 手工采用接口，业务放 services/studio/manual_media.py。服务核对七类目标归属、图片/视频类型，原子比较并递增槽位/视频版本；空槽位在明确采用时创建，不能仅打开窗口就写入。手工采用使用 API/网页发布器相同版本字段，旧生成不能覆盖用户新选择；冲突必须重新核对。镜头信息确认状态、时长、提示词不因采用而更改；镜头/项目角色/共享资产项目引用同步，原文件不删除，不伪造生成候选记录。既有“参考素材”关联用途保持独立。

验证：48 项后端回归通过（9 项新手工采用、39 项既有网页任务/交接），OpenAPI/generated 已同步，tsc 与生产构建通过（保留原大包提示）。7 类目标隔离浏览器验证覆盖文件选择、上传预览、显式采用、409 冲突、对象切换和窄屏；实际路由覆盖资产列表/详情、项目工作台五类资产和工作室帧图/视频，测试项目没有道具/服装关联时只在浏览器响应内补关系，不写入生产。无真实生成/收费、无生产素材替换；生产正式读取验收和部署状态见 local-reports/manual-media-20260923/runtime.json。

映射 LC-005/006/007（复用/预览/用户入口）、LC-009/012（契约、目标与版本保护）、LC-014/015（验证、文档与成片素材）；PL-004 本次入口阶段完成，原架构收尾与其他全部未关闭计划保持，不关闭 PL-020 网页生成真实验收。源码备份 backups/manual-media-20260923；无数据库结构/迁移、无 Git 提交或清理。


## 2026-09-23 同批追加：账号状态统一

生成弹窗、账号下拉与网页平台账号页共用 webAccountStatus，同一后端 readiness 显示相同的“可用 / 待登录 / 待验证 / 忙碌，可排队 / 不可用”；登录、验证与不可用处理提示一致。账号页不再使用旧 session_state 长描述，不再宣称历史型号缓存是分配门槛。执行器连接及模型适配信息与账号状态分开显示；自动分配汇总最佳可受理账号，受限邻居不影响整池。全局模型或本机助手阻塞仍由提交检查单独解释。

类型检查、生产构建及两个真实组件的只读浏览器验证通过：可用、待登录状态跨页面一致，登录等待仍可提交，不可用提供管理入口，无业务写入、无新生成。验证夹具初次导入方式错误已修正；不计为业务验收。前端发布记录见 local-reports/web-account-readiness-20260923/runtime.json；compact-ui.json 为页面证据。LC-006/010/011/015（交互）、LC-001/014（验证部署），PL-020 仍未关闭，工作树待提交；其他未关闭计划不变。


## 2026-09-23 同批追加：账号状态提示精简

按用户要求，生成弹窗不再逐账号展示执行器离线、型号尚未核实等重复技术明细。可用仅显示“账号可用，可提交任务”；需要登录/验证或排队时保留一句自动继续说明；不可用时显示首要原因及重新检查/管理账号等必要入口。账号下拉标签精简为可用、待登录、待验证、忙碌可排队或不可用。后台整池分配与唯一任务绑定规则不变。

tsc/生产构建通过，可用/等待登录/不可用三种界面状态测试通过，验证按钮可用性、不可用操作入口和零业务写入。前端已发布，证据 local-reports/web-account-readiness-20260923/compact-ui.json；该目录 runtime.json 为最终镜像记录。LC-006/010/011/015（交互）、LC-001/014（验证部署），PL-020 保留未关闭，工作树待提交。


## 账号可用与自动分配（2026-09-23）

已登录账号可提交网页生成，不需要先逐个探测所选型号。执行器未运行时会自动启动；尚未登录或出现验证码时，任务保留，在原窗口完成操作后自动继续。忙碌账号可排队。自动分配优先其他已登录空闲账号，不会因某一个账号不可用禁用整池。

“型号将在执行时核实”是提示，不是账号不可用。实际所选型号不存在、平台未适配、账号停用/受限或模型目录排除时，会分别说明，不自动换号换模型。修改已部署，旧页面可刷新后重开生成弹窗查看。

## 参考图与提示词分开显示时的回填（2026-09-23）

豆包将参考图和提示词显示为多条消息是可能的正常表现。平台已修复因此无法识别回执的问题，并按同一次任务的附件数量和消息编号恢复结果。不要因官网已出图而再次点击生成；采集仍按原任务继续。当前“铅笔”任务已完成回填，可在原道具第 7 图片槽位查看。多参考图场景已通过离线验证，官网其他结构未知时仍需核实，不能随意将最新图片认作本任务结果。


## 网页生成等待与恢复（2026-09-23 性能优化）

提交后可继续操作或离开页面。当前页面有运行任务时会更频繁查询进度，完成后原对象的候选入口及时刷新，不抢焦点。多结果中的后续下载失败时，系统保留已校验原件并继续收集；不会因此重新生成或额外消耗权益。仍需等整批收齐后选择采用，并非每下载一张立即发布。文件被修改或结果来源无法核实时会保留任务等待处理。登录/验证码完成后按原任务续跑。

本批没有切换 Chrome/Edge 或提供去水印功能；实际官网新生成耗时、长视频与验证码全流程仍待验收。


## 多结果直接在原页面选择（2026-09-23 已上线）

在资产卡片、资产编辑页，或工作室对应关键帧/视频区域，直接点击蓝色“选择生成结果（N）”。弹窗可以切换生成批次、放大图片/播放视频、下载及“采用此结果”，不必去任务中心找任务。当前使用版本有标记；主动换选不会删除其他候选。若等待期间别人或另一页面已换过版本，会提示刷新后再选。

自动网页任务提交后留在原业务页面，结果到齐会出现入口；可以离开或刷新，返回后仍按原对象找到。真实道具页已恢复并显示 10 张候选。下方“去网页生成任务详情选择”的描述仍可作为备用入口，不是默认路径。


## 2026-09-23 网页生成操作更新

自动分配只唤醒任务预留账号；指定账号不换号。同账号排队，不在一个浏览器争用输入；不同账号并行。实际速度仍取决于官网登录、上传与生成耗时。

登录或验证码在原专用窗口完成后自动继续，不要重按生成。“已暂停”表示需要核对归属或采集异常，应在原任务恢复或显式取消；关闭 Jellyfish 页面不会取消后台任务。

一次生成多张图片或多个视频时，所有原文件归档到原任务，成功后释放账号。在“网页生成”任务详情的原文件区域查看、下载并选择“采用此版本”。单槽位不被任意候选覆盖，目标版本已变化时拒绝覆盖并保留文件；普通单结果仍按原版本自动回填。

本批已恢复三个历史多图任务共 10 张，状态已完成，候选等待用户选择采用。未执行新官网生成；历史结果恢复不能替代新图片/长视频的真实验收。下方 9/17 部署数据仅作历史记录。


## 当前交付：2026-09-17 四批可靠性修复已部署

四批工程实施已完成并部署，原十步逐项映射见 site/content/docs/plans/web-generation-reliability-repair.md。后端全量795项、Node/隔离Edge34项、Windows模拟与真实启动、OpenAPI/generated、tsc/生产构建通过。正式资产场景/分镜首帧目标与版本、视频三种布局、账号登录启动、暂停恢复取消与小屏回归通过，写请求全部拦截，0新增官网生成。

北京时间18:57：API/worker/beat/migrate同镜像26435c4fc9a615908e3a576bda60e5d6216ae191ce8814d389ea603e530b8c28，前端b6d5ad7da0fd84ada2127c9f9b0d7edd419a996020c31278406d8a75e4e9d6e7；migrate退出0，DB仍b4d6f8a0c016，worker ping pong，助手在线。349后端源文件与运行容器一致，正式schema与front/openapi.json一致；隔离迁移55表内容不变。备份 backups/web-deployment-20260917-182154，回滚标签 rollback-reliability-20260917，未删除任何旧备份/业务卷。

原任务9d2428f0ab475c52bab41fe26ef3f5c1仍为受理未知，已迁移为持久暂停，账号与回执保留。可从网页生成任务详情“恢复原任务”或“取消并释放账号”；不重发、不宣称已回填。新版本真实图片/视频产物、多参考官网语义顺序和其他平台仍按PL-020待验收。PL-021/022运维修复完成；PL-016/017原业务含义及其他未关闭计划保留。未提交Git，仍为工作树待提交批次。

本批语义映射：账号/入口/恢复LC-006/010/011/015，媒体与回执LC-011/012/015，守护/镜像/迁移/测试/文档LC-001/014。已审查git diff --name-status和未跟踪清单，并以本批前源码SHA隔离原有二开。验证详情及剩余边界见四批计划；全树历史generated EOF空行与原大bundle提示保留。

## 本轮阶段证据（以下为部署前记录）

第二批更新：持久暂停与恢复版本CAS、原结果人工接手、取消后迟到报告保护、自动拉起退避、独立Windows守护已实现。49项后端、33项Node/隔离Edge、Windows模拟入口及真实React恢复/取消页面通过；OpenAPI/generated与tsc通过。暂停仍保留原账号，可在网页生成任务详情恢复原任务或显式取消；只有已绑定回执且暂停的任务可人工上传原件。关闭页面不取消。第三批部署处理中，候选代码不等于生产已更新。当前Redis鉴权只读探测正常，旧Celery仍停止；积压2条官方资料同步、2条已取消outbox，无待执行收费任务。

用户授权连续实施四批；LC-001/006/010/011/012/014/015，PL-020延伸。第一批代码及离线验收完成：账号池任一合格账号可用、完整候选扫描、助手离线提示；图/视频共享消息匹配、视频容器去重、严格型号/画幅/时长确认。原件下载不点击任意高亮按钮，多候选不默认首张；规格不符保留历史，时长容差min(0.5秒,3%)、画幅相对误差3%。后端45项、Node/隔离Edge32项通过，0真实生成。源码快照 backups/web-reliability-20260917-175335（1471文件及SHA清单）。第二批恢复/启动闭环进行中，部署未开始，不能将候选当作当前运行服务。完整十步及四批映射见 site/content/docs/plans/web-generation-reliability-repair.md。

原PL-016仍为多参考创作、PL-017仍为非阻塞任务；此前重复编号的Celery和迁移问题分别改用PL-021/PL-022，原未关闭计划不丢弃。历史“最新状态”按原日期解释，原“采集暂停即释放账号”表述不成立：暂停保留占用，显式取消才释放。

## 换电脑 / 新电脑首次部署入口

完整步骤见 [新电脑部署与数据迁移](Jellyfish-新电脑部署与数据迁移.md)。先选择“全新空库”或“延续旧系统”，按环境与磁盘准备、精确二开代码、受保护配置、数据服务创建、SQL/对象恢复、迁移/seed、应用检查、任务调度、验收与回滚顺序操作。

新电脑没有既有容器，不能直接运行日常 Start 脚本；首次部署成功后才转第3节日常启停。只克隆 GitHub 不包含未提交二开、数据库、素材或 .env。保留旧机直到新机验收完成，严禁双机同时执行同批任务；清理旧数据/备份需单独确认。本流程文档已核对当前代码和官方安装资料，未在新电脑实机演练。

## 2026-09-08 Windows 脚本复查修正（LC-001 / LC-014）

工作树待提交批次：共享脚本新增 API/页面 HTTP 就绪检查和最终容器复核，避免 running 即报成功；停止/提交前输入 YES 确认，自动化需显式 -Yes，Preview 始终只读；add 后再次检查实际索引敏感路径。三份根目录 CMD 入口路径与退出码处理正确，保留不变。详细用法见根目录 WINDOWS-SCRIPTS.md。

12 项隔离模拟测试、三项本机只读预览、UTF-8 BOM 检查通过。未实际启停、暂存、提交、推送、迁移或调用模型。停止仍需人工确认任务已结束；推送包含全部未忽略改动，文件名防护不能代替源码内容密钥审查。当前 personal origin 与固定分支通过本地预览，未验证远程登录权限。所有业务 PL 待办保持不变。

## Windows 一键脚本（2026-09-08）

日常启动/停止优先使用根目录 Start-Jellyfish.cmd / Stop-Jellyfish.cmd：双击后显示服务清单，启动等待数据服务健康，停止先应用后数据。只操作已有七个容器，不执行迁移、seed 或构建。启动前打开 Docker Desktop；停止前等待生成任务结束。

Push-Jellyfish.cmd 提交全部未忽略改动到个人 origin 的 local/stable-codex-0718 分支；普通 push，不自动合并或强推。提交前完成测试与文档语义同步，之后用输出的精确 SHA 更新台账。首次可能需要 GitHub 登录。

详细服务清单、核心边界、预览命令、自定义提交说明及失败处理见 [Windows 一键脚本说明](../WINDOWS-SCRIPTS.md)。共享脚本采用 UTF-8 BOM；三项只读预览和七项模拟测试通过，本批未实际启停或推送。

## 新对话、换项目目录时如何接续工作

项目记忆入口为根目录 [PROJECT_MEMORY.md](../PROJECT_MEMORY.md)。它记录核心业务目标、二开现状、未完成计划、部署证据及升级规则。AGENTS.md 已设置接手必读和同批更新约定，但新对话不是天然继承旧聊天；请主动提供入口。

1. 在新对话打开实际项目根目录；换目录时一并保留 AGENTS.md、PROJECT_MEMORY.md、docs 台账/手册及 site 中的架构/计划/升级 SOP。不要只复制代码文件。
2. 发送：“当前根目录是【实际路径】。先完整读取 AGENTS.md、PROJECT_MEMORY.md 及其指定的台账和相关计划，只读核实现状，简述核心目标、已完成与未完成内容后，继续【本次任务】。完成后同步项目记忆和二开文档。”
3. 若无法直接访问目录，先附上 PROJECT_MEMORY.md，再按需提供相关台账/计划；未读到的代码不能视为已核实。
4. 换目录不等于迁移 Docker 数据或切换生产服务；历史路径、镜像、数据库版本需现场确认。原有付费/破坏性授权不自动扩大。
5. 每次二开由执行者在同批修改中更新记忆和台账；未新增后台自动同步程序。完整开场指令和维护检查见项目记忆第 9、10 节。

本次仅完善交接文档，无需部署重启或数据库迁移。

## 2026-09-08 15:34 筛选下拉显示修复（已部署）

总览的下拉选项被抽屉遮挡，并非模型数据缺失。本次已统一提高四个筛选框及“每页条数”菜单层级，只更新前端。打开 http://127.0.0.1:7788 后Ctrl+F5，再进入模型管理→模型→场景选型说明，检查场景、类型、供应商、状态和每页条数菜单。无需重建模型或修改供应商配置。

前端16项测试、类型和生产构建通过，线上主文件index-DrsraUNG.js；浏览器工具启动失败，实际点击验收待补。后端及数据库未重启、无迁移或收费调用。回滚镜像jellyfish-front:rollback-overview-dropdown-20260908已保留。

## 2026-09-08 场景选型总览说明（已部署）

2026-09-08 15:17（北京时间）经授权部署重启完成，“场景选型说明”已调整为完整型号总览。打开 http://127.0.0.1:7788 后按Ctrl+F5刷新，再按以下步骤查看。不需数据库迁移，未修改默认模型或现有账户。

1. 进入“模型管理 → 模型 → 场景选型说明”。默认列出项目离线已接入的国产型号与已保存型号，不要求先创建模型；已保存的排在前面。
2. 横向阅读：供应商/型号 → 系统接入能力 → 我的配置 → 适用场景 → 限制与配置说明。系统支持只针对列出的业务模式，不代表官网所有功能。
3. 按场景、生成类型、供应商、配置状态或名称/账户搜索缩小范围；“全部已保存”包括待完善记录。分页固定在窗口底部，可切8/16/32条。
4. “未配置”表示还没保存该型号；“已保存·待完善/核验”可能缺少密钥、禁用、端点不符或精确型号待核对；“已配置·待实测”不等于真实调用成功。展开行可查看各账户原因。
5. 未配置型号点击“配置此型号”，预填型号/类型；有多个账户需手动选择。“先配置供应商”会引导到供应商页。同型号多个账户可分别“查看/完善配置”，不会自动覆盖其他账户。
6. 自定义文本接口可显示协议适用方向，但未经核验的具体型号不会被标成已接通。切换“全部厂商”时，缺少离线精确目录的厂商会显示覆盖边界；不把厂商名称当作型号支持证据。
7. 官方文档/开通指引用于核对地域、权限与购买条件；本页不自动购买、不读取账户余额、不调用生成接口，也不会切换默认模型。第一次生成前自行确认API计费，网页积分不等于API额度。

本轮编码自检：全量后端590项、前端15项通过，OpenAPI/客户端及类型/生产构建通过。部署验收：前后端健康、总览接口、新前端index-aOZLRtSh.js均通过；任务执行器pong。当前国产目录62条（已保存7、未配置55），全部厂商67条；数量随目录及配置变化。浏览器连接此前被本机沙箱错误阻断，尚未完成真实浏览器验收；未做收费调用。MiniMax人物参考、即梦图片公网参考等原计划待办保持不变。

回滚镜像保留为四个jellyfish-<服务>:rollback-overview-20260908-1515，原备份未删除。数据库revision a7b9c1d3e508不变，MySQL/Redis/RustFS未重启，业务数据沿用原有一份；回退应用不需还原数据库。

## 2026-09-08 国产模型新接入（PL-014，已部署）

2026-09-08 14:17（北京时间）经用户授权，已构建并重启四个应用服务。本节功能已部署到 http://127.0.0.1:7788；请先 Ctrl+F5，再进入模型管理查看供应商配置及场景选型说明。未修改现有默认模型/账户；没有新增数据库表或迁移。代码与 Mock 测试不等于真实账户验收；网页/App 会员和积分不能等同 API 余额，价格/地区/免费期限须由用户在厂商控制台核实。

编码阶段自检：后端571项通过（含45项新用例）、前端现有刷新/帧图7项通过，接口客户端重新生成，类型检查和生产构建通过。部署阶段：前后端health/首页、场景建议、供应商支持接口均200，任务执行器pong，前端已提供index-DOMcQMbP.js；数据库revision保持a7b9c1d3e508，MySQL/Redis/RustFS未重启。真实浏览器交互及收费小样尚未验收。

回滚保障：四个旧应用镜像保留为 jellyfish-<服务>:rollback-pl014-20260908（服务为backend、celery-worker、celery-beat、front），原备份/业务卷未删除。本次未改数据库，若需回滚应只切回这四个应用镜像，不还原或清空业务数据。MiniMax人物参考、即梦图片公网参考导出等未完成项仍按PL-014跟踪，不因部署而视作完成。

### 配置顺序

1. 模型管理→供应商→新增，填写名称并选择实际调用协议。读取新出现的开通提示，前往官方平台核对账户地域、模型权限、API 价格与后付费开关；自行决定是否开通/购买，系统不代购。
2. 设置对应 Base URL 和凭据。已有端点不自动覆盖；MiniMax 国内/国际账户、混元旧平台/TokenHub、方舟套餐/即梦 Visual 均不可混用。
3. 模型管理→模型→新增，选择供应商、类别及内置目录候选。候选只代表适配代码范围，不证明你有权限/免费额度。配置好后按需在“默认模型”指定默认值。
4. 打开“场景选型说明”：默认国产，从全部已保存模型读取候选。查看“可作为候选/暂不推荐”、具体输入要求和官方文档；不会替你改默认模型。编辑配置后点“重新读取配置”。
5. 先预览最终提示词与参考素材；超长提示会在提交前拦截，须保留角色、动作和关键细节后人工精简，不能把增加规则当成允许无限长输入。
6. 第一次真实生成可能收费。先小样验证：任务成功→资产/帧/视频可见→下载/播放→采用→后期使用。界面“连接成功”及静态核查均不代替这一步。

| 协议 | API 地址与凭据 | 当前开放范围 / 约束 |
| --- | --- | --- |
| MiniMax | 中国站 https://api.minimax.cn/v1；国际账户保留其官方地址；API Key | image-01/live 标准档文生图；Hailuo-2.3/Fast/02 为768P、6/10秒；Fast须首帧，只有02开放首尾帧。当前不接入通用图片参考 |
| 智谱 BigModel | https://open.bigmodel.cn/api/paas/v4；API Key | GLM文本；GLM-Image/CogView文生图（不能参考生图）；CogVideoX-3文生/首帧/双帧5/10秒。不能用Coding Plan地址替代媒体API |
| 腾讯混元（TokenHub） | https://tokenhub.tencentmaas.com/v1；TokenHub API Key | HY文本；hy-image-v3单图、最多3参考图、面积≤1024×1024；hy-video-v1.5为5秒720p文生/首帧，图生画幅遵循首帧，无尾帧 |
| 即梦（独立视觉API） | https://visual.volcengineapi.com；API Key填AK，API Secret填SK | t2i_v40_jimeng标准2K单图文生；jimeng_i2v_first_tail_v30为720p双帧5/10秒。必须首尾帧；不是文字修改原视频 |

MiniMax 配音：新建音频模型，例如 speech-2.8-hd，在参数JSON中显式填写：
```json
{
  "audio_endpoint": "https://api.minimax.cn/v1/t2a_v2",
  "voice": "填写该账户已授权的官方音色ID",
  "voice_setting": { "speed": 1, "vol": 1, "pitch": 0 }
}
```
该值是音色占位说明，不能原样用于生成。镜头配音当前开放 Chinese/English/auto、MP3非流式输出；不接受自由配音指令，不等同音效/音乐/声音克隆。新建后选择模型和音色，配音结果进入镜头音轨及已有音频合成流程。音色/模型仍须真实账户验收。

### 场景补充与边界

- 剧本提取/角色检查：在千问、豆包、DeepSeek、MiniMax、GLM、HY中按实际结构化输出和延迟对比；不是越大越好，需检查遗漏、拒答与截断。
- 概念图：可比较原有模型与新MiniMax、CogView/GLM-Image、混元、即梦；演员一致性优先选项目已接通参考图的型号。
- 单帧驱动表演：海螺/可灵/Seedance/混元均为候选方向，不保证复杂互动零穿帮。先确认首帧道具比例、服装和人物，再付费生成。
- 首尾过渡：Hailuo-02、CogVideoX-3、即梦双帧或原有支持双帧的型号；必须根据该型号时长/图片比例设置，不把关键帧当尾帧。
- 原视频文字修改：仍用已有专用编辑入口和已接入编辑模型。本批新增图生/双帧生成不是原片编辑，不复用旧视频当免费修改。
- 即梦图片参考需要公网URL，本地文件导出尚未开放；MiniMax人物参考语义入口待补。选择不支持的素材会明确拒绝，不会静默丢弃。可以改用已接通本地参考图的模型，不能因此把RustFS改成匿名公开。
- 取消只保证本地停止等待；远程已提交任务可能继续执行/计费，不承诺退款或崩溃自动续跑。
- 未配置/禁用/缺凭据/端点错误会被标为暂不推荐。未知额度显示未核实，不提示“免费生成”。

官方核对入口：[MiniMax](https://platform.minimaxi.com/docs/api-reference/api-overview)、[智谱图像](https://docs.bigmodel.cn/api-reference/模型-api/图像生成)、[混元TokenHub](https://cloud.tencent.com/document/product/1823/130078)、[即梦图像4.0](https://www.volcengine.com/docs/85621/1863351)。最新验收/限制/未完成项见 site/content/docs/plans/domestic-provider-integration.md 和二开全量变更清单。


## 2026-09-08 部署确认（PL-013，当前运行版本）

已获用户授权，约 11:51（北京时间）从 E:\JellyfishNew 构建并替换 backend、celery-worker、celery-beat、front；部署后健康及资源核验通过。本节部署状态优先于下方历史“候选未部署”记录。工作树待提交，本次未变更业务代码。

- 使用同一 jellyfish Compose 项目、.env 和 secure-local 覆盖，执行 up -d --no-deps --no-build --timeout 60，仅替换应用四服务。未运行数据库迁移/初始化，未重启 MySQL、Redis、RustFS，未删除业务卷或既有备份；迁移版本仍为 a7b9c1d3e508。切换前没有 pending/running/streaming 生成任务。
- 后端 /health、/api/v1/health、前端首页及新 JS 均 HTTP 200；前端 index-C0l3Rg-U.js 包含编辑接口。线上 OpenAPI 已出现 video-edit-preflight、quality-review、video-edits、video-edits/{task_id}/adopt；Celery Worker ping 返回 pong，定时任务正常投递执行。
- 运行镜像摘要（sha256 前缀）：backend f847190a2b15；worker 0eb771c74f0a；beat a8306190fe27；front 28dbf18299c3。四个原镜像均保留 rollback-pl013-20260908 标签；不自动清理。
- 验证边界：这是部署/健康验收，不是付费端到端验收。未调用收费模型，未修改模型配置；真实浏览器交互、真实编辑输出/音轨对齐仍待验收，PL-013 不整体关闭。此前 526 项后端、7 项前端及隔离音轨验证保持为代码自检证据。

访问 http://127.0.0.1:7788，旧标签页先 Ctrl+F5。进入“分镜工作室”：在首帧/关键帧/尾帧或视频提示词窗口使用可选质量预检；在主预览区域打开视频编辑，按面板配置 fal.ai 或 Runway 的独立 API 账户后选择原片、编辑说明与可选附件。免费规格检查不调用模型；AI 预检及编辑提交需确认外发和费用，结果须人工采用，不自动覆盖原片。详细配置步骤见下节。

## 2026-09-08 新功能使用步骤（候选代码，尚未部署）

本节覆盖下方同日历史说明。代码已写入 `E:\JellyfishNew` 并做离线自检；当前运行页面不会仅因文件修改就自动升级。未重启生产服务、未调用收费接口、未删除备份。本批无新增数据库表或列，不需要新增迁移。

### 一、生成前检查质量

1. 进入项目→章节→分镜工作台，打开帧图或视频的提示词预览。
2. 展开“生成依据与质量规则”，检查当前镜头、章节、关联资产及来源警告。找不到精确摘录仅表示需要核对，不代表剧情一定错误。来源发生变化时重新渲染，否则提交会提示重新预览。
3. 可展开“可选 AI 质量预检”，选择已配置文本模型，确认外发与费用后提交。默认只检查文字；结果为修改建议，你需自行审核后调整提示词、重新预览，不会自动付费重生成。
4. 如需图片检查，明确勾选当前参考图（最多4张、单图10MB内），使用已核验的百炼 Qwen 视觉型号，例如 qwen3.8-max、qwen3-vl-plus。没有百炼额度时不要启用这一路图片预检；仍可用其他已接入文本模型检查描述。其他供应商视觉协议尚未开放，不会静默丢弃图片改成纯文字。
5. 同样的来源、提示词、模型版本和图片版本复用任务。失败/取消后需要重试时，点击“准备手动重试”并重新确认费用；取消不保证远端退款。结果不等于“零穿帮认证”。

已核验的 Wan2.7 指定版本及 HappyHorse i2v 提示词超限会阻止提交，而非让模型截掉尾部细节。其他型号显示未核验，不代表没有长度限制；不要把整章无关内容塞入单镜头。

### 二、配置非百炼的视频编辑模型

在模型管理新增独立供应商和视频模型；使用提供 API 的平台自己的账户和 Key，不要把其他平台的会员或套餐 Key 复制过来。

| 服务 | Base URL | 模型名（精确复制） | 使用限制 |
| --- | --- | --- | --- |
| fal.ai（视频编辑） | `https://queue.fal.run` | `fal-ai/kling-video/o3/pro/video-to-video/edit` | MP4/MOV，3–15秒，720–3840px，200MB内；最多4张参考图片 |
| Runway（视频编辑） | `https://api.dev.runwayml.com` | `aleph2` | MP4/MOV，2–30秒，最高1080p、30FPS；最多5张指定秒数的参考图；本次直传单文件编码后最多5MB |

购买前到 [fal 官方型号页面](https://fal.ai/models/fal-ai/kling-video/o3/pro/video-to-video/edit/api) 或 [Runway Dev 官方说明](https://docs.dev.runwayml.com/guides/models/) 核对价格、地区和账户开通情况。Runway 网页会员与 API 余额不同；fal 接口不消耗现有可灵会员或百炼额度。系统不会代充值、自动使用另一计费通道或保证套餐覆盖。

这两种模型只在“文字编辑视频”使用，不能设置为普通视频生成默认模型。供应商的普通文本连通性测试不适用于它们；先免费检查输入，再另行确认真实生成费用。

### 三、用文字和图片修改已有视频

1. 分镜工作台主预览区点击“文字编辑视频”。已有视频可作为原片，也能从素材库选择或本次上传 MP4/MOV。
2. 输入具体修改内容和希望保持的内容。fal 示例：“调整 @Video1 中道具与使用者的相对比例，参照 @Image1；保留角色、动作与机位。”不要仅写“改好看一点”。不能保证模型逐像素只改指定区域。
3. 上传或从素材库选择参考图片；TXT/MD 会加入可编辑说明，不作为图片传入。Runway 参考图需填写它指导的画面时间（秒），不使用 fal 的 @Image 编号语法。非支持附件不要替代为另一个媒体类型。
4. 点击“免费检查输入规格”。系统读取真实时长和分辨率，超限要求换片段/模型，不自动裁剪、降画质或截断。检查通过不代表账号有额度或内容必然合格。
5. 选择是否保留原音轨，明确确认外发和费用，再生成编辑版本。任务可取消，窗口可关闭；下次打开从“编辑历史”查询任务和结果。
6. 对比原片与新片后点击“比较后采用”。仅更新当前镜头文件，原片仍在素材文件库；别人已修改当前视频时会阻止覆盖，要求重新比较。既有时间线不会被偷偷替换，需按后期流程确认采用内容。
7. Runway 保留音轨由本地处理，源片与输出时长不一致会失败并保留供应商原始结果供下载；先排查后处理，不要因此重复付费生成。

### 四、本次自检与后续验收

全量后端526项通过，既有前端任务/图库7项通过，类型与生产构建通过。隔离 Docker 内已用合成素材验证音轨合成（3.008秒、1280×720、AAC），未接触数据库或密钥；AAC 重编码不代表音频字节无损。真实浏览器交互因工具启动失败尚未验收，真实模型输出仍需做小样。生产部署和收费小样需分开确认。Luma、百炼编辑、掩码及裁剪拼接等条件增强仍记录在二开台账，不能视为已上线。

## 2026-09-08 候选修复：资产描述进入视频指令（未部署）

部署本批后，修改已关联道具、角色、场景或服装的描述，再重新预览视频提示词，将读取资产当前内容，不只使用导入时的旧候选描述；明确清空描述也会生效。描述是否逐字包含只用于追溯，不能保证模型完全遵循。

内部任务来源记录可定位镜头、章节和关联资产，章节摘录被改写时记为待核对，不自动判错或改写剧本。当前没有新增来源审核页面；无需修改模型配置。本批仍未部署，文字编辑视频等后续功能仍不可用。

## 2026-09-08 开发进度说明（未部署）

本次视频质量与视频编辑仍在分阶段开发，不能将候选代码当成线上功能。当前基础规则与预览已编码，任务内部新增最终提示词哈希及保留规则追溯；没有新增任务中心调试面板，不需要用户修改配置。预览后手动删改规则，以最终提交文本为准，内部记录不会把已删除要求记为已应用。

这不是 AI 视觉检查，也不是完整章节事实审计。AI 预检、fal.ai/Runway 文字编辑视频、附件编辑及购买配置引导尚未接通；当前不用购买新套餐。计划与实际进度统一见 site/content/docs/plans/video-quality-and-editing.md，二开迁移时同时核对已实现项与未关闭 PL-013。

## 待部署功能：生成依据与质量规则（2026-09-08）

本批候选代码在帧图/视频提示词预览增加“生成依据与质量规则”。部署后可展开查看相关描述来源及实际补入的规则。它是免费基础编译，不代表 AI 检查图片正确、也不保证零穿帮；改动输入后应重新渲染报告。

当前未部署，线上暂看不到该功能。fal.ai/Runway 视频编辑、附件编辑与购买配置引导仍在后续开发，不要根据此前调研名单尝试调用未接入型号。

## 分镜帧图片成功但不显示（2026-09-07 修复）

已于 18:49 更新前端。请刷新浏览器后查看；如仍使用旧页面，可按 Ctrl+F5 强制刷新，无需重新点击生成。

任务中心成功、卡片暂无图片曾由新旧任务关系查询不兼容导致。修复后进入分镜工作室会读取该镜头已保存帧图和新旧任务历史；生成完成/手动采用后自动刷新帧列表。已有图片不需要重新生成。

首帧、关键帧、尾帧属于各自分镜：当前镜头没有首帧时，不会借用另一个镜头的首帧。任务中心显示其他镜头的成功任务不代表当前镜头三类图片都已生成。慢任务在当前页面以 10 秒间隔跟踪；离开页面不取消后台任务，重新进入可读取已完成产物。

## 视频报错：首帧与 Agent Plan（2026-09-07）

本批代码已于 18:38 部署，浏览器刷新后生效；前端/API/Worker 健康检查通过，真实付费视频由用户验证。

1. happyhorse-1.1-i2v 是图生视频：在分镜工作室先生成或上传该镜头的首帧，再选择首帧参考生成。只有资产关联照片不代表镜头已有首帧；系统不擅自把角色照片当首帧。批量生成会使用每个镜头已有首帧，缺失的镜头不能生成。
2. 只有文本时应明确选择支持文生视频的型号，例如已配置且账号可用的 t2v；不会自动切换型号或计费入口。I2V 官方要求单张首帧，首帧决定画幅，不发送 ratio。来源：[官方 I2V API](https://help.aliyun.com/zh/model-studio/happyhorse-image-to-video-api-reference)。
3. 火山 HTTP 404 若同时为 UnsupportedModel 且提示 agent plan，说明所请求型号未被该 Plan 接受，不代表应删除 URL 中 /plan。此次控制台截图提示 Seedance 1.5 即将下线且不支持新增接入、Medium 暂不支持 Seedance 2.0；向火山工单提供精确型号、套餐及 request_id，请其确认可用权限或替代型号，不要提供 API Key。
4. 保留控制台给定的 Plan 视频地址。标准 /api/v3 可能另行计费，禁止为了消除 404 擅自替换。语音的 openspeech /api/v3/plan/tts/unidirectional 与视频协议不同，不能填进视频地址；语音截图不表示本系统已支持该语音适配。
5. 本批修复后请刷新浏览器，旧失败任务不会自动重试。先检查镜头首帧与准备度，再由用户发起真实生成；离线回归通过不等于供应商付费生成已验收。

## 自助供应商与新适配器（2026-09-07 已部署）

本节对应迁移 a7b9c1d3e508，已执行并切换新版服务。原供应商无需删除或重录；若仍看到旧界面，请刷新网页。486 项后端回归、前端类型检查与构建通过；真实新供应商生成仍需自行配置账户并确认费用。

此次恢复备份保留在 `E:\JellyfishNew\backups\provider-adapter-a7b9-20260907`，包含 SQL 和原运行应用文件，不是第二份在线业务数据库。正式系统仍使用原数据库和 RustFS 数据卷。不要上传备份（可能包含敏感配置）；验证用临时内存数据库已销毁，磁盘备份尚未删除。

1. 模型管理 → 供应商 → 添加：填写任意便于识别的供应商名称，再选择“调用协议 / 适配器”。供应商名字不决定实际接口。
2. 新增兼容文本服务可选择“自定义兼容接口（仅文本）”，填写该服务官方 Base URL 与 API Key；模型名称按官网填写。目录接口不存在时手动录入，不自动改端点或模型。兼容文本不是图片/视频/音频通用协议。
3. Google 使用 GenerateContent、Claude 使用 Messages、MiniMax 使用兼容文本协议；分别配置各自官方账户端点和 Key，不混用套餐 Key。新文本协议仅接入文本业务，未实现工具或直接图片理解。可用于业务 JSON 提取，但仍需检查候选证据与字段，不能认为所有模型都会遵守 Schema。
4. BFL 图片当前支持 flux-2-pro / flux-2-pro-preview 文生图及 flux-kontext-pro 单参考图。FLUX.2 不能在当前适配中使用本地参考图；系统会明确拒绝，不静默丢弃参考。BFL 未接入视频/音频。
5. 已有模型的供应商可以改名；更换调用协议请新建供应商，再调整需要迁移的模型。切换协议选择不会覆盖已经填写的 Base URL。已提交任务保留当时协议、型号及端点；凭据仍读取所引用供应商当前 Key。
6. 免费“接入核查/读取官方正文”不等于真实生成验收；“测试生成”或业务生成可能产生费用。Google 图片/视频、MiniMax 视频/语音、自动官网语义比对仍在开发，请勿按已完成使用。

开发验证：正常仍运行 `pnpm run openapi:update` 从本机 8000 同步 API。需要在数据库迁移前检查候选接口时，可设置 `JELLYFISH_OPENAPI_URL` 指向隔离 schema-only 服务；不要把运行中的旧版 OpenAPI 覆盖候选新版类型。此次未更换模型默认值、套餐端点或凭据。

## 场景选型与官方文档读取（2026-09-07 新批）

模型管理 → 模型：顶部“场景选型说明”；添加/编辑模型时也可点“如何选模型？”。按业务阅读必需能力，再选供应商、具体型号和账户套餐；参考图/首尾帧/TTS 指令不能从模型类别推断。

模型列表更多 → 接入核查 → 读取官方正文：只访问登记的官方文档，不带模型 Key 或剧本、不产生生成调用。页面显示获取时间、正文哈希及纯文本；遇到跳转、动态页面或网络限制可能失败，可打开官网人工核对。此批按点击读取，未实现保存模型后自动读取；正文仅本次临时展示，获取成功不等于智能核对通过。

新增 DeepSeek 供应商只支持文本，可用自己的 Key 获取目录并配置模型；没有账号可暂不配置，不影响原有默认模型。Google/MiniMax/Claude/FLUX 在计划内，尚未全部接入。真实测试可能收费，不能把说明中的候选当作已在本账户验收。

## 模型接入核查（2026-09-07 首批）

模型管理 → 模型 → 列表行“更多” → “接入核查（免费）”。查看当前端点、类别适配器、官方文档入口及业务验收清单；修改配置后可重新核查。

此功能目前只检查本地配置，不自动读取官网，不调用付费生成，不外发剧本/密钥，也不读取历史真实验收结果。“已注册类别适配器”不等于该具体模型可用于全部业务；“契约待核对”也不表示已有模型必然不可用。

自动官网采集、智能差异分析与版本化验收仍在开发计划中。不要仅凭供应商连接测试或模型目录断定已打通；套餐地址不会自动切换到按量地址。后续失败详情可包含供应商错误码和请求 ID，便于定位。

> 适用环境：Windows 10 + WSL2 + Docker Desktop，本机目录 `E:\JellyfishNew`
> 适用代码：作者 `codex/0718` 分支（基线 `508f2c7`）+ 本地二开集成分支
> 更新日期：2026-09-07
> 文档原则：不记录数据库密码、Redis 密码、对象存储密钥或模型 API Key

## 1. Jellyfish 是什么

模型兼容边界：相同供应商的不同模型、思考模式与套餐端点可能支持不同参数。当前深度分析的格式兼容不代表所有模型均已通过原生结构化输出验收。供应商文档及逐模型适配按 `site/content/docs/plans/model-aware-script-analysis.md` 跟踪；不会因失败擅自切模型、改标准付费端点或再发一次分析。

深度分析可靠性更新（2026-09-07）：系统将完整输出规范与语义规则一起交给模型，不要求剧本采用固定标题或 Markdown 模板。结构错误时不会由 Agent 自动再发起第二次分析；错误展示字段位置而非剧本文本。转换过的项目设定会提示复核，缺失人物类型/镜头归属不能猜测入库，所有结果仍需审查确认。历史失败任务不会因更新自动恢复；原始响应留存、免费重新解析及细化阶段进度仍在完善中。

### 近期修正：模型保存与图片结果展示（2026-09-07）

新建模型成功后，列表自动切到该模型类别（含音频）、清空旧搜索条件并回到第一页。图片快速生成或资产详情生成提交后，每 10 秒检查任务，成功后自动更新图片；超过一分钟仍会继续跟踪，不需要离开页面重新查询。任务失败可到任务中心查看原因。此更新上线后，已打开的旧页面需要刷新一次以加载新版前端。

参考素材是否参与，应以该次生成冻结的参考文件输入为准，而不是仅看当前资产关联。图片作为模型参考输入，不代表人物外观会被严格复刻；后续调整关联不会改写历史任务的参考记录。

Jellyfish 是一套 AI 短剧制作工作台。它把以下流程放在同一套系统中：

`剧本 → 章节 → 分镜拆解 → 角色/场景/道具确认 → 关键帧 → 视频生成 → 任务跟踪 → 素材复用`

Jellyfish 自己负责编排、数据管理和任务调度，不自带免费的生成模型。文本、图片和视频生成需要配置第三方模型 API，费用由对应模型供应商收取。

## 2. 本机部署信息

| 项目 | 本机配置 |
| --- | --- |
| Jellyfish 源码 | `E:\JellyfishNew` |
| Docker Desktop | `E:\Docker\Desktop` |
| Docker/WSL 数据 | `E:\Docker\DockerData` |
| 前端 | `http://127.0.0.1:7788` |
| 后端 API | `http://127.0.0.1:8000` |
| API 文档 | `http://127.0.0.1:8000/docs` |
| Jellyfish MySQL | `127.0.0.1:3306` |
| Jellyfish Redis | Windows 侧 `127.0.0.1:6380`，容器内 `redis:6379` |
| RustFS S3 API | `http://127.0.0.1:9000` |
| RustFS 控制台 | `http://127.0.0.1:9001` |

正式切换后以 Compose 中的 MySQL、Redis 和 RustFS 为 Jellyfish 运行依赖。Windows 原有 `127.0.0.1:6379` Redis 与容器 Redis 的宿主机映射必须错开；实际端口以 `deploy\compose\.env` 为准。

所有端口只绑定到 `127.0.0.1`，仅本机可以访问。

### 2.1 RustFS 控制台登录

打开 <http://127.0.0.1:9001>，登录字段与本机配置的对应关系如下：

| RustFS 登录项 | 本机配置来源 |
| --- | --- |
| 用户名 / Access Key | `E:\JellyfishNew\deploy\compose\.env` 中的 `RUSTFS_ACCESS_KEY` |
| 密码 / Secret Key | 同一文件中的 `RUSTFS_SECRET_KEY` |

本机使用的是部署时随机生成的密钥，不是示例文件中的 `rustfsadmin`。为避免密钥随源码、截图或文档泄漏，本手册不记录具体值。不要把 `deploy\compose\.env` 提交到 Git，也不要在控制台截图中暴露密钥。

后端启动时会幂等检查并创建 `.env` 中 `S3_BUCKET_NAME` 指定的 bucket。Docker 内网端点 `http://rustfs:9000` 会自动使用 path-style 寻址。若上传日志出现 `NoSuchBucket` 或尝试访问 `桶名.rustfs`，先确认正在运行的是本手册对应版本，再重启 `backend` 并检查启动日志；不要通过修改 hosts 文件绕过。

## 3. 日常启动与关闭

### 3.1 启动前检查

1. 启动 Docker Desktop。
2. 等待 Docker Desktop 显示 Engine running。
3. 打开 PowerShell。

### 3.2 启动 Jellyfish

日常启动请双击根目录 `Start-Jellyfish.cmd`，或在 PowerShell 中执行同一共享实现：

```powershell
Set-Location "E:\JellyfishNew"
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\windows\Jellyfish.ps1 -Action Start
```

脚本核对七个现有常驻容器的归属，先启动数据服务并检查健康，再启动应用并检查 API/页面。它调用 `docker start`，不读取新的 `.env`，不构建、不重建、不执行迁移或 seed。容器缺失时中止，应按部署恢复流程处理。

启动后访问：

- 主界面：<http://127.0.0.1:7788>
- 后端文档：<http://127.0.0.1:8000/docs>

### 3.3 查看运行状态

```powershell
$env:Path = "E:\Docker\Desktop\resources\bin;$env:Path"
Set-Location "E:\JellyfishNew"
docker compose `
  --env-file "E:\JellyfishNew\deploy\compose\.env" `
  -f "E:\JellyfishNew\deploy\compose\docker-compose.yml" `
  -f "E:\JellyfishNew\deploy\compose\docker-compose.secure-local.yml" `
  ps -a
```

正常状态：

- `front`、`backend`、`celery-worker`、`celery-beat` 为 `Up`。其中 Worker 执行任务，Beat 每 10 秒可靠投递数据库 outbox 中的待执行任务；缺少 Beat 时任务只会停留在“等待中”，不会调用模型。
- `mysql`、`redis` 为 `Up (healthy)`。
- 若使用 Compose up 留下一次性容器，`backend-migrate`、`backend-seed-system-data` 应为 `Exited (0)`。若按新电脑流程使用 `run --rm`，成功后容器已移除，缺少该行是正常的；以执行退出码及数据库版本/系统数据核验为准，不必重新跑迁移制造状态行。

### 3.4 停止 Jellyfish

先确认任务中心没有未结束任务，再双击 `Stop-Jellyfish.cmd`；或执行：

```powershell
Set-Location "E:\JellyfishNew"
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\windows\Jellyfish.ps1 -Action Stop
```

输入大写 `YES` 确认后，按前端/API/Beat → Worker → 数据服务停止，保留容器、网络和数据卷。Worker 最多等待120秒，其他停止命令为60秒；超时可能强制结束，远端生成不保证取消或停止计费。恢复时使用 3.2 的启动入口，不用 `up -d` 代替。

> 不要执行 `docker compose down -v`。参数 `-v` 会删除数据库和对象存储卷，可能造成项目数据无法恢复。

### 3.5 重启、加载新配置与更新部署的区别

| 需求 | 正确操作 | 配置与数据边界 |
| --- | --- | --- |
| 开机后恢复已有服务 | Start-Jellyfish.cmd | 沿用容器创建时配置和已部署镜像 |
| 日常停止 | Stop-Jellyfish.cmd，输入 YES | 保留容器与数据；先等任务结束 |
| 原配置下重启整套服务 | 停止成功后再启动；停止失败先排查 | 不会加载修改后的 .env 或新代码 |
| 修改 deploy/compose/.env | 核对变量用途，备份配置，按变更范围重新创建受影响容器 | 只有 Compose 引用或 env_file 注入的变量才进入容器；不能靠 restart/start 加载新值 |
| 修改代码 | 测试后构建对应应用镜像，再定向替换应用容器 | 新源码不等于线上版本已更新；无 schema 变化不运行迁移/seed |
| GitHub 升级或数据库结构变化 | 完整执行第9节及升级 SOP | 候选目录、隔离验证、备份、审核迁移与切换；不是日常启停 |

日常启停的唯一推荐入口是根目录脚本；`docker compose up` 属于创建/更新部署，不与 `docker start` 混称。无 `-v` 的 `down` 虽通常保留命名卷，仍会移除容器和网络，导致现有启动脚本无法恢复，不能作为日常停止命令。

应用配置更新必须使用同一 `jellyfish` 项目、明确的 `--env-file`、基础 Compose 和 secure-local 覆盖；按受影响的 backend/worker/beat/front 定向重建，并显式控制依赖、构建与迁移范围。更新时先核实实际容器/卷，不重建保留历史标签的 RustFS。数据库或对象存储密码不能只改 .env：持久化服务端凭据需专门的协调变更流程。

本节不提供可盲目执行的全栈重建命令；更新部署应按具体差异制定命令并验证健康、Worker、历史文件和业务结果。详细日常参数见 [Windows 脚本说明](../WINDOWS-SCRIPTS.md)。

## 4. 首次模型配置

打开模型管理页面：<http://127.0.0.1:7788/models>

当前版本的供应商适配能力：

| 供应商 | 文本 | 图片 | 视频 | 语音 |
| --- | ---: | ---: | ---: | ---: |
| OpenAI | 支持 | 支持 | 支持 | 未接入 |
| 阿里百炼 | 支持 | 支持 | 支持 | 支持非实时 TTS |
| 火山引擎 | 支持 | 支持 | 支持 | 未接入专用鉴权 |
| Vidu | 不支持 | 支持 | 支持 | 不支持 |
| 可灵 AI | 不支持 | 支持 | 支持 | 不支持 |

“支持”表示 Jellyfish 已有相应协议适配器，不代表同一 Model ID 能同时完成多类任务。每条模型记录只能选择一个类别，Model ID 必须确实具备该类别的能力。

### 4.1 本机当前模型配置快照

本机当前有两个活跃供应商，API Key 已配置但不会在接口和本手册中回显：

| 供应商 | 文本/通用 Base URL | 图片 Base URL | 视频 Base URL |
| --- | --- | --- | --- |
| 阿里百炼 | `https://token-plan.cn-beijing.maas.aliyuncs.com/compatible-mode/v1` | `https://token-plan.cn-beijing.maas.aliyuncs.com/api/v1` | `https://token-plan.cn-beijing.maas.aliyuncs.com/api/v1` |
| 火山引擎 | `https://ark.cn-beijing.volces.com/api/plan/v3` | 留空，回退到通用 URL | 留空，回退到通用 URL |

注意：上表是已保存配置，不代表各模态已验证成功。2026-09-07 火山视频继承 Plan 通用地址后请求 `/contents/generations/tasks` 返回 404，当前不可作为视频已接通的依据。需要按套餐的官方视频 API 示例核对 Base URL、鉴权及模型；不要未经核对就改用标准 API，避免凭据不兼容或改变费用来源。

配音音效上传只接受 MP3/WAV/M4A/AAC/OGG/FLAC（最大 500MB）。MP4 即使包含声音也属于视频，请先提取音轨再上传；此版本不自动提取。以前“上传成功、创建音频失败”的视频仍在文件库，不必重复上传。

当前已录入模型：

| 供应商 | Model ID | 类别 | 当前用途 |
| --- | --- | --- | --- |
| 阿里百炼 | `qwen3.8-max` | 文本 | 当前默认文本模型 |
| 阿里百炼 | `wan2.7-image-pro` | 图片 | 可选图片模型 |
| 阿里百炼 | `happyhorse-1.1-i2v` | 视频 | 可选图生视频模型 |
| 火山引擎 | `doubao-seedream-5.0-lite` | 图片 | 当前默认图片模型 |
| 火山引擎 | `doubao-seedance-1.5-pro` | 视频 | 推荐设为默认视频模型 |
| 火山引擎 | `doubao-seedream-5.0-lite` | 视频 | 配置类别不匹配，不建议使用 |

当前全局设置为：文本默认 `qwen3.8-max`，图片默认 `doubao-seedream-5.0-lite`，视频默认值也指向了按“视频”类别录入的 `doubao-seedream-5.0-lite`，API 超时为 30 秒，日志级别为 `info`。

需要修正两项：

1. 把默认视频模型改为 `doubao-seedance-1.5-pro`；`doubao-seedream-5.0-lite` 是图片生成模型，不应作为视频模型。
2. 视频任务通常超过 30 秒。建议把全局 API 超时提高到 300 秒；异步任务仍会由后台轮询，但创建请求和中间网络请求也需要合理余量。

当前推荐组合：

- 文本：阿里百炼 `qwen3.8-max`。
- 图片：日常默认使用火山 `doubao-seedream-5.0-lite`；需要比较画面效果时可切换阿里 `wan2.7-image-pro`。
- 视频：日常默认使用火山 `doubao-seedance-1.5-pro`；需要图生视频对比时可选择阿里 `happyhorse-1.1-i2v`。

### 4.2 配置阿里百炼文本、图片、视频和语音模型

先在阿里云百炼控制台开通模型服务、创建 API Key，并设置消费提醒。

在 Jellyfish 中进入“模型管理 → 供应商 → 添加供应商”：

| 字段 | 内容 |
| --- | --- |
| 名称 | `阿里百炼` |
| 文本/通用 Base URL | 标准百炼 Key 使用 `https://dashscope.aliyuncs.com/compatible-mode/v1`；本机 Token Plan 使用当前已配置地址 |
| 图片 Base URL | 标准百炼使用 `https://dashscope.aliyuncs.com/api/v1`；本机使用当前已配置的 Token Plan `/api/v1` 地址 |
| 视频 Base URL | 与图片 Base URL 相同 |
| API Key | 在本机页面粘贴百炼 API Key |
| API Secret | 留空 |
| 状态 | 活跃 |

Base URL 和 API Key 必须来自同一套服务。不要把标准 DashScope Key 与 Token Plan 地址混用，也不要把 Token Plan Key 填到标准 DashScope 地址。保存供应商后，“测试连接”会使用该供应商最近更新的文本模型发起低费用真实请求。

进入“模型”页分别添加模型：

| Model ID 示例 | 类别 | 说明 |
| --- | --- | --- |
| `qwen3.8-max` | 文本 | 剧本分析、角色检查、智能精简和提示词处理 |
| `wan2.7-image-pro` | 图片 | 文生图/参考图生成；走百炼图片接口 |
| `happyhorse-1.1-i2v` | 视频 | 图生视频；走百炼异步视频接口 |
| `happyhorse-1.1-t2v` | 视频 | 纯文本生成视频，3–15 秒 |
| `happyhorse-1.1-r2v` | 视频 | 参考图片/视频生成，2–10 秒 |
| `qwen3-tts-flash` | 语音 | 非实时对白/旁白；可使用系统音色 |
| `qwen-audio-3.0-tts-flash` | 语音 | 高表现力语音；需要 Workspace 专用端点和音色 |

模型名称必须是供应商 API 接受的准确 Model ID，不要填写营销展示名称。文本模型可以设置 `{"temperature": 0.7}`；图片和视频参数优先在具体生成页面选择，未确认供应商是否接受的字段不要写进通用参数。

语音模型参数示例：

- `qwen3-tts-flash`：可填 `{"voice":"Cherry"}`；不填时 Jellyfish 默认使用 `Cherry`。系统使用官方 multimodal-generation 地址，不复用文本兼容地址。
- `qwen-audio-3.0-tts-flash` 或 CosyVoice：必须填 `{"voice":"你的音色ID","audio_endpoint":"https://你的WorkspaceId.cn-beijing.maas.aliyuncs.com/api/v1/services/audio/tts/SpeechSynthesizer"}`。`audio_endpoint` 是完整地址，不是 Provider 的文本 Base URL。

百炼非实时 TTS 返回的 URL 只临时有效。Jellyfish Worker 会在任务成功时立即下载到 RustFS，并建立全局音频资产和镜头音轨；任务数据中不会保存 API Key。

Token Plan 的 compatible-mode `/models` 可能不返回视频或语音模型。当前版本会先尝试业务空间目录，再把实时结果与 Jellyfish 维护的已接入目录合并，所以“阿里百炼 → 视频生成”应至少显示上述三个 HappyHorse 1.1 模型，“语音生成”会显示已接入的 TTS 模型。ASR 和实时语音对话不会误入文本或语音生成类别。仍可手工输入 Model ID，但手工录入不等于该模型参数已经适配。

在视频实验室选择模型后，首帧、尾帧、关键帧、主体参考、画幅和时长会按该模型能力变化：t2v 不要求素材，i2v 必须选择首帧，r2v 必须选择参考图片或视频。HappyHorse r2v 和 Wan 2.7 还会显示“参考音频”，每个主体最多选择一条，音频必须与该主体的图片或视频一起使用，作为 `reference_voice` 音色参考。素材关联不是硬性要求；只有选择了素材且模型支持时才作为生成参考。

### 4.3 配置火山引擎文本、图片和视频模型

先在火山方舟控制台完成实名认证、开通对应模型、创建 API Key，并设置余额与用量提醒。

在“模型管理 → 供应商 → 添加供应商”中填写：

| 字段 | 内容 |
| --- | --- |
| 名称 | `火山引擎` |
| 文本/通用 Base URL | 标准方舟使用 `https://ark.cn-beijing.volces.com/api/v3`；本机按 Token Plan 使用当前已配置地址 |
| 图片 Base URL | 留空，默认回退到通用 URL |
| 视频 Base URL | 留空，默认回退到通用 URL |
| API Key | 在本机页面粘贴方舟 API Key |
| API Secret | 留空 |
| 状态 | 活跃 |

然后按实际能力分别添加模型：

1. 文本模型：填写豆包文本模型或推理接入点的准确 Model ID，类别选择“文本”。
2. 图片模型：本机为 `doubao-seedream-5.0-lite`，类别选择“图片”。
3. 视频模型：本机为 `doubao-seedance-1.5-pro`，类别选择“视频”。

不要因为同属“豆包”就复用类别：Seedream 用于图片，Seedance 用于视频。不同版本模型的 ID、尺寸、时长和比例能力可能不同，应以供应商控制台为准。

点击“添加模型”并选择火山引擎时，标准 Ark v3 会优先读取实时模型列表；本机 Token Plan 地址不提供 `/models`，遇到 404/405 时系统会自动显示内置的 `doubao-seed-2.0-lite`、`doubao-seedream-5.0-lite` 和 `doubao-seedance-1.5-pro`。这不会修改已保存的 Base URL、API Key 或计费通道；401/403 仍会直接提示，需检查密钥和模型权限。

### 4.4 配置默认模型

进入“模型管理 → 模型”，点击“默认模型”：

- 默认文本生成模型：选择 `qwen3.8-max`。
- 默认图片生成模型：选择图片类别的 `doubao-seedream-5.0-lite`。
- 默认视频生成模型：选择 `doubao-seedance-1.5-pro`，不要选择同名的 Seedream 记录。
- 默认语音生成模型：需要 AI 配音时选择已配置的阿里百炼 TTS；只上传真人配音或音效时可以留空。
- API 超时：本机当前是 30 秒，建议调整为 300 秒，以覆盖图片和视频创建请求。
- 日志级别：日常使用选 `Info`，排障时临时改为 `Debug`。

保存后再开始剧本拆解和媒体生成。

### 4.5 测试方式与 API Key 安全

- 供应商“测试连接”和文本模型“测试生成”会发起真实的低费用文本请求。
- 图片、视频、语音模型不在模型管理页直接试生成，应分别在图片、视频和镜头音轨工作台进行单任务测试，避免误触发高费用任务。
- 测试成功只证明本次请求可用，不代表所有比例、尺寸、参考图和时长参数都受该模型支持。

- 不要把 API Key 发到聊天、邮件、截图或 GitHub。
- 使用专门为 Jellyfish 创建的 Key，不要复用主账号管理密钥。
- 在供应商平台设置消费上限、余额提醒和调用告警。
- 当前版本不会在 API 响应中回显 Key，但 Key 保存在本地 Jellyfish 数据库中。能访问本机或 Docker 数据的人仍可能获得它。
- 当前界面的退出登录仍是占位功能，Jellyfish 不能作为带完整权限管理的公网服务使用。

## 5. 中文界面和基础设置

### 5.1 切换中文

在页面右上角语言菜单选择“中文”。语言选择保存在当前浏览器中。

### 5.2 系统设置

左侧进入“系统设置”，可以设置显示昵称和角色。当前这些字段主要用于界面展示，不构成真实的用户鉴权系统。

### 5.3 主要菜单

- 项目列表：创建和管理短剧项目。
- 资产管理：管理演员/角色图、场景、道具和服装资产。
- 提示词模板：维护可复用的图片、视频和分镜提示词。
- 模型管理：配置供应商、模型和全局默认模型。
- 系统设置：界面用户信息与偏好设置。
- 任务中心：查看文本、图片、视频异步任务的状态、耗时和结果。

## 6. 标准短剧制作流程

### 6.1 第一步：准备模型

在创建正式项目之前，至少配置一个默认文本模型。需要生成画面时，再配置默认图片和视频模型。

建议先做最小测试：

1. 文本模型完成一次短文本处理。
2. 图片模型生成一张测试图。
3. 视频模型只生成一个低成本测试片段。

测试成功后再批量运行，避免因模型 ID、余额、比例或权限错误造成大量失败任务。

### 6.2 第二步：建立提示词模板

进入“提示词模板”：

1. 先使用系统内置的演员、角色、场景、道具、服装、首帧、关键帧、尾帧和视频模板完成一次单镜头测试。
2. 需要定制时，不修改系统模板；复制思路新建同类别模板，设为默认后再预览验证。
3. 把稳定的画风、镜头语言和负面约束写入模板；项目特有的人物与剧情信息留在项目/镜头层。
4. 修改默认模板只影响之后的新渲染；已提交任务继续使用其冻结的最终提示词。

建议模板至少包含：

- 画面风格和时代背景。
- 人物一致性要求。
- 镜头景别、角度、运动方式。
- 画幅比例，如竖屏短剧常用 `9:16`。
- 禁止出现的字幕、水印、畸形手指和无关人物等约束。

当前生产链的实际使用关系如下：

| 模板类别 | 实际入口 | 作用 |
| --- | --- | --- |
| 演员/角色/道具/服装 | 资产详情生成图片 | 建立可复用身份或外观基准 |
| 场景正面/补充视角 | 场景详情生成图片 | 建立并补全稳定空间 |
| 首帧/关键帧/尾帧图片 | 章节生成工作台 | 分别控制触发、峰值和收束 |
| 视频提示词 | 视频提示词预览与视频生成 | 组织动作、运镜、对白和连续性 |

`分镜提示词`、`首/中/尾帧文案`、`配乐`、`音效`、`组合提示词`等类别当前并非全部接入生产调用链。可以在实验室试验，但不要仅因列表中存在就认为正式业务已经使用。系统暂不为未接通类别灌入默认模板，后续会先完成结构化任务、预览和快照，再开放系统默认值。

系统会按实际模型做少量必要适配：阿里百炼 Wan 2.7/3.x 的项目视频会自动加入单镜头声明，并按请求顺序生成“图1/视频1”参考映射；Vidu 的命名主体使用 `@主体名`。火山引擎等其他供应商不会被强行加入阿里或 Vidu 专有语法。适配发生在任务提交时，规则名保存在任务快照中；实验室自由提示词保持原样。

### 6.3 第三步：建立资产库

进入“资产管理”，优先建立会跨镜头复用的内容：

- 角色/演员：姓名、年龄、外观、服装和参考图。
- 场景：空间结构、时间、光线、色调和参考图。
- 道具：外观、材质、颜色和使用方式。
- 服装：所属角色、款式、颜色和适用场景。
- 配音音效：对白、旁白、配乐、环境声、音效和转场音频。

先建立核心人物和主场景，能显著降低后续跨镜头的人物漂移。

资产详情支持上传照片、视频、音频及 TXT/MD/PDF/DOCX 文档。上传默认只进入全局“文件管理”，不会自动绑定当前资产；文件的数据库记录保存在 MySQL，原始二进制保存在 RustFS 的 `files/` 对象路径中。以后需要使用时，进入任一资产详情的参考素材区域，点击“从文件管理选择”，即可按名称、类型和分页找到此前未关联的文件，再选择用途并关联。确实要立即用于当前资产时，也可在上传时勾选“上传后关联到当前资产”。

已关联素材可通过“参与推荐”开关随时停用，未关联或已停用不会阻塞文字生成。解除关联只删除资产关系，不删除全局文件；只有在“文件管理”中执行删除才会删除 MySQL 文件记录和 RustFS 原始对象，而且仍被资产或音频业务引用时会拒绝删除。

### 6.4 第四步：创建项目

进入“项目列表 → 新建项目”：

- 项目名称：使用清晰、唯一的名称。
- 项目简介：写明题材、时代、核心冲突和整体画风。
- 全局种子值：当前仅作为项目元数据保存，尚未自动传入图片或视频生成。新建随机填入0～99998的整数（可能重复），修改不会改变已有作品或后续生成参数。
- 默认视频比例：竖屏通常选 `9:16`，横屏通常选 `16:9`。

### 6.5 第五步：创建章节并录入剧本

进入项目工作台：

1. 可继续“新建章节”后粘贴正文，也可点击“导入剧本”。
2. 导入支持 TXT、MD/MARKDOWN、带文字层的 PDF 和 DOCX，单文件最大 25MB；扫描版 PDF 当前需要先做 OCR。
3. 文件先进入全局文件库，再由后端执行不收费的结构解析；此时不会直接创建章节，也不会把正文发送到模型供应商。
4. 在预览中核对文档类型、解析警告、章节标题、时长、主题和规范正文。概述、完整提示词、完整配音、音效及制作备注不会当作章节。
5. 如需提取演员、角色、场景、道具、服装、镜头和声音计划，点击“开始深度分析”。系统先展示当前默认文本模型、供应商和可能费用；只有点击“同意发送并开始分析”后才会把剧本正文发送给该供应商。任务冻结所确认的模型，不会因排队期间默认模型变化而改用其他供应商；API Key、数据库密码不会随剧本发送。
   深度分析采用一次完整的结构化模型调用：任务进入“进行中”且模型控制台已有用量，表示请求已经发出；结果返回前进度是阶段值，不会逐字增长，长剧本可能需要数分钟。可从任务中心取消，取消后导入窗口会恢复“开始深度分析”；若任务失败，可点“失败原因”查看供应商或系统记录的具体错误。
6. 深度分析完成后逐项审查资产候选。每项默认“忽略”，可改为“新建资产”“关联已有资产”或“仅作细节参考”；页面同时展示原文证据、来源类型、置信度和相似资产。没有人工选择的候选不会进入正式资产表。
7. 可勾选导入镜头草稿；这一步只写入镜头和业务关联，不生成图片或视频。点击“按默认视频模型规划时长”只在本地读取模型能力，展示合法时长、画幅和参考素材能力，不调用供应商；确认导入时会再次校验，并按所选模型把不合法的时长拆成合法片段。
8. 可同时把对白/旁白写入镜头台词。字幕、BGM、环境声、音效和静音会作为镜头级声音计划保存，在“章节生成工作台 → 配音音效”中可见；选择真实音频素材后才形成可播放音轨和后期合成配置。
9. 勾选需要导入的章节后点击“确认导入”。章节、人工确认的资产、镜头、关联、对白和声音计划在同一数据库事务中创建；失败整体回滚，重复点击不会重复创建。
10. 如果不使用深度分析，仍可只导入规范章节，并选择“导入后自动启动 AI 分镜提取”。该旧流程与智能导入并存；开启后会创建文本模型任务并产生费用。

上传并完成结构解析后，内容只作为当前导入窗口的临时预览，不会自动进入“导入草稿与历史”。只有人工点击“保存并稍后继续”，系统才保存当前勾选章节、标题/主题/正文修改、资产候选决策、镜头/对白选项和视频模型规划。关闭未人工保存的预览时，系统会清理临时工作记录，但原始上传文件仍保留在全局文件库。

人工保存后，再次打开“导入剧本”，可在顶部“导入草稿与历史”中点击“继续编辑”恢复，不需要重复上传。列表支持分页和刷新；未提交的已保存草稿可删除，但原始文件仍保留。已完成导入的记录用于追溯，只能查看，不能重新修改或删除，也不会影响已生成的章节。

原文件会保留；解析预览只有人工保存后才成为可恢复草稿。章节 `raw_text` 使用清理后的业务正文。长剧本建议先检查免费结构预览，再决定是否调用 AI；PDF 无文字层、乱码或未识别章节时不要直接提交。

解析器 `1.1.0` 会把 TXT、Markdown 表格、PDF 文字层和 DOCX 中识别到的章节统一为规范业务正文：章节标题中的 `(0-12 秒)` 会拆成独立起止时间和目标时长，标题不再残留半个括号；Markdown 的表头、竖线和分隔符会删除，但 `【主题】【画面】【镜头】【时长】【配音】【字幕】【画面提示词 · 中文/英文】` 等业务标签会保留。标签用于防止后续模型把配音、字幕误认为画面描述，并为分镜拆解、信息提取和提示词渲染提供明确上下文。

导入窗口右上方提供“查看格式与内容规范”。这些内容只是提高识别率的建议，不是固定模板要求。系统会对每章的画面、镜头、时长和声音字段做质量检查；看到“结构差异较大”时，应先核对该章预览。无法归类的自由文本仍会保留，不会因未命中字段名称而静默删除；可以直接人工编辑，也可以在确认供应商和费用后使用 AI 深度分析。

解析器版本参与草稿幂等判断。旧预览若显示“解析器 1.0.0”，需关闭当前预览并重新选择原文件，系统会按 `1.1.0` 创建新预览；已经提交成章节的旧内容不会被系统擅自覆盖。

本机正式数据库已迁移到 Alembic revision `f6a8b2c4d507`。最近一次迁移前逻辑备份为 `E:\JellyfishNew\backups\jellyfish-before-f6a8b2c4d507-20260907-115705.sql.gz`，已经过 gzip 完整性检查和一次性临时库恢复验证；备份只是恢复文件，不是第二套在线数据库。此前备份继续保留，除非用户在完整验收后明确同意清理。`backups/` 已加入 Git 忽略，禁止把含业务数据的备份推送到 GitHub。

导入得到的作者视觉提示和项目创作约束以“软参考”进入镜头描述、动作拍点和现有图片/视频 PromptRenderer，不会覆盖结构化剧本事实，也不会把某一供应商的专有参数原样强加给其他模型。已确认的角色、场景、道具和服装会形成镜头级关联，后续参考素材仍遵循“用户显式选择优先、模型不支持则不发送”的规则。

### 6.6 第六步：AI 拆解分镜

在章节中执行分镜提取/拆解：

1. 发起文本处理任务。
2. 在任务中心观察状态。
3. 任务结束后检查镜头序号、标题、剧本摘录和摘要。
4. 必要时手动新建、编辑、排序或删除分镜。

正常情况下，任务中心显示“成功”后，章节分镜数应大于 0，章节准备状态也会离开“待提取分镜”。对于模型明确判定为单镜头但遗漏镜头数组的响应，系统会根据本次任务冻结的章节原文自动补成一个镜头；其他无法安全补全的空响应会显示为失败，不再出现“成功但分镜数为 0”。

不要直接接受所有 AI 结果。重点检查：

- 一个镜头中是否包含过多动作。
- 对白归属是否正确。
- 场景和时间是否发生隐式切换。
- 人物、道具、服装是否漏提取。

### 6.7 第七步：确认镜头候选信息

在分镜准备阶段：

1. 刷新或提取资产候选和对白候选。
2. 接受正确候选，忽略错误候选。
3. 优先关联已有角色、场景、道具和服装，避免重复创建。
4. 修正镜头标题、摘要、景别、动作和对白。
5. 完成必要项后将镜头推进到 ready 状态。

“准备完成”只表示镜头资料齐全，不代表图片或视频已经生成。

### 6.8 第八步：生成关键帧

进入章节生成工作台：

1. 选择一个 ready 镜头。
2. 检查关联角色、场景、道具、服装和参考图；只有明确关联并启用的图片才作为可选参考候选。
3. 选择首帧、关键帧或尾帧。
4. 编写基础提示词和镜头指令。
5. 先预览最终提示词，再确认生成。
6. 对结果标记“采用”或“废弃”。

每个镜头先生成少量候选图。确认人物和画风后再批量生成，可显著降低费用。

首帧、关键帧和尾帧现在会实际读取各自的默认图片模板，而不是只在“提示词模板”页面展示。新建同类别模板并设为默认后，重新点击提示词预览即可看到新模板生效；已经提交的任务不会被追溯修改。

### 6.9 第九步：生成视频

生成前检查：

- 默认视频模型已配置并测试成功。
- 镜头比例与模型支持能力一致。
- 已选择正确的关键帧/参考图。
- 视频提示词包含动作、运镜、节奏和时长方向。
- 供应商账户余额充足。

操作流程：

1. 打开视频提示词预览。
2. 检查生成快照中的首帧、尾帧、关键帧和显式启用的参考素材。
3. 修正提示词和画面运动描述。
4. 先对单个镜头发起测试任务。
5. 在任务中心等待完成。
6. 预览、下载并采用满意结果。
7. 单镜头流程稳定后再使用批量生成。

视频提示词按“上一镜头结束 → 当前触发 → 动作过程/峰值 → 当前结束 → 下一镜头目标”组织。关联素材是柔性参考：用于身份、外观、场景或动作一致性，不要求机械复刻；没有关联素材时仍可按文字生成。

视频生成通常是成本最高、耗时最长的环节。不要在模型或比例未经验证时直接批量提交。

### 6.10 第十步：配音、音效和字幕

如需使用已有配音或音效，先在“资产管理 → 配音音效”上传素材，再在章节工作台选中镜头，打开“音视频控制”，从素材库加入配乐、配音或音效，并设置开始时间、音量和循环。

如需 AI 配音：

1. 先在模型管理添加阿里百炼“语音生成”模型，并设为默认语音模型。
2. 在分镜编辑页确认对白；无对白的镜头不会自动臆造配音文本。
3. 进入章节生成工作台，选中镜头，在“配音音效”点击“对白转配音”。
4. 音色和表演指令可留空使用模型参数；点击“确认调用并生成”后才会产生供应商请求和可能费用。
5. 任务完成后音频自动进入 RustFS、资产管理和当前镜头音轨；页面以 8 秒间隔查询状态，任务中心保留完整终态。

字幕不要求先生成配音。项目导出默认把已确认的镜头对白按真实镜头时段生成 SRT，并作为可开关软字幕轨写入 MP4；成片工作台可关闭“包含对白字幕”。

音轨是可选后期增强：没有音轨时视频按原流程生成；有音轨时 Worker 在模型视频生成成功后使用 FFmpeg 合成新文件。原始视频不会被删除，合成失败也会回退原视频，不会让已经成功的模型生成任务变成失败。

### 6.11 第十一步：成片导出

完成镜头视频后点击“进入后期剪辑”打开成片剪辑工作台。首次按章节顺序建立草案，调整镜头排序、入出点和原声音量；按需添加配音、音乐、音效并校时字幕。点击“保存工程”后才能按该修订“合成预览 / 导出”。视频版本固定在工程内，分镜新生成不会静默替换。缺片时须明确确认。导出统一画幅、H.264/AAC、25 fps，可选择720p/1080p；MP4和可选SRT存入RustFS，只处理已有媒体，不调用付费模型。

### 6.12 第十二步：任务和素材管理

任务中心用于：

- 默认查看“未结束”任务；该范围包含等待中、进行中和取消中。“已结束”包含已完成、失败和已取消，范围筛选与单条任务状态不再混用。
- 查看运行耗时和生成结果。
- 对失败任务点击“失败原因”，按需查看持久化错误详情。
- 取消仍在运行且支持取消的任务。
- 返回对应项目、章节或镜头。

生成结果会进入文件/媒体和镜头关联体系。对于重要结果，应及时下载到独立素材目录，不要只依赖容器数据。

删除演员、角色、场景、道具或服装时，系统会先查询并逐类展示具体关联对象，例如项目、章节、分镜、角色、对白、配音音效及参考文件；关联查询失败时不会允许继续删除。确认后，后端会在同一数据库事务中先解除这些业务关联，再删除资产数据，任一步失败都会整体回滚。底层上传文件仍保留在文件管理中。若只是暂时不让素材参与生成，应关闭“参与推荐”或解除关联，不要删除全局资产。

## 7. 成本控制建议

图片实验室若出现 `image generation snapshot target is unsupported`，原因是旧 Worker 遗漏实验室结果回写，供应商可能已经完成生图并产生用量。2026-09-07 已补齐图片实验室回写；遇到历史失败记录先核查 RustFS 原图与任务时间，避免直接重复生成。恢复工具 `backend/scripts/recover_lab_image.py` 默认只校验，指定精确任务 ID、对象 key 并通过校验后才可用 `--apply` 补回记录。视频实验室的独立发布路径尚待验证，详见二开台账 PL-012。

- 文本：按输入/输出 Token 计费，长剧本应按章节处理。
- 图片：通常按张计费，先生成低数量候选。
- 视频：通常按视频 Token、时长、分辨率或任务规格计费，是主要成本项。
- 在供应商控制台设置日限额、月限额、余额提醒和异常调用告警。
- 项目测试期使用性价比较高的模型；最终关键镜头再切换高质量模型。
- 保持人物参考图和画风模板一致，减少反复重生成；项目全局种子尚未接入生成，不能据此保证一致性。
- 失败任务也可能产生部分费用，应先排查失败原因再重试。

## 8. 日志与故障排查

以下命令均在 PowerShell 中执行。

先设置 Docker 命令路径：

```powershell
$env:Path = "E:\Docker\Desktop\resources\bin;$env:Path"
Set-Location "E:\JellyfishNew"
```

### 8.1 查看全部服务状态

```powershell
docker compose `
  --env-file "E:\JellyfishNew\deploy\compose\.env" `
  -f "E:\JellyfishNew\deploy\compose\docker-compose.yml" `
  -f "E:\JellyfishNew\deploy\compose\docker-compose.secure-local.yml" `
  ps -a
```

### 8.2 查看后端日志

```powershell
docker compose `
  --env-file "E:\JellyfishNew\deploy\compose\.env" `
  -f "E:\JellyfishNew\deploy\compose\docker-compose.yml" `
  -f "E:\JellyfishNew\deploy\compose\docker-compose.secure-local.yml" `
  logs --tail 200 backend
```

### 8.3 查看任务队列日志

```powershell
docker compose `
  --env-file "E:\JellyfishNew\deploy\compose\.env" `
  -f "E:\JellyfishNew\deploy\compose\docker-compose.yml" `
  -f "E:\JellyfishNew\deploy\compose\docker-compose.secure-local.yml" `
  logs --tail 200 celery-worker
```

### 8.4 常见故障

#### 页面打不开

1. 确认 Docker Desktop 正在运行。
2. 执行 `ps -a`，确认 `front` 为 Up。
3. 访问 `http://127.0.0.1:7788`，不要使用错误端口。
4. 查看 `front` 日志。

#### API 返回 500 或任务失败

1. 查看 `backend` 和 `celery-worker` 日志。
2. 检查默认模型是否已设置。
3. 检查 Model ID、Base URL 和 API Key。
4. 检查供应商余额、权限、地域和限流。

#### Redis 端口疑问

Windows 原有 Redis 使用 `127.0.0.1:6379`。Jellyfish 专用 Redis 在容器内部仍使用 6379，但 Windows 侧映射为 `127.0.0.1:6380`，两者互不影响。

#### Docker Hub 拉取失败

本机部署已通过以下本地 Dockerfile 使用镜像前缀：

- `deploy/docker/backend.local.Dockerfile`
- `deploy/docker/front.local.Dockerfile`

仅在通过 Compose 创建/更新部署时，必须同时加载 `docker-compose.secure-local.yml` 及指定 `.env`，否则可能绕过本地镜像和安全端口配置。日常脚本启动已有容器不加载 Compose 文件或新的 `.env`，沿用容器既有设置。

## 9. GitHub Fork、本地二开与上游更新

本节适用于当前二开仓库。历史迁移基线是作者 `codex/0718`，但下次更新时必须重新检查作者真正的最新活跃分支和精确提交，不能永久假设 `main` 或 `codex/0718` 最新。不要把二开提交直接写入作者跟踪分支，不要用 ZIP 覆盖 `E:\JellyfishNew`，也不要在当前正式目录直接 merge 上游。

```text
作者最新活跃分支 + 精确 SHA
          └── E:\JellyfishCandidate-时间戳（全新候选目录）
                └── integration/upstream-时间戳（逐项智能迁移）
                      └── E:\JellyfishNew / local/stable-*（验证后的唯一正式版本）
```

约定：`upstream` 指向作者仓库；`origin` 指向自己的 Fork。

二开全量主台账为 `docs/Jellyfish-二次开发变更清单.md`，当前能力摘要为 `site/content/docs/architecture/local-customization-inventory.md`。以后每次二开修改都必须同步这两份文档；收到“检查 GitHub 最新代码并同步更新”指令后，必须按 `site/content/docs/guide/upstream-upgrade-sop.md` 执行。

### 9.1 一次性安装和登录 GitHub CLI

本机目前没有 `gh`，打开普通 PowerShell：

```powershell
winget install --id GitHub.cli --source winget
```

关闭整个终端窗口，重新打开后执行：

```powershell
gh --version
gh auth login --hostname github.com --git-protocol https --web
gh auth setup-git
gh auth status --hostname github.com
```

浏览器已登录不代表命令行已登录。不要使用 `--show-token`，不要在命令、聊天或截图中保存 Token。

### 9.2 自动 Fork、配置远程并首次推送

以下远程配置只执行一次：

```powershell
Set-Location "E:\JellyfishNew"
gh repo fork Forget-C/Jellyfish --clone=false
$GitHubUser = gh api user --jq .login
git remote rename origin upstream
git remote add origin "https://github.com/$GitHubUser/Jellyfish.git"
git remote -v
```

预期 `origin` 是自己的 Fork，`upstream` 是 `https://github.com/Forget-C/Jellyfish.git`。如果提示远程已存在，停止重复执行，先用 `git remote -v` 检查。

确认环境文件不会上传，然后推送：

```powershell
git check-ignore deploy/compose/.env
git status --short
git branch --show-current
git push -u origin local/stable-codex-0718
$ReleaseTag = "local-stable-codex-0718-$(Get-Date -Format 'yyyyMMdd-HHmm')"
git tag -a $ReleaseTag -m "Jellyfish codex/0718 本地二开稳定版"
git push origin $ReleaseTag
```

`git check-ignore` 应输出 `deploy/compose/.env`，`git status --short` 应为空。Fork 只备份代码，不包含 MySQL、RustFS 和 `.env`。

### 9.3 每次更新前检查和备份

先确认任务中心没有执行中或排队任务：

```powershell
Set-Location "E:\JellyfishNew"
git switch local/stable-codex-0718
git status --short
$env:Path = "E:\Docker\Desktop\resources\bin;$env:Path"
$ComposeFiles = @(
  "--env-file", "E:\JellyfishNew\deploy\compose\.env",
  "-f", "E:\JellyfishNew\deploy\compose\docker-compose.yml",
  "-f", "E:\JellyfishNew\deploy\compose\docker-compose.secure-local.yml"
)
```

工作区必须为空。9.3 至 9.8 建议在同一个 PowerShell 窗口连续执行；如果中途重新打开终端，应重新执行上面的 `$ComposeFiles` 定义。记录更新前数据数量：

```powershell
docker compose @ComposeFiles exec -T mysql sh -lc 'mysql -uroot -p"$MYSQL_ROOT_PASSWORD" "$MYSQL_DATABASE" -N -e "SELECT ''projects'',COUNT(*) FROM projects; SELECT ''chapters'',COUNT(*) FROM chapters; SELECT ''models'',COUNT(*) FROM models; SELECT ''providers'',COUNT(*) FROM providers; SELECT ''generation_tasks'',COUNT(*) FROM generation_tasks; SELECT ''files'',COUNT(*) FROM files;"'
```

创建备份目录并备份 MySQL：

```powershell
$Stamp = Get-Date -Format "yyyyMMdd-HHmmss"
$BackupRoot = "E:\JellyfishBackups\$Stamp"
New-Item -ItemType Directory -Path $BackupRoot -Force
git bundle create "$BackupRoot\jellyfish-current.bundle" --all
git bundle verify "$BackupRoot\jellyfish-current.bundle"
docker compose @ComposeFiles exec -T mysql sh -lc 'mysqldump -uroot -p"$MYSQL_ROOT_PASSWORD" --single-transaction --routines --triggers "$MYSQL_DATABASE" | gzip -c > /tmp/jellyfish-before-update.sql.gz'
docker compose @ComposeFiles cp mysql:/tmp/jellyfish-before-update.sql.gz "$BackupRoot\jellyfish-before-update.sql.gz"
```

备份 RustFS 和配置：

```powershell
docker run --rm `
  -v jellyfish_rustfs_data:/source:ro `
  --mount "type=bind,source=$BackupRoot,target=/backup" `
  alpine sh -lc "tar -czf /backup/rustfs-data.tar.gz -C /source ."
Copy-Item "E:\JellyfishNew\deploy\compose\.env" "$BackupRoot\compose.env.backup"
Copy-Item "E:\JellyfishNew\deploy\compose\docker-compose.secure-local.yml" "$BackupRoot\docker-compose.secure-local.yml"
Get-ChildItem $BackupRoot
```

至少应看到 Git bundle、数据库、RustFS 和两份配置备份。为大文件生成校验和，并把 SQL 恢复到临时数据库做最小还原验证；仅看到文件存在不算可恢复。备份含密码，不要上传 GitHub。

### 9.4 检查作者最新分支并创建全新候选目录

```powershell
git fetch --all --prune --tags
git for-each-ref refs/remotes/upstream --sort=-committerdate --format="%(committerdate:iso8601) %(objectname:short) %(refname:short)"
git log --oneline --decorate --graph --max-count=20 --all
```

结合分支提交时间、共同祖先、发布标签、变更日志和作者 CI 确认最新活跃分支。历史上是 `codex/0718`，但如果作者已合回 `main` 或建立更新分支，应以证据选择新基线。记录 `$UpstreamBranch` 和不可变的 `$UpstreamSha`：

```powershell
$UpstreamBranch = "codex/0718" # 示例，必须替换为本次核对结果
$UpstreamSha = git rev-parse "upstream/$UpstreamBranch"
git rev-list --left-right --count "local/stable-codex-0718...$UpstreamSha"
$Candidate = "E:\JellyfishCandidate-$Stamp"
git clone --origin upstream https://github.com/Forget-C/Jellyfish.git $Candidate
Set-Location $Candidate
git fetch upstream --prune --tags
git switch --detach $UpstreamSha
git switch -c "integration/upstream-$Stamp"
```

`E:\JellyfishNew` 此时继续运行当前稳定版。候选目录必须由作者精确 SHA 全新创建，不能复制旧目录冒充新基线。比较新版 `.env.example` 和 Compose 后只逐项迁移本机配置，不能用旧 `.env` 整文件覆盖新版。

### 9.5 建立台账并逐项智能迁移

先把 `docs/Jellyfish-二次开发变更清单.md` 的全部 `LC-*` 已生效能力和所有未关闭 `PL-*` 阶段计划复制成当次迁移决策台账。每一项必须记录上游证据、本地证据、最终决定、代码/迁移/API、测试和文档，决定分为：采用上游、保留本地、兼容合并、重新实现、淘汰；计划条目还要记录已完成阶段、未完成阶段、阻塞条件，以及升级后继续、合并、重写、暂缓或取消。

候选仓库可以把自己的 Fork 加为 `origin` 并 fetch，用于审计本地稳定分支，但不能直接把全部 commit 合并进去：

```powershell
git remote add origin https://github.com/15802964985/Jellyfish.git
git fetch origin local/stable-codex-0718
git log --reverse --oneline 508f2c7..origin/local/stable-codex-0718
git diff --name-status 508f2c7..origin/local/stable-codex-0718
```

迁移原则：

1. 先比较业务结果、数据契约和测试，再比较文件 diff；不能批量选择本地或作者版本。
2. 作者已完整且更好实现的能力直接采用作者版本，删除本地临时补丁；作者只覆盖一部分时，以其新架构为底座补齐本地缺口。
3. 本地旧实现与新架构冲突时只迁移业务意图和数据，使用新版扩展点重新实现；不能为减少改动恢复作者已废弃接口。
4. `front/openapi.json` 和 `front/src/services/generated/` 不手工拼接；先确定后端接口，再重新生成。
5. 先正确合并依赖声明，再生成锁文件；保留 E 盘和本机监听，同时吸收作者新增参数；`.env` 不参与代码合并。
6. 数据库变更统一使用 Alembic revision；作者和本地 revision 都保留，产生多头时创建 merge revision，不能覆盖已执行迁移。
7. 对安全、密钥、权限、文件删除和付费调用采用双方实现中更严格的边界。

每完成一个独立 LC 领域就补测试、更新台账并提交，避免一次提交混入全部迁移。无法判断时保留正式目录不动，在候选分支撤销该项或重新实现；不要使用 `git reset --hard`。

### 9.6 在候选目录重新生成客户端并隔离测试

先用集成分支构建独立后端测试镜像并运行完整测试：

```powershell
Set-Location $Candidate
docker build -f deploy/docker/backend.local.Dockerfile -t jellyfish-backend-update-test .
docker run --rm jellyfish-backend-update-test uv run --group dev pytest -q
```

如果后端 API、路由或 Schema 有变化，切换前先从刚构建的测试镜像直接导出 OpenAPI，避免误读仍在 8000 端口运行的旧服务：

```powershell
docker run --rm `
  --mount "type=bind,source=$Candidate\front,target=/front" `
  jellyfish-backend-update-test `
  uv run python -c "import json; from pathlib import Path; from app.main import app; Path('/front/openapi.json').write_text(json.dumps(app.openapi(), ensure_ascii=False), encoding='utf-8')"
Set-Location "$Candidate\front"
pnpm install --frozen-lockfile
pnpm run openapi:gen
pnpm run build
```

这样生成的是集成分支的新接口，而不是当前仍在运行的旧容器接口。新后端正式运行到 8000 端口后还必须执行一次 `pnpm run openapi:update`，并用 `git diff --exit-code front/openapi.json front/src/services/generated` 确认服务契约与已生成客户端一致。如果冻结安装失败，先检查 `package.json` 冲突，再运行 `pnpm install` 并核对锁文件；pnpm 要求放行依赖安装脚本时，只批准项目锁定的必要包，不要启用全局无限制脚本。

任何测试或构建失败都不要迁移当前业务数据库。静态测试通过后，把 9.3 的 SQL 备份恢复到临时数据库，并使用独立 Compose project、临时端口和隔离对象存储执行 Alembic、seed、后端/前端/Worker/Beat 健康检查及核心烟测。候选配置不得指向当前业务库和当前 RustFS 写入路径。

若重新生成产生合理修改：

```powershell
git add -A
git status
git commit -m "chore: resolve upstream integration and regenerate clients"
```

没有修改时跳过提交；提交前确认 `.env` 未出现。

### 9.7 隔离验证通过后的正式切换

只有完整自动测试、临时数据库迁移和候选烟测均通过，才安排停机窗口。先确认任务中心没有运行任务，再做最终增量备份；停止应用服务时不得删除卷：

```powershell
Set-Location "E:\JellyfishNew"
docker compose @ComposeFiles stop front backend celery-worker celery-beat
Set-Location "E:\"
$Previous = "E:\JellyfishPrevious-$Stamp"
Move-Item -LiteralPath "E:\JellyfishNew" -Destination $Previous
Move-Item -LiteralPath $Candidate -Destination "E:\JellyfishNew"
```

切换前必须确认 `$Previous`、`$Candidate` 和目标目录都是预期的绝对路径。以下是完整上游升级的示例，不是日常启动或普通配置修改命令：只有已备份、隔离验证并确认需要迁移/seed及相应服务重建时才采用。无数据库变化的应用更新应定向替换应用，不能照抄全栈 up。现有 RustFS 历史标签与业务卷必须先核对，不得因配置差异意外重建数据服务。新目录就位后重新定义 `$ComposeFiles`，按审核后的候选迁移和 seed 方案构建启动：

```powershell
Set-Location "E:\JellyfishNew"
$ComposeFiles = @(
  "--env-file", "E:\JellyfishNew\deploy\compose\.env",
  "-f", "E:\JellyfishNew\deploy\compose\docker-compose.yml",
  "-f", "E:\JellyfishNew\deploy\compose\docker-compose.secure-local.yml"
)
docker compose @ComposeFiles up -d --build
docker compose @ComposeFiles ps -a
docker compose @ComposeFiles logs --tail 200 backend-migrate backend-seed-system-data backend celery-worker front
Invoke-WebRequest -UseBasicParsing http://127.0.0.1:8000/health
Invoke-WebRequest -UseBasicParsing http://127.0.0.1:8000/openapi.json
Invoke-WebRequest -UseBasicParsing http://127.0.0.1:7788/
```

必须满足：

- `backend-migrate` 和 `backend-seed-system-data` 均为 `Exited (0)`。
- MySQL、Redis 为 `healthy`。
- 后端、Worker、Beat、前端均为 `Up`。
- 三个 HTTP 请求均返回 `200`。
- 日志无迁移失败、表不存在、Traceback 或前端启动失败。
- `generation_dispatch_outbox` 等统一生成表的 `created_at`、`updated_at` 默认值为 `CURRENT_TIMESTAMP`；否则章节智能操作会在创建任务时回滚，任务中心也不会产生记录。
- `celery-beat` 日志应周期出现 `task.dispatch_generation_outbox` 调度；新任务通常在下一个10秒周期内获得 `executor_task_id`。若任务长期为 `pending` 且 outbox 的 `dispatched_at` 为空，应先检查 Beat，而不是修改模型密钥。

重新执行 9.3 的数量 SQL，确认项目、章节、模型、供应商、任务和文件没有异常减少。浏览器按 `Ctrl + F5`，人工验证：

1. 项目、章节、分页及增删改刷新。
2. 模型默认设置、连接测试和文本调用。
3. 任务中心全部状态、类型筛选、分页、收起和展开。
4. 剧本上传、本地结构预览、章节勾选和事务提交；确认概述/汇总/音效/备注没有误建成章节。
5. 资产附件上传、停用、排序和解除关联。
6. 配音音效上传、试听及镜头音轨。
7. 图片生成使用或全部取消推荐图。
8. 先用单个低成本镜头验证视频和音轨合成，再批量生成。

不要执行带 `-v` 的 `docker compose down`。

### 9.8 验证通过后固化、更新文档并推送

```powershell
Set-Location "E:\JellyfishNew"
git status --short
$StableBranch = "local/stable-$(Get-Date -Format 'yyyyMMdd')"
git branch -M $StableBranch
git push -u origin $StableBranch
$ReleaseTag = "local-stable-$(Get-Date -Format 'yyyyMMdd-HHmm')"
git tag -a $ReleaseTag -m "Jellyfish 上游更新智能迁移验证版"
git push origin $ReleaseTag
```

推送前必须把当次迁移决定同步到：

1. `docs/Jellyfish-二次开发变更清单.md`：更新基线 SHA、HEAD、统计、提交映射和全部 LC 条目。
2. `site/content/docs/architecture/local-customization-inventory.md`：更新当前真实能力。
3. `site/content/docs/guide/upstream-upgrade-sop.md`：流程有改进时同步修订。
4. 本手册：同步用户可见的功能、配置、部署和验证方法。
5. 所有受影响的 `site/content/docs/plans/`：同步阶段状态，并从仍未完成的 `PL-*` 确定下一步系统优化顺序。

确认 Fork 能看到稳定分支和标签后，升级代码才算完成。

旧代码、候选失败目录和 9.3 备份不会自动删除。先观察新版本并列出拟删除的精确绝对路径；只有用户人工确认后才逐项删除。不得使用模糊通配符，不得删除 Docker 业务卷。清理完成后复核只保留 `E:\JellyfishNew` 这一份正式代码和当前业务数据。若尚未获准清理，应明确记录“升级已完成，清理待人工确认”。

### 9.9 更新失败和回退

尚未切换时直接保留 `E:\JellyfishNew`，在候选目录修复或放弃候选即可。已完成目录切换但尚未迁移正式数据库时，可以停服并把 `$Previous` 恢复为 `E:\JellyfishNew`。已经迁移数据库时，不要自行恢复 SQL、删表或删卷；保留日志和备份，再判断只回退代码还是停服恢复 MySQL、RustFS。恢复数据库会覆盖现有数据，必须再次人工确认。

### 9.10 官方参考

- GitHub CLI Windows 安装：<https://github.com/cli/cli/blob/trunk/docs/install_windows.md>
- GitHub CLI 登录：<https://cli.github.com/manual/gh_auth_login>
- 自动 Fork：<https://cli.github.com/manual/gh_repo_fork>
- 同步 Fork：<https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/working-with-forks/syncing-a-fork>
- 合并冲突：<https://docs.github.com/en/get-started/using-git/resolving-merge-conflicts-after-a-git-merge>

任何情况下都不要把 API Key、数据库密码、`.env`、SQL 备份或 RustFS 备份推送到 GitHub。

## 10. 备份与恢复原则

需要备份三类内容：

1. 源码和本地部署覆盖文件：`E:\JellyfishNew`。
2. 数据库：项目、章节、分镜、模型和任务记录。
3. RustFS 对象：上传图片、参考图、生成图片和视频。

Docker 数据位于 `E:\Docker\DockerData` 的 WSL 虚拟磁盘中，但不建议只依赖复制正在运行的虚拟磁盘作为业务备份。

推荐策略：

- 每周进行一次数据库逻辑备份。
- 重要视频和图片生成后立即另存到普通素材目录。
- 更新代码或修改数据库结构前做一次完整备份。
- 恢复操作、删除卷或替换 WSL 虚拟磁盘前必须人工确认。

## 11. 当前版本限制

- Jellyfish 不自带免费模型，需要第三方 API。
- 当前没有完整的登录、权限和多用户隔离机制。
- 不应直接暴露到公网或不可信局域网。
- 时间线支持保存工程、数字入出点裁剪、排序、多音轨定位/音量和字幕校时，并实际导出MP4/SRT；复杂转场、波形拖拽、叠画轨和自动对口型仍待增强。
- 模型供应商可能调整 Model ID、价格和 API 参数，应以供应商官方控制台为准。
- API Key 虽然不会从读取接口回显，但当前保存在本地数据库中，应保护 Docker 数据和 Windows 账户。

## 12. 安全操作清单

以下操作必须先确认目标和备份，再执行：

- 删除项目、章节、分镜或素材。
- 删除模型供应商或更换 API Key。
- 执行带 `-v` 的 Docker Compose 删除命令。
- 删除 `E:\Docker\DockerData` 或 WSL 虚拟磁盘。
- 修改 Windows 防火墙并将 Jellyfish 开放到局域网/公网。
- 使用数据库 root 账户连接外部数据库。
- 覆盖 `.env`、安全覆盖 Compose 文件或本地 Dockerfile。

## 13. 快速检查清单

开始制作前：

- [ ] Docker Desktop 正常运行。
- [ ] Jellyfish 所有长期服务为 Up。
- [ ] MySQL 和 Redis 为 healthy。
- [ ] 文本、图片、视频默认模型已按需要设置；需要 AI 配音时已设置默认语音模型。
- [ ] API Key 有效且余额充足。
- [ ] 项目画幅比例正确。
- [ ] 核心角色和场景参考资产已建立。

批量生成前：

- [ ] 单个文本任务测试成功。
- [ ] 单张图片测试成功。
- [ ] 单个视频测试成功。
- [ ] Model ID 和比例与供应商能力一致。
- [ ] 已设置供应商消费告警。

更新或维护前：

- [ ] 没有运行中的生成任务。
- [ ] 重要素材已另存。
- [ ] 数据库和对象存储已备份。
- [ ] 已检查 Git 状态。
- [ ] 不会删除 Docker volumes。

---

项目主页：<https://github.com/Forget-C/Jellyfish>
本地前端：<http://127.0.0.1:7788>
本地 API 文档：<http://127.0.0.1:8000/docs>
资产管理除演员、角色、场景、道具和服装外，还提供“配音音效”页。音频可按角色配音、旁白、背景音乐、环境声、音效和转场音分类，并支持试听、搜索、分页、编辑和删除。

进入任一演员、角色、场景、道具或服装的详情页，可在“参考素材（照片 / 视频 / 文档 / 音频）”区域上传：

- 图片：JPG、PNG、WebP、GIF。
- 视频：MP4、MOV、MKV、AVI、WebM。
- 音频：MP3、WAV、M4A、AAC、OGG、FLAC。
- 文档：TXT、Markdown、PDF、DOCX。

上传可以选择“仅上传”或“上传并关联”。仅上传的文件统一进入左侧“文件管理”；下次进入资产详情，点击“从文件管理选择”，可搜索、按类型筛选、分页预览并重新关联，不需要重复上传。关联时选择素材用途并填写参考说明，例如“只参考动作，不参考人物脸部”。可以指定主参考图、调整顺序，或关闭“参与推荐”。解除关联不会删除底层文件；底层文件仍可在文件管理中查看。

图片缩略图采用完整适配而不是裁切，可通过“预览”查看大图。音频提供网页试听。TXT、Markdown、PDF 和 DOCX 提供只读预览，其中 DOCX 只提取正文；复杂排版仍以下载原件为准。视频预览支持 Range 分段加载；若上传的是 HEVC、MOV、MKV、AVI 等浏览器兼容性较差的格式，首次预览会按需生成 H.264/AAC MP4 缓存副本，下载仍是原始文件。

关联不是硬性要求。没有参考素材时仍按文字生成；有关联时按模型真实能力智能使用：资产图片生成读取启用的参考图片，并把素材说明/文档正文作为软提示词；分镜视频生成会从镜头关联的角色、演员、服装、场景和道具中读取启用素材，只有供应商模型支持对应图片、视频或参考音频时才传给 API。显式手工选择优先于自动推荐，不支持的素材不会强行发送。


### 提交窗口停在冒号 / 换行符警告

LF will be replaced by CRLF 是换行符提示，本身不是提交失败。旧脚本若停在底部冒号，按英文 Q 退出 Git 分页器；再次出现时再按 Q，随后按确认提示输入 YES。不要同时重复启动提交脚本。2026-09-08 已对脚本内所有 Git 调用加入 --no-pager，后续运行不会自动进入分页器，不改用户全局配置。出现“推送完成”及精确 HEAD 才代表脚本成功；若出现其他错误，应按错误诊断。12 项隔离模拟回归通过，未实际代为推送。


2026-09-08 提交收尾：清理35个文件末尾多余空行以通过 Git 暂存差异检查，业务契约不变；前端类型与相关 Python 语法检查通过。本轮保存累计二开，既有部署/真实验收边界及全部未关闭 PL 不变，无服务重启或数据迁移。提交 SHA 见主台账后续追溯记录。


## 2026-09-08 视频编辑素材选择预览（代码已更新，待部署）

“文字编辑视频”的“从素材库选择原视频/参考图”改为画面卡片。图片点击可放大；视频可以播放，或点“放大播放”使用兼容预览。核对后再点击“选择此视频/图片”，播放与放大不会直接选中。卡片保留文件名、已有尺寸/时长/大小及文件ID，便于区分同名素材；支持搜索、分页、失败重试及已选择标记。当前仅代码更新，需构建替换前端后线上生效，真实浏览器交互待验收；选择和本地预览不发起付费模型生成。


## 2026-09-08 19:11 素材可视选择前端部署（LC-006 / LC-015，PL-013）

用户授权部署后，仅构建并替换 front；本条覆盖上一代码批“待部署”状态。Docker 内 tsc 与 vite build 通过（仅既有大 bundle 提示）。首页与新静态资源 /assets/index-BQanViR2.js 均 HTTP 200，资源包含选择此视频/图片、放大播放和失败重试文案；后端 health HTTP 200。镜像 sha256:e26491dd25630538573ca160dccee2fa10578e67301527f11b496d2f6c38c002，前端启动时间 2026-09-08 19:11:29（北京时间）。切换后首次请求发生短暂连接中断，有限重试后正常。

backend/worker/beat/MySQL/Redis/RustFS 容器ID保持不变，未执行迁移、seed、模型调用或凭据配置变更。旧前端镜像保留为 jellyfish-front:rollback-media-picker-20260908-190958；原备份、数据卷和历史初始化容器均未删除。Compose 提示的历史孤立初始化容器不在本次清理范围。

用户 Ctrl+F5 后打开“文字编辑视频 → 从素材库选择原视频/参考图”，可查看卡片、放大/播放并明确选择。部署健康与资源检查已通过；浏览器工具启动失败，实际点击/播放与 PL-013 真实付费编辑验收仍待完成，不因前端部署而关闭。代码及部署文档为工作树待提交批次，本轮未自动提交或推送。


## 全局文件管理与删除检查（2026-09-08）

左侧“文件管理”集中查看数据库中的上传和生成文件，包括未关联素材。原资产详情“从文件管理选择”仍用于关联当前资产，两处共用同一个文件库。可按名称/类型筛选、分页、预览、下载、修改名称和标签。

每个文件可“查看关联”。点击“删除”时先查询关联，展示资产/项目/分镜名称及ID、任务、历史或时间线等使用信息；有关联时不能删除，须先在对应业务中处理。历史生成记录、任务冻结参考和剧本导入证据也是引用，不会自动清理。查询失败时只可重试，不能继续删除。

无关联时才可点“确认永久删除”，同时删除数据库记录与存储原文件；删除时后端再检查，避免按旧结果误删。原对象存储失败保留事务记录并提示重试，误删后的恢复依赖备份。部署后刷新页面可见新菜单；本轮不自动删除任何现有文件。


## 2026-09-08 19:32 文件管理部署验收（LC-005 / LC-006 / LC-012 / LC-014）

按本轮授权已构建并替换 backend/front，前后端健康及 /files 页面HTTP200，新资源 index-Ct62KRL7.js 包含全局文件管理、查看关联及删除前检查。使用正式MySQL对全部28个文件调用只读delete-impact：28个请求均成功，21个有引用受保护、7个当前未发现引用；部署前后文件总数均28，无真实删除请求。

新镜像：backend sha256:fced191cd92aec556b070b39b15710273a46b9f593d958f3ad11fcea7e046feb；front sha256:aaef3c8c36661b3d862817481549c13d105f9c112a4d034080b0eb038c1991d8。回滚保留 jellyfish-backend:rollback-file-management-20260908-193010 与 jellyfish-front:rollback-file-management-20260908-193010。Worker/Beat/MySQL/Redis/RustFS容器ID和启动时间未变，不重启无关服务。无迁移、seed、凭据调整、付费调用、备份清理或历史容器清理。

43项后端回归、前端类型和本地/镜像生产构建通过；OpenAPI及generated客户端已同步，一次性schema服务已退出。浏览器工具此前启动失败，未把HTTP/静态资源核验写成实机点击验收。用户Ctrl+F5后从左侧文件管理进入；仍保留原资产素材选择快捷入口。工作树待提交，未自动推送；此前素材可视选择改动已包含在本次前端构建中，原PL待办不关闭。


## 2026-09-09 文件管理固定分页布局（LC-005 / LC-014）

工作树待提交批次：FileManager 复用 app-workspace/app-fill-card 高度约束，顶部提示与筛选、底部分页保持在页面可视区域，中间文件列表独立滚动。分页移出列表 Card，加载中及空列表也保留底栏，窄屏分页允许换行。未改变接口、关联保护和文件操作语义；无需重新生成 OpenAPI。既有未关闭 PL 保持不变。部署及验证结果见本批续记。

验证与部署续记：pnpm exec tsc --noEmit、Docker 内 tsc + vite build、git diff --check 通过（构建仅既有大包提示）。2026-09-09 09:45:06（北京时间）仅替换 front，镜像 sha256:88fce31afc952ab30f046b9d132f54e057b81fa4be42e5b99571cec827e8eeca；/files 与 /assets/index-DbPGKaaq.js HTTP 200，新资源包含文件列表分页标记。回滚镜像 jellyfish-front:rollback-file-pagination-20260909-094202 保留。其他服务容器ID未变。浏览器工具此前无法启动，尚未取得实际视口截图验收。用户 Ctrl+F5 刷新后，可直接使用页面底栏翻页、修改每页数量，仅中间卡片列表滚动。文档与改动仍为工作树待提交批次，无自动提交或推送。

## 2026-09-09：参考图、生成规格、调用详情与官网同步

1. 在分镜工作室打开首帧/关键帧/尾帧的生成预览，勾选要使用的参考图，按左右按钮调整图N顺序；可从文件库补选。点击保存仅保存当前帧，不影响其他帧；清空后保存表示不用参考图。恢复推荐后可继续选择，确认保存。单张和批量使用已保存的帧设置。
2. 图片/视频生成前出现“生成规格与预计费用”。按窗口显示的实际厂商和型号选档位；未指定模型时读取当前默认模型，换默认模型后下次生成重新读取。没有配置默认档时优先合法低成本档，可勾选“将此规格设为该模型默认”。批量窗口的费用按单项输入估算，不是全批总价。
3. 费用有官方依据才显示金额与链接、核对日期；未知价格不是免费。自定义网关或套餐计费不套用官方按量单价。未核验规格不提供猜测选项，也不保证全部型号已有多档选择。若模型配置版本已变化，请返回重新打开生成窗口复核。
4. 任务中心可按厂商/型号搜索历史，点击“调用详情”查看冻结配置、提示词、实际 HTTP 方法/地址/参数、请求ID和错误，并下载脱敏 JSON 给厂商反馈。历史任务不会补造调用参数。sending/transport_error 不表示远端未受理，先核对厂商状态，避免重复计费。
5. 模型管理 → “接口与价格同步”可查看支持/配置型号范围、官方来源状态、差异和当前规则，设置1–168小时周期或手动检查。默认24小时，服务启动异步检查过期数据。读取失败保留旧依据；“发现变化”不表示已完成所有接口适配。回退规则会固定旧版本，之后可点击恢复自动应用。

界面根据实际模型动态读取参数，供应商适配器负责官方字段映射。当前自动应用限已验证价格与兼容参数规则；新鉴权或请求结构等变化需开发适配，不自动切换模型、套餐端点或发起收费重试。部署是否生效请查看 PROJECT_MEMORY.md 最后部署条目。

本节功能已于2026-09-09 11:37部署。刷新页面（必要时 Ctrl+F5）后使用；默认模型切换后，在下次生成规格窗口核对显示的厂商/型号和该模型的档位。当前套餐计费端点的金额显示未知，不代表免费；历史任务没有实际请求记录时显示说明，新任务才开始记录。未核实的模型额外规格及不可读官网仍会提示待核对。

### 2026-09-09 复盘发现的使用限制

工作室旧侧栏“标准2K/高清3K”仍是通用固定标签；请以最终生成复核窗口的实际像素/型号为准。该页初始standard会优先于保存的模型默认档，保存高清默认后下一次也应在复核窗口确认，不代表默认值消费已完全修复。当前Seedance1.5Pro尚无额外分辨率可选项。型号搜索会进入全部任务范围，切回当前/未结束后不会继续按型号过滤；搜索范围交互待统一。以上已记入PL-015，本次复盘未改动业务实现。

## 2026-09-09 生成规格与模型编辑更新

- 工作室点击图片/视频生成后，在统一复核窗口选择规格。窗口每次读取实际选定或当前默认模型；勾选“将此规格设为该模型默认”可保存分辨率，下次优先使用。旧侧栏不再固定显示2K/3K。
- 万相2.7Pro可选1K/2K/4K；使用参考图时最高2K。Seedream5.0Lite可选2K/3K/4K。Seedance1.5Pro可选480p/720p/1080p，并可勾选生成原生音频；未配置时关闭原生音频。其模型参数 generation_defaults.generate_audio 可设为 true/false。切换型号后按该型号选项重新复核。
- 套餐扣费依据不足时仍显示“暂无法准确估算”；另列的“按量原价参考”仅用于北京区标准价格对比，不是套餐账单。可点击官方依据，费用随数量/时长和档位计算，不逐次在线询价。
- 编辑模型时，改供应商或类别会清空旧名称；请从新目录选择或输入准确自定义名称，再保存。关闭窗口不会保存测试值。
- 任务中心搜索厂商/型号后，切换当前/未结束等范围也保留搜索条件。调用详情的新HTTP记录含headers/body信封、安全版本头、表单字段与上传文件摘要；未捕获的流式内容明确标记，历史记录不补造。
- “模型管理 → 接口与价格同步”可查看“已知协议范围内兼容”或“结构未匹配，需适配”。火山动态官网正文已能读取；同步不会切换模型/套餐地址，也不自动付费试运行。完整范围与边界见PL-015。

只读页面回归：安装外置Playwright并设置 PLAYWRIGHT_MODULE 为其 index.mjs 的绝对路径，使用本机Edge运行 node scripts/verify-model-edit.mjs；默认访问7788，可通过JELLYFISH_TEST_URL指定隔离前端。脚本拦截非GET/OPTIONS请求，并取消编辑，不保存配置。

部署续记：本节功能已于2026-09-09 14:16部署，Ctrl+F5刷新使用。模型编辑实际浏览器回归通过；收费生成未执行，套餐报价未知不代表免费。

## 2026-09-09 生成规格、整批费用与诊断补充

1. 生成复核窗口按当前模型提供规格。支持原生音频的型号可勾选音频，并用“将此规格及音频设置设为该模型默认”一起保存；其他模型仅保存规格。默认值版本变化时须重新确认，不能继续用旧参数提交。
2. 批量关键帧/视频先准备请求，分别确认各规格组，再显示“确认整批生成”。最后点击“提交整批任务”才开始创建生成任务；取消整批不生成，但前面主动保存的模型默认值仍保留。提交期间如网络/服务失败，已成功提交的任务不会自动撤回，应先查看任务中心再决定是否补交。
3. “预计人民币费用”“官方按量原价参考”“Agent Plan AFP套餐参考”是不同口径，不能相加。未知项单列，不代表免费。Seedance估算使用官网尺寸、24fps及音频选项；实际token/帧数以响应usage为准。Seedream5.0Lite官网套餐参考99AFP/张。账户套餐资格、额度、优惠与实际扣费以官网控制台为准；Ali Token Plan Credits没有公开可靠单次换算时显示未知。
4. 火山官网AFP页将Seedance1.5Pro标记为即将下线，系统展示提示，不自动切模型或计费端点。若需迁移，先核对新型号接口并主动配置。
5. 模型管理的官方同步页“差异与规则”增加candidate_validation，显示字段前后值和比例/参考模式的离线规格检查。它不代表真实调用通过；不兼容时保留旧规则，待部署适配代码。
6. 任务中心按厂商/型号搜索，切换未结束等范围仍应用过滤；调用详情支持失败后刷新和导出脱敏JSON，历史未记录的数据不会补造。

维护者离线浏览器验收：启动front的Vite（关闭自动开窗），设置PLAYWRIGHT_MODULE为已有Playwright的index.mjs绝对路径，执行node scripts/verify-generation-governance.mjs，访问127.0.0.1:5174/governance-acceptance.html。该开发fixture不属于生产路由，测试拦截全部API，不会保存正式配置或调用收费模型。

部署续记：2026-09-09 15:23已完成本批部署（前端15:15:55），健康200/worker pong；全量619项及最终14项定向、类型/构建、隔离浏览器回归通过。另修复仅价格来源恢复自动应用被契约校验误拦截的边界。刷新页面Ctrl+F5可使用；完整镜像、25源同步和未验收边界见主台账15:23快照。数据库版本与业务数据不变，无付费/提交/推送。


## 2026-09-09 生成参数与费用前置展示（LC-007/010/011/013/014，PL-015）

工作树待提交批次。修正此前仅在最终提交复核弹窗显示参数/费用、生成面板不可直接发现的问题。新增共享GenerationOptionsPanel：工作室“关键帧与参考图”“视频生成”、实验室图片/视频输入区、资产图片生成提示词弹窗直接显示当前默认或选定型号、可选分辨率、支持时的音频开关、人民币/AFP费用参考。字段随型号/比例/参考数量刷新，费用随数量/秒数/音频选项变化；失败可刷新，切回窗口可核对最新默认模型。未知实际套餐扣费仍显示未知，按量/AFP参考另列。

页面选择与最终复核通过模型revision绑定传递，模型版本变化不沿用旧选择，最后仍重新核对合法规格；这不修改模型全局默认，主动保存默认仍在最终复核窗口。工作室公共图片面板按关键帧参考数量预览，首/尾帧使用不同参考数量时由最终复核按实际输入更新合法档位；不代表三帧输入完全一致。删除隐藏的模型A/B、ControlNet、固定时长示例参数。

使用位置：展开工作室右侧“属性/分镜生成面板”，切换“关键帧与参考图”或“视频生成”，在“图片参数与费用/视频参数与费用”区域选择。实验室在输入区查看；资产图片在生成提示词窗口查看。金额未知时仍可看下方有依据的原价/AFP参考，不代表免费。

前端tsc通过；实际工作室页面连接正式只读API的浏览器回归通过，动态读取现场默认Seedream/视频型号，可切换分辨率，所有写请求受拦截（本次0写尝试）。隔离fixture另验证面板720p选择进入最终复核结果，原逐帧/任务/默认/整批回归通过。仅前端变更，无API/schema修改，不需重新生成OpenAPI或跑后端测试；历史619+14项属于上批证据。已审查本批语义差异，保留既有未提交改动及全部未关闭PL；生产部署结果后续登记，无付费生成或正式配置试写。


部署续记（2026-09-09 16:31）：参数前置展示已部署，仅重建front，启动16:31:22北京时间；镜像sha256:4091c0e2216b4204200da4be3a759ac65e5391b4ca674a3743b2ecf22459a47d，资源index-DibNv7Oi.js。保留rollback-visible-parameters-20260909；backend/worker/beat及数据服务保持原运行版本，无迁移。Docker内tsc/vite build通过；正式7788工作室浏览器再次验证图片/视频参数区域可见、动态读取实际默认型号、可切换分辨率、费用参考显示，0写请求。资源及后端健康200，开发5174已停止。无付费或提交推送。验证脚本scripts/verify-visible-generation-options.mjs使用PLAYWRIGHT_MODULE、JELLYFISH_TEST_URL与JELLYFISH_STUDIO_PATH环境变量，不把固定业务ID写入脚本。PL-015原有未完成范围保留。

## 文字编辑视频没有可选模型（2026-09-09 核查）

视频生成模型与视频编辑模型不是同一能力。目前项目编辑入口仅接通 fal Kling O3 Pro edit、Runway aleph2；仅配置 Seedance1.5Pro 或 HappyHorse1.1I2V 时，编辑列表为空，刷新无法解决。不要把现有模型名称直接改成其他编辑型号，也不要为此自动切换套餐端点。

官网另有 Wan2.7-videoedit、可灵原生Omni、ViduQ2Pro等编辑能力，但项目仍需适配。完整核查和待办见 [视频编辑官方核查](../site/content/docs/plans/video-edit-official-api-audit.md)。带原视频不必然便宜；Wan北京原价按输入加输出总秒数计费，5秒输入加5秒输出约6元（720P）或10元（1080P），以地域、账户和实际账单为准。此次为核查说明，未部署新的编辑功能。

## 分镜准备：补建场景与确认资产（2026-09-09）

在镜头详情的资产候选确认区，场景/角色/道具/服装均可点击“新建”；没有提取结果时入口仍显示。填写名称与描述，点击“保存并关联当前镜头”，无需返回资产管理、无需重新提取。新建不收费生成图片；随后可在资产详情补图。若资产已创建但关联失败，点击“继续关联当前镜头”，不要重复创建。

候选卡片显示匹配到的已有资产照片，“匹配资产”不等于已关联。点击“选择 / 新建”，可搜索、看图后选择已有资产，即使名称不同也能确认；也可以切换新建。角色只列当前项目；无照片资产仍能关联，系统不会借用其他角色照片。长名称、状态不再压成省略号，提取描述可展开。修改图片后回到准备窗口会刷新。资产管理新标签页的 /assets 路由现已修复端口丢失问题。

## 文字编辑视频：型号、参数与费用（2026-09-09）

打开工作室“文字编辑视频”，先刷新编辑型号。列表明确显示未接通、无凭据、禁用或不兼容端点原因；可查看已支持精确型号后去模型管理配置，不自动替换普通生成默认模型。选定型号后显示其分辨率及可指定时长；无保存默认值先选低档。选择原片、调整参数后免费读取本地输入并更新估价；提交前再次核对型号版本和费用。

参考图可上移/下移，点击标记插入提示词；排序后核对编号。Wan北京按原片加输出秒数估算，Runway以Credits显示；其他未核实价格显示未知，不代表免费或更便宜。最终费用以账单为准。保留音轨的本地合成要求同长，不能满足时预检提示。

Wan/原生可灵/Seedance2.5等仅收URL的接口，需要可公网读取的HTTPS对象存储；管理员配置 S3_PUBLIC_BASE_URL 为 `https://公网S3域名/桶名`，系统只生成所选文件2小时签名，不公开桶。现有localhost地址不能直接用于这些接口。Vidu/H3等仍按各自直传与大小限制检查。未配置编辑模型时列表仍无可用项，但会显示原因；不能据此认为已有普通生成模型已支持编辑。当前新增协议为文档与离线验证，未做真实收费生成验收。

现场核对补充：截图候选“清晨小区林荫道”与已有“小区林荫道”存在前缀差异，名称检测增加双向包含建议、精确优先；只展示匹配照片，不自动关联。完全不同名称仍需用户选择，不能根据照片猜测角色身份。


## 2026-09-10 备份与磁盘空间整理

当前保留两套Docker回滚标签：rollback-preparation-edit-20260909和rollback-generation-governance-20260909，另有当前运行镜像；其他本轮核实无引用的26个旧回滚镜像已清理。历史说明中的旧标签不再保证存在。

可再生构建缓存可以按需清理，下一次构建可能变慢；正式停止的容器仍属于业务服务，不能把“未运行”当作“无用”。不要使用包含数据卷的全局清理指令。数据库、素材及必要迁移备份保留，测试数据库副本需要按项目清理规则单独确认。Docker内部释放空间不等于.vhdx立即变小，不能直接删除虚拟磁盘。

最终清理结果：构建缓存分两轮回收148.2MB+879.3MB（合计约1.03GB）；26个旧回滚镜像已删除。另核查jellyfish_front_node_modules零容器引用、只读检查含.pnpm/react/typescript后删除此可再生依赖缓存卷，回收218.8MB。缓存合计约1.25GB；没有删除数据库/素材卷，测试MySQL卷227.7MB继续保留。Docker报告剩余构建缓存为共享/不可回收引用，RECLAIMABLE=0B；不为追求缓存显示为零删除正常镜像。VHDX逻辑文件仍约10.79GiB，未进行离线压缩，不能将Docker内部回收量称为Windows立即增加的可用空间。


## 2026-09-10 分镜面板与完整文字查看

工作室右侧“分镜生成面板”的六个标签现在顶部两行展示，滚动只滚内容区；候选名称过长会换行，“项目内可关联”等状态完整显示。图片、视频参数与费用继续在相应标签中查看。

页面、卡片、表格及素材选择弹窗中，CSS省略号截断的名称/说明可将鼠标停留片刻，通过浏览器原生提示查看全文；原有专用提示继续保留。输入框不自动读取，密码等敏感输入不加入提示。若仍看到旧布局，用Ctrl+F5强制刷新。2026-09-10 09:38已部署前端，其他服务和数据不变。


## 中文 Docker 镜像清单

双击项目根目录的 `查看Docker镜像清单.cmd`。先确保Docker Desktop引擎已启动；脚本自动查询当前Docker context并用默认浏览器打开 `local-reports/docker-images.html`。当前目录入口为 `E:\JellyfishNew\查看Docker镜像清单.cmd`，换目录后使用新根目录同名文件。

清单提供用途说明、引用状态、引用容器、保留原因和镜像大小，可以搜索和筛选。相同镜像ID的多个标签合并显示；Unused不等于无用，回滚镜像和构建基础环境可能没有容器引用。保留原因是当前已知用途建议，未知项需核对，不执行删除。

这是本机快照。需要更新时重新双击CMD；只刷新浏览器不会查询Docker。每次覆盖同一文件，不积累历史报表。Docker不可用时脚本报错并保留旧快照，请留意页面采集时间。命令行仅生成而不打开：`powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/windows/Show-DockerImages.ps1 -NoOpen`。无需启动Jellyfish业务服务。


## 2026-09-10 网页镜像清单入口及脚本修复

网页入口：左侧“系统设置”→右上角“Docker 镜像清单”。本机可直接访问 http://127.0.0.1:7788/settings/docker-images 。支持用途/保留原因查询、引用状态过滤与分页。

数据暂采用手动采集：需要最新状态时运行根目录“查看Docker镜像清单.cmd”，随后点击网页“刷新清单”。网页显示采集时间；刷新不会直接运行Docker命令，当前尚未安装定时采集。Docker不可用时保留上次数据，不代表最新状态。独立HTML入口仍可使用。

双击CMD的Windows中文编码报错已修复，脚本强制按UTF-8接收Docker输出；重复覆盖清单也兼容Windows PowerShell 5.1。无需调整系统编码。Web清单依赖secure-local覆盖的只读报告挂载；其他部署未配置时会提示尚未取得清单，不会启动Docker采集。


## 2026-09-10 设置镜头情绪与处理视频缺失项

工作室→分镜生成面板→生成参数→情绪标签：下拉多选建议词，也可输入自定义内容后按回车添加；点×取消、清空全部取消。建议词不代表已经选中，实际以输入框内标签为准。修改自动即时保存，失败恢复原选择并提示，标签仅作用于当前镜头。

保存的标签会进入之后渲染的帧图提示词上下文和视频情绪说明；清空后不再自动追加。已存在的手写/旧提示词仍保留，若其中已经写了旧情绪，需要自行检查；正在执行的任务不会被修改。

视频生成→视频准备度：顶部直接列出未通过条件及处理入口。缺提取确认回分镜准备，缺时长去生成参数，缺首/关键/尾帧去关键帧与参考图；模型不兼容需检查当前参考模式和型号配置。已通过条件可展开查看，情绪标签本身不是强制生成条件。


## 2026-09-10 编辑帧图提示词与选择版本

基础提示词可连续编辑，停止输入约700ms后更新最终提示词；中文输入法组词期间等待，不再因后台刷新禁用输入框。旧预览在同步过程中保留，以“已同步”状态为准，不把旧结果当最新提示词。

多次生成图片会保留历史版本，每种帧的“使用中”图片只有一张。点击“选择使用版本”，对满意的图片点“使用”；缩略图会标明当前版本。视频按当前选择的参考模式使用首帧/关键帧/尾帧的生效图片，不自动使用所有历史版本。新结果通常尝试成为当前图片，若与人工选择等发生版本冲突，需检查使用中标记。已提交的视频任务不会因之后切换版本而变化；图片生成弹窗的参考素材列表另行选择。


2026-09-10参考模式同步修复：视频生成面板选择首帧/尾帧/关键帧/首尾帧后，视频准备度立即按该选择重新检查，无需先打开提示词预览。清空选择按纯文本检查。检查期间显示加载；若仍有缺图片、未确认或其他条件，以刷新后的具体缺失项为准，选择模式本身不等于已经具备图片。

## 2026-09-10 放大查看生成图片

分镜首/关键/尾帧及历史版本、资产照片、实验室生成结果、素材库与参考图片可点击放大。预览工具栏支持放大、缩小、旋转；放大后拖动查看细节，按Esc或关闭按钮返回。也可Tab聚焦图片后按Enter/空格打开。

点击图片不会自动选为参考或切换使用版本；选择素材点击名称/选择按钮，切换帧图仍点“使用”。资产卡片“查看全部视角图片”可打开视角列表，列表图片也可放大。预览清晰度受原始图片实际分辨率限制，不会重新生成或付费。

## 2026-09-10 复用预检并智能调整视频提示词

视频生成提示词预览→AI智能预检与提示词优化：直接读取当前镜头历史，刷新和重进免费。历史可查看当时提示词、实际送审图片、目标视频模型及参考模式/参数；用途未标注的旧记录会提示，不当成本次视频已经预检。

选择成功的旧预检，可补充“哪些建议误判、哪些设定必须保留”，选文本模型并确认本次费用后点击“依据此预检智能调整”。系统给出完整优化稿、修改说明和待核对项，无需自己逐句改写。已有优化方案直接选择查看，不必再次调用。

核对后点击“应用优化并保存标记（免费）”；标明已应用及时间。改变当前文本或参数会显示版本不同，旧方案不能直接覆盖。重新进入相同原稿及参数时恢复已应用稿；如果来源已变，保留历史供参考。还没有应用的方案明确标注，应用不等于已经生成视频。

需要再次检查时自行点击“重新预检（调用模型）”，不会自动收费。参考图身份/剧情有歧义、必须换图等问题保留待核对；不同模型返回格式尽量自动整理，无法可靠识别时保留原文，不会自动收费重试。最终视频仅关联同镜头且输入匹配的预检/优化记录，避免不同版本混用。

视频提示词预览中的“动作节拍”优先读取分镜保存的信息，无保存内容时从描述、摘录和对白整理；触发/峰值/收束是本地推断标签，不是AI预检结论。长节拍自动换行，点击“展开其余N条”查看全部；弹窗正文滚动，生成/取消按钮保持可见。

### 预检优化不是反复审核循环

入口默认展开，操作区位于报告前面。已有报告→选择文本模型、确认一次费用→“依据此预检智能调整”返回完整优化稿、修改说明和待核对项→左右对比→“应用优化并保存标记（免费）”→直接生成视频。不强制再次预检。只有用户主动重新预检才另发请求；生成视频本身按视频模型计费。

旧报告已有方案时点击“查看此预检的优化方案（免费）”，不必重复调用。顶部明确当前视频是否已应用、是否版本不同或尚未优化；应用后的对比显示实际保存稿。历史创建/应用时间按本地时区统一显示，修复先前8小时差；任务中心开始时间与创建时间可因排队略有不同。

### 历史预检记录筛选

“选择成功返回的预检或优化记录”只列出成功且有非空返回内容的记录。失败、取消、执行中或空结果不作为可复用建议；旧失败任务可在任务中心查看。新触发的预检/优化仍单独展示进度和本次错误，执行期间不能切换历史记录。读取成功历史不会调用模型或产生新费用。

## 视频生成：先确认场景目标，再确认发送内容

工作室点击“生成视频”进入“确认视频生成内容”。主页面先核对角色、场景、道具和本次参考帧；点击图片放大，首帧对应开始、尾帧对应结束。设定不对点“修正镜头与资产设定”，图片不适合点“选择或生成参考帧”。这些来源不是AI质量检查结论。

“提示词与发送预览”左边可编辑、右边显示本次完整发送稿。编辑后点“更新发送预览（免费）”，可展开查看本次系统补充内容；未同步不能生成。模型和参数中途变化也需重新更新。只删除某条系统内容可能在更新时补回，请在来源中修正冲突设定。参考图片和模型参数独立发送，不能只靠提示词改分辨率或换图。

“AI预检与优化”可选：免费读取成功历史；新预检或智能优化才调用模型。只有当前完整发送稿已同步才能发起；已有优化方案可看前后对比并免费应用，应用后回主预览。未解决的换图、身份或剧情疑点仍要核对，“已应用”不等于质量通过，也不要求重复预检。

“来源与规则”解释镜头/资产依据、通用质量约束和连续性信息。渲染按已选择的视频模型版本提前补充现有厂商单镜头/参考编号规范。宽屏左右对照，小屏上下排列；页签保留草稿，底部按钮固定可见。

核对后点“确认费用并生成视频”，最后费用窗口沿用已预览规格，不能在这里另改分辨率或音频，修改请返回工作室。确认前取消不创建任务；若请求发出但未取得编号，先到任务中心核对，避免再次付费生成。费用只是参考，最终以账单为准；成片仍需播放验收人物、场景、动作与衔接。


## 短剧视频：按模型选择参考方式与成片版本（2026-09-10）

入口：项目列表 → 项目工作台 → 章节 → 分镜工作室 → 右侧“视频生成”。先确认本镜头的剧情、角色和动作，再选择“本次视频模型”（留空使用系统默认）。这不会修改系统默认配置。

- 首帧：先确认开始画面，再生成动作；角色/场景可先通过参考图用于制作首帧。
- 首尾帧：引用已采用的首帧与尾帧两张图，控制开始和结束；无需额外生成一张“首尾帧图片”。
- 主体多图：直接参考人物、场景或道具，需要已配置且本系统已核验的参考生视频型号。当前支持 HappyHorse 1.1 R2V、Vidu Q2/Q1/2.0；当前配置的 HappyHorse I2V 和 Seedance 1.5 不会自动升级或替换为这些接口。

选择主体多图后，从素材库选图片，填写用途（如“角色·妈妈，只参考外观”“场景·林荫道”），上移/下移设置顺序。不要把不同服装、人物或场景全部默认加进来。每条一张图片，HappyHorse最多9条、Vidu最多7条；同一主体多角度组内编排暂未开放。未提交参考草稿保存在当前浏览器，换浏览器需重选；已提交任务会保存当时的图片、用途与顺序。

点击“生成视频”先看发送预览，核对实际发送图片、用途映射和完整提示词。可选预检只看勾选发送的图片，当前最多4张；旧报告读取免费，重新预检/智能调整需手动触发并确认收费。改图、改用途或顺序后，旧报告不再代表本版本。模型时长不兼容时应修改时长或拆分动作，系统不自动删剧情。

“视频候选与成片版本”里，点击视频缩略图只切换播放；“当前用于成片”才是后续剪辑导出使用的版本。要换用旧候选，先播放比较再点“采用此版本”。原文件和已经导出的成片保留。普通新生成完成时仍可能自动更新当前版本，因此导出前请复核标记。建议按人物身份/服装、动作、空间、前后镜头衔接、音轨和字幕逐项检查；“生成成功”不表示质量已通过。


## 多轨剪辑操作与人物复核（2026-09-10）

入口在 Jellyfish Web：项目工作台 → 进入后期剪辑（`/projects/项目ID/editor`）。

1. 点击视频轨片段，设置源片入点、出点、原声音量；前移/后移改变顺序。移出工程不删原文件。补入镜头使用时间线下方目录。
2. “添加已有音频”可先试听再选用。选择配音/音乐/音效轨，设置成片起点、音频入点、使用时长、音量。同一时间的音频会同时播放。若原视频已有对白，另加相同配音前先调低或静音原声。
3. 字幕初始时间仅为字数估算，需对照声音校时。改变片段长度或顺序不会猜测配音/字幕该如何移动，须重新核对其成片绝对时间；越界会阻止保存。
4. 人物与连续性复核显示当前角色基准；点击主图放大，对照前镜头和当前片段，看脸型、年龄、发型、服装、道具归属、声音、动作衔接。填写待复核/人工已复核/需返工和备注，保存。改片、声音、顺序或基准变化后须重新复核。
5. 需修正画面时点“返回此分镜修正 / 重生成”，在新标签页完成后返回剪辑台，刷新素材目录并主动替换。工程保留原视频版本和剪辑设置；新视频更短时检查被收紧的出点及字幕。
6. 保存工程后合成预览/导出。源片预览不等于混音试听，实际混音与字幕以生成的成片为准；最近成片不会因修改草稿而立即改变。MP4软字幕部分浏览器无法呈现，可下载后打开字幕轨。

角色三视图已纳入计划：确认主形象后补侧面/背面，按当前模型能力和镜头角度选择参考；不会自动付费生成，当前复核也不是AI像素一致性鉴定。复杂转场、波形拖拽和付费跨镜头视觉检查尚未实现。


## 三视图、转场、波形与AI视觉检查（2026-09-10新增）

1. 三视图入口：项目工作台→角色→编辑，或资产管理→演员→编辑→“三视图与多角度参考”。点击“建立三视图槽位”免费建立正面、左侧、背面；先生成/采用满意的主形象，选择图片并点击“确认主形象”，再分别生成侧面、背面。每张仍需核对提示词、模型参数及费用。重新进入要再次确认主形象。可以放大和从历史结果采用满意版本，不会自动把所有图加入视频参考。
2. 转场入口：项目工作台→进入后期剪辑→点击视频轨片段→“与下一镜头转场”。选择类型和时长，保存工程，再“合成预览 / 导出”观看真实效果。最后一个片段没有出场转场。转场重叠会缩短总长，音轨与字幕需检查绝对时间，越界会明确提示。
3. 波形：添加已有音频后自动解析真实波形。拖片段中部移动，拖两侧边缘裁剪；方向键微调0.04秒，Shift加方向键1秒。可撤销，点击保存后刷新恢复。体积超过30MB、过长或浏览器无法解码时使用下方数字校时，不影响原文件。
4. 视觉检查入口：后期剪辑→选定片段→“AI视觉一致性检查”→“打开视觉检查”。先保存工程，再“准备检查材料（本地免费）”；逐张放大核对源片采样、上镜头采样、人物基准及图号。选择已配置视觉模型，点击“确认费用并检查”，再次确认外发后才调用供应商。暂不提供不准确的金额承诺，以供应商账单为准。
5. 成功报告保留在同片段历史中，刷新或重新进入可查看，不重复收费。报告窗口显示工程版本及是否适用于当前保存工程，时间按浏览器时区展示；修改后提示历史版本，需要时主动重新准备、重新检查。检查失败不进成功历史，不自动重试，任务中心可查原因。
6. 视觉检查最多4图，仅针对显示的采样位置；不代表全部人物、整段动作、声音或转场合成效果已验收。采样报告可误判，需结合原片人工决定返工或“人工已复核”。三视图也不能保证绝对人物一致。


## 角色可以先创建、后选演员（2026-09-11）

项目工作台→角色→新建角色：“关联演员”为可选，不选择也可创建。之后进入该角色“编辑”→“角色演员关联（可选）”，从本项目演员中选择并点击“保存演员关联”。候选为空时先在项目“演员”页从资产库关联，再刷新角色编辑页。清空选择可解除关联；更换或解除已有演员时需确认。此操作只改关联，不覆盖角色描述、三视图或既有视频，请在后续生成前重新核对主形象与参考图片。


## 三视图怎样参与后续生成（2026-09-11）

1. 分镜工作室 → 关键帧与参考图 → 生成/编辑首帧、关键帧或尾帧 → 参考图映射 → 按角色选择多角度图片。展开角色，勾选本帧需要的角色定妆/演员基准角度，在下方调整图号后点击“保存本帧参考”。三类帧独立保存，取消全部可只用文字；新角度不会自动全选。
2. 视频生成 → 选择支持主体图片的本次视频模型 → 主体多图。展开角色选角度，同角色自动放入一个主体组，可改名称、补同主体图片、排序或移除。查看已选组数/总图数/每组限制；最终提示词窗口展示所有将发送图片。Vidu已核验型号每组3张、总7张；HappyHorse R2V按图片顺序编号，总9张。只支持首帧的当前型号不会显示多图入口，系统不会自动替你换模型。
3. 项目工作台 → 进入后期剪辑 → 选片段并保存工程 → 打开视觉检查。选择采样方式，再勾选人物角度，点击“准备检查材料（本地免费）”，核对列出的每张图后才选择模型并确认费用。只取起始画面可加3张人物基准；取起止画面可加2张；再加上一片段结束时只剩1张。更多角度和更多视频时刻是本次材料容量分配，不会自动丢图。未选人物基准时，只能检查采样画面和文字。

已经成功的报告及其材料可免费查看。换基准/顺序或采样方式后先重新准备；历史报告不代表新选择已经检查过。演员图片服装可能与角色定妆不同，应优先选适合当前场景的图片。三视图改善身份参考，不保证模型最终人物一致；仍需查看出片并人工采用。


## 视频生成使用最新设置（2026-09-11修复）

“本次视频模型”会显示当前默认型号；可手动改选，清空后恢复默认。当前项目设置中的默认模型是系统共享配置，并非每个项目分别保存。缺少默认配置时请明确选型。

在生成参数修改时长/比例、维护设置修改标题/剧情备注后，返回“视频生成”会先保存并同步；同步完成后准备度及费用使用最新值。保存失败会保留输入并显示“重试保存”，不能用旧数据继续生成。生成前也会重新读取设置和已采用帧；确认期间发生变化，需要更新发送预览并重新核对，不会静默按旧参数收费提交。

刷新模型参数时，如果型号修订和选项未变，已选分辨率/音频设置会保留；切换模型或模型配置修订变化后，请核对新默认规格。上述同步和准备度检查不调用收费模型。

## 百炼图片低分辨率任务报错（2026-09-11修复）

若历史任务显示 resolution_profile 不接受 preview，这是系统执行契约遗漏，已修复部署。当前模型参数中提供的低分辨率档位可以正常通过本地任务校验，不需要切换模型或提高分辨率来绕过。具体可选尺寸仍取决于当前模型与是否携带参考图片。

历史失败记录不会自动变成功。需要生成时，回到原图片槽位，重新核对提示词、参考图、规格和费用后主动生成；新任务会产生正常模型费用。任务aa9d96dc5a5c4c648679932537424382在本地输入校验失败，审计没有供应商请求。此次修复仅做离线模拟与部署验证，没有自动调用百炼重试。

## 手动新建分镜可直接编辑（2026-09-11修复）

分镜列表“新建”会同步建立镜头详情，保存后可直接进入编辑准备页和分镜工作室，不必先调用AI提取来创建详情。默认中景、平视、固定、4秒只是可编辑的基础参数，仍需按剧情调整并完成提取确认/生成准备。

此前缺少详情的“测试新增分镜”已补齐，可刷新后继续使用，不需要删除重建。未执行提取的镜头仍显示相应待办，补齐详情不等于可以立即生成视频。

维护时可在backend容器运行 `PYTHONPATH=/app /app/.venv/bin/python scripts/repair_missing_shot_details.py` 只读列出缺失ID，核对后追加 `--apply` 仅补缺失；不会覆盖已有镜头详情。本次已完成正式库修复，无需用户重复执行。


## 首帧、关键帧、尾帧提示词与可选检查（2026-09-11）

入口：分镜工作室→关键帧与参考图→对应帧“生成”。已有保存提示词直接显示；没有时免费从本镜头剧本/画面/关联资产整理初稿，可直接改写。点击“保存本帧提示词”保留自己的版本；未保存编辑关闭后不会持久化。按规则渲染会更新下方最终发送预览，生成实际使用此预览。

按参考图、图片规格、基础提示词、最终发送预览核对后即可生成，不要求做AI预检。需要辅助时展开“AI检查与优化（可选）”，选检查模型并确认本次费用，点击“检查并生成优化稿”，一次调用返回完整方案。查看前后对比、修改点和未解决项后可免费应用；出现“本帧已应用智能优化”才表示采用。恢复原始发送稿免费。参数/参考/来源改变后旧方案不再作为本次已应用版本。

已有报告从成功历史中查看，读取不收费；“依据此预检智能调整”会另调用一次模型，只在确实需要重新调整时使用，无需反复预检。旧视频或用途不明确的报告在“引用其他用途的历史建议”中显式选择，只作线索，不混成本帧记录。首次没有报告可以直接一次检查并优化。

检查生成结果：在对应帧的历史图片点击“检查此图片（可选）”，或在辅助区切到“生成后”。必须选择具体图片版本，可另选角色正/侧/背面基准，核对下方实际送检图片。需要支持看图的检查模型，图片上限4张且包含目标图。应用建议不会修改旧图片，只改变下一次生成提示词；新图片仍需主动生成并采用。

本地初稿/基础检查/读取历史/应用/恢复免费；明确调用AI检查或优化、生成图片分别收费。自动整理不等于模型理解或质量保证，资料缺失时请补充人物、场景和动作。

本节功能已于2026-09-11 21:12部署。刷新页面后重新打开对应帧的生成窗口；内容较多时在窗口内部滚动，底部生成/取消按钮固定可见。


## 2026-09-12 模型生成时可以继续其他工作

看到“任务已在后台执行”后，可以关闭窗口、切换镜头/资产/实验室会话或进入其他页面。关闭不会取消任务；如需停止，在任务中心或原业务里的“取消任务”单独操作。同一项任务依赖结果的按钮仍可能暂不可用，其他工作不受影响。

- 任务中心：查看进度、成功/失败/取消，并回到对应业务查看结果；完成时有短暂提醒。
- 帧图/资产图片：受理后返回原页面，结果回到原对象；可同时准备其他帧或其他资产。
- 章节智能精简/优化：关闭后可重新打开原章节编辑器，查看“AI处理结果 · 待采用”；点击“采用到编辑区（仍需保存）”，核对后再保存，不会自动覆盖原文。
- 剧本导入分析：直接关闭可后台运行；重新打开导入窗口或从任务中心返回原导入批次查看。分析结果与任务会保留，人工编辑仍需主动保存；明确删除另行操作。
- 文本实验室：发送受理后可切换其他会话；完成后完整回复保存在原会话。当前改为后台完成后显示全文，不再要求页面一直保持逐字输出。
- AI检查：重新进入相同用途/输入可恢复进行中任务；成功建议仍需预览和明确应用。图片、视频用途以及不同镜头不会互相冒用。

刷新不会自动重提模型任务。短暂断网先恢复查询，不要连续点击重新生成。界面支持并行工作不等于供应商无限并发；仍按worker资源及供应商规则排队。尚未保存的人工草稿请先主动保存，后台任务恢复不代替保存草稿。

上述交互已于2026-09-12 08:50部署。打开 http://127.0.0.1:7788 并按 Ctrl+F5 刷新后使用；任务中心仍在全局入口。

## 2026-09-12 即梦配置更新：选择服务版本

进入“模型管理 → 模型 → 添加模型”，供应商选择已保存的即梦账户；选择图片生成或视频生成后，“服务版本”显示对应目录。开通图片3.0请选择“即梦AI-图片生成3.0”，无需自己填写req_key；视频请选择“即梦AI-视频生成3.0”。版本仍为必选，因为系统不能猜测账户开通权限。不要把普通4.0和SP服务混用。

图片3.0：无图为文生图，有1张参考图为智能参考；多图会提示不支持。视频3.0：生成页选择纯文字、首帧或首尾帧，再选720P/1080P及5/10秒；系统按实际选择匹配官方接口标识。1080P须有对应权限，不自动降级或付费重试。专用运镜模板尚未开放。

已有固定req_key配置不会自动改变；可在“编辑模型”中改选对应服务版本，之后新任务采用新配置修订，已有任务仍使用原冻结配置。目录读取不调用收费模型，也不验证账户额度。4.0当前仍只接通2K文生，公网参考等边界见PL-014。

此目录与分流已于2026-09-12 11:38部署；刷新Jellyfish页面后进入模型管理。正式只读浏览器验证通过，不代表账户收费生成验收。


## 2026-09-12 官方来源与型号匹配复核（工作树待提交批次）

LC-010/011/012/014；PL-007/014继续跟踪。动态枚举16个注册供应商，并只读核实生产7个型号（阿里4、火山3；即梦账户未保存型号）。阿里Token Plan与火山/api/plan/v3配置保持原计费渠道，不自动转标准API。

本批修复：官方来源按具体型号/类别/计费渠道绑定，厂商总入口不再冒充每个型号的接口依据；即梦语言查询参数可读取，3.0/4.0与旧固定req_key分别绑定对应文档。MiniMax中文文档迁移到platform.minimax.cn，API账户地址不随文档地址改动。Vidu Q2/Pro/Turbo、Q1 Classic、Q3 Pro按官方模式矩阵分别约束，禁止错误帧/主体模式。BFL精确型号检查、Kontext参考图场景推荐与尺寸16倍数校验补齐；MiniMax未实现分辨率/音频参数明确拒绝，不静默丢弃。

同步规则绑定来源URL与正文SHA，读取失败/正文变化后未解析出的规则不再沿用；显式回退固定版本单独标记。价格仅接受已登记精确型号、北京区域、人民币单位和唯一完整价格行；不推断其他币种、不把标准原价当套餐实扣。场景选型区分本地接入与官方型号证据；接口与价格同步页显示来源覆盖、尚未检查/读取失败/变化待核验、当前生效规则与北京时间。

首轮公开文档只读审计55地址，42可直接读取。腾讯/可灵等动态正文及部分国际站无法被后台读取的来源保持待核对；浏览器/搜索可读不等于后台定时同步成功。注册覆盖不等于全部型号官方字段或真实生成验收。审计脚本scripts/audit-official-model-sources.py仅访问公开文档，原始快照在.local；新增来源后按动态范围继续检查。无收费调用、凭据/默认模型变更或schema迁移。完整测试及部署状态另记；既有未关闭PL不取消，复杂API变动仍需适配代码与验证。

查看入口：Jellyfish Web → 模型管理 → 场景选型说明 / 接口与价格同步。点击“立即检查更新”只排队读取官方文档，稍后“刷新状态”查看；型号级文档表示来源绑定，不能理解为账户已开通或生成验收通过。


## 2026-09-12 新增查看与操作入口

1. **恢复原生成任务**：在对应业务的“生成调用详情”查看恢复提示。存在可靠回执时可点“继续查询原任务”；已保存厂商结果时可点“仅补归档”。可以关闭窗口继续其他操作。历史没有回执、提交是否受理不确定、任务取消或成功时不开放恢复。不要用“重新生成”代替恢复；重新生成是另一笔模型调用。
2. **查看模式边界**：模型管理 → 场景选型说明 → 展开型号行，按文生/首帧/首尾帧/主体/编辑查看输入要求和本地约束。“型号文档已绑定”不是账户或所有API参数验收。
3. **逐帧参考建议**：分镜工作室 → 首帧/关键帧/尾帧图片生成 → 参考图映射 → 参考方案建议。选角度和用途，检查图片及原因，点“用此方案替换本帧选择”，再取消或排序，最后“保存本帧参考”。此步骤是本地规则，无模型费用。
4. **框选修正**：实验室 → 图片 → 选择已接通的即梦图片3.0/FLUX Kontext Pro及一张原图 → 拖动框选 → 输入修改要求 → 核对规格费用并生成。原图使用标准比例；结果以新文件展示，保留原图并供比较。不是原生掩码接口，也不保证更便宜；素材和关键帧可随后从文件库选择新结果。未框选仍按普通生成，其他型号不自动替换。
5. **视频编辑禁用说明**：HappyHorse1.1I2V是首帧图生视频；Seedance1.5Pro的文生/首帧/首尾帧也不等于原视频编辑。下拉原因换行显示，展开型号状态可查看官方依据。支持型号还要分别核对账户权限与API渠道。

本批未调用收费模型，框内效果和供应商实际费用仍应根据真实结果验收。

上述入口已于2026-09-12部署至Jellyfish Web；刷新页面即可查看。


### 恢复与重复提交保护（2026-09-12修订）

实验室同一会话中，相同模型、提示词、参考顺序和参数的任务仍在排队或执行时，再次提交会提示查看原任务，不额外创建生成任务。调整输入可以并行处理；原任务结束后允许主动再生成。不同会话不作全局去重，主动再生成仍可能计费。

调用详情中的“继续查询原任务”需适配器确认有效任务回执，不能仅因HTTP成功而开放。已保存完整结果的“仅补归档”继续复用结果。历史回执未确认、提交结果不确定时不自动重提；不要将重新生成当成免费恢复。


### 查看附件与项目型号核对结果

完整对照见`site/content/docs/architecture/official-model-identity-audit.md`。Jellyfish Web → 模型管理 → 场景选型说明，展开型号查看本地模式、限制与官方链接；接口与价格同步仍需区分来源绑定和最新正文读取状态。新补的来源在下一次手动/定时同步后更新读取结果，不代表保存链接就已同步成功。

即梦Visual服务版本与方舟Seedream不同；MiniMax-H3与Hailuo-02不同。2026-09-12火山Agent Plan正文标注Seedance1.5Pro即将下线，日期需核对账户通知；系统不会自动换模型或计费渠道。


2026-09-12：人工“额度与预算”业务已取消，模型管理不再提供登记入口。生成页面的费用估算与官方价格同步保留，仍以实际账户账单为准。

## 项目与资产题材风格候选（2026-09-12，待用户确认，尚未上线）

在“新建/编辑项目 → 视觉风格、视频风格”选择；演员、角色及场景/道具/服装使用同一目录。
- 现实：新增真人玄幻修真、真人仙侠、真人穿越。
- 动漫：新增动漫穿越、动漫古装、动漫武侠、动漫玄幻修真、动漫仙侠、动漫国风神话、动漫末世科幻。

现实不表示只能创作现实题材。玄幻修真可选真人写实或动漫表现；希望3D或水墨等具体质感时，补充到项目简介/提示词并选一致的参考图。风格会沿现有剧本上下文和相关图片/视频提示词链路传递，不替代具体人物、服装、世界观与动作描写。最终以生成前预览内容为准；自定义模板须包含style、visual_style变量才会使用风格。

修改项目风格不会自动修改已有资产的独立风格、重写已编辑提示词或重绘历史图片/视频；复用旧资产时需核对画风。没有自动更换模型或新增收费操作。此次未做各风格的真实付费效果验收。

穿越为叙事题材，具体目标时代、前后服装与场景变化必须在剧本中说明，风格选项不会自动补造穿越剧情。


## 创作设定与风格联动（PL-019）

新建项目先选择“表现形式、画面风格、主题材”，展开后可设置辅助题材、穿越/重生等叙事标签、时代、地域和世界/美术规则。快捷组合只是初值，可以继续修改。例如“动漫 + 3D国漫 + 玄幻修真 + 穿越”，不再把这些概念混在一个风格下拉框中。

项目、章节、分镜及资产页的“创作设定”打开共享抽屉；工作室也有当前分镜入口。每个字段显示本层设置或继承上级，点击“恢复继承”删除本层覆盖，清空表示不使用上级该值。下方“有效设定与来源”展示已保存值与版本。设定参考图可点击放大；它是美术依据，不会自动全部送给模型，实际生成仍需选择参考图。

实验室可先点击“先设置创作方向”建立免费草稿，再设置独立方向或按项目名称关联；不会因为此前浏览过某项目就自动继承。保存配置不会重绘旧作品，也不会重新收费预检。生成前查看最终提示词，出现配置过期提示时重新预览；历史预检只适用于当时输入，必要时手动重新预检。

已有演员身份和服装关联不因题材变化而改写。需要古装/现代等不同造型时保留原角色/服装，创建独立造型角色并关联同一演员，按镜头选择，避免替换旧造型影响其他镜头。旧数据补值来源在设定抽屉展示；待核对线索应由用户确认，系统不把正文关键词直接当作世界设定。


### 创作设定入口复盘后的说明纠正

此前“创建独立造型角色并关联同一演员”的说明不能替代原方案中的同角色造型版本；该版本管理尚未完成。剪辑页的AI视觉一致性检查现为当前/上一片段源视频抽帧对照，并非最终成片全量检查；上一镜头或资产独立设定变化暂不能保证自动标记报告过期，应重新核对检查材料。后续补齐范围见PL-019第六项复盘。


## 2026-09-12 角色造型、模板试稿与跨镜头检查

1. 项目工作台 → 角色 → 编辑 → **角色造型版本**：新建版本，填写适用剧情与服装，从素材库选择已生成/上传的图片并标注角度，再保存。已有版本可“以此另存新版本”，旧版本不覆盖。身份仍是同一角色，不用创建同名角色。
2. 分镜工作室 → 人物多角度参考目录 → 展开角色 → **本镜头造型**：选择版本，然后分别为首/尾/关键帧勾选角度、排序保存。切换版本后请重新检查已选图片和生成提示词；不会自动重新生成旧素材。
3. 提示词模板 → 选中模板 → **绑定业务对象试渲染（免费）**：选择项目/章节/分镜或资产，可试填模板变量，点击免费试渲染查看正文、缺项和实际来源。不调用模型、不保存业务草稿，最终生成仍以生成页预览为准。
4. 项目剪辑 → **AI视觉一致性检查**：本地准备后可展开当前/上一片段设定与剧情依据，看到实际送检图片。旧报告在相关设定变化后标历史版本；需要时手动重新准备、确认费用并检查。抽取源视频样图，不是最终成片逐帧/转场字幕或声音检查，不自动判定质量通过。


## 创作方向怎样填写（2026-09-12 表单简化）

先选“画面表现”和“故事主题材”。前者决定看起来像真人影视、2D或3D等，后者决定故事主线；新项目两项必填。资产未覆盖时使用项目/默认设定，编辑页可恢复继承。

“补充设定”不必全部填写。辅助题材不能重复主题材；选择混合题材必须说明统一背景和主辅融合方式，例如“现代都市里修炼者隐居，普通人不知道法术存在”。该说明保存为世界规则。没有说明不能保存或新生成；旧作品不会被改变。融合说明不是无冲突证明，预检还需按原剧本核对。

时代写时间背景（如架空古代）；地域写地点环境（如江南水乡）；世界规则写既有剧情特殊规则及边界；美术约束写配色、建筑材质和应避免物件等具体画面要求。不确定可留空，不要写没有在剧本确认的新情节。叙事标签可多选，但只选真实存在的特点，不会凭标签自动添加情节。

这些文字会被放入生成提示词及检查依据，乱填可能影响输出；不是厂商硬性参数，不保证模型完全遵守。可在生成前最终预览查看实际文本，需要时手动进行AI预检，不会自动产生费用。


## 2026-09-12 已保存有效设定显示修正（LC-014/015，工作树待提交）

用户确认时代/地域/世界规则/美术约束是填写后主动删除；正式只读核实项目版本5中这些值为null，辅助题材和叙事标签为[]，没有保存丢失证据。按用户最新要求，已保存有效设定表过滤null、空白文本和空列表，只显示非空有效值；不展示“已清空”或空列表空白行。保留有效字段来源，区分本层与继承；标题注明本对象版本，来源列显示来源对象版本，两者无需相同。无有效项时仅显示暂无有效设定。

只改前端显示，不改保存/清空/继承语义、生成上下文、API或数据库。无需OpenAPI变更；前端类型与构建/正式验收续记。此前拟定的“无（已留空）”展示按用户要求撤回，未作为最终交付。

## 豆包网页生成进展（2026-09-13，尚未上线）

目前仍使用原API生成入口。豆包网页方式处于开发验证，不需要修改现有模型配置，不要把豆包网页Seedream名称当成火山API型号。登录与人机验证在本机独立Edge窗口完成，勿发送密码/验证码。此次楼房图片测试尚未取得成功产物，不能开始视频测试或称为完整接入。正式操作入口在图片闭环验收后补齐；当前开发边界见scripts/browser-runner/README.md。

### 2026-09-13 用户允许一次追加图片验证后的进展（未部署）

用户明确允许一次追加的相同楼房参考图测试，已取得豆包正式会话与生成消息，网页显示 Seedream 4.5；参考图和提示词生成一张建筑插画成功。独立 Edge 重启后按固定会话/消息恢复成功，使用网页原图下载按钮取得 PNG 2048×2048、1464525 字节，SHA256 为 013f34d2608368e9a8c05485ca9da15ef170b2c196cbfd844afcba93f9540216。聊天缩略图实际仅384×384，已区分缩略图与原图，不能把元素标注尺寸当作下载尺寸。首轮未知提交仍保留，未伪造成功回执；追加尝试与首轮分开记录。

原图采集增加页面异步加载等待及唯一图片/下载控件核对；后端会话与消息仅接受实际格式的数字标识，拒绝临时会话、无效ID及带其他参数地址。3项后端、5项Node离线回归通过（pytest缓存目录权限警告不影响测试结果）。这些验证不是多账号并发或正式业务验收。

本批归LC-001/010/011/012/014，工作树待提交。真实图片网页生成与原图下载已验证；Jellyfish正式账号配对、领取执行、业务入口及回填端到端仍未完成，未部署、未迁移、未回填任何现有分镜；视频仍待图片业务闭环后进行。图片带AI生成标识，公开发布授权及水印处理仍待核对；其他未关闭PL不变。
### 2026-09-13 02:01 网页账号池与图片候选入口已部署，真实验收按用户安排转下午

PL-020 / LC-001/006/009/010/011/012/014/015 工作树待提交。新增“系统设置→网页平台账号”维护、启停/配对状态/实际型号/执行器在线状态；生成请求默认自动账号，按平台和实际模型能力筛选、90秒在线窗口、空闲账号最久未使用优先。分派后assigned_account_id固定；每账号至多一个任务，忙碌/离线/未登录/受限不参与，不换号重发未知提交。领取前本地保存凭据，领取HTTP回包丢失仍恢复原任务；冻结参考下载与sha校验、任务原图回填CAS保留。

首/尾/关键帧最终提示词弹窗新增独立“网页生成”，默认自动账号。新增启动网页生成执行器.cmd、scripts/windows/Start-WebGeneration.ps1 和 browser-runner/worker.mjs；账号资料和令牌位于忽略的local-browser，亦从Docker构建上下文排除。当前已登记豆包本机创作账号并复用用户已登录的独立Edge；不读取日常浏览器密码或复制手机号。

验证：736项全量后端通过，后续6项网页专项及5项Node通过、前端tsc/生产构建通过。独立临时MySQL容器中实际Alembic全量迁移、账号表升降级、双账号并发和领取恢复通过。直接ORM建全库曾因既有MySQL约束差异失败，已改为真实Alembic链路验证，不据SQLite冒称MySQL通过。自动审批拒绝提取现有MySQL root凭据的原验证方法；未执行该方法，改用独立无业务挂载测试容器通过。

正式业务账号只读备份：backups/web-generation-20260913-015944/database.sql，SHA256 EBCAEFB0F433142E38DDC375E40DDA58FAD5D5E789A0064CEAAD5B45E6210E40。active任务为空后迁移a3c5e7f9b015，仅新增网页账号表；backend/front已切换，worker/beat保持。HTTP health与账号接口正常。

真实链路仍未完成：新增独立场景“网页通道验收·楼房参考图”（54e02bef-17a2-43ab-ab0f-3940050bdc87）、图片槽5、任务f1e7bc8db7b158efade6c46e2209c200；参考来自本地合成楼房。任务停在prepared，尚未提交平台。联调发现网页会保留未发送草稿/附件，重复prepare可能累计参考图，必须在提交前清理且精确核对；型号模式切换也需与此一并修复。保留回执不重建任务，已暂停真实执行并按用户最新安排下午人工配合。首轮网页单图生成/2048原图下载曾成功，不代表本次Jellyfish端到端通过；视频未打通。

当前Windows执行器已停止，无自动平台提交。后续自动推进文档、代码和离线测试；下午再实测登录/验证码/图像视频。需补：草稿与参考清理、人工核对/恢复入口、取消锁协调、资产生成入口复用、视频DTO/执行/业务回填、其他平台官方流程与适配、正式页面验收。去水印最后核对官方导出权益与授权，保留AI来源标识边界。所有其他PL保持。

### 2026-09-13 网页通道后续离线补齐（候选，尚未再次部署）

PL-020 / LC-001/006/009/010/011/012/014/015，工作树待提交。本批在02:01已部署版本之后继续补齐：资产图片生成弹窗复用网页入口；七平台账号登记和官方核验状态，非豆包执行器拒绝启动；补视频请求契约、参考角色数量校验、图片/视频分队列领取、真实MP4校验和原镜头版本保护，视频提交仍关闭。视频时长/画幅/分辨率不符时归档为skipped，不采用，不改变shot.status。已确认远端结果的重复绑定校验覆盖恢复阶段；实际参考文件哈希漂移拒绝发送。草稿附件逐一清除并校验数量/顺序/上传完成，禁止清除历史会话。已提交后恢复不再依赖重新下载参考；后端阶段回报丢失保留已核实会话。当前回执文件原子更新，取消/409时停止执行保留回执。

新增官方核对表：site/content/docs/reference/web-platform-integration-audit.md。网页产品与API供应商分别核实；即梦官网协议存在自动化工具限制，本阶段转人工交接/已有官方API路径设计，不能自动改收费通道。可灵文档读取受限，万相/元宝/清言动态页面不足以确认当前生成流程，海螺地区站不能混用；未知参数不硬编码为已支持。豆包协议本次展示2026-09-17生效版本，核验日期早于生效日，保留版本边界。水印最后核对官方导出权益，不承诺自动去除AI标识。

验证：OpenAPI已重新生成；前端tsc和执行器语法检查通过；12项网页后端专项、6项回执测试、3项无网络真实Edge DOM测试通过；独立无网络Docker容器实际生成1秒测试MP4并使用ffprobe解析320×180成功（仅本地合成，无模型调用）。测试发现规格不符不能使用conflicted，已修为现有skipped规则，专项复验通过。完整后端复验结果另记。MySQL临时验证容器已核实无业务挂载/宿主端口、仅tmpfs数据后按精确名称清理；未删除业务卷或备份。

仍待完成：Jellyfish图片回填真实端到端、豆包视频页面真实型号/参数及浏览器提交下载适配、未知提交人工核对入口与取消锁协调、其他平台逐项适配验收。并未因新增视频DTO就完成网页视频生成。用户下午配合真实验收，当前测试任务f1e7bc8db7b158efade6c46e2209c200保持prepared/needs_user，执行器停止，不新增平台提交。既有02:01部署保持；本次候选未部署。其他未关闭PL全部保留。

本批最终离线复验：744项后端测试全部通过（89.43秒）；前端tsc通过；Node回执6项、隔离Edge页面3项通过。临时OpenAPI服务已停止，临时MySQL验证容器/网络已清理；真实执行器仍停止。未进行本次候选部署或新增平台生成。


### 2026-09-13 多平台交接与官方导出代码批次（未部署）

用户明确要求其他平台与水印先实现代码，真实场景测试统一下午。本批归PL-020、LC-001/006/009/010/011/012/014/015，工作树待提交；全部其他PL保留。原先先图片验收再扩展的代码推进顺序按本次授权调整，真实验收与自动启用门槛不放宽。

新增web_handoff服务及contracts/API/OpenAPI：七平台人工交接、LRU预留账号、冻结输入与有序参考、回填指纹/实际模型/官网回执校验、提交未知保留账号、明确未提交取消释放。手工交接不被自动执行器领取；同条结果不复用，引用下载核验原快照。资产/三帧图片和视频确认入口增加交接，网页业务工作台恢复原任务，任务中心保持轻量。视频只允许明确纯文本/首帧/首尾帧，不压平主体参考组；无动态官网规格保证。

水印不只是登记：新增同一官网结果的官方无水印原文件导入、原件保留、SHA/尺寸/时长校验、幂等和显式CAS采用。相同带水印原件字节和缩略尺寸拒绝；新导出默认历史，采用时检查原目标仍选原文件及版本。人工来源标为user_attested，不冒称自动水印检测或发布授权；不裁剪/涂抹/剥离来源、购买或重生成。海螺国际站官方会员无水印规则有资料依据，不映射国内账户权益。

新增“打开网页平台账号.cmd”和PowerShell实现，按账号独立Edge目录打开固定官网，只供人工操作；代码未实际打开任何新账号。入口：系统设置→网页生成交接与导出（/web-generation），或网页平台账号的原任务链接；原任务页查看导出版本并选择采用。架构与手册详见architecture/web-platform-handoff.md、guide/web-platform-handoff.md。

验证：755项全量后端通过；最后水印原件/缩略图保护补强后11项专项通过。前端tsc/生产构建通过（保留既有大包提示）；隔离真实Edge运行前端代码，所有API与外站请求拦截，验证刷新不提交、multipart原件/导出/采用、1280/540宽布局，无页面异常、无真实写入或平台调用。PowerShell语法检查通过，非法账号参数负向检查另记。无数据库迁移、本批生产部署或付费调用。

边界：其他平台本批是可用的人工交接降级代码，不是七平台自动生成全部完成。豆包图片完整真实业务回填、豆包视频DOM适配及各平台自动登录/型号/参数/下载核验、自动执行器未知提交与取消锁完整交互仍待下午/后续逐项实现验收。自动化目标未取消；当前原豆包测试任务仍暂停，不新增相同测试。不宣称水印视觉检测或所有平台免费无水印可用。

最终收尾：最后调用身份字段补齐后23项网页专项通过；最终tsc与9项Node/离线Edge回归通过。非法账号路径在Windows PowerShell中按预期拒绝，脚本未打开官网。临时5187前端及8011 OpenAPI服务已停止；既有5174端口非本批启动，未终止。运行服务保持，本批0平台生成/0付费/0生产迁移，候选代码尚未部署。


## 图片/视频生成选择 API 或平台网页（2026-09-14 已部署）

1. 在 **系统设置 → 网页平台账号** 登记豆包、即梦AI、可灵AI、通义万相、腾讯元宝、海螺AI、智谱清言账号。平台选项由后端统一提供；不在系统中保存密码、验证码或网页 Cookie。
2. 生成入口选择 **API 调用 / 平台网页**。API 使用现有模型配置及费用确认。网页点击 **平台网页生成**，选择平台及官网实际型号；目录标明名称来源，未列出的名称可输入。官网作品中的型号不代表当前账号已开通。
3. 默认自动分配账号，也可指定账号。未验证自动网页适配的平台使用人工交接；豆包图片只有观察到的模型才显示执行器选项。网页规格独立核对，不能使用 API 计价作为网页费用。
4. 创建后可关闭，去 **系统设置 → 网页生成交接与官方导出**，或实验室消息的“继续原网页任务”。按原任务指定账号和材料在官网操作；批量任务独立排队，不能拿一张结果回填多个任务。
5. 交接页可复制提示词，下载帧参考、按角色分组的图片/视频/音频和编辑原视频。回填必须核对同一账号、模型、输入及官网结果编号。实验室回原会话，图片回原槽位；视频编辑另存后点击“采用此版本”，原镜头版本变化时不会覆盖。
6. 图片框选修正需返回同画幅整图，系统只合成选区；平台原件同时保留。此类合成结果不能直接被官网无水印整图替换。

入口覆盖：资产快捷/详情及三视图、三帧、分镜视频、批量、快捷键、图片/视频实验室、文字编辑视频。没有模型调用的本地剪辑/导出仍走原有工具链。以上统一网页入口已部署；豆包图片和双视频实测结果见下文，其他平台仍待真实验收。

## 2026-09-14 网页入口实测部署

此前候选统一API/网页入口已部署，刷新Jellyfish后可从生成弹窗选择通道，系统设置→网页生成交接与官方导出查看固定任务。豆包图片实测完成，资产管理→场景→网页通道验收·楼房参考图可预览2048原图。登录使用同账号专用Edge；准备失败暂停后核对再恢复，勿重复发送。视频Mini/Fast各一次4秒正在受监督测试，完整自动视频执行器尚未验收。


## 2026-09-14 实测视频查看及浏览器恢复

在 Jellyfish 点击 **实验室**，打开 **网页视频实测·楼房·20260914**，可以分别播放 Seedance 2.0 Mini 与 Seedance 2.0 Fast 的4秒视频。直达地址：`http://127.0.0.1:7788/lab?session=37d51fd9-0e81-4e1c-bcbe-0b173b50cc27`。两条记录各绑定自己的任务、官网会话、原文件；均720×720、实际4.064秒，保留官网AI水印。

浏览器关闭不等于生成失败，先到原任务继续查看，不要重新点击生成。官网可能先返回参数确认，再执行视频；“参数已确认”和“视频已完成”不是同一阶段。本次原文件下载已避开Edge 152的崩溃路径，专用窗口启用沙箱并不显示旧恢复气泡，实际错误仍由任务记录保留。自动化运行中不要切换该窗口的账号。

图片自动执行已实测；本次两视频为监督操作和交接回填验收，其他平台及通用视频无人值守仍需独立验收。


## 网页账号自动连接（2026-09-14）

入口：**系统设置 → 网页平台账号**。

1. 新增：选择平台、填写便于识别的备注，保存后系统自动打开专用官网窗口。无需复制编号，也无需运行单账号CMD。
2. 在官网完成登录或验证码。豆包登录后由当前窗口自动核验并接入图片执行器，无需关闭重开；视频与其他平台仍使用网页交接。打开窗口不等于系统已经识别你的官网用户ID。
3. 已有账号直接点击“打开官网登录”；改备注只改显示，编号、登录资料和原任务不变。生成时选择图片/视频型号，同一账号不因型号不同重复登记。
4. 助手随Windows用户登录和Jellyfish一键服务启动，不自动生成。助手离线时先启动Jellyfish服务，页面恢复连接后重试；不会要求抄账号编号。切换窗口方式前先关闭旧窗口；有任务时从原任务恢复，不在官网换账号。

维护：当前用户启动文件夹中的“Jellyfish 网页本机助手.lnk”只启动本机助手。项目移目录后，从新根目录执行 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/windows/Start-WebDesktop.ps1 -RegisterLogon` 更新入口。移除该快捷方式只取消Windows登录自启，不删除账号或生成记录。


## 网页模型维护与执行方式（2026-09-14）

入口：**系统设置 → 网页平台模型**（`/settings/web-models`）；网页平台账号页面也有同名链接。先选平台，再选生成图片或生成视频，可新增/编辑官网完整型号、启用/停用、设默认、填写限制说明，排除没有权限的账号。只有一个默认；目录并发修改会提示刷新后重试。改名保留停用旧名，旧任务仍按旧名查看。网页型号与API的模型名或req_key分别管理。

生成弹窗默认选中该平台、该用途已保存的默认型号；主动修改后自动刷新不改你的选择或提示词。未在目录中的名称仍可输入进行人工交接；已停用型号不能通过手动输入绕过。要长期使用新名称，请在网页平台模型中维护。

人工交接由你在官网上传/生成/下载，再到原任务回填。当前本机执行器仅支持豆包Seedream 4.5单张普通图片，批量、局部修图、视频与其他平台仍需人工交接。型号维护本身不会新增自动操作适配。

自动执行默认选中；条件不足时下方常显具体原因并每3秒检查，提交按钮保持禁用，人工交接需主动选择。需要启动时点击“管理账号 / 启动执行器”打开账号页，豆包由同一窗口完成登录与自动执行；回到原弹窗保留草稿并等待状态更新，也可点击“刷新可用状态”。登录/验证、占用、型号未核实与账号排除分别处理，窗口打开不等于已核验可调度。

“刷新目录与账号状态”只查询Jellyfish已有的本机回报及成功记录，不调用生成、不抓取官网余额。模型可选不保证免费；权益未知时显示未核实，不自动升级或切换收费型号。停用影响新任务和待分配任务，不取消已绑定的运行任务。关闭生成窗口或跳转账号页也不会取消任务。

## 启动网页本机助手（2026-09-16）

豆包等网页生成依赖本机 Windows 助手（desktop-host.mjs）常驻轮询：它让后端能自动 fork worker、网页端“提交后台任务”按钮可用。本机助手不随 Docker 启动，需单独拉起。

一键启动（推荐）：双击项目根目录 `启动网页本机助手.cmd`。脚本调用 `Start-WebDesktop.ps1 -RegisterLogon`，拉起助手并注册开机自启（含掉线自愈守护）；窗口提示完成后可关闭，不影响助手运行。

手动命令（在真实 Windows 桌面打开 PowerShell，不是本对话）：

    cd E:\JellyfishNew
    powershell -ExecutionPolicy Bypass -File scripts\windows\Start-WebDesktop.ps1 -KeepAlive -RegisterLogon

- `-KeepAlive`：当前窗口成为自愈守护（每 30 秒校验，僵死自动重启），窗口保持打开属正常，关掉只停守护不影响助手。
- `-RegisterLogon`：把助手写入当前用户启动文件夹，下次 Windows 登录自动带自愈守护拉起，无需再手动。

首次手动启动一次后，助手即开机自启；日常业务全自动，无需每次手动。若助手已在运行但网页端仍显示离线，检查后端 `WEB_GENERATION_RUNNER_TOKEN` 是否已注入（详见二开主台账本批）；该令牌已固化进 compose/.env，正常重启不再丢失。
### 2026-09-14 网页多账号与自动执行恢复

豆包官网登录和图片执行器合为同一独立账号窗口；登录后自动核验、回报心跳并执行已受理任务。生成弹窗优先自动执行，条件不足明确原因，人工交接由用户主动降级选择。离线旧登录记录不作实时登录判断；在线窗口不可重复打开。视频和其他平台的自动化边界不变。

官网已识别功能介绍弹窗自动处理；验证码/授权仍由用户完成。验证码期间暂停提交等待计时，完成后接续原请求；原会话+用户消息checkpoint用于恢复。下载或后端短暂故障只重取原结果/回填原文件，不重发。没有正式回执时禁止把local_地址或其他任务最新图片当结果。取消请求不显示成可继续生成。

本轮三次真实单图发送：指定主账号成功、自动分配跳过待验证账号并成功；第二账号验证后官网仍显示发送错误、仅有本地会话，原任务4ef6cccc8a3e5939bd37f826f5a56753保持待核对且不重发。两项成功均独立实验室回填，原业务未覆盖。第二账号完整闭环、通用视频与其他平台自动生成仍待验收（PL-020）。详见二开主台账本批和PROJECT_MEMORY最新记录。


### 网页视频自动执行与即梦登录（2026-09-14）

生成入口选“平台网页”后默认“本机执行器 · 自动执行（优先）”。豆包图片选择Seedream 4.5；视频选择当前账号已核实的Seedance 2.0 Mini或Fast。使用纯文本或单张首帧，时长/画幅须符合官网实际控件，分辨率留空表示官网当前规格。其他参考模式或原视频编辑未适配时会说明原因，不能擅自改参考含义；需要人工降级时明确选择“人工交接”。

确认当前材料与账号权益后提交后台任务，即可关闭窗口或做其他业务；本机执行器负责上传、设置、提交、等待、下载与回填。普通参数确认可在精确匹配后自动处理一次；登录、验证码、素材授权等仍需用户处理。任务不确定时只恢复原记录，不重复发送或换账号。

系统设置→网页平台账号：分别显示图片与视频已核实型号，点击“打开官网登录”或“启动自动执行器”无需复制编号。即梦官网已登录会持续回报“官网已登录 · 自动生成待适配”，不再一直显示待登录；这不等于即梦自动生成已经开放。旧即梦窗口需要正常重新打开一次以加载新观察器，登录资料保留。


### 2026-09-14 22:21 当前使用与验收边界

豆包网页生成默认自动执行：图片Seedream 4.5；视频Mini/Fast按账号已核实型号、纯文本或单首帧条件选择。Mini的4秒单首帧自动确认、生成、下载和回填已实测成功，Fast此前为受监督实测。无需手动点普通“开始生成”；仅登录、验证码、授权确认仍由用户处理。关闭业务弹窗不取消任务，异常只恢复原会话，不重复发送。

本次两条视频可在Jellyfish实验室会话“豆包自动视频闭环验收20260914”查看（/lab?session=14909469-497b-48f4-a36b-14a4304bb801）。首条含人工确认，第二条为全自动验收。

即梦仍显示“官网已登录 · 自动生成待适配”，这是当前真实边界；候选代码不自动提交。今天已暂停平台实测，即梦没有产生新增生成任务。前后端已于22:21重启。


### 网页生成取消与重新使用账号（2026-09-15）

图片或视频没有返回、下载失败、执行器离线时，可从任务中心取消。网页自动任务会显示“已取消”并释放账号；账号恢复在线且已登录后，可以发起新的生成。每次新生成是独立任务，可能再次消耗官网权益，系统不会自动重发已取消任务。

取消并不保证官网撤回已提交内容；原会话/回执保留，迟到结果不会覆盖新任务。账号页“打开官网登录”和“启动自动执行器”均由系统传递账号并打开官网，有旧取消任务也会清除本地执行占用。不要在执行过程中改登录账号。原图已生成但取消的，可保留原件后自行决定采用，不自动恢复取消任务。


## 视频预检与已保存优化稿（2026-09-15）

智能调整返回候选稿，查看对比、微调后点击“应用并保存为生成稿（免费）”，自动同步编辑区与发送预览。再次打开优先恢复保存稿；在优化区微调后可再次免费应用保存。

时长、模型、参考图或镜头/资产来源改变后仍保留优化稿，提示核对变化。检查后点击“更新发送预览（免费）”，按最新来源整理发送内容；旧预检仅供参考。不需要跨标签复制，不强制重新收费预检。编辑区直接修改并更新预览只影响本次草稿；持久保存优化微调请在优化区再次应用。

图文连接错误不等于图片不受支持，也不能保证官网未计费。失败任务记录阶段、异常类型和实际图片数。系统不自动重发；用户确认后重新操作会建立新任务。受理响应丢失时相同输入保留原请求编号，防止重复提交。


视频生成入口依次为“预览生成内容（免费）”→“下一步：确认生成费用”→费用窗“按此规格生成”。前两步不会创建视频生成任务；本地预览接口会整理提示词但不调用模型。若确认页未出现，应提供入口/截图或任务编号排查，不把所有POST接口都等同于收费生成。


### 统一视频入口（2026-09-15，替代上节入口说明）

分镜生成面板只点击“生成视频”，进入生成视频页面后核对提示词、参考图和优化稿，再在底部选择“API 调用”或“平台网页”。API下一步确认费用后提交；平台网页打开模型与账号确认窗口后提交。点击外部按钮与切换通道不会开始生成。API型号限制在API提交时核验，不阻止用户先进入页面改选平台网页。


视频页面“下一步：确认生成费用”灰色时，直接查看底部“暂不能继续”的原因。提示预览过期时点击旁边“现在更新预览（免费）”，无需滚回页面顶部；更新通过后可继续费用确认。若为空提示词、模型未就绪或保存失败，按对应提示修正。API通道已选中不等于已经提交生成。
